# Relatorio de Bugfix

- Total de bugs no escopo: 14
- Corrigidos: 14
- Testes de regressao adicionados: 14
- Pendentes: nenhum
- Estado final: done

Nota: 6 bugs da rodada COVERAGE-1/VALIDATIONLANG-1/QUALITYGATE-1/GITGATE-VAREXPAND-1/JSONLREPAIR-1/SANITIZE-CONNSTR-1 (sessoes concorrentes anteriores) + 6 bugs da rodada HOOKINV-1..6 (review estrita da tarefa 1.0, RF-01/RF-02/RF-03/RF-04/RF-70) + 2 bugs da rodada HOOKTIMEOUT-1/TELEMETRY-RETRY-1 (review estrita da tarefa 12.0, RF-66/RF-67/RF-48).

## Bugs
- ID: COVERAGE-1
- Severidade: major
- Origem: RF-74, task 1.0, finding de review
- Estado: fixed
- Causa raiz: o teste denominado P95 usava o pior valor de cinco amostras, medindo P100 e falhando sob ruído de agendamento e coleta de lixo; a execução com `-race` adicionava contenção do runner, não latência do caminho medido.
- Arquivos alterados: internal/runtime/memory/durable/facade_bench_test.go
- Teste de regressao: TestBuildContextP95PreliminaryBenchmark_1000ActiveFactsInSinglePage calcula o P95 por nearest-rank de vinte amostras e preserva o orçamento de 200ms fora de `-race`.
- Validacao: `make coverage` -> pass; `make coverage-packages` -> pass, durable 88.1%; `go test ./... -count=1 -race` -> pass.

- ID: VALIDATIONLANG-1
- Severidade: major
- Origem: RF-31, RF-32, RF-33, RF-34 (PRD `.specs/prd-hooks-canonicos-vendor-neutral/prd.md`), finding de review da tarefa 7.0, debito residual registrado na tarefa 3.0 ("internal/contextgen.ValidationLangOrder/ValidationLangLabels ainda sao literais hardcoded (nao derivados de skills.AllLangs); a 7.0 precisa substitui-los por derivacao")
- Estado: fixed
- Causa raiz: `ValidationLangOrder` e `ValidationLangLabels` em `internal/contextgen/contextgen.go` eram literais `[]string`/`map[string]string` mantidos manualmente em paralelo a `skills.AllLangs`; a tarefa 7.0 apenas ampliou os literais de 3 para 5 entradas ao adicionar `.NET`/Java, sem eliminar a duplicacao. Uma linguagem nova adicionada a `skills.AllLangs` no futuro nao propagaria automaticamente para esses dois simbolos — a unica protecao era `TestAllLangsAreExhaustivelyWired` falhando tardiamente.
- Arquivos alterados: internal/contextgen/contextgen.go (`ValidationLangOrder`/`ValidationLangLabels` passam a ser calculados em tempo de inicializacao por `deriveValidationLangOrder`/`deriveValidationLangLabels`, iterando `skills.AllLangs`; rotulo por linguagem centralizado em `validationLangLabel`, que faz `panic` para `skills.Lang` sem rotulo mapeado em vez de silenciar)
- Teste de regressao: internal/contextgen/validation_lang_derivation_test.go — `TestValidationLangOrderDerivesFromAllLangs` compara `len(ValidationLangOrder)` e a ordem de cada entrada com `skills.AllLangs`; `TestValidationLangLabelsCoverAllLangs` compara `len(ValidationLangLabels)` com `skills.AllLangs` e garante rotulo nao vazio para cada linguagem; `TestValidationLangLabelPanicsOnUnknownLang` confirma que `validationLangLabel` falha explicitamente (nao silencia) para uma `skills.Lang` desconhecida.
- Validacao: `go build ./...` -> pass; `go vet ./...` -> pass; `go test ./internal/contextgen/... ./internal/skills/... -v` -> 230 passed; `go test -tags integration ./internal/skills/... -run TestAllLangsAreExhaustivelyWired -v` -> 6 passed (5 linguagens + wiring); `make lint` -> 0 issues; cobertura combinada `internal/contextgen` + `internal/skills` -> 91.3% (acima dos gates de 75%/70%).

- ID: QUALITYGATE-1
- Severidade: critical
- Origem: finding de review estrita da tarefa 10.0, RF-27/RF-30
- Estado: fixed
- Reprodução: chamar `Gate.Evaluate()` duas vezes com o mesmo `EvaluationInput` e um check obrigatório sempre falho. Antes da correção, a 1ª chamada retornava `DecisionBlock` e a 2ª retornava `DecisionNotApplicable` ("skip - fingerprint unchanged since last run") mesmo com o mesmo estado ainda falho — bypass de segurança real, pois `internal/runtime/hooks/quality_gate.go` só bloqueia em `DecisionBlock`/`DecisionError`.
- Causa raiz: (1) `Gate.Evaluate` em `internal/qualitygate/gate.go:72-78` aplicava o skip por fingerprint inalterado independentemente da decisão cacheada anterior, inclusive quando essa decisão era `DecisionBlock`/`DecisionError`; (2) o único wiring de produção do gate, em `internal/runtime/runner.go:717-722`, nunca preenchia `EvaluationInput.StateDigest`, deixando o fingerprint dependente só de `TaskID+TaskType+Risk+Lang+Toolchain` — idênticos entre invocações repetidas da mesma tarefa, o que tornava o dedup por fingerprint indistinguível de "nada mudou de verdade".
- Correção:
  1. `internal/qualitygate/gate.go`: nova função `isSkippableDecision` restringe o skip por fingerprint inalterado a decisões cacheadas não bloqueantes (`ALLOW`/`WARN`); uma decisão cacheada `BLOCK`/`ERROR` nunca é usada para pular a reavaliação — o gate sempre reexecuta os checks nesse caso, preservando o bloqueio real quando o estado que falhou ainda não mudou.
  2. `internal/runtime/runner.go` (`prepareHooksDispatcher`, wiring do `QualityGateHook`): `EvaluationInput.StateDigest` agora recebe `qualitygate.ComputeStateDigest(j.WorkDir)`, um hash SHA-256 de `git rev-parse HEAD` + `git status --porcelain=v1` + `git diff HEAD` do `WorkDir` (novo arquivo `internal/qualitygate/statedigest.go`). Em diretório sem git ou sem HEAD, retorna string vazia (zero-value, preserva comportamento anterior); com git disponível, o fingerprint passa a refletir mudanças reais no working tree, evitando que RF-30 deduplique qualquer execução repetida independentemente de mudança de estado.
- Arquivos alterados: internal/qualitygate/gate.go, internal/qualitygate/statedigest.go (novo), internal/runtime/runner.go
- Teste de regressao: `TestQualityGate_NeverMasksCachedBlockOnUnchangedFingerprint` (internal/qualitygate/gate_test.go) — chama `Evaluate()` duas vezes com o mesmo `EvaluationInput` e um check obrigatório sempre falho; exige `DecisionBlock` nas duas chamadas. Testes de suporte: `TestComputeStateDigest_ChangesWhenWorkingTreeChanges` e `TestComputeStateDigest_NonGitDirReturnsEmpty` (internal/qualitygate/statedigest_test.go) provam que o digest muda com modificação real do working tree e é vazio fora de um repo git. Os testes preexistentes `TestQualityGate_SkipsWhenFingerprintUnchanged` e `TestQualityGate_ReexecutesWhenFingerprintChanges` continuam passando, confirmando que o dedup legítimo (estado inalterado, decisão anterior não bloqueante) não foi quebrado.
- Validacao: `go build ./...` -> pass; `go vet ./...` -> pass; `go test ./internal/qualitygate/... ./internal/runtime/hooks/... ./internal/runtime/... -v` -> 1047 passed; `make lint` -> 0 issues; `make coverage` -> pass, total 82.1%, internal/qualitygate 90.2%.

- ID: GITGATE-VAREXPAND-1
- Severidade: critical
- Origem: finding de review estrita da tarefa 6.0, RF-18, RF-19, RF-22
- Estado: fixed
- Reprodução: `printf '%s' '{"tool_input":{"command":"echo $PATH"}}' | bash .agents/scripts/git-operation-gate.sh` bloqueava com "GOVERNANCE BLOQUEIO: invocacao de interpretador com conteudo dinamico detectada (variable_expansion)", exit 2 — mesmo resultado para `cat $HOME/.bashrc`, `grep -rn foo $REPO_ROOT`, `for f in *.go; do echo $f; done`, ou seja, qualquer comando Bash comum contendo `$` de expansão de variável, mesmo sem nenhuma relação com `git`, era bloqueado incondicionalmente. Reproduzido ao vivo nesta própria sessão de bugfix antes da correção (o hook `validate-preload.sh`, que consome o mesmo classificador, bloqueou um `source` legítimo do `check-invocation-depth.sh` contendo `"$candidate"`/`"$d"`).
- Causa raiz: no classificador Python embutido em `classify_git_command_structurally` (bloco heurístico final), a condição `elif "$" in command_text: interpreter_hits.append("variable_expansion")` tratava qualquer ocorrência de `$` em **todo o texto do comando** como "invocação de interpretador com conteúdo dinâmico", sem checar se o `$` estava na posição de palavra de comando (candidata a ocultar `git`) ou apenas em um argumento comum (`$PATH`, `$HOME`, `$REPO_ROOT`). `$VAR` isolado não executa nada novo por si só — a heurística original não distinguia esse caso do de ofuscação real.
- Correção: escopo do sinal `variable_expansion` restringido às posições de token que o classificador já examina como candidatas a nome de comando (`resolve_candidate_positions` — a posição da palavra de comando de cada segmento, e todas as posições subsequentes quando o segmento inicia por um wrapper transparente como `sudo`/`env`/`exec`), excluindo tokens que casam `ASSIGNMENT_RE` (`VAR=valor`, para não penalizar `env FOO=$BAR comando`). A detecção de substituição de comando (`$(...)`/crase) permanece global e inalterada, pois sempre executa algo novo independentemente de posição. Arquivo: `.agents/scripts/git-operation-gate.sh` (linhas ~296-313, bloco Python embutido), mirrors `.claude/scripts/git-operation-gate.sh`, `internal/embedded/assets/.agents/scripts/git-operation-gate.sh`, `internal/embedded/assets/.claude/scripts/git-operation-gate.sh` (sincronizados via `scripts/sync-skills.sh`).
- Arquivos alterados: .agents/scripts/git-operation-gate.sh, .claude/scripts/git-operation-gate.sh, internal/embedded/assets/.agents/scripts/git-operation-gate.sh, internal/embedded/assets/.claude/scripts/git-operation-gate.sh, scripts/test-validators.sh
- Teste de regressao: nova seção em `scripts/test-validators.sh` ("Não-regressão: expansão de variável comum sem relação com git não bloqueia") cobre `echo $PATH`, `cat $HOME/.bashrc`, `grep -rn foo $REPO_ROOT`, `for f in *.go; do echo $f; done`, `go test ./...`, `make test`, todos exigindo exit 0. Os dois casos de ofuscação real já existentes na mesma seção (`git${IFS}push${IFS}--force` e `G=git; P=push; $G $P --force`, ambos exit 2) permanecem cobertos e passando, confirmando que a correção não reabriu esses bypasses.
- Validacao: `bash scripts/test-validators.sh` -> 111 passaram, 0 falharam (inclui os 8 casos novos e todos os 6 bypasses previamente fechados: payload >65KB com `git push`, `sudo git push`, homônimo aninhado, separadores compostos `&&`/`||`, `git push` entre aspas, `sudo -u git -- git push`); `bash scripts/test-hooks.sh` -> 73 asserts OK, 0 FAIL; `make check-hooks-sync check-scripts-sync` -> 0 drift, todos os mirrors sincronizados; `go build ./...` -> pass; `go vet ./...` -> pass. Reprodução manual pós-correção: `echo $PATH`/`cat $HOME/.bashrc`/`go test ./...`/`make test` -> exit 0; `git push origin main` puro -> exit 2; `git${IFS}push${IFS}--force` e `G=git; P=push; $G $P --force` -> exit 2; payload >65KB terminando em `; git push origin main` -> exit 2.

- ID: JSONLREPAIR-1
- Severidade: major
- Origem: RF-42, RF-43 (PRD `.specs/prd-hooks-canonicos-vendor-neutral/prd.md`), finding de review estrita da tarefa 11.0 (`.specs/prd-hooks-canonicos-vendor-neutral/task-11.0-evidence-gate-checkpoint-atomico-e-deteccao-de-corrupcao.md`, `11.0_execution_report.md`)
- Estado: fixed
- Reprodução: gravar duas linhas válidas em `events.jsonl` via `JSONLWriter.Append`, truncar o arquivo no meio da última linha (simulando um crash de processo durante um `AppendFile`) e reabrir com `NewJSONLWriter`. Antes da correção, `NewJSONLWriter` chamava `VerifyJSONLIntegrity` sobre o arquivo inteiro e retornava erro para qualquer linha invalida, inclusive quando a corrupção estava isolada à última linha; `NewSessionPersistence` propagava o erro sem tentativa de reparo e `ACPRunner.createPersistence` (`internal/runtime/runner.go:333-336`) então abortava `Run` inteira (`return Summary{}, err`) — a sessão inteira ficava bloqueada, contrariando o criterio de sucesso da tarefa 11.0 e o proprio `11.0_execution_report.md`, que afirmam "apenas o ultimo registro parcial e perdido, nunca a sessao".
- Causa raiz: `NewJSONLWriter` (`internal/runtime/persistence/jsonl.go`) não distinguia corrupção isolada à última linha (consistente com um crash mid-append) de corrupção no meio do arquivo (cenário mais grave); qualquer falha em `VerifyJSONLIntegrity` era tratada como fatal, sem nenhum mecanismo de reparo automático.
- Correção: nova função `repairTrailingCorruption` em `internal/runtime/persistence/jsonl.go` verifica, ao detectar corrupção na abertura, se todas as linhas anteriores à última são válidas (`VerifyJSONLIntegrity(prefix)`). Se sim, a corrupção é isolada à última linha e é tratada automaticamente: linha final vazia/em branco ou com JSON inválido é truncada (dados anteriores preservados byte a byte); linha final válida porém sem terminador `\n` final recebe o terminador ausente (nenhuma perda de dado). Se a verificação do prefixo também falhar (corrupção não isolada à última linha, ex.: linha invalida no meio do arquivo), `NewJSONLWriter` continua falhando fechado com `ErrCorruptedJSONL`, preservando o comportamento de seguranca para esse cenário mais grave. O arquivo reparado é regravado via `fsys.WriteFileAtomic` antes do writer ser devolvido utilizável — `NewSessionPersistence`/`createPersistence` (nao alterados; jah delegavam corretamente) passam a receber um writer funcional em vez de erro, e a sessao pode continuar (`Run` nao aborta mais nesse cenario).
- Arquivos alterados: internal/runtime/persistence/jsonl.go, internal/runtime/persistence/jsonl_crash_recovery_test.go
- Teste de regressao: `TestNewJSONLWriter_RepairsTruncatedMidLine` prova que um arquivo truncado no meio da ultima linha (sem terminador final) reabre com sucesso, preservando byte a byte a primeira linha valida e descartando apenas a ultima linha parcial. `TestNewJSONLWriter_OnlyLastRecordIsLostOnCrash` reproduz o cenario descrito no criterio de sucesso original (append -> crash simulado apos append parcial -> reopen) e agora exige sucesso (nao mais erro), com os 3 registros pre-crash byte-identicos ao estado pre-crash e o registro parcial descartado, alem de provar que a sessao e continuavel (`w3.Append` apos o reparo). `TestNewJSONLWriter_RejectsCorruptionInTheMiddleOfTheFile` (renomeado/reescrito a partir do antigo `TestNewJSONLWriter_RejectsInvalidJSONLine`, que testava corrupcao isolada a ultima linha e cujo nome descrevia um comportamento que deixou de ser correto) prova que corrupcao no meio do arquivo (linha 2 de 3 invalida) continua falhando fechado com `ErrCorruptedJSONL`.
- Validacao: `go build ./...` -> pass; `go vet ./internal/runtime/...` -> pass; `go test ./internal/runtime/persistence/... -v` -> 33 passed; `go test ./internal/runtime/persistence/... ./internal/runtime/... -v` -> falha unica e nao relacionada (`TestParityGateHasZeroViolationsWithDispatchProofs` em `internal/runtime/specs`, dependente de `.github/hooks/governance.json`/dispatch proofs alterados por tarefa concorrente fora deste escopo); `make lint` -> 0 issues; `make coverage` -> pass, total 82.0%, internal/runtime/persistence 75.1% (acima do gate de 70% por pacote critico).

- ID: SANITIZE-CONNSTR-1
- Severidade: major
- Origem: RF-49 (PRD `.specs/prd-hooks-canonicos-vendor-neutral/prd.md`), finding de review estrita da tarefa 11.0
- Estado: fixed
- Reprodução: anexar ao YAML de resultados de uma wave uma URL com credencial embutida (ex.: `postgres://admin:hunter2@db.internal:5432/app`) e rodar `post-wave.sh`; a credencial aparecia em claro no `_orchestration_report.partial.md`, apesar do relatório da tarefa 11.0 afirmar que os seis padrões de `sanitization_policy.go` (incluindo `connection_string_credential`) estavam cobertos pelo `sed` do hook shell.
- Causa raiz: `sanitize_stream()` em `.agents/hooks/post-wave.sh` (e nos 7 mirrors) implementava apenas 5 dos 6 padrões definidos em `internal/runtime/memory/durable/sanitization_policy.go:35-59`; faltava o equivalente shell ao padrão `connection_string_credential` (URL com credencial embutida, `scheme://user:pass@host`).
- Correção: novo `sed -e` em `sanitize_stream()` equivalente ao padrão Go `connection_string_credential` (redige para `[REDACTED:connection_string_credential]` qualquer `scheme://usuario:senha@host` sem espaços nos campos de usuário/senha), propagado byte a byte para os 8 arquivos (`.claude/hooks/post-wave.sh` como canônico de `check-hooks-sync.sh`, e mirrors `.agents/hooks/`, `.codex/hooks/`, `.github/hooks/`, `internal/embedded/assets/.claude/hooks/`, `internal/embedded/assets/.agents/hooks/`, `internal/embedded/assets/.codex/hooks/`, `internal/embedded/assets/.github/hooks/`). Alinhamento opcional também aplicado ao `dotenv_secret`: adicionado `SESSION_ID` ao alternation de sufixo e uma linha `sed` dedicada para a variante minúscula `api_key=`, espelhando exatamente `sanitization_policy.go:54` (`(?im)^[A-Z][A-Z0-9_]*(?:SECRET|TOKEN|KEY|PASSWORD|PASS|CREDENTIAL|SESSION_ID)[A-Z0-9_]*=.+$|(?im)^api_key=.+$`).
- Arquivos alterados: .claude/hooks/post-wave.sh, .agents/hooks/post-wave.sh, .codex/hooks/post-wave.sh, .github/hooks/post-wave.sh, internal/embedded/assets/.claude/hooks/post-wave.sh, internal/embedded/assets/.agents/hooks/post-wave.sh, internal/embedded/assets/.codex/hooks/post-wave.sh, internal/embedded/assets/.github/hooks/post-wave.sh, scripts/test-hooks.sh
- Teste de regressao: novo caso `H5-5` em `scripts/test-hooks.sh` (bloco 5, post-wave.sh) — anexa `postgres://admin:hunter2@db.internal:5432/app` ao YAML de uma wave nova (`1.1`), roda `post-wave.sh` e exige que `admin:hunter2@` nunca apareça no `.partial.md` e que o marcador `REDACTED:connection_string_credential` esteja presente.
- Validacao: `bash scripts/test-hooks.sh` -> Bloco 5 (H5-1 a H5-5) 100% OK, incluindo o novo H5-5; `bash scripts/check-hooks-sync.sh` -> post-wave.sh com 0 drift/missing nos 4 hooks canônicos x 7 mirrors (28 combinações verificadas); os 8 arquivos de post-wave.sh confirmados byte-identicos via `diff -q` par a par. Teste sintetico manual isolado do `sed` (fora do hook) confirmado com exemplos de `sanitization_policy_test.go` (`postgres://admin:hunter2@db.internal:5432/app` -> redigido; `postgres://db.internal:5432/app` sem credencial -> nao redigido, comportamento identico ao Go).

- ID: HOOKINV-1
- Severidade: major
- Origem: RF-02, task 1.0 (`.specs/prd-hooks-canonicos-vendor-neutral/task-1.0-inventario-e-classificacao-de-hooks-com-gate-anti-orfao.md`), finding de review estrita
- Estado: fixed
- Reproducao: `grep '"blocking"' testdata/hook-inventory.json` retornava `"NAO BLOQUEANTE"` para as 63 entradas, inclusive para hooks comprovadamente bloqueantes (`git-operation-gate.sh` com `exit 0` explicito no caminho de negacao mas `!command` propagando `exit "$PRELOAD_BLOCK_EXIT"=2` em `validate-preload.sh`; `hook-prereq-gate.sh:53 exit 1`; `validate-governance.sh:38,48 exit 1`).
- Causa raiz: `internal/hookinventory/inventory.go:135` atribuia a constante fixa `"NAO BLOQUEANTE"` ao campo `Blocking` de todo `Entry`, sem nenhuma analise do conteudo do hook.
- Correcao: nova funcao `(s *Service) blocking(location string, data []byte) string` deriva o campo por analise real do conteudo: para hooks shell, `shellHookBlocks` casa `exit N` literal (N>0) e tambem `exit "$VAR"`/`exit $VAR` resolvendo `VAR` para uma atribuicao numerica nao-zero no mesmo arquivo (cobre o padrao `readonly PRELOAD_BLOCK_EXIT=2 ... exit "$PRELOAD_BLOCK_EXIT"` de `validate-preload.sh`, que um regex literal simples nao capturaria); para hooks Go, `goHookBlocks` verifica se o arquivo declara um metodo `Run(` que efetivamente retorna erro nao-nil (`return fmt.Errorf(`/`errors.New(`/sentinela `Err*`), espelhando o contrato documentado em `dispatcher.go`: "Retornar erro interrompe o fan-out sequencial".
- Arquivos alterados: internal/hookinventory/inventory.go, testdata/hook-inventory.json, docs/hook-inventory.md
- Teste de regressao: `TestInventoryGenerateAndCheck` (existente) continua validando o round-trip; a correcao e exercitada indiretamente por toda a suite de `internal/hookinventory` porque `Blocking` agora e campo derivado e `docs/hook-inventory.md`/`testdata/hook-inventory.json` regenerados sao comparados byte a byte pelo gate `Check`.
- Validacao: `go run . hooks inventory .` seguido de `grep -c '"blocking": "BLOQUEANTE"' testdata/hook-inventory.json` confirma hooks conhecidos como bloqueantes (`validate-preload.sh`, `validate-governance.sh`, `hook-prereq-gate.sh`, `subagent-stop-wrapper.sh`, `post-execute-task.sh`, `post-wave.sh`, `pre-execute-all-tasks.sh`, `validate-session-end.sh`, `governance.go`, `spec_drift.go`, `quality_gate.go`, `memory_persist.go`) deixam de aparecer como `"NAO BLOQUEANTE"`; `go build ./...`, `go vet ./...`, `go test ./internal/hookinventory/... -v` -> pass.

- ID: HOOKINV-2
- Severidade: major
- Origem: RF-01, RF-02, task 1.0, finding de review estrita
- Estado: fixed
- Reproducao: `jq '[.[] | .event] | unique' testdata/hook-inventory.json` retornava `["lifecycle"]` (um unico valor para as 63 entradas); o mesmo para `purpose` e `policy_gate`, que repetiam a frase generica `"Aplica governanca operacional no ponto de hook declarado."`/`"Politicas em .agents/policies/ nao sao lidas diretamente; gate auto-contido."` sem diferenciar hooks de proposito completamente distinto (ex.: `validate-preload.sh` vs `post-wave.sh` vs `.opencode/plugin/governance.js`).
- Causa raiz: `internal/hookinventory/inventory.go:131-134` atribuia strings literais identicas a `Event`/`Purpose`/`PolicyGate` no construtor `newEntry`, independentemente do hook escaneado.
- Correcao: novo modelo `hookProfile` (Event/Purpose/PolicyGate/Justification) resolvido por `(s *Service) profile(location string)`, com dois mapas de evidencia real: `basenameHookProfiles` (perfil por script, valido para os mirrors identicos de `.agents/`/`.claude/`/`.codex/`/`.github/`) e `uniqueHookProfiles` (perfil individual para os arquivos sem mirror — `.github/hooks/governance.json`, `.opencode/plugin/governance.js`, `scripts/git-hooks/pre-commit` e os 8 hooks Go de `internal/runtime/hooks/`). O evento de cada perfil foi extraido de fonte primaria real, nao inferido: `.claude/settings.json` (PreToolUse/PostToolUse/SubagentStop/Stop), `.codex/config.toml` (`hooks.PreToolUse`/`hooks.PostToolUse`/`hooks.Stop` — confirmando a lacuna documentada de `SubagentStop` ausente no Codex), `.github/hooks/governance.json` (`agentStop`/`postToolUse`/`preToolUse`) e `internal/runtime/hooks/dispatcher.go`/`quality_gate.go:42`/`token_budget.go:32`/`memory_persist.go` (pontos canonicos `PointRuntimePreOpen`/`PointSessionPostEnd`/`PointPromptPostBuild`). `adaptProfileToProvider` marca a entrada `.agents/` como fonte canonica (evento + nota de distribuicao) sem duplicar o mapa por provedor.
- Arquivos alterados: internal/hookinventory/inventory.go, testdata/hook-inventory.json, docs/hook-inventory.md
- Teste de regressao: `TestInventoryGenerateAndCheck` cobre o round-trip do novo modelo.
- Validacao: `jq '[.[] | .event] | unique | length' testdata/hook-inventory.json` passa de 1 para 19+ valores distintos, e `jq '[.[] | .purpose] | unique | length'` confirma ausencia de boilerplate repetido em massa; `go build ./...`, `go vet ./...`, `go test ./internal/hookinventory/... -v` -> pass.
- Riscos residuais: hooks sem perfil individual mapeado (nenhum no scan atual) caem no fallback explicito `"NAO MAPEADO"`/`"revisar em rodada dedicada de inventario"` em vez de reaproveitar silenciosamente o texto de outro hook; uma origem nova adicionada ao scanner sem entrada em `basenameHookProfiles`/`uniqueHookProfiles` fica visivelmente marcada para revisao futura, nao mascarada.

- ID: HOOKINV-3
- Severidade: major
- Origem: RF-03, task 1.0, finding de review estrita
- Estado: fixed
- Reproducao: `jq '[.[] | .justification] | group_by(.) | map({j:.[0], n:length}) | sort_by(-.n)' testdata/hook-inventory.json` mostrava a frase `"Mantido por proteger governanca ativa ou por ser espelho distribuido."` repetida em 61 das 63 entradas, violando RF-03 ("justificativa de KEEP nunca pode ser apenas 'por ja existir'").
- Causa raiz: mesmo defeito estrutural de HOOKINV-2 — `entry.Justification` recebia a mesma string literal no construtor, independentemente do hook.
- Correcao: `Justification` agora vem do mesmo `hookProfile` de HOOKINV-2, com texto individual ancorado em evidencia verificavel do repositorio (numero de linha, arquivo que encadeia o hook, SKILL.md que o invoca, ou consequencia concreta de removê-lo) — ex.: `validate-preload.sh` cita `PRELOAD_BLOCK_EXIT=2` como unico ponto de entrada PreToolUse; `post-execute-task.sh` cita `SKILL.md:112` e o comportamento "failed: hook ausente" sem modo legado; `hook-payload.sh` cita ser o proprio gatilho do fail-open documentado em `validate-governance.sh:33`. `.agents/scripts/validate-governance-references.sh` deixa de usar o boilerplate e passa a `Classification=REMOVE` com justificativa especifica (ver HOOKINV-6); `.claude/hooks/validate-token-budget.sh` recebe justificativa dedicada citando `scripts/check-hooks-sync.sh:154,158`.
- Arquivos alterados: internal/hookinventory/inventory.go, testdata/hook-inventory.json, docs/hook-inventory.md
- Teste de regressao: mesma cobertura de round-trip de `TestInventoryGenerateAndCheck`.
- Validacao: `jq '[.[] | .justification] | unique | length' testdata/hook-inventory.json` confirma que a maior frequencia de qualquer justificativa unica cai de 61 para no maximo o numero de mirrors identicos de um mesmo basename (proporcional, nao indiscriminado); `go build ./...`, `go vet ./...`, `go test ./internal/hookinventory/... -v` -> pass.

- ID: HOOKINV-4
- Severidade: major
- Origem: RF-01, subtarefa 1.3 do task file, finding de review estrita
- Estado: fixed
- Reproducao: `internal/hookinventory/inventory.go:196` citava `.agents/hooks/validate-governance.sh:33,38` como FAIL-OPEN; a leitura de `.agents/hooks/validate-governance.sh` mostra que a linha 38 e `exit 1` dentro de `if ! file_path=...; then` (payload invalido nega a edicao — FAIL-CLOSED), enquanto o ponto real de fail-open nao citado e a linha 41 (`[[ -n "$file_path" ]] || exit 0`, ausencia de alvo aprova silenciosamente).
- Causa raiz: transcricao incorreta de numero de linha no mapa `failOpen()` (dado estatico, nunca validado contra o arquivo real).
- Correcao: citacao corrigida para `.agents/hooks/validate-governance.sh:33,41`, mantendo a linha 33 (correta: `hook-payload.sh` ausente vira `exit 0`) e substituindo a linha 38 (incorreta) pela 41 (correta).
- Arquivos alterados: internal/hookinventory/inventory.go, testdata/hook-inventory.json, docs/hook-inventory.md
- Teste de regressao: sem teste automatizado dedicado (correcao de dado estatico de citacao, sem ramo de codigo condicional a testar); corretude confirmada por leitura direta do arquivo fonte e a mesma correcao e continuamente protegida pelo teste de round-trip `TestInventoryGenerateAndCheck` (o `Check` falha se `docs/hook-inventory.md`/`testdata/hook-inventory.json` divergirem do valor gerado pelo mapa `failOpen()`).
- Validacao: `sed -n '30,42p' .agents/hooks/validate-governance.sh` confirma que a linha 38 e `exit 1` (dentro de bloco de negacao) e a linha 41 e `[[ -n "$file_path" ]] || exit 0` (fail-open real); `grep -n 'validate-governance.sh:33,41' testdata/hook-inventory.json docs/hook-inventory.md` confirma a citacao corrigida propagada nos dois artefatos; `go build ./...`, `go vet ./...` -> pass.

- ID: HOOKINV-5
- Severidade: major
- Origem: RF-70, subtarefa 1.9 do task file, finding de review estrita
- Estado: fixed
- Reproducao: `grep -rn "skills-lock" internal/hookinventory/*.go` nao retornava nenhuma ocorrencia antes da correcao; `internal/doctor/doctor.go:257` (`checkSkillIntegrity`) consumia `internal/skillscheck.Service.Verify`, que so validava entradas de `skills-lock.json` cujo `skillName` correspondesse a um diretorio real em `.agents/skills/<nome>/SKILL.md` — nenhum hook critico tinha entrada, entao uma alteracao em `validate-preload.sh` nao era detectavel por `ai-spec doctor`.
- Causa raiz: (1) `internal/hookinventory` calculava `IntegrityHash` apenas para uso interno da propria entrada do inventario, sem nenhuma escrita em `skills-lock.json`; (2) `internal/skillscheck.Service.Check` resolvia o caminho do conteudo hasheado exclusivamente como `filepath.Join(skillsDir, skillName, "SKILL.md")`, sem suporte a um caminho arbitrario de arquivo — mesmo que uma entrada de hook fosse adicionada manualmente ao lock, o check reportaria `DriftNoSkill` (falso "skill nao instalada") para sempre, o que quebraria permanentemente o gate em vez de detectar drift real.
- Correcao: (1) `internal/hookinventory/inventory.go` ganha `syncSkillsLock` (chamado por `Generate`) e `checkSkillsLock` (chamado por `Check`), que leem/escrevem `skills-lock.json` via `map[string]json.RawMessage` — preservando byte a byte qualquer campo de entradas de skills externas existentes (ex.: `skillPath`) que o struct Go do pacote nao modela — e upsertam uma entrada `"hook:<location>"` (`source:"internal"`, `sourceType:"hook"`, `path:<location>`, `computedHash:<IntegrityHash>`) para cada hook critico canonico (`critical(location)==true` — contem `validate-`, `gate` ou `pre-commit` — restrito a `.agents/*` e `scripts/git-hooks/pre-commit`, a fonte unica, nao os mirrors por provedor). (2) `internal/skillscheck/skillscheck.go` ganha o campo `LockEntry.Path` (`json:"path,omitempty"`); quando presente, `Check` hasheia o arquivo em `filepath.Join(projectDir, entry.Path)` em vez do caminho de skill, e pula o parsing de frontmatter (`installedVer` fica vazio, `classifyDrift` cai em `DriftUnknown` que so falha se o hash tambem divergir) — mudanca aditiva e retrocompativel, todos os testes preexistentes de `skillscheck_test.go` continuam passando sem alteracao porque nao setam `Path`.
- Arquivos alterados: internal/hookinventory/inventory.go, internal/skillscheck/skillscheck.go, skills-lock.json, testdata/hook-inventory.json (campo `integrity_hash` inalterado na forma, mas agora efetivamente consumido), internal/hookinventory/inventory_test.go, internal/skillscheck/skillscheck_test.go
- Teste de regressao: `TestGenerateSyncsSkillsLockAndCheckDetectsHookDrift` (internal/hookinventory/inventory_test.go) prova o ciclo completo: `Generate` escreve `skills-lock.json` com a entrada `hook:.agents/hooks/validate-preload.sh`; `skillscheck.Verify` nao acusa falha com o hook inalterado; apos alterar o conteudo do hook no filesystem fake, `skillscheck.Verify` passa a reportar exatamente 1 falha com `Reason == "hash diverge do registrado em skills-lock.json"` **antes** de qualquer regeneracao do inventario (prova de que a alteracao e detectavel pelo check de integridade, RF-70), e `Check` do inventario tambem passa a falhar; apos `Generate` novamente, ambos voltam a ficar verdes. `TestCheck_PathEntryDetectsHookDrift` (internal/skillscheck/skillscheck_test.go) isola o mesmo comportamento diretamente no pacote `skillscheck`, sem depender de `hookinventory`.
- Validacao: `go run . hooks inventory .` seguido de `jq '.skills["hook:.agents/hooks/validate-preload.sh"]' skills-lock.json` confirma a entrada real no arquivo do repositorio, com `computedHash` igual ao SHA-256 atual do arquivo.

- ID: HOOKINV-6
- Severidade: major
- Origem: RF-04, secao "Testes da Tarefa > Testes unitarios" do task file, finding de review estrita
- Estado: fixed
- Reproducao: `grep -n "REMOVE" internal/hookinventory/inventory_test.go` nao retornava nenhum teste antes da correcao, apesar do task file listar explicitamente como teste unitario obrigatorio: "Classificacao: REMOVE sem cobertura previa do invariante no core nao autoriza remocao (RF-04)".
- Causa raiz: ausencia pura de teste — o pacote nunca teve nenhuma capacidade de remocao de arquivo fonte (Generate/Check so leem hooks e escrevem projecoes em `docs/`/`testdata/`/`skills-lock.json`), mas essa garantia estrutural nunca foi provada por um teste que force um cenario real de classificacao `REMOVE`.
- Correcao: `.agents/scripts/validate-governance-references.sh` (e seu mirror `.claude/scripts/`) — ja documentado na subtarefa 1.5 do task file como "orfao de invocacao... candidato a REMOVE ou ON-DEMAND, sem remocao nesta tarefa por RF-04" — passa de `Classification="ON-DEMAND"` para `Classification="REMOVE"` (classificacao textualmente autorizada pelo proprio task file), com justificativa que cita a evidencia de grep repo-wide (nenhum call-site executavel) e registra explicitamente que RF-04 impede a remocao nesta tarefa. Novo teste `TestClassificationRemoveDoesNotAuthorizeDeletion` gera o inventario, localiza toda entrada com `Classification=="REMOVE"`, falha o teste se a lista estiver vazia (garante que o cenario e realmente exercitado, nao vacuo) e falha se o arquivo fonte correspondente nao existir mais no filesystem apos `Generate`/`Check` — provando que a classificacao e puramente diagnostica.
- Arquivos alterados: internal/hookinventory/inventory.go, internal/hookinventory/inventory_test.go, testdata/hook-inventory.json, docs/hook-inventory.md
- Teste de regressao: `TestClassificationRemoveDoesNotAuthorizeDeletion` (internal/hookinventory/inventory_test.go).
- Validacao: `git status --short .agents/scripts/validate-governance-references.sh .claude/scripts/validate-governance-references.sh` confirma que nenhum dos dois arquivos foi removido do working tree por esta correcao (permanecem intocados, apenas a classificacao no inventario mudou).

- ID: HOOKTIMEOUT-1
- Severidade: major
- Origem: finding de review da tarefa 12.0, RF-66, RF-67
- Estado: fixed
- Reproducao: `grep -rn timeout .agents/hooks/*.sh .claude/hooks/*.sh` retornava vazio antes da correcao. A tarefa 12.0 conectou timeout real (`hookcontract.HookTimeoutRegistry`) e guarda de recursao real (`hookcontract.CheckRecursionGuard`, `dispatcher.go:168-176`) apenas ao dispatcher Go do modo orquestrado (`internal/runtime/hooks/dispatcher.go`). Os hooks shell do modo interativo — uso dominante do harness, citados nomeadamente pelo Fato #7 do PRD (`.agents/hooks/`, `.agents/scripts/`) — continuavam sem timeout ou protecao de recursao proprios.
- Causa raiz: `validate-preload.sh`, `validate-governance.sh` e `git-operation-gate.sh` nao declaravam nenhum orcamento de tempo para si nem para os subprocessos que encadeiam (`validate-preload.sh` chama `hook-prereq-gate.sh` e `git-operation-gate.sh`; `git-operation-gate.sh` chama um classificador `python3`), e nao existia nenhuma guarda que impedisse uma cadeia `hook -> ferramenta -> hook` de se realimentar indefinidamente no nivel do processo shell — apenas `check-invocation-depth.sh` existia, e cobre um concern distinto (profundidade de skill `execute-task -> review -> bugfix`, nao profundidade de processo de hook).
- Correcao: tres primitivas novas em `.agents/lib/hook-payload.sh` (lib ja sourced por `validate-preload.sh`, `validate-governance.sh` e `git-operation-gate.sh`, evitando novo arquivo de lib e nova entrada em `required_libs`/mirrors): (1) `hook_recursion_guard <hook_name> <block_exit>` — le/incrementa `AI_HOOK_DEPTH` (default max 3 via `AI_HOOK_MAX_DEPTH`), exportando o valor incrementado para os processos filhos; ao atingir o maximo, nega (`exit <block_exit>`) antes de qualquer trabalho, impedindo por construcao — nao por convencao — que uma cadeia `hook -> ferramenta -> hook` se aprofunde sem limite, mesmo entre hooks diferentes que se invocam em sequencia (`validate-preload.sh -> git-operation-gate.sh -> ...`); (2) `hook_measure_start <hook_name> <timeout_s>` — instala uma `trap ... EXIT` que emite `hook.duration_ms=<n> hook=<nome> hook.timeout_s=<n> hook.decision_exit=<código>` em stderr em toda saida (normal ou por erro), tornando o tempo gasto por hook mensuravel (RF-66) sem depender de telemetria opcional; (3) `hook_timeout_watch <hook_name> <timeout_s> <child_pid>` — mata de fato o processo filho (nao o proprio processo) apos o timeout e trata isso como negacao, devolvendo `124`. A escolha de matar o PID do filho diretamente (nao `kill -TERM "$$"` do proprio processo) foi validada empiricamente: em bash 3.2 (padrao do macOS sem coreutils), um sinal enviado ao proprio processo so e processado apos o comando bloqueante em primeiro plano terminar naturalmente (reproduzido isoladamente antes da correcao final: um watchdog que mandava TERM para `$$` so disparava a trap depois que um `sleep 5` interno terminava sozinho, nunca antes) — matar o PID do filho, em contraste, e imediato e portatil sem exigir GNU `coreutils`/`timeout`/`gtimeout`. `validate-preload.sh` usa `hook_recursion_guard`+`hook_measure_start` e envolve as duas chamadas a `hook-prereq-gate.sh`/`git-operation-gate.sh` com `hook_timeout_watch` (orcamento `AI_HOOK_TIMEOUT_VALIDATE_PRELOAD`, default 40s); `git-operation-gate.sh` usa `hook_recursion_guard`+`hook_measure_start` (orcamento `AI_HOOK_TIMEOUT_GIT_OPERATION_GATE`, default 35s) e envolve seu proprio classificador `python3` com `hook_timeout_watch` (orcamento `AI_HOOK_TIMEOUT_GIT_CLASSIFIER`, default 30s — dimensionado apos medir empiricamente ~11.2s para um payload benigno de 1 MiB, o maior tamanho ja coberto por `scripts/test-validators.sh`, com margem de seguranca); `validate-governance.sh` usa `hook_recursion_guard`+`hook_measure_start` (orcamento `AI_HOOK_TIMEOUT_VALIDATE_GOVERNANCE`, default 10s). Nenhuma semantica existente foi alterada: todos os `exit`/decisoes previos permanecem identicos, a mudanca e estritamente aditiva (timeout/recursao somam-se as decisoes existentes, nunca as substituem).
- Arquivos alterados: .agents/lib/hook-payload.sh, .agents/hooks/validate-preload.sh, .agents/hooks/validate-governance.sh (canonico real: .claude/hooks/validate-governance.sh, propagado por `scripts/sync-hooks.sh`), .agents/scripts/git-operation-gate.sh, .claude/hooks/validate-governance.sh, .claude/scripts/git-operation-gate.sh, .codex/hooks/validate-governance.sh, .github/hooks/validate-governance.sh, scripts/lib/hook-payload.sh, scripts/test-hooks.sh, e os mirrors embarcados correspondentes em internal/embedded/assets/{.agents,.claude,.codex,.github}/{hooks,scripts,lib}/ (todos sincronizados via `scripts/sync-hooks.sh`/`scripts/sync-skills.sh`, verificados por `make check-hooks-sync check-scripts-sync`).
- Teste de regressao: novo "Bloco 4b" em `scripts/test-hooks.sh` (P7-1 a P7-4) contra as primitivas reais de producao — nao mocks: P7-1 cria um filho real (`bash` executando `sleep 5`) e prova que `hook_timeout_watch` com orcamento de 1s o mata e nega em <=3s de parede (nao espera os 5s do filho); P7-2 cria uma cadeia real de reinvocacao (`bash "$0"` recursivo) com `AI_HOOK_MAX_DEPTH=3` e prova que exatamente 3 entradas sao permitidas antes do bloqueio, com a mensagem de negacao esperada; P7-3 roda `git-operation-gate.sh` real com um comando benigno e prova que a duracao mensuravel (`hook.duration_ms=`) e emitida sem regressao no `exit 0`; P7-4 roda `git-operation-gate.sh` real com `AI_HOOK_DEPTH=3 AI_HOOK_MAX_DEPTH=3` pre-setados e prova que ate um comando benigno e negado pela guarda de recursao, confirmando que a protecao vale mesmo quando o hook e chamado isoladamente (nao apenas encadeado).
- Validacao: `bash scripts/test-hooks.sh` -> 85 asserts OK, 0 FAIL (75 preexistentes + 10 novos do Bloco 4b, nenhuma regressao); `bash scripts/test-validators.sh` -> 111 passaram, 0 falharam (inclui a fronteira de 1 MiB que exigiu o ajuste do orcamento do classificador — ver Riscos Residuais); `make check-hooks-sync check-scripts-sync` -> 0 drift; `go build ./...`, `go vet ./...` -> pass; `go test ./... -count=1` -> 4304 passed; `make lint` -> 0 issues; `make coverage` -> pass, total 82.0%.

- ID: TELEMETRY-RETRY-1
- Severidade: minor
- Origem: finding de review da tarefa 12.0, RF-48
- Estado: fixed
- Reproducao: `TestHookTelemetry_FailureDoesNotPropagateOrCountAsRetry` (`internal/telemetry/hook_events_test.go:79-100`) declarava `retryAttempts`/`taskCompleted` como variaveis locais do proprio teste, nunca lidas por nenhum codigo de producao — o teste podia passar mesmo se o dispatcher real (`internal/runtime/hooks/dispatcher.go`) algum dia passasse a propagar falha de telemetria como erro de `Dispatch` ou a contar como retentativa; nao provava nada sobre o comportamento real.
- Causa raiz: ausencia de um caminho de teste que injete um `telemetry.HookTelemetryWriter` falho no `dispatcher` real; `dispatcher.New(rootDir ...string)` nao expunha nenhuma forma de substituir o writer de telemetria padrao (`telemetry.NewHookTelemetry()`, que escreve em arquivo), entao nenhum teste de `internal/runtime/hooks` conseguia provar RF-48 contra o dispatcher de producao.
- Correcao: nova funcao exportada `hooks.NewWithTelemetry(rootDir string, tel telemetry.HookTelemetry) Dispatcher` em `internal/runtime/hooks/dispatcher.go` (aditiva; `hooks.New` existente inalterado) permite injetar `telemetry.NewHookTelemetryWithWriter(writer)` com um writer de teste. Novo teste `TestDispatcher_TelemetryFailureDoesNotBlockDispatch` em `internal/runtime/hooks/dispatcher_test.go` injeta um `alwaysFailingTelemetryWriter` (sempre retorna erro) no dispatcher real, registra um hook real, chama `Dispatch` no ponto `session.post_end` e prova: (1) `Dispatch` retorna `nil` mesmo com a escrita de telemetria falhando; (2) o hook registrado executa ate o fim (`called` contem seu nome); (3) o writer falho foi de fato invocado (`writer.calls > 0`, prova que a falha realmente aconteceu e foi absorvida, nao apenas nunca exercitada); (4) nenhuma retentativa e contabilizada (`retryAttempts == 0`). O teste tautologico legado em `internal/telemetry/hook_events_test.go` foi reescrito para `TestHookTelemetry_WriterFailureIsSwallowed`, escopado corretamente para provar apenas o comportamento de nivel de writer (erro nao propaga de `RecordDuration` para o chamador), com comentario apontando para a prova real no dispatcher.
- Arquivos alterados: internal/runtime/hooks/dispatcher.go, internal/runtime/hooks/dispatcher_test.go, internal/telemetry/hook_events_test.go
- Teste de regressao: `TestDispatcher_TelemetryFailureDoesNotBlockDispatch` (internal/runtime/hooks/dispatcher_test.go) — prova real contra o dispatcher de producao, nao mock isolado.
- Validacao: `go test ./internal/runtime/hooks/... -run TestDispatcher -v` -> 10 passed; `go test ./internal/telemetry/... ./internal/runtime/hooks/...` -> 150 passed; `go test ./... -count=1` -> 4304 passed; `go build ./...`, `go vet ./...` -> pass; `make lint` -> 0 issues (corrigido um `SA4006` intermediario do proprio teste novo, variavel `retryAttempts` incrementada sem uso antes do `t.Fatalf`).

## Comandos Executados
- `go test ./internal/runtime/memory/durable -run '^TestBuildContextP95PreliminaryBenchmark_1000ActiveFactsInSinglePage$' -count=20 -v` -> pass em 20 execuções.
- `go test -cover ./internal/runtime/memory/durable -run '^TestBuildContextP95PreliminaryBenchmark_1000ActiveFactsInSinglePage$' -count=20 -v` -> pass em 20 execuções.
- `go test -bench '^BenchmarkRecoveryWith1000ActiveFactsInSinglePage$' -benchmem ./internal/runtime/memory/durable` -> pass, p95_ms 15.39.
- `make coverage` -> pass (na sessao COVERAGE-1).
- `make coverage-packages` -> pass.
- `go test ./... -count=1 -race` -> pass.
- `go build ./...` -> pass (VALIDATIONLANG-1).
- `go vet ./...` -> pass.
- `go test ./internal/contextgen/... ./internal/skills/... -v` -> 230 passed em 2 pacotes.
- `go test -tags integration ./internal/skills/... -run TestAllLangsAreExhaustivelyWired -v` -> 6 passed.
- `make lint` -> 0 issues.
- `go test ./internal/contextgen/... ./internal/skills/... -coverprofile=/tmp/cov_contextgen.out -covermode=atomic && go tool cover -func=/tmp/cov_contextgen.out` -> total 91.3%.
- `make coverage` (rodada VALIDATIONLANG-1) -> falhou por causa raiz preexistente e nao relacionada: `internal/Makefile` na regra `coverage` gera path malformado `.../ai-spec-harness/ingithub.com/.../internal/taskloop` (concatenacao indevida de `internal/taskloop` a outro pacote), provavelmente de edicao concorrente de outra tarefa em `Makefile` (fora do escopo `internal/contextgen/contextgen.go` desta correcao); `make coverage` restrito a `internal/contextgen`/`internal/skills` via `go test -coverprofile` confirma 91.3%, acima dos gates.
- `go build ./...` -> pass (QUALITYGATE-1).
- `go vet ./...` -> pass.
- `go test ./internal/qualitygate/... ./internal/runtime/hooks/... ./internal/runtime/... -v` -> pass (1047 testes).
- `make lint` -> 0 issues.
- `make coverage` (rodada QUALITYGATE-1, apos o Makefile ja estar estavel) -> pass, total 82.1%, internal/qualitygate 90.2%.
- `bash scripts/test-validators.sh` (rodada GITGATE-VAREXPAND-1) -> 111 passaram, 0 falharam.
- `bash scripts/test-hooks.sh` (rodada GITGATE-VAREXPAND-1) -> 73 asserts OK, 0 FAIL.
- `make check-hooks-sync check-scripts-sync` (rodada GITGATE-VAREXPAND-1) -> 0 drift, 32 validadores e 28 hooks em sync.
- `go build ./...` -> pass (GITGATE-VAREXPAND-1).
- `go vet ./...` -> pass (GITGATE-VAREXPAND-1).
- Reprodução manual dos 9 cenários do bug (`echo $PATH`, `cat $HOME/.bashrc`, `grep -rn foo $REPO_ROOT`, `for f in *.go; do echo $f; done`, `go test ./...`, `make test`, `git push origin main`, `git${IFS}push${IFS}--force`, `G=git; P=push; $G $P --force`) -> todos com exit code correto.
- `make coverage` (rodada GITGATE-VAREXPAND-1) -> falhou por causa raiz preexistente e nao relacionada a esta correção: `FAIL github.com/JailtonJunior94/ai-spec-harness/internal/runtime/persistence [build failed]`, originado de edição concorrente em `internal/runtime/persistence/jsonl.go`/`jsonl_crash_recovery_test.go` (fora do escopo `.agents/scripts/git-operation-gate.sh` desta correção, presumivelmente outra tarefa em andamento no mesmo working tree). `go build ./...`, `go vet ./...`, `bash scripts/test-validators.sh` e `bash scripts/test-hooks.sh` — os alvos proporcionais ao escopo shell-only desta correção — passaram integralmente.
- `go build ./...` -> pass (JSONLREPAIR-1/SANITIZE-CONNSTR-1).
- `go vet ./...` -> pass; `go vet ./internal/runtime/...` -> pass.
- `go test ./internal/runtime/persistence/... -v` -> 33 passed.
- `go test ./internal/runtime/persistence/... ./internal/runtime/... -v` -> 995 passed, 1 failed (`TestParityGateHasZeroViolationsWithDispatchProofs` em `internal/runtime/specs`, dependente de `.github/hooks/governance.json`; nao toca `internal/runtime/persistence` nem `.agents/hooks/post-wave.sh`, fora do escopo desta correção).
- `go test ./internal/hookinventory/...` -> 6 passed (falha transitoria de build reportada por `make coverage` numa execucao concorrente anterior nao se reproduziu isoladamente).
- `make lint` -> 0 issues (6 linters, `internal/runtime/persistence` incluido).
- `make coverage` -> pass apos retry (uma execucao concorrente falhou por artefato de build transitorio em `internal/hookinventory`, fora deste escopo); total 82.0%, `internal/runtime/persistence` 75.1% (gate 70% critico).
- `bash scripts/test-hooks.sh` -> primeira rodada: Bloco 5 (post-wave.sh) 100% OK incluindo H5-5 novo; Bloco 4 (`git-operation-gate.sh`) com 6 falhas (exit=127) causadas por edição concorrente fora de escopo, em andamento em `.agents/scripts/git-operation-gate.sh`/`.agents/lib/hook-payload.sh` (funcao `hook_runtime_guard` ausente durante uma janela da sessao). Rodada final, apos a tarefa concorrente concluir: **85 asserts OK, 0 FAIL** (suite inteira, incluindo Bloco 4 e Bloco 5/H5-5).
- `bash scripts/check-hooks-sync.sh` -> rodada final: `Hooks em sync: 28`, `Drift / missing: 0`, "Todos os hooks orquestrador sincronizados." (post-wave.sh incluido, 4 hooks canonicos x 7 mirrors).
- `make check-scripts-sync` -> rodada final: `Validadores em sync: 32`, `Drift / missing: 0`, "Todos os validadores de evidencia sincronizados." (o drift transitorio em `git-operation-gate.sh`, de tarefa concorrente em andamento, foi resolvido pela propria tarefa antes do encerramento desta correção).
- `diff -q .claude/hooks/post-wave.sh <mirror>` (8 arquivos) -> byte-identicos apos a propagacao.
- `go build ./...` -> pass (rodada HOOKINV-1..6).
- `go vet ./...` -> pass.
- `go test ./internal/hookinventory/... ./internal/doctor/... ./internal/skillscheck/... -v` -> 64 passed (3 pacotes), incluindo `TestClassificationRemoveDoesNotAuthorizeDeletion`, `TestGenerateSyncsSkillsLockAndCheckDetectsHookDrift`, `TestCheck_PathEntryDetectsHookDrift` e o teste preexistente `TestCheckSkillIntegrity_ConsumesRF60LockMismatch` (confirma que a extensao de `LockEntry.Path` nao regrediu o consumidor `internal/doctor`).
- `go test ./... -count=1` -> 4304 passed em 92 pacotes.
- `make lint` -> 0 issues (rodada final; uma execucao intermediaria falhou por `SA4006` em `internal/runtime/hooks/dispatcher_test.go`, fora do escopo `internal/hookinventory`/`internal/skillscheck` desta correcao — edicao concorrente de outra tarefa no mesmo working tree, resolvida pela propria tarefa antes do encerramento desta correcao; `golangci-lint run ./internal/hookinventory/... ./internal/skillscheck/...` isolado -> 0 issues em todas as execucoes, inclusive na janela em que o `make lint` completo falhava).
- `go run . hooks inventory .` -> "Inventario de hooks gerado: 63 entradas" (regenerado 2x, a segunda apos edicoes concorrentes de outras tarefas em `.agents/hooks/validate-governance.sh`/`validate-preload.sh`/`.agents/scripts/git-operation-gate.sh`/`.agents/lib/hook-payload.sh` terem alterado o conteudo escaneado).
- `bash scripts/check-hooks-inventory.sh` -> exit 0 (gate anti-orfao bidirecional, 2 execucoes).
- `make coverage` -> pass apos retry; total 82.0%, `internal/hookinventory` 81.4%, `internal/skillscheck` 89.8% (ambos acima do gate de 70% por pacote critico); uma execucao intermediaria falhou com `FAIL github.com/JailtonJunior94/ai-spec-harness/internal/runtime/hooks [build failed]` (`dispatcher_test.go` com import nao usado de `internal/telemetry`), fora do escopo desta correcao — transitorio de edicao concorrente, nao se reproduziu na execucao seguinte.
- `bash .agents/scripts/validate-bugfix-evidence.sh .specs/prd-hooks-canonicos-vendor-neutral/bugfix_report.md --rf RF-01 --rf RF-02 --rf RF-03 --rf RF-04 --rf RF-70` -> ver linha de validacao final abaixo.
- `bash scripts/test-hooks.sh` (rodada HOOKTIMEOUT-1) -> 85 asserts OK, 0 FAIL (Bloco 4b novo: P7-1 a P7-4).
- `bash scripts/test-validators.sh` (rodada HOOKTIMEOUT-1) -> 1a execucao: 110 passaram, 1 falhou (fronteira de 1 MiB negada por timeout do classificador com orcamento inicial de 5s); apos ajuste dos orcamentos default (`AI_HOOK_TIMEOUT_GIT_CLASSIFIER` 5s->30s, `AI_HOOK_TIMEOUT_GIT_OPERATION_GATE` 10s->35s, `AI_HOOK_TIMEOUT_VALIDATE_PRELOAD` 15s->40s): 111 passaram, 0 falharam.
- `make check-hooks-sync check-scripts-sync` (rodada HOOKTIMEOUT-1) -> 0 drift, 28 hooks e 32 validadores em sync.
- `go build ./...`, `go vet ./...` (rodada HOOKTIMEOUT-1/TELEMETRY-RETRY-1) -> pass.
- `go test ./... -count=1` (rodada HOOKTIMEOUT-1/TELEMETRY-RETRY-1) -> 4304 passed em 92 pacotes.
- `go test ./internal/runtime/hooks/... -run TestDispatcher -v` (rodada TELEMETRY-RETRY-1) -> 10 passed, incluindo `TestDispatcher_TelemetryFailureDoesNotBlockDispatch`.
- `go test ./internal/telemetry/... ./internal/runtime/hooks/...` (rodada TELEMETRY-RETRY-1) -> 150 passed.
- `make lint` (rodada HOOKTIMEOUT-1/TELEMETRY-RETRY-1) -> 0 issues (uma execucao intermediaria falhou com `SA4006` em `dispatcher_test.go`, corrigida nesta mesma correcao antes do encerramento).
- `make coverage` (rodada HOOKTIMEOUT-1/TELEMETRY-RETRY-1) -> pass, total 82.0%.
- Reproducao manual pos-correcao: filho lento (`sleep 5` sob orcamento de 1s) negado em ~1s de parede; recursao pre-setada (`AI_HOOK_DEPTH=3 AI_HOOK_MAX_DEPTH=3`) nega `git-operation-gate.sh` mesmo com comando benigno; payload benigno de 1 MiB via `git-operation-gate.sh` real -> `exit 0` em ~12s, com `hook.duration_ms=12000 hook=git-operation-gate hook.timeout_s=35 hook.decision_exit=0` emitido em stderr.

## Riscos Residuais
- Nenhum para COVERAGE-1; o teste de desempenho permanece fora de `-race`, que valida correção sob instrumentação e não é uma medição de latência comparável.
- Nenhum para VALIDATIONLANG-1 dentro do escopo `internal/contextgen/contextgen.go`. `make coverage` (alvo completo do Makefile) esta quebrado por mudanca concorrente fora de escopo em `Makefile`/`internal/taskloop`; nao foi tocado por esta correcao e deve ser resolvido pela tarefa que esta alterando `Makefile` no momento.
- Nenhum para QUALITYGATE-1 no escopo `internal/qualitygate/`/`internal/runtime/runner.go`. `ComputeStateDigest` depende do binário `git` em PATH e do `WorkDir` ser um repositório git com pelo menos um commit; sem isso o digest retorna vazio e o fingerprint volta a depender apenas de `TaskID+TaskType+Risk+Lang+Toolchain` — a correção 1 (nunca mascarar `BLOCK`/`ERROR` cacheado) permanece válida de qualquer forma, pois independe do StateDigest. `cache.Put` continua gravando qualquer decisão, inclusive `BLOCK`; a filtragem foi implementada no ponto de leitura (`Gate.Evaluate`), que é onde o bypass de segurança se manifestava.
- JSONLREPAIR-1: o reparo automatico so cobre corrupcao isolada a ultima linha do arquivo (consistente com crash mid-append). Corrupcao no meio do arquivo continua fail-closed por design (nao coberto pela promessa "apenas o ultimo registro e perdido" — cenario mais grave, tratado como falha explicita). `createPersistence`/`NewSessionPersistence` nao foram alterados nesta correção (nao precisavam: ja propagavam corretamente o erro/sucesso de `NewJSONLWriter`); o reparo acontece inteiramente dentro de `jsonl.go`. Nao ha reparo para arquivos com mais de uma linha corrompida em sequencia no final (ex.: duas linhas parciais empilhadas por dois crashes consecutivos sem escrita valida entre eles) — esse caso tambem cai no fail-closed, o que e uma postura conservadora aceitavel.
- SANITIZE-CONNSTR-1: assim como no Go (`sanitization_policy.go`), o padrao `connection_string_credential` so redige URLs cujo usuario/senha nao contem `:`/`/`/`@`/espaco; um token ja redigido por um padrao anterior (ex.: `provider_token` dentro de uma URL `https://x-access-token:ghp_...@host`) nao e re-casado por este padrao especificamente porque o marcador `[REDACTED:...]` contem `:`, mas isso nao representa vazamento — o segredo original ja foi substituido pelo padrao anterior na mesma passada (mesma ordem de aplicacao usada no catalogo Go). O item opcional de alinhamento do `dotenv_secret` (`SESSION_ID`, `api_key=` minusculo) foi implementado por portar literalmente a mesma expressao regular do Go; a limitacao conhecida upstream de que `[A-Z][A-Z0-9_]*(?:...|SESSION_ID)` nao casa um nome de variavel que comeca exatamente com a palavra-chave sem nenhum prefixo (ex.: `SESSION_ID=valor` sozinho, sem um caractere antes de "SESSION_ID") existe identicamente no regex Go original e nao foi introduzida por esta correção; nao é uma regressao desta correção, mas fica registrado aqui pois foi descoberto durante a validação manual do item opcional.
- HOOKINV-1: a heuristica de deteccao de bloqueio e estatica (regex sobre o texto-fonte), nao execucao real do script; um hook que monta o codigo de saida dinamicamente por concatenacao de string em vez de uma atribuicao direta de variavel escapa da deteccao e cairia (conservadoramente) em `NAO BLOQUEANTE`. Nenhum hook atual do repositorio usa esse padrao (verificado por leitura de todos os 63 arquivos durante esta correcao).
- HOOKINV-2/HOOKINV-3: os dois mapas de perfil (`basenameHookProfiles`, `uniqueHookProfiles`) sao mantidos manualmente; uma origem de hook nova precisa de entrada dedicada ou cai no fallback `"NAO MAPEADO"` — comportamento deliberado (falha visivel, nao boilerplate silencioso), mas exige atencao humana na proxima rodada de inventario quando uma das nove origens ganhar um arquivo novo.
- HOOKINV-4: nenhum.
- HOOKINV-5: `syncSkillsLock`/`checkSkillsLock` cobrem apenas hooks canonicos (`.agents/*` e `scripts/git-hooks/pre-commit`) classificados `critical()`; os mirrors por provedor (`.claude/`, `.codex/`, `.github/`) nao ganham entrada propria em `skills-lock.json` porque ja sao cobertos por `make check-hooks-sync`/`check-scripts-sync` (paridade byte a byte com o canonico) — duplicar a cobertura em `skills-lock.json` adicionaria redundancia sem detectar nada que os gates de sync ja nao detectem. `internal/skillscheck.Service.Check` com `entry.Path` setado nao populamos `LockedVer`/`InstalledVer` (hooks nao tem conceito de versao semantica); `classifyDrift("", "")` sempre retorna `DriftUnknown`, que so vira falha via `!HashMatch` — comportamento correto para este caso de uso, mas quem ler `SkillVersionCheck.Drift` isoladamente para um hook sempre ve `unknown`, nunca `ok`/`minor`/`breaking`.
- HOOKINV-6: a reclassificacao de `.agents/scripts/validate-governance-references.sh` para `REMOVE` e apenas diagnostica (nenhum arquivo foi removido, provado pelo teste); a decisao final de remover esse script de fato, apos cobertura do invariante existir no core, permanece para uma tarefa futura fora do escopo desta correcao de bugs.
- Nota transversal desta rodada: os artefatos regenerados (`testdata/hook-inventory.json`, `docs/hook-inventory.md`, `skills-lock.json`) refletem o estado do repositorio no momento da ultima execucao de `go run . hooks inventory .` desta sessao, que ja incorporava edicoes concorrentes de outras tarefas em `.agents/hooks/validate-governance.sh`, `.agents/hooks/validate-preload.sh`, `.agents/lib/hook-payload.sh` e `.agents/scripts/git-operation-gate.sh`. Nenhum desses arquivos foi editado por esta correcao (fora do escopo `internal/hookinventory/`/`internal/skillscheck/`/`docs/hook-inventory.md`/`testdata/hook-inventory.json`/`skills-lock.json`); caso essas tarefas concorrentes ainda alterem o conteudo desses arquivos apos o encerramento desta correcao, `bash scripts/check-hooks-inventory.sh` volta a detectar o drift e uma nova execucao de `go run . hooks inventory .` e necessaria antes do merge final do PRD — isso e esperado e nao e uma falha desta correcao.
- Nenhum para GITGATE-VAREXPAND-1 no escopo `.agents/scripts/git-operation-gate.sh`/mirrors. O sinal `variable_expansion` ainda pode gerar falso positivo em um caso não coberto por teste: `sudo $VAR` ou `env $VAR` (variável usada dentro da posição de comando de um wrapper transparente) continua bloqueando mesmo quando `$VAR` de fato expande para algo inofensivo em runtime — isso é uma troca deliberada de fail-closed, consistente com a postura de ADR-003 e com o próprio histórico de bypass documentado no `6.0_execution_report.md` (`$G $P --force`), já que o classificador nunca executa o comando e não pode saber o valor real da variável. Durante esta correção, `.agents/scripts/git-operation-gate.sh` também recebeu, de uma tarefa concorrente (não desta correção), a adição de `hook_runtime_guard(...)` logo após o `source "$parse_lib"` (linhas ~29-31); essa adição foi preservada intacta no working tree e deliberadamente **não** incluída no commit desta correção (apenas o hunk do bloco Python do classificador, linhas ~296-313, foi staged via `git apply --cached` de um patch cirúrgico) para não misturar escopos de tarefas concorrentes no mesmo arquivo — cabe à tarefa responsável por essa adição commitá-la separadamente.

- HOOKTIMEOUT-1: (1) o classificador `python3` embutido em `git-operation-gate.sh` e genuinamente lento para um payload de ~1 MiB sem espacos (~11.2s medidos nesta maquina de desenvolvimento) — causa raiz preexistente e fora do escopo desta correcao (nao e sobre timeout/recursao, e sobre a complexidade algoritmica do tokenizador `shlex`/regex por caractere aplicado a um unico token gigante); esta correcao apenas dimensionou os orcamentos de timeout com margem generosa (30s/35s/40s) para nao introduzir falso-positivo sobre o caso legitimo ja coberto por `scripts/test-validators.sh`, mas nao investigou nem corrigiu a lentidao em si — uma maquina mais lenta que a de desenvolvimento (CI, container com CPU limitada) poderia ainda assim estourar o orcamento para paylaods legitimos proximos de 1 MiB; se isso ocorrer, o orcamento e ajustavel via `AI_HOOK_TIMEOUT_GIT_CLASSIFIER`/`AI_HOOK_TIMEOUT_GIT_OPERATION_GATE`/`AI_HOOK_TIMEOUT_VALIDATE_PRELOAD` sem exigir mudanca de codigo. (2) `hook_timeout_watch` mata apenas o PID direto do filho (ex.: o processo `bash` que roda `git-operation-gate.sh`, ou o processo `python3` do classificador); um neto de processo que o filho tenha lancado e que sobreviva ao pai morto (ex.: um sub-subprocesso ja destacado) nao e limpo automaticamente — isso e uma limitacao conhecida e aceita de implementacoes de timeout puro-bash sem `setsid`/grupo de processo dedicado, documentada aqui para transparencia; nao ha nenhum hook atual do repositorio cujo `Run` lance subprocessos destacados, entao o caso nao e exercitado na pratica. (3) `hook_recursion_guard` usa uma variavel de ambiente (`AI_HOOK_DEPTH`) propagada via heranca de processo filho — funciona corretamente para a cadeia real `validate-preload.sh -> hook-prereq-gate.sh`/`git-operation-gate.sh -> classificador python3` (mesma arvore de processos, herança de env garantida), mas **nao** protege contra recursao que atravesse a fronteira do proprio Claude Code (ex.: um hook que de alguma forma disparasse uma nova chamada de ferramenta atraves do host, reiniciando o ciclo PreToolUse do zero com um processo novo e env limpo do host) — esse segundo tipo de recursao, se existir, exigiria um guarda no nivel do proprio host/runtime, fora do alcance de um hook shell individual; nao ha evidencia de que esse caminho exista hoje no repositorio (hooks shell nao chamam de volta para o Claude Code).
- TELEMETRY-RETRY-1: nenhum. `hooks.NewWithTelemetry` e uma funcao nova, puramente aditiva; `hooks.New` (caminho de producao real, usado em `internal/runtime/runner.go:674`) permanece inalterado e continua usando `telemetry.NewHookTelemetry()` (writer de arquivo real). A funcao nova existe para permitir injecao de teste; nenhum caminho de producao foi alterado.
