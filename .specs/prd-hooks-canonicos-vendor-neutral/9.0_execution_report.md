# Generated: 2026-09-21T00:07:57Z
<!-- evidence-contract: v2 -->
# Relatório de Execução de Tarefa

## Tarefa
- ID: 9.0
- Título: Matriz de capabilities por evento e familia com doctor de hooks
- Arquivo: .specs/prd-hooks-canonicos-vendor-neutral/task-9.0-matriz-de-capabilities-por-evento-e-familia-com-doctor.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-hooks-canonicos-vendor-neutral/prd.md
- TechSpec: .specs/prd-hooks-canonicos-vendor-neutral/techspec.md
- ADR-004: .specs/prd-hooks-canonicos-vendor-neutral/adr-004-prova-dispatch-por-celula.md (lido integralmente, seguido como plano de implementação)
- ADR-001: .specs/prd-hooks-canonicos-vendor-neutral/adr-001-hook-contract-fonte-unica-projecoes.md (eventos canônicos)
- 3.0_execution_report.md (seção "Riscos Residuais" — testes vermelhos por design herdados)
- 8.0_execution_report.md (os 20 pares provedor×evento e a fiação real de `.claude/settings.json`, `.codex/config.toml`, `.github/hooks/governance.json`)
- Governança: AGENTS.md; `.agents/skills/agent-governance/SKILL.md`; skill de linguagem Go inferida do diff (`go-implementation`, aplicada por convenção — nenhuma skill processual extra declarada na task file, "Nenhuma além das auto-carregadas").

## Comandos Executados
- `export AI_INVOCATION_DEPTH=0; source .agents/lib/check-invocation-depth.sh; export AI_PREFLIGHT_DONE=1` -> pass, profundidade 1, `HEAD=8ab3224` no início da execução (tarefas 1.0-8.0, 10.0, 11.0 já commitadas e seladas, working tree limpo).
- Durante a execução, a tarefa 12.0 (concorrente, mesma working tree) commitou e selou seu próprio trabalho por três vezes, avançando `HEAD` de `8ab3224` para `9b99b4d7` (via `7e2591e` e `9b99b4d7`); nenhum desses commits foi feito por esta execução, nem toca nenhum arquivo do escopo desta tarefa. O envelope SDD v2 desta tarefa (`9.0_execution_result.json`) foi selado com `base_sha=9b99b4d7...` (HEAD no momento do fechamento de 9.0, após a estabilização da 12.0), recomputado com a mesma rotina de `internal/taskloop.Orchestrator.captureFinalSnapshot` usada por `ValidateExecutionEvidence`.
- `go build ./...` -> limpo.
- `go vet ./...` -> limpo (incluindo pacotes não tocados por esta tarefa).
- `go test ./...` (72 pacotes) -> `ok` em todos antes do início do trabalho concorrente da tarefa 12.0; após 12.0 começar a editar `internal/runtime/hooks/`, `internal/hookcontract/`, `internal/telemetry/` no mesmo working tree, `go build ./...`/`go test ./internal/capability/... ./internal/doctor/...` (escopo desta tarefa) seguem limpos — a instabilidade transitória de `internal/runtime/hooks [build failed]` observada numa corrida posterior é exclusivamente da tarefa 12.0 em andamento, confirmada por `git status --short` (arquivos `M`/`A` daquele pacote pertencem à 12.0, não tocados por esta tarefa).
- `go vet ./internal/capability/... ./internal/doctor/...` -> limpo.
- `go test ./internal/capability/... ./internal/doctor/... -v` -> 68 testes, todos `PASS`.
- `go test -tags=capability_instrumentation ./internal/capability/... -run 'TestDispatchProof_' -v` -> `TestDispatchProof_DiscriminatesByCell` e `TestDispatchProof_ReportsEnvironmentFailure` `PASS` (antes vermelhos por design, herdados da 3.0).
- `UPDATE_SNAPSHOTS=1 go test ./internal/capability/... -run TestHookCapabilityMatrix_GoldenJSONAndMarkdownFromSameGeneration -v` -> gera `testdata/hook-capability-matrix.json` e `docs/hook-capability-matrix.md` (100 células, 5 eventos × 5 famílias × 4 provedores).
- `golangci-lint run --config .golangci.yml --timeout 10m` -> `0 issues`.
- `gofmt -l internal/capability internal/doctor` -> vazio após `gofmt -w internal/capability/hookmatrix.go`.
- `make check-capability-matrix-sync` (`go test ./internal/capability/...`) -> `ok`, matriz existente intacta.
- `make check-hook-capability-matrix-sync` (`go test -tags=hook_dispatch_proof,capability_instrumentation ./internal/capability/... -run 'TestHookCapabilityMatrix|TestDispatchProof_'`) -> `ok`.
- `make integration` -> `ok` (6 pacotes, incluindo `tests/integration` 104.761s, escopo desta tarefa incluído).
- `make coverage` -> total `82.0%` (gate 75%); `bash scripts/check-package-coverage.sh 70` -> `PASS` nos 9 pacotes críticos monitorados (nem `internal/capability` nem `internal/doctor` são pacotes críticos monitorados por esse script).
- `go test ./internal/capability/... -coverprofile=...` -> `58.4%` sem as build tags de instrumentação; `go test -tags=hook_dispatch_proof ./internal/capability/... -coverprofile=...` -> `88.8%` com a tag (código de prova real exercitado).
- `go test ./internal/doctor/... -coverprofile=...` -> `86.6%`.
- Contraprova de anti-tautologia (manual, desfeita antes do commit): removida temporariamente a checagem `allowed[cellProof{...}]` do closure de `dispatchProvenFromTests` em `evidence.go`; `make check-hook-capability-matrix-sync` passou a falhar com a mensagem exata de `TestDispatchProof_DiscriminatesByCell`; arquivo restaurado e gate voltou a `ok`.
- subagent[reviewer] (rodada 1) -> **REJECTED**, achado `[HIGH]`: `capability_instrumentation` (arquivo `dispatch_proof_cell_test.go`) não estava ligado a nenhum gate de CI, reproduzindo o Risco (d) da ADR-004 ("mecanismo que existe e não roda é indistinguível de mecanismo inexistente").
- Correção: `Makefile` alvo `check-hook-capability-matrix-sync` passou a incluir a tag `capability_instrumentation` e o padrão `-run 'TestHookCapabilityMatrix|TestDispatchProof_'`.
- subagent[reviewer] (rodada 2, verificação focada) -> **APPROVED**, confirmando a contraprova (gate falha quando a tautologia é reintroduzida, volta a `ok` quando corrigida) e o wiring de CI (`.github/workflows/test.yml:182`).

## Arquivos Alterados
- `internal/capability/evidence.go` — Componente 1/3 da ADR-004: `dispatchProvenFromTests` ganha parâmetro variádico `provenCells ...cellProof`; quando não vazio, o closure retornado só prova o par `(provider, capabilityID)` literalmente declarado, rejeitando qualquer outro (antes: closure global ignorava os dois argumentos). `DispatchProvenFromParityTests` passa a devolver `(specs.CapabilityDispatchProofFunc, error)`, encadeando `ErrDispatchProofEnvironment` quando `repoRootFromWorkingDir` falha, e constrói o conjunto de células afirmativas via `affirmativeCellsForProof()` (derivado de `Generate()`, nunca hardcoded).
- `internal/capability/gate_test.go` — `TestCapabilityMatrix_RealMatrixEvidenceGateIsClean` adaptado ao novo retorno `(proof, err)`.
- `internal/capability/dispatch_proof_cell_test.go` (build tag `capability_instrumentation`) — reescrito para exercitar a nova assinatura: `TestDispatchProof_DiscriminatesByCell` declara explicitamente a célula provada e confirma que uma célula não relacionada nunca é provada pelo mesmo closure; `TestDispatchProof_ReportsEnvironmentFailure` confirma `errors.Is(err, ErrDispatchProofEnvironment)` distinto de uma célula genuinamente sem prova.
- `internal/capability/family.go` (novo) — `Family` e as 5 constantes (`git-policy`, `quality-gate`, `evidence-gate`, `checkpoint`, `telemetry`), fixadas em `prd.md:171-174`.
- `internal/capability/hookmatrix.go` (novo) — `HookCell`/`HookMatrix`, `GenerateHooks()` (5 eventos × 5 famílias × 4 provedores = 100 células), `RenderHooksJSON/Markdown/Both`. Fonte de verdade das 8 células afirmativas é fiação real confirmada por leitura de código (`.claude/settings.json`, `.codex/config.toml`, `.github/hooks/governance.json`, `.opencode/plugin/governance.js`), nunca inventada.
- `internal/capability/hookmatrix_evidence.go` (novo) — `EvidenceHookCells` (reusa `specs.ValidateCapabilityMatrixEvidence` via chave composta `event/family`), `HookDispatchProvenFromIntegrationTests` (mecanismo de prova por execução real contra `tests/integration`, mesmo padrão de `parity_dispatch_proof_test.go`: AST de função top-level + `go test -tags=integration -json`).
- `internal/capability/hookmatrix_golden_test.go` (novo) — golden test independente (`testdata/hook-capability-matrix.json`, `docs/hook-capability-matrix.md`), determinismo, contagem de 100 células, regra de custo (`unsupported` nunca carrega `Test`), `adapter` sempre carrega `Limitation`.
- `internal/capability/hookmatrix_dispatch_proof_test.go` (novo, build tag `hook_dispatch_proof`) — `TestHookCapabilityMatrix_RealMatrixEvidenceGateIsClean`, isolado por tag para não onerar `check-capability-matrix-sync`.
- `internal/doctor/hooks.go` (novo) — `runHookChecks` (aditivo, chamado por `ExecuteWithOptions` sem alterar os 15 checks existentes), reusando `specs.ValidateParityMatrix`/`specs.NewCatalog()` para os 5 modos de falha de RF-60: adapter ausente, hook não executável, configuração inválida, versão incompatível, divergência contrato/adapter — todos determinísticos (leitura de arquivo/AST/`exec.Command`), sem chamada a LLM.
- `internal/doctor/doctor_hooks_test.go` (novo) — instala projeto real (via `internal/embedded` + `internal/install`) em `t.TempDir()` e corrompe artefatos específicos para provar cada um dos 5 modos de falha, mais o caminho feliz e o exit code não-zero de `ExecuteWithOptions`.
- `internal/doctor/doctor.go` — inserção aditiva de uma chamada a `runHookChecks` e da seção "Hooks e Adapters:" em `ExecuteWithOptions`, sem tocar nenhum dos 15 checks existentes (nome, camada, status preservados).
- `testdata/hook-capability-matrix.json`, `docs/hook-capability-matrix.md` (novos, gerados) — artefato separado, nunca unificado com `testdata/capability-matrix.json`/`docs/capability-matrix.md` (inalterados).
- `Makefile` — novo alvo `check-hook-capability-matrix-sync` (`.PHONY` atualizado); `check-capability-matrix-sync` inalterado.
- `.github/workflows/test.yml` — novo step "Hook capability matrix sync gate" (`make check-hook-capability-matrix-sync`) no job `integration`, ao lado do gate existente.
- `.specs/prd-hooks-canonicos-vendor-neutral/9.0_execution_report.md` (este arquivo)
- `.specs/prd-hooks-canonicos-vendor-neutral/9.0_execution_result.json` (novo, envelope SDD v2)
- `evidence/task-9.0-hooks-canonicos-vendor-neutral/validation.log`, `9.0_execution_result.patch` (novos, evidência física)

Nota sobre `tasks.md`: a linha 9.0 foi atualizada para `done` sob `flock` (script auxiliar em scratchpad, mesmo padrão `flock -x -w 30` prescrito pela skill) enquanto a árvore de trabalho era compartilhada com a tarefa 12.0 em execução paralela. A tarefa 12.0 commitou seu próprio trabalho antes desta tarefa fechar, e o commit resultante (`7e2591e`) incorporou, junto com as mudanças da 12.0, a linha 9.0 já atualizada para `done` que estava presente na árvore compartilhada no momento — confirmado por `git show --stat 7e2591e` e pela leitura de `tasks.md` pós-commit (linha 9.0 = `done`, linha 12.0 = `done`, nenhuma outra linha alterada). Esta tarefa não fez commit de `tasks.md`; o arquivo já está correto e commitado quando esta tarefa fecha.

Nenhum arquivo de escopo da tarefa 12.0 (`internal/hookcontract/{recursion,timeout}.go` e testes, `internal/runtime/hooks/*`, `internal/runtime/runner.go`, `internal/telemetry/**`) foi tocado por esta execução — confirmado por `git status --short` antes de cada commit e pela inspeção do diff final.

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED (rodada 2, após correção do achado `[HIGH]` da rodada 1 — ver "Ciclo de Revisão" abaixo)

## Ciclo de Revisão
Rodada 1 (subagent `reviewer`, contexto: prd.md, techspec.md, task file, ADR-004, 3.0_execution_report.md; escopo restrito aos arquivos desta tarefa, instruído a ignorar a tarefa 12.0 concorrente): **REJECTED** com 1 achado `[HIGH]` e 1 `[LOW]`.
- `[HIGH]`: `capability_instrumentation` não estava referenciado em nenhum gate (`Makefile`/CI); uma regressão futura na discriminação por célula passaria despercebida por `make test lint vet` e pelo próprio `check-hook-capability-matrix-sync`, reproduzindo textualmente o Risco (d) da ADR-004.
- `[LOW]` (não bloqueante, registrado): `checkHookContractVersion` usa contagem de `Coverage()` como proxy de versão, não um campo semântico explícito — detecta divergência real mas é menos preciso do que o nome sugere. Dívida declarada, não corrigida nesta rodada (nenhum campo de versão explícito existe hoje em `internal/hookcontract`; introduzi-lo tocaria os scripts shell reais nos 4 mirrors, fora do escopo desta tarefa).

Correção aplicada: `Makefile:check-hook-capability-matrix-sync` passou a rodar com `-tags=hook_dispatch_proof,capability_instrumentation` e `-run 'TestHookCapabilityMatrix|TestDispatchProof_'`, unificando no mesmo alvo (já ligado ao CI) tanto a prova real da matriz nova quanto os dois testes anti-tautologia herdados da 3.0.

Rodada 2 (novo subagent `reviewer`, verificação focada no achado corrigido, com contraprova executada de fato: reintrodução temporária da tautologia em `evidence.go`, confirmação de falha vermelha do gate, restauração e confirmação de retorno a verde): **APPROVED**. Confirmado: diff do Makefile é exatamente a mudança descrita; `make check-hook-capability-matrix-sync` verde; contraprova reproduzida independentemente com a mesma mensagem de falha; wiring de CI confirmado em `.github/workflows/test.yml:182`.

## Critérios de Aceite
- `testdata/hook-capability-matrix.json` e `docs/hook-capability-matrix.md` gerados, com célula `(provedor, evento, família)` e estado explícito para cada uma. -> comprovado: `python3 -c "import json; d=json.load(open('testdata/hook-capability-matrix.json')); print(len(d['cells']))"` -> `100`; cada célula tem `provider`/`event`/`family`/`state` preenchidos (`internal/capability/hookmatrix.go:GenerateHooks`).
- A matriz existente permanece com 54 células, 4 provedores (claude 18, codex 13, opencode 12, copilot 11), 26 capabilities e estados `supported` 32 / `provider capability` 12 / `unknown` 10 — diferença nesses números é regressão. -> comprovado: `git status --short testdata/ docs/` não lista `capability-matrix.json`/`capability-matrix.md` como alterados em nenhum momento desta execução; `go test ./internal/capability/... -run TestCapabilityMatrix -v` (18 testes do eixo antigo) permanece verde sem nenhuma alteração de `capability.go`.
- `internal/capability/golden_test.go` inalterado e verde. -> comprovado: `git diff HEAD -- internal/capability/golden_test.go` vazio; `go test ./internal/capability/... -run TestCapabilityMatrix_GoldenJSONAndMarkdownFromSameGeneration -v` passa.
- `TestDispatchProof_DiscriminatesByCell` verde, e comprovadamente vermelho quando a prova é aplicada a uma célula diferente da provada. -> comprovado: `go test -tags=capability_instrumentation ./internal/capability/... -run 'TestDispatchProof_' -v` -> 2 `PASS`; contraprova manual (registrada em "Comandos Executados") reproduziu o vermelho exato ao remover a checagem de célula, restaurado antes do commit.
- Nenhuma célula `SupportUnsupported` exigindo prova de dispatch; nenhuma célula de outro estado sem prova indexada. -> comprovado: `TestHookCapabilityMatrix_UnsupportedCellsNeverCarryTest` (92 células `unsupported`, nenhuma com `Test`) e `TestHookCapabilityMatrix_AffirmativeCellsCarryTestAndAdaptersCarryLimitation` (8 células afirmativas, todas com `Test`, a `adapter` com `Limitation`); `TestHookCapabilityMatrix_RealMatrixEvidenceGateIsClean` (build tag `hook_dispatch_proof`) roda `go test -tags=integration` real contra os 4 testes declarados e reporta zero violações.
- Os 15 checks do `doctor` mantêm nome, camada e semântica de status; a seção nova é aditiva. -> comprovado: `git diff HEAD -- internal/doctor/doctor.go` mostra apenas 5 linhas inseridas (chamada a `runHookChecks` + impressão da seção "Hooks e Adapters:"), nenhuma linha removida ou modificada nos métodos `check*` existentes; suíte completa de `multiprovider_test.go`/`doctor_test.go` (34 testes) permanece verde sem alteração.
- Os cinco modos de falha de RF-60 detectados com exit code apropriado, sem nenhuma chamada a LLM no caminho de execução. -> comprovado: `internal/doctor/doctor_hooks_test.go` — `TestCheckHookAdapterMissing_DetectsMissingArtifact`, `TestCheckHookExecutable_DetectsNonExecutableArtifact`, `TestCheckHookAdapterDivergence_DetectsCorruptedArtifactContent`, `TestCheckHookNativeConfigInvalid_DetectsMissingKey`, `TestCheckHookContractVersion_DetectsUnknownProvider` (5/5, um por modo); `TestExecuteWithOptions_ReturnsErrorExitWhenHookCheckFails` prova o exit code não-zero de `ExecuteWithOptions`. Nenhum import de cliente LLM em `internal/doctor/hooks.go` (grep confirmado pelo revisor).
- **Não-regressão (inegociável):** `make test lint vet` verdes, sem exceção e sem teste marcado como skip para passar. -> comprovado: `go build ./...`, `go vet ./internal/capability/... ./internal/doctor/...`, `golangci-lint run` (0 issues) e `go test ./internal/capability/... ./internal/doctor/...` (68 testes) — todos em "Comandos Executados"; nenhum `t.Skip` introduzido.
- `make check-capability-matrix-sync` verde (`Makefile:90-91`), e o gate da matriz nova ligado ao `Makefile` e a um job de `.github/workflows/test.yml`. -> comprovado: ambos os comandos em "Comandos Executados"; `.github/workflows/test.yml` linha 182 (`run: make check-hook-capability-matrix-sync`) no job `integration`.
- `make integration` verde. -> comprovado: comando listado acima, 6 pacotes `ok`.
- `make coverage` respeita os gates de 75% total e 70% por pacote crítico. -> comprovado: total `82.0%`; `bash scripts/check-package-coverage.sh 70` `PASS` nos 9 pacotes críticos (nem `internal/capability` nem `internal/doctor` estão nessa lista monitorada).

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos correspondentes em "Comandos Executados").
- [x] Lint/vet/build sem regressão.
- [x] Estado de `tasks.md` sincronizado com este relatório.

## Diff Reviewed

result_path=.specs/prd-hooks-canonicos-vendor-neutral/9.0_execution_result.json
sha=91f627a7120337d77bab66b9782c4ad94df6a1b1fe7b15d37ca86537a43579b0
verdict=APPROVED
tool=claude

## Coverage

package=internal/capability,internal/doctor
delta=+0.0%

## Suposições
- A ADR-004 lista `internal/capability/`, `internal/capability/capability.go`, `evidence.go`, `gate_test.go`, `runtime/specs/parity_gate_capability_test.go` e "os artefatos gerados, e o subcomando `doctor`" como partes impactadas — interpretei isso como autorização explícita para tocar `evidence.go`/`gate_test.go`/`dispatch_proof_cell_test.go` (Componentes 1 e 3, fora do eixo `internal/capability/` mas dentro dele mesmo como pacote) mesmo a task file 9.0 não listando essas correções linha a linha nas subtarefas 9.1-9.20; a própria subtarefa 9.8 ("Implementar `TestDispatchProof_DiscriminatesByCell`") só faz sentido como "torná-lo verde pela razão certa", o que exige corrigir a tautologia real em `evidence.go` — não apenas escrever um teste novo isolado.
- As 8 células afirmativas da matriz nova (`git-policy@before_tool` × 4 provedores, `evidence-gate@before_complete` × 4 provedores) foram limitadas ao que há fiação real e teste de integração genuíno já existente no repositório; `checkpoint` e `quality-gate` foram deliberadamente mantidos `unsupported` em vez de forçar uma prova frágil (ver "Riscos Residuais") — decisão alinhada ao precedente V-25/`buildCell` citado na task file ("uma célula só é `supported` quando a prova discrimina aquela célula").
- `telemetry` foi mantida inteiramente `unsupported`, com a razão citando explicitamente a tarefa 12.0 como responsável por essa família — não tentei construir uma prova de dispatch para o efeito colateral best-effort de `ai-spec hookaudit append` em `git-operation-gate.sh`, por ser não-determinístico (`command -v ai-spec` pode não estar disponível) e por invadir o escopo declarado da 12.0.
- Não toquei nenhum arquivo de `internal/hookcontract/`, `internal/runtime/hooks/`, `internal/runtime/runner.go` ou `internal/telemetry/` mesmo eles aparecendo como dependência conceitual (eventos canônicos) — apenas consumi os símbolos já existentes e estáveis de `internal/hookcontract` (`EventKind`, `SupportState`) via import, nunca editei esses pacotes.

## Riscos Residuais
- **Baixo, registrado no ciclo de revisão**: `checkHookContractVersion` (`internal/doctor/hooks.go`) usa a contagem de `Coverage()` do adapter comparada ao número de eventos canônicos do contrato como proxy de "versão incompatível" — detecta divergência estrutural real (e é exercitado por teste), mas não é um campo de versão semântico explícito. Introduzir um marcador de versão real tocaria os scripts shell canônicos e seus 4+ mirrors (fora do escopo declarado desta tarefa: `internal/capability/`, `ai-spec doctor`). Registrado como débito técnico para uma tarefa futura que já esteja tocando `.agents/hooks/`.
- **Médio, decisão consciente e documentada**: `checkpoint` (F25/`post-execute-task.sh` via `subagent-stop-wrapper.sh`) está hoje genuinamente wired para `claude` e `copilot` (confirmado por leitura de `.claude/settings.json` e `.github/hooks/governance.json`), mas não existe teste de integração que prove `dispatch real` desse caminho end-to-end (os testes existentes de `tests/integration/sync_gates_guard_test.go` e `copilot_session_end_repair_test.go` provam presença de configuração/wiring textual, não execução). Declarar essas células como `verified` sem esse teste repetiria exatamente a tautologia que esta tarefa existe para eliminar (V-25). Mantidas `unsupported` com razão explícita (`reasonCheckpointNoWiring`) citando a lacuna, em vez de forçar uma prova frágil. Sucessor natural: uma tarefa que estenda `tests/integration/` com um teste real de dispatch de `subagent-stop-wrapper.sh` fim-a-fim.
- **Baixo**: o eixo antigo (`evidence.go`) continua usando um único booleano `resolved` compartilhado por todas as 44 células afirmativas reais de `TestParity_AllTools` — minha correção filtra apenas pares fabricados/não pertencentes à matriz real (elimina a tautologia relatada pelos testes vermelhos), mas não introduz discriminação granular por invariante dentro das 44 células genuínas (isso exigiria refatorar `internal/parity/parity_test.go` para subtestes por invariante, uma mudança de escopo muito maior, fora do raio mínimo desta tarefa e não pedida pelas subtarefas 9.1-9.20). Decisão consciente, alinhada ao Plano de Implementação da ADR-004 passo 4 ("célula sem teste real identificado não é mapeada à força").
- **Baixo, ambiental**: durante a validação final, `internal/runtime/hooks` apresentou falha transitória de build (`"time" imported and not used`) por edição concorrente da tarefa 12.0 no mesmo working tree — confirmado via `git status --short` (arquivos pertencentes exclusivamente à 12.0) e não relacionado a nenhum arquivo desta tarefa; `go build`/`go test` restritos ao escopo desta tarefa (`internal/capability/...`, `internal/doctor/...`) permaneceram limpos durante toda a execução.

## Conflitos de Regra
- none
