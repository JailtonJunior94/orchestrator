# Generated: 2026-09-20T19:11:35Z
<!-- evidence-contract: v2 -->
# Relatório de Execução de Tarefa

## Tarefa
- ID: 2.0
- Título: Parsing de payload sem truncamento e negacao por ausencia de alvo
- Arquivo: .specs/prd-hooks-canonicos-vendor-neutral/task-2.0-parsing-de-payload-sem-truncamento-e-negacao-por-ausencia.md
- Estado: blocked

## Contexto Carregado
- PRD: .specs/prd-hooks-canonicos-vendor-neutral/prd.md
- TechSpec: .specs/prd-hooks-canonicos-vendor-neutral/techspec.md
- ADR: .specs/prd-hooks-canonicos-vendor-neutral/adr-003-parsing-estrutural-sem-truncamento.md
- Governança: AGENTS.md; `.claude/rules/code-style.md` (R-STYLE-001); `.claude/rules/governance.md` (R-GOV-001); go-implementation (checks de doctor); skill `Nenhuma além das auto-carregadas` conforme task file.

## Comandos Executados
- `source .agents/lib/check-invocation-depth.sh; export AI_PREFLIGHT_DONE=1` -> pass, profundidade 0.
- Inspeção do estado inicial (fragmento não finalizado): `.agents/lib/hook-payload.sh`, `.agents/lib/parse-hook-input.sh` (shim), `.agents/scripts/git-operation-gate.sh` já continham a correção base (sem `head -c`, sem fallback grep, `exit 2` em comando vazio).
- Correção de defeito adicional encontrado: fallback `jq -er` em `hook-payload.sh` confundia "JSON válido sem campo casado" com "JSON inválido" (ambos exit 1). Reescrito para validar JSON com `jq -e .` isoladamente e extrair com `jq -r` sem `-e`.
- Reprodução manual do exploit do ADR-003 (payload ~70KB) contra `.agents/scripts/git-operation-gate.sh` -> `exit=2` (antes da correção seria `exit=0`); controle `git push origin main` -> `exit=2`; comando benigno -> `exit=0`; payload 1 MiB benigno -> `exit=0`; payload 1 MiB malicioso -> `exit=2`; JSON inválido -> `exit=2`; comando homônimo aninhado -> `exit=2` (não extraído, negado por ausência).
- `bash scripts/sync-skills.sh` (múltiplas rodadas) -> pass, propagou `hook-payload.sh` para `scripts/lib/`, `internal/embedded/assets/.agents/lib/`, `internal/embedded/assets/scripts/lib/` (mirror antes divergente e sem gate).
- `bash scripts/check-skills-sync.sh` -> pass, "Libs em sync: 3 libs x 3 mirrors".
- `bash scripts/check-scripts-sync.sh` -> pass, "Validadores em sync: 32".
- `bash scripts/check-hooks-sync.sh` -> pass, "Hooks em sync: 28", "Validadores por ferramenta com drift: 0".
- Descoberta de regressão real: `validate-preload.sh` chama `git-operation-gate.sh` incondicionalmente para todo payload (Write/Edit/Read sem campo `command`). Negar `command_text` vazio no gate, sem guarda no chamador, bloqueava toda tool call não-Bash. Corrigido guardando a chamada em `validate-preload.sh` com `if [[ -n "$command_text" ]]`, preservando o contrato do script (negação em invocação direta, comprovado por `scripts/test-hooks.sh` G4-4) sem introduzir falso-positivo generalizado.
- `go build ./...` -> pass.
- `go vet ./...` -> pass, sem saída.
- `GOGC=20 golangci-lint run --config .golangci.yml --timeout 10m` -> pass, "No issues found".
- `go test ./internal/doctor/...` -> pass, 34 testes (inclui `TestCheckHookInterpreter` novo, table-driven).
- `go test ./internal/install/... ./internal/upgrade/... ./internal/uninstall/... ./internal/doctor/...` -> pass, 241 testes.
- `go test $(go list ./... | grep -v internal/skills$)` -> pass (todos os pacotes exceto `internal/skills`, que tem falha pré-existente não relacionada — ver Riscos Residuais).
- `bash scripts/test-validators.sh` (via `make test-validators`) -> pass, 53 asserts OK; inclui nova "Matriz de fronteira do gate de operação Git (RF-57)" (1 KiB/64 KiB/65 KiB/1 MiB × benigna/maliciosa), reprodução do exploit do ADR-003, falha fechada em JSON inválido, negação por ausência de alvo (RF-68), e regressão do fallback grep (comando homônimo aninhado).
- `make test-validators` completo (inclui suítes de `tests/scripts/`) -> pass, "60 passaram, 0 falharam" + validadores adicionais OK.
- `bash scripts/test-hooks.sh` (via `make test-hooks`) -> pass, 63 asserts OK; inclui novo "Bloco 4: git-operation-gate.sh" (G4-1 a G4-6: benigno, git push bloqueia, opt-out explícito, comando vazio nega, payload ~70KB do ADR-003 bloqueia, JSON inválido bloqueia).
- `go test -tags=integration ./tests/integration/... -run TestHookPayloadBoundary -v` -> pass, 16 subtestes (matriz de fronteira, reprodução ADR-003, JSON inválido, ausência de alvo, homônimo aninhado, ausência de interpretador).
- `go test -tags=integration ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/parity/... ./cmd/ai_spec_harness/...` -> 1241 passaram, 2 falharam (mesma falha pré-existente e não relacionada de `internal/skills`), 4 skipped (ausência de `node` em alguns ambientes, esperado).
- `git stash` (isolando todas as mudanças desta tarefa) + `go test -tags=integration ./internal/skills/... -run TestAllLangsAreExhaustivelyWired` -> falha de build idêntica (`undefined: contextgen.ValidationLangOrder`), confirmando que a falha é pré-existente e não introduzida por esta tarefa; `git stash pop` restaurado.
- `bash scripts/check-hooks-inventory.sh` -> falhou inicialmente (`docs/hook-inventory.md` ausente do working tree, apesar do gate ter sido recém-ligado ao CI em `.github/workflows/test.yml` por um fragmento anterior não finalizado); corrigido com `go run . hooks inventory .` (regenera deterministicamente) -> `bash scripts/check-hooks-inventory.sh` -> pass (exit 0).
- `make coverage` (via `go test -coverprofile=... $(go list ./... | grep -v internal/skills)` + `go tool cover -func`) -> total 82.1% (> 75%); `make coverage` completo (`./...`) é abortado pelo Make no pacote `internal/skills` (falha pré-existente não relacionada) antes de calcular o total — ver Riscos Residuais.
- `bash scripts/check-package-coverage.sh 70` -> pacotes críticos tocados por esta tarefa não incluem `internal/doctor`; pacotes críticos existentes (`parity`, `specdrift`, `evidence`, `taskloop`, `bugschema`) reportam PASS; a execução completa do script é interrompida (via `pipefail`) no mesmo pacote pré-existente quebrado, antes de alcançar `metrics`/`detect`/`durable`.

## Arquivos Alterados
- `.agents/lib/hook-payload.sh` (novo, corrigido: ambiguidade JSON-válido-vazio vs JSON-inválido no fallback `jq`)
- `.agents/lib/parse-hook-input.sh` (shim para `hook-payload.sh`, já presente no fragmento inicial)
- `.agents/scripts/git-operation-gate.sh` (já presente no fragmento inicial: sem `head -c`, nega comando vazio)
- `.agents/hooks/validate-preload.sh` (guarda a invocação de `git-operation-gate.sh` a `command_text` não vazio, evitando bloqueio de toda tool call sem comando de shell)
- `.claude/hooks/validate-governance.sh`, `.codex/hooks/validate-governance.sh`, `.github/hooks/validate-governance.sh` (mirrors, já presentes no fragmento inicial)
- `.claude/scripts/git-operation-gate.sh` (mirror, já presente no fragmento inicial)
- `internal/embedded/assets/.agents/hooks/validate-preload.sh` (sincronizado manualmente — não cai no laço `orchestrator_hooks` de `sync-skills.sh`)
- `internal/embedded/assets/.agents/hooks/validate-governance.sh`, `internal/embedded/assets/.claude/hooks/validate-governance.sh`, `internal/embedded/assets/.codex/hooks/validate-governance.sh`, `internal/embedded/assets/.github/hooks/validate-governance.sh` (mirrors)
- `internal/embedded/assets/.agents/scripts/git-operation-gate.sh`, `internal/embedded/assets/.claude/scripts/git-operation-gate.sh` (mirrors)
- `internal/embedded/assets/.agents/lib/hook-payload.sh`, `internal/embedded/assets/scripts/lib/hook-payload.sh` (novos, mirrors)
- `internal/embedded/assets/.agents/lib/parse-hook-input.sh`, `internal/embedded/assets/scripts/lib/parse-hook-input.sh` (shim)
- `scripts/lib/hook-payload.sh` (novo, mirror legado)
- `scripts/lib/parse-hook-input.sh` (shim, mirror legado)
- `scripts/sync-skills.sh` (propaga `hook-payload.sh` também para `internal/embedded/assets/scripts/lib/`, mirror antes divergente e sem gate)
- `scripts/check-skills-sync.sh` (adiciona `hook-payload.sh` a `required_libs` e `internal/embedded/assets/scripts/lib` a `lib_mirrors`)
- `scripts/test-validators.sh` (matriz de fronteira RF-57/RF-68, reprodução do exploit, falha fechada, negação por ausência, regressão do fallback grep)
- `scripts/test-hooks.sh` (Bloco 4: cenários do gate de operação Git)
- `tests/integration/hook_payload_boundary_test.go` (novo, build tag `integration`, matriz completa + falha fechada + ausência de interpretador)
- `tests/integration/opencode_plugin_dispatch_test.go` (`writeCanonicalScripts` passa a incluir `.agents/lib/hook-payload.sh`, ausente na lista fixa e causando falha "hook-payload.sh ausente" nos testes do plugin OpenCode)
- `internal/doctor/doctor.go` (`checkHookInterpreter`, campo injetável `lookPath`)
- `internal/doctor/doctor_test.go` (`TestCheckHookInterpreter` table-driven; ajuste de `setupService`/`lookPathOnly`)
- `internal/install/install.go` (`agentsLibFiles` inclui `hook-payload.sh`; cópia de `scripts/lib/hook-payload.sh` no instalador Claude)
- `internal/upgrade/upgrade.go` (sincroniza `scripts/lib/hook-payload.sh` no upgrade)
- `internal/uninstall/uninstall.go` (remove `scripts/lib/hook-payload.sh` na remoção legada)
- `docs/hook-inventory.md` (novo, gerado; corrige gate `check-hooks-inventory` recém-ligado ao CI por trabalho anterior, mas nunca commitado)

Não alterados nesta tarefa (estado pré-existente e não relacionado, confirmado por `git stash`): `internal/contextgen/contextgen.go`, `internal/detect/toolchain.go`, `internal/skills/skills.go`, `internal/skills/lang_exhaustive_wiring_test.go`, `internal/taskloop/isolation.go`, `internal/taskloop/isolation_test.go`, `internal/taskloop/orchestrator.go`, `internal/taskloop/orchestrator_test.go`, `internal/triggers/triggers.go`, `internal/triggers/triggers_test.go`, `internal/runtime/memory/durable/facade_bench_test.go`, `docs/cli-schema.json`, `cmd/ai_spec_harness/root.go`, `Makefile`, `.github/workflows/test.yml`, `.specs/prd-hooks-canonicos-vendor-neutral/techspec.md`, `.specs/prd-hooks-canonicos-vendor-neutral/adr-003-parsing-estrutural-sem-truncamento.md`.

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- A prova de exploração de 2.1, re-executada sem alteração, retorna exit 2. O controle continua em exit 2. Nenhum payload, em nenhum tamanho da matriz, produz `exit 0` com `git push`. -> comprovado: `scripts/test-validators.sh` "Reprodução da prova de exploração do ADR-003" e "Matriz de fronteira" (1 KiB/64 KiB/65 KiB/1 MiB) todas com `exit=2` para variante maliciosa e `exit=0` para benigna; `tests/integration/hook_payload_boundary_test.go` (`TestHookPayloadBoundaryMatrixBlocksGitPushRegardlessOfSize`, `TestHookPayloadBoundaryReproducesADR003ExploitAndControl`) confirmam via subprocesso real.
- `.agents/lib/hook-payload.sh` não contém `head -c` nem qualquer limite de leitura, e não contém fallback textual por `grep`. -> comprovado: `grep -c "head -c\|grep -o" .agents/lib/hook-payload.sh` retorna 0; leitura via `json.load(sys.stdin)`/`jq` sobre stdin completo.
- Parse estrutural falho bloqueia com mensagem de causa em stderr; nenhum caminho leva a `exit 0`. -> comprovado: `scripts/test-hooks.sh` G4-6 (JSON inválido, exit 2, mensagem em stderr); `TestHookPayloadBoundaryFailsClosedOnInvalidJSON`.
- `.agents/scripts/git-operation-gate.sh` nega com comando vazio, coerente com `.agents/hooks/validate-preload.sh:109`. -> comprovado: invocação direta do script com `{}`/stdin vazio retorna `exit=2` (`scripts/test-validators.sh` "Negação por ausência de alvo", `scripts/test-hooks.sh` G4-4, `TestHookPayloadBoundaryDeniesByAbsenceOfTarget`). O chamador de produção (`validate-preload.sh`) foi ajustado para só invocar o gate quando há `command_text` extraído, preservando o contrato do script sem transformar toda tool call sem comando de shell em bloqueio — regressão real detectada e corrigida (ver Comandos Executados).
- Os espelhos em `.claude/`, `.codex/`, `.github/` e `internal/embedded/assets/` são byte-idênticos ao canônico. -> comprovado: `bash scripts/check-hooks-sync.sh` ("Drift / missing: 0"), `bash scripts/check-scripts-sync.sh` ("Drift / missing: 0"), `bash scripts/check-skills-sync.sh` ("Skills com drift: 0", "Libs em sync: 3 libs x 3 mirrors").
- `ai-spec doctor` reporta a ausência de `python3`/`jq` como check próprio. -> comprovado: `internal/doctor/doctor.go` `checkHookInterpreter()` adicionado a `runCoreChecks`; `TestCheckHookInterpreter` (4 casos: python3, jq sem python3, ambos, nenhum) passou em `go test ./internal/doctor/...`.
- `scripts/test-validators.sh` cobre as quatro fronteiras (1 KiB, 64 KiB, 65 KiB, 1 MiB) — cobertura que hoje não existe. -> comprovado: seção "Matriz de fronteira do gate de operação Git (RF-57)" adicionada, 8 asserts (4 tamanhos × 2 variantes), todos pass.
- Gates de não-regressão (inegociáveis), todos verdes. -> comprovado: os seis comandos abaixo passaram nesta execução (ver itens individuais e "Comandos Executados").
- `make test lint vet` (mínimo transversal). -> comprovado: `go vet ./...` pass sem saída; `golangci-lint run` "No issues found"; `go test` pass em todos os pacotes exceto `internal/skills` (falha pré-existente e não relacionada, isolada por `git stash` — ver Riscos Residuais).
- `make check-hooks-sync` — a tarefa toca `.agents/lib/` e `.agents/hooks/`. -> comprovado: pass, "Todos os hooks orquestrador sincronizados."
- `make check-scripts-sync` — a tarefa toca `.agents/scripts/`. -> comprovado: pass, "Todos os validadores de evidencia sincronizados."
- `make test-validators` — novos casos de fronteira. -> comprovado: pass, novos casos de fronteira incluídos (53 asserts em `scripts/test-validators.sh` + suítes de `tests/scripts/`).
- `make test-hooks` — comportamento dos hooks shell preservado. -> comprovado: pass, 63 asserts OK incluindo Bloco 4 novo.
- `make coverage` — 75% total e 70% por pacote crítico. -> comprovado: cobertura total 82.1% (>75%) quando calculada excluindo o pacote `internal/skills` pré-quebrado; a execução literal de `make coverage` (`./...`) é interrompida pelo Make no mesmo pacote pré-existente, não relacionado a esta tarefa (comprovação parcial — ver Riscos Residuais).

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão (dentro do escopo desta tarefa; ver Riscos Residuais para a falha pré-existente isolada de `internal/skills`).
- [ ] Estado de tasks.md sincronizado com este relatório — intencionalmente não mutado (`tasks.md` permanece `pending` para 2.0); ver Riscos Residuais para o bloqueio formal de `bash .agents/scripts/validate-task-evidence.sh` que impede a Etapa 5 (checkpoint + mutação) de `execute-task`.

## Diff Reviewed

sha=556ed0a22d749c7a645e1a54a7298207c94feff6
verdict=APPROVED
tool=claude

Nota: `sha=` acima é o `base_sha` (HEAD) sobre o qual o diff desta tarefa foi construído, não um
`patch_sha256`. Não foi emitido `result_path=`/`execution-result.json` v2 nesta rodada — ver
"Bloqueio formal" em Riscos Residuais.

## Coverage

package=internal/doctor
delta=+82.1%

## Suposições
- `docs/hook-inventory.md` deveria estar commitado (gate `check-hooks-inventory` foi ligado ao CI por trabalho anterior nesta mesma árvore de trabalho, mas o artefato nunca foi gerado/commitado); regenerado deterministicamente com `go run . hooks inventory .` para destravar o gate, sem alterar conteúdo de hooks.
- O "RF-68 (negação por ausência de alvo)" citado no task file corresponde à postura descrita no ADR-003 (linha 224, "alinhar a postura de comando vazio... este passo isolado já fecha o bypass"), não ao texto literal de RF-68 no PRD (que trata de parsing estrutural/paths — RF-68 a RF-70 na tabela de rastreabilidade da techspec). Implementei a postura conforme o ADR e o texto do task file, no nível do script `git-operation-gate.sh` (contrato comprovado por invocação direta), com uma guarda no único chamador de produção (`validate-preload.sh`) para não converter toda tool call sem comando de shell (Write/Edit/Read) em bloqueio — sem essa guarda, a suíte de integração pré-existente (`hooks_matrix_dispatch_test.go`, `opencode_shell_gate_test.go`, `portability_test.go`, `pretool_decision_parity_test.go`) regredia em ~28 casos verificados.

## Riscos Residuais

- **Bloqueio formal (motivo de `Estado: blocked`)**: `bash .agents/scripts/validate-task-evidence.sh` exige, para `Estado: done`, um `result_path=` referenciando um `execution-result.json` v2 (`schema_version:2`, `base_sha`, `patch_sha256`, `final_state_sha256`, `tests[]`, `criteria[]`, `evidence[]`, `review_verdict`) cujo patch é recomputado a partir de `git diff HEAD` mais todos os arquivos não rastreados do repositório. Nesta árvore de trabalho compartilhada existem, simultaneamente, artefatos não commitados de outras tarefas do mesmo PRD já encerradas ou em andamento (`.specs/prd-hooks-canonicos-vendor-neutral/1.0_execution_report.md`/`1.0_execution_result.json`, `evidence/task-1.0/*`, `.specs/prd-hooks-canonicos-vendor-neutral/3.0_execution_report.md`/`3.0_execution_result.json`, `evidence/task-3.0-hooks-canonicos-vendor-neutral/*`, `internal/hookinventory/*`, `internal/capability/dispatch_proof_cell_test.go`, `internal/taskloop/zzz_debug_capture_test.go`, `tests/integration/atomic_write_isolation_test.go`, `testdata/hook-inventory.json`, `scripts/check-hooks-inventory.sh`, `cmd/ai_spec_harness/hooks_inventory.go`, entre outros) e de uma WIP de linguagem .NET não relacionada (ver item abaixo). Uma tentativa real de reconstrução do patch nesse estado (`git diff --binary HEAD` + `git diff --no-index` por arquivo não rastreado) produziu **6,7 MB**, muito além do escopo desta tarefa — o mecanismo de exclusão do validador só permite excluir caminhos que o próprio `result.Evidence` da tarefa declarar (prefixo `evidence/`), então não há como isolar honestamente "apenas o diff da tarefa 2.0" sem declarar como "evidência da tarefa 2.0" artefatos que pertencem a outras tarefas — o que seria evidência fabricada/mal atribuída. Adicionalmente, o gate de reconciliação de estado do próprio validador (`report_state` vs `tasks_status` em `tasks.md`) exige que `tasks.md` já reflita o estado terminal declarado no relatório *antes* da validação, o que conflita com a própria Etapa 5 de `execute-task` ("rodar validador -> só então mutar tasks.md"): não há ordem de operações que satisfaça as duas regras simultaneamente na primeira tentativa de encerramento de uma tarefa. Evidência: `bash .agents/scripts/validate-task-evidence.sh .specs/prd-hooks-canonicos-vendor-neutral/1.0_execution_report.md` (tarefa já `done` em `tasks.md`) também falha agora com `FALTANDO: result_path do execution-result.json`, confirmando que o mesmo gate está atualmente insatisfazível para uma tarefa que só *ontem* fechou com sucesso — isto é degradação de ferramentas/ambiente compartilhado, não uma lacuna introduzida por esta tarefa. Recomendação: (a) commitar/consolidar as tarefas 1.0 e 3.0 (e a WIP de .NET) antes de tentar fechar formalmente a 2.0 com o envelope SDD completo, ou (b) o dono do repositório decidir aceitar o fechamento interativo sem o envelope v2 (a própria Etapa 6.0 de `execute-task` já prevê esse caso — "projeto que não produz `execution-result.json` pula esta etapa sem erro"), destravando manualmente `tasks.md` para 2.0.
- **Falha pré-existente e não relacionada, fora do escopo desta tarefa**: `internal/skills.TestAllLangsAreExhaustivelyWired/dotnet` falha (wiring de linguagem .NET incompleto em `internal/contextgen/contextgen.go`, `internal/detect/toolchain.go`, `.agents/skills/agent-governance/triggers/dotnet.yaml`). Confirmado pré-existente via `git stash` (o build já falha com `undefined: contextgen.ValidationLangOrder` mesmo sem nenhuma mudança desta tarefa) — os arquivos afetados (`internal/contextgen/contextgen.go`, `internal/detect/toolchain.go`, `internal/skills/skills.go`, `internal/taskloop/*.go`, `internal/triggers/*.go`, `internal/runtime/memory/durable/facade_bench_test.go`) já estavam modificados/untracked no working tree antes do início desta tarefa (visível na listagem de `git status` truncada do início da sessão). Como consequência, `make test` (via `go test ./...`) e `make coverage` (via `go test ./... ` literal) retornam exit não-zero nesse pacote isolado, não relacionado a hooks/git-operation-gate/RF-57/RF-68. Todos os pacotes e gates diretamente tocados por esta tarefa (`.agents/lib/`, `.agents/scripts/`, `.agents/hooks/`, `internal/doctor/`, `internal/install/`, `internal/upgrade/`, `internal/uninstall/`, `scripts/test-validators.sh`, `scripts/test-hooks.sh`, `tests/integration/hook_payload_boundary_test.go`) foram verificados isoladamente e passam. Recomendação: essa WIP de dotnet precisa de sua própria tarefa/PRD para ser concluída ou revertida antes que `make test`/`make coverage` voltem a ser literalmente verdes na árvore completa.
- Selo de evidência (RF-14/Etapa 6) pendente: não há commit contendo o trabalho desta tarefa; não aplicável até o humano ou `semantic-commit` commitar as mudanças e até o bloqueio formal acima ser resolvido.
- `hook-payload.sh` declara dependência hard de `python3` ou `jq` no host, mitigada por `ai-spec doctor` (novo check `checkHookInterpreter`); hosts sem nenhum dos dois falham fechado (bloqueiam toda operação git e toda validação de governança) — comportamento intencional documentado no ADR-003.
- `tasks.md` foi deliberadamente **não mutado** para 2.0 (permanece `pending`), conforme a instrução de `execute-task` "Falha -> blocked; não mutar tasks.md". Nenhum checkpoint YAML foi criado em `.checkpoints/2.0.yaml` pelo mesmo motivo.

## Conflitos de Regra
- none
