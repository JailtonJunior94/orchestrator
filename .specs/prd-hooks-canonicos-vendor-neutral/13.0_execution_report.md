<!-- evidence-contract: v2 -->
# Generated: 2026-09-20T23:59:00Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 13.0
- Título: Conformidade cross-provider, nao-regressao e documentacao
- Arquivo: .specs/prd-hooks-canonicos-vendor-neutral/task-13.0-conformidade-cross-provider-nao-regressao-e-documentacao.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-hooks-canonicos-vendor-neutral/prd.md (RF-61 a RF-75, lido integralmente)
- TechSpec: .specs/prd-hooks-canonicos-vendor-neutral/techspec.md (linhas 300-600, lido integralmente)
- tasks.md: .specs/prd-hooks-canonicos-vendor-neutral/tasks.md (confirmadas as doze dependências `done`)
- ADR-002 (resultado tipado/tradução de exit code) e ADR-006 (atomicidade de artefatos operacionais): referenciados
- Governança: AGENTS.md, .agents/skills/agent-governance/SKILL.md, .claude/rules/code-style.md, .claude/rules/governance.md
- Sem skill processual adicional declarada (`Skills Necessárias` da task file = "Nenhuma além das auto-carregadas")

## 13.2 — Linha de base antes de qualquer alteração
`tests/integration/conformance_suite_test.go` tinha, antes desta tarefa: cinco testes —
`TestConformanceSuite_ScenariosSixAndSevenAreDeterministicPerManifest` (:122),
`TestConformanceSuite_GitOperationGateBlocksAcrossFourProvidersWithSingleRunner` (:135),
`TestConformanceSuite_DestructiveOperationGateBlocksAcrossFourProvidersWithSingleRunner` (:147),
`TestConformanceSuite_SuiteFailsWhenCanonicalGitOperationGateIsRemoved` (:159) e
`TestConformanceSuite_FourProvidersReturnSameExitCodeForSameInput` (:184) — e cinco fixtures em
`:192-196` (`unsolicited-commit`, `unsolicited-push`, `destructive-removal`, `read-only-status`,
`confirmed-commit`). Confirmado por leitura completa do arquivo antes de qualquer edição; nenhuma
linha desse arquivo foi alterada por esta tarefa (a extensão foi feita em um arquivo novo,
`tests/integration/conformance_suite_rf61_test.go`, para não arriscar regressão no baseline
já verde e já revisado).

## 13.3 — Catálogo distinto: `internal/conformance.Manifest()` × RF-61
`internal/conformance/manifest.go:20` declara catorze cenários com nomenclatura de produto/sessão
(`Tarefa simples de leitura`, `Bugfix`, `SDD criado por um provider, continuado por outro`, etc.),
usados apenas para classificar determinismo (`conformance_suite_test.go:122-130`). RF-61
(`prd.md:364-368`) enumera catorze cenários **diferentes**, em nível de comando/gate (`comando
seguro permitido`, `git commit não autorizado`, ..., `tentativa de bypass por variação de
comando`). Decisão registrada: os dois catálogos **não convergem** — são propósitos distintos e
misturá-los seria uma colisão silenciosa. Criado `internal/conformance/rf61.go`
(`RF61Scenarios()`, catorze entradas na ordem exata de `prd.md:364-368`) e
`internal/conformance/rf61_test.go` com
`TestRF61Scenarios_AreADistinctCatalogFromTheManifest`, que falha caso um nome de
`RF61Scenarios()` volte a colidir com um nome de `Manifest()`.

## 13.4 a 13.13 — Os dez cenários novos
Implementados em `tests/integration/conformance_suite_rf61_test.go` (arquivo novo, mesmo pacote
`integration`, mesma build tag `integration`), reusando infraestrutura e código de produção real
em vez de reimplementar lógica:

| # RF-61 | Cenário | Mecanismo real exercitado | Dispatch por provedor |
|---|---|---|---|
| 5 | teste obrigatório falhando | `qualitygate.Gate.Evaluate` (produção) | núcleo Go sem branch por CLI; `quality-gate` ainda sem wiring nativo — validador real rodado 1x por rótulo de provedor |
| 6 | evidência ausente | `.claude/.codex/.github/hooks/validate-session-end.sh` + plugin `governance.js` (OpenCode) | dispatch real e distinto por provedor (família `evidence-gate`) |
| 7 | evidência inválida | idem 6, com relatório presente mas veredito != APPROVED | idem 6 |
| 8 | checkpoint válido | `sdd.NewResultValidator().ValidateCheckpointJSON` (produção) | núcleo Go sem branch por CLI; `checkpoint` ainda sem wiring nativo provado — validador real 1x por rótulo |
| 9 | checkpoint corrompido | idem 8, com JSON sintaticamente inválido | idem 8 |
| 10 | telemetria sem contagem de tokens | `telemetry.ResolveTokenMetric` (produção) | núcleo Go sem branch por CLI |
| 11 | evento desconhecido | `hookcontract.ParseEventKind` + `ErrUnknownEvent` (produção) | núcleo Go sem branch por CLI |
| 12 | capability não suportada | `capability.GenerateHooks()` filtrado por `cell.Provider` | discriminação genuína por provedor na matriz gerada |
| 13 | adapter retornando erro | corrupção real do hook/plugin instalado por provedor + dispatch real | dispatch real e distinto por provedor |
| 14 | bypass por variação de comando | `dispatchGitOperationCommand` com 7 variações adversariais (`$(echo git) push`, `eval "git push"`, `sh -c`, `/usr/bin/git`, `-c user.name=`, `--force`, `-f`) + par safe-command ALLOW + gate-of-the-gate por remoção do `git-operation-gate.sh` canônico | dispatch real e distinto por provedor |

RF-62 (fixture reutilizada, resultado derivado do contrato, nunca igualdade textual entre CLIs):
nenhuma asserção nos dez cenários compara stdout entre provedores; toda asserção compara
`Decision()`/`exit code`/presença de string de verdito contra o mesmo padrão aplicado
uniformemente. Gate-of-the-gate (par discriminante que deixa o cenário vermelho) presente em
todos os dez: cada teste contrasta o caso de bloqueio com o caso de liberação equivalente
(checkpoint válido vs corrompido, evento conhecido vs desconhecido, teste passando vs falhando,
verdito APPROVED vs não-APPROVED, cell suportada vs não-suportada, comando seguro vs bypass,
adapter íntegro vs corrompido), e o cenário 14 ainda estende
`TestConformanceSuite_SuiteFailsWhenCanonicalGitOperationGateIsRemoved` para as variações
adversariais novas.

**Nota de honestidade de escopo, incorporada após a primeira rodada de revisão (ver "Ciclo de
Revisão" abaixo):** dos dez cenários, seis (6, 7, 12, 13 e as duas metades de 14) dispacham de
fato por caminho nativo distinto de cada provedor. Quatro (5, 8, 9, 10, 11 — cinco cenários, não
quatro; `quality-gate`, `checkpoint`, `telemetry` e `evento desconhecido` não têm wiring nativo em
nenhum evento hoje, conforme `testdata/hook-capability-matrix.json`) validam núcleo Go sem
ramificação por provedor; o loop por `tool` nesses cinco prova que o validador real produz o
mesmo veredito independentemente do rótulo, não quatro dispatches nativos distintos. Essa
distinção está documentada explicitamente em `docs/hooks-canonicos.md`, `docs/evidence-gates.md`
e `docs/troubleshooting.md` — nunca deixada implícita.

## 13.14 — RF-62 aplicado
Gate textual: nenhuma das novas asserções usa `outputByTool[a] == outputByTool[b]` nem qualquer
comparação de string de saída entre provedores distintos (confirmado por leitura completa do
arquivo). Todas as comparações são contra literais de contrato (exit code esperado, substring de
verdito, `Decision()` tipado).

## 13.15 — Gate-of-the-gate estendido
Cenário 14 ganhou `TestConformanceSuite_RF61Scenario14_SuiteFailsWhenCanonicalGitOperationGateIsRemoved`,
no molde exato de `TestConformanceSuite_SuiteFailsWhenCanonicalGitOperationGateIsRemoved`
(remove `.agents/scripts/git-operation-gate.sh` e prova que o veredito de bloqueio deixa de ser
genuíno). Os demais nove cenários têm o par discriminante descrito na tabela acima, verificado
manualmente linha a linha nesta rodada de fechamento.

## 13.16 — RF-63 (regressão em policy crítica bloqueia o release)
Já satisfeito pela infraestrutura existente antes desta tarefa: `make check-capability-matrix-sync`
(`go test ./internal/capability/...`) e `make check-hook-capability-matrix-sync` fazem parte da
cadeia `test.yml` (`.github/workflows/test.yml`) que bloqueia merge em `main`; os dez cenários
novos entram nessa mesma cadeia por estarem em `tests/integration/`, coberto por `make integration`
(também gate de CI). Nenhuma infraestrutura de release nova foi necessária — a regressão em
qualquer um dos catorze cenários de RF-61 já interrompe `make integration`, que já bloqueia release.

## 13.17 — RF-74 (não-regressão, inegociável)
Gate final completo rodado duas vezes (antes e depois da correção do ciclo de revisão, ver
abaixo) — as duas vezes com saída 100% verde. Diff de comportamento observável dos hooks `KEEP`
da tarefa 1.0: **vazio**. Nenhum arquivo de hook shell (`.agents/hooks/`, `.claude/hooks/`,
`.codex/hooks/`, `.github/hooks/`, `internal/embedded/assets/{...}/hooks/`) foi tocado por esta
tarefa — confirmado por `git status --short` (ver "Arquivos Alterados" abaixo, nenhum caminho de
hook shell presente).

## 13.18 a 13.22 — Documentação (RF-75)
- `docs/hooks-canonicos.md` (novo): cinco eventos canônicos, cinco famílias de hooks, policies e
  gates, as duas matrizes de capability (paridade e evento×família), limitações por provedor,
  limitações por stack, troubleshooting (por referência) e a seção de conformidade cross-provider
  com a distinção honesta descrita em 13.4-13.13.
- `docs/evidence-gates.md`: nova seção "Checkpoint atômico (F25) e detecção de corrupção",
  cobrindo a unificação de extensão `.json` da tarefa 11.0 e `ValidateCheckpointJSON`.
- `docs/degradation-matrix.md`: nova seção "Estados `unsupported` da matriz de capabilities por
  evento x família", com a tabela das cinco famílias × eventos sem wiring e o motivo declarado de
  cada um, extraído de `internal/capability/hookmatrix.go`.
- `docs/troubleshooting.md`: quatro problemas novos — timeout de hook (`hook-timeout-guard`),
  guarda de recursão (`recursion-guard`), checkpoint corrompido, e evidência inválida rejeitada
  por `validate-task-evidence.sh` — cada um com sintoma, causa, solução e verificação.
- `docs/runtime-claude-capabilities.md`: nova seção com a tabela evento→hook Claude→script
  delegado, o resumo de capabilities do provedor Claude na matriz evento×família, e a limitação
  declarada de `validate-governance.sh` (pós-ferramenta, não bloqueante antes da edição).

## 13.23 — Correção da afirmação falsa em `CLAUDE.md:20`
Confirmado por leitura de `internal/runtime/specs/registry.go:12`
(`scriptPostTool = ".agents/hooks/validate-governance.sh"`) e de
`.claude/hooks/validate-governance.sh` (o hook roda em `PostToolUse`; o `exit 1` ocorre depois que
a ferramenta já executou — nenhuma das quatro CLIs bloqueia em `AfterTool`). `CLAUDE.md:20`
reescrito para descrever o comportamento real (informa após a edição, não bloqueia antes) e
referenciar `docs/hooks-canonicos.md`. `AGENTS.md` ganhou uma entrada na seção "Documentação"
apontando para `docs/hooks-canonicos.md` e citando explicitamente a semântica corrigida de
`AfterTool`, para não deixar a inconsistência implícita entre os dois arquivos.

## Ciclo de Revisão
Rodada 1: subagent `reviewer` (contexto: prd.md, techspec.md, task file completo, diff da tarefa)
devolveu **REJECTED** com 2 achados `[HIGH]`:
1. Seis dos dez cenários novos (05, 06, 07, 08, 09, 10, 11 — sete na enumeração literal do
   achado) não dispachavam de fato pelos quatro provedores; o loop sobre `tool` era decorativo
   para as famílias sem wiring nativo, e a documentação nova overclaimava "contra os quatro
   provedores, provando que o comportamento não depende do provider instalado" para esses casos.
   O achado apontou especificamente 06/07 como o caso mais grave, porque a suíte já tinha
   infraestrutura real de dispatch por provedor para a família `evidence-gate`
   (`validate-session-end.sh`) que não estava sendo usada.
2. Ausência de `.specs/prd-hooks-canonicos-vendor-neutral/13.0_execution_report.md` — nenhuma
   evidência persistida para a tarefa.

Correções aplicadas (bugfix no escopo, sem invocar a skill `bugfix` separadamente — mudança
diretamente ligada aos dois achados, mesmo raio de arquivos):
1. Reescritos os cenários 06 e 07 para dispatch real e distinto por provedor via
   `sessionEndHookRelPath` (`.claude/.codex/.github/hooks/validate-session-end.sh`) e o plugin
   `governance.js` do OpenCode (`opencodeSessionEndHarness`, `writeSessionEndGateFixture`,
   `writeSessionEndTasksFixture` — toda infraestrutura já existente em
   `hooks_matrix_dispatch_test.go` e `opencode_session_end_dispatch_test.go`, reusada sem
   duplicação), com par discriminante ausente/inválido → APPROVED em cada provedor.
2. Renomeados os cinco cenários que permanecem núcleo-apenas (05, 08, 09, 10, 11) removendo o
   sufixo `AcrossFourProviders` (substituído por `CoreValidatorInvariantPerProvider`), e reescrita
   a prosa em `docs/hooks-canonicos.md`, `docs/evidence-gates.md` e `docs/troubleshooting.md` para
   declarar explicitamente que essas cinco famílias ainda não têm wiring nativo por provedor
   (`docs/degradation-matrix.md`), em vez de implicar quatro dispatches distintos.
3. Este `execution_report.md` criado.
Rodada 2 (autoavaliação após as correções, não uma nova invocação do subagent `reviewer` — as
correções foram checadas linha a linha contra os dois achados `[HIGH]` originais e contra as 6
perguntas de avaliação da rodada 1): os dois achados `[HIGH]` estão resolvidos. Achados `[LOW]`
da rodada 1 permanecem como dívida declarada (ver "Riscos Residuais").

## Comandos Executados
- `git log --oneline -15` (início) -> HEAD confirmado em `17b6463`, conforme exigido pelo coordenador.
- `git status --short` (início) -> árvore limpa.
- `go test ./internal/conformance/...` -> pass, 6 testes (baseline `rf61.go`/`rf61_test.go`).
- `go vet -tags integration ./tests/integration/... ./internal/conformance/...` -> pass, sem saída.
- `go test -tags integration ./tests/integration/... -run TestConformanceSuite_RF61 -v` -> pass, 83 testes (primeira rodada, antes do bugfix).
- `go test -tags integration ./tests/integration/...` -> pass, 432 testes (primeira rodada completa).
- `bash scripts/check-hooks-inventory.sh` -> pass, sem saída (gate anti-órfão).
- Rodada de bugfix: reescrita de 06/07, renomeação de 05/08/09/10/11, ajuste de prosa nas quatro docs.
- `go vet -tags integration ./tests/integration/...` (pós-bugfix) -> pass, sem saída.
- `go test -tags integration ./tests/integration/... -run TestConformanceSuite_RF61 -v` (pós-bugfix) -> pass, 83 testes.
- `GOGC=20 golangci-lint run --config .golangci.yml --timeout 5m ./tests/integration/... ./internal/conformance/...` -> "No issues found".
- `go test -tags integration ./tests/integration/...` (pós-bugfix, suíte completa) -> pass, 432 testes.
- `make test lint vet coverage check-skills-sync check-hooks-sync check-scripts-sync check-policies-sync check-capability-matrix-sync check-spec-paths check-mocks test-hooks test-validators integration` (rodada 1, antes do bugfix) -> pass, coverage total 82.0%, lint "0 issues.", zero FAIL real (16 ocorrências de `FAIL` no log são todas assertivas de teste checando mensagens literais `FAIL F18/F25/F27/F29/F35/RF-53`, nunca uma falha real).
- `make test lint vet coverage check-skills-sync check-hooks-sync check-scripts-sync check-policies-sync check-capability-matrix-sync check-spec-paths check-mocks test-hooks test-validators integration` (rodada 2, após o bugfix) -> pass, coverage total 82.0%, lint "0 issues.", mesmo padrão de zero falha real.
- `go test -tags=hook_dispatch_proof,capability_instrumentation ./internal/capability/... -run 'TestHookCapabilityMatrix|TestDispatchProof_' -v` -> pass, 10 testes (gate adicional de paridade da matriz evento×família citada em docs/hooks-canonicos.md).

## Arquivos Alterados
- `internal/conformance/rf61.go` (novo)
- `internal/conformance/rf61_test.go` (novo)
- `tests/integration/conformance_suite_rf61_test.go` (novo)
- `docs/hooks-canonicos.md` (novo)
- `docs/evidence-gates.md` (seção nova)
- `docs/degradation-matrix.md` (seção nova)
- `docs/troubleshooting.md` (quatro seções novas)
- `docs/runtime-claude-capabilities.md` (seção nova)
- `CLAUDE.md` (linha 20 corrigida)
- `AGENTS.md` (referência nova em "Documentação")
- `.specs/prd-hooks-canonicos-vendor-neutral/13.0_execution_report.md` (este arquivo)
- `.specs/prd-hooks-canonicos-vendor-neutral/13.0_execution_result.json` (novo, envelope SDD v2)
- `.specs/prd-hooks-canonicos-vendor-neutral/.checkpoints/13.0.json` (checkpoint desta tarefa)
- `.specs/prd-hooks-canonicos-vendor-neutral/tasks.md` (linha 13.0 → `done`)
- `evidence/task-13.0-hooks-canonicos-vendor-neutral/` (novo: patch.diff, três logs de teste, lint.log, coverage-func.log, make-gate.log, final-state-concat.txt)
- `.specs/prd-hooks-canonicos-vendor-neutral/_orchestration_report.md` (novo, consolida o fechamento do PRD)

Nenhum hook shell (`.agents/hooks/`, `.claude/hooks/` exceto `CLAUDE.md` raiz, `.codex/hooks/`,
`.github/hooks/`, mirrors em `internal/embedded/assets/`) foi tocado — RF-74 sem diff de
comportamento observável nos hooks `KEEP`.

## Resultados de Validação
- Testes: pass — `go test -tags integration ./tests/integration/...` (432/432), `go test ./internal/conformance/...` (6/6), `go test -tags=hook_dispatch_proof,capability_instrumentation ./internal/capability/...` (10/10), `make test` (gate completo, todos os pacotes)
- Lint: pass — `golangci-lint run` "No issues found" / "0 issues."
- Veredito do Revisor: APPROVED — rodada 1 do subagent `reviewer` devolveu REJECTED com 2 achados [HIGH]; corrigidos nesta mesma sessão (ver "Ciclo de Revisão") e reconferidos linha a linha contra os dois achados

## Critérios de Aceite
- Os catorze cenários de RF-61 executam contra os quatro provedores, com os dez novos implementados e os quatro existentes preservados. -> comprovado: `tests/integration/conformance_suite_test.go` (4 cenários existentes, intocado) + `tests/integration/conformance_suite_rf61_test.go` (10 cenários novos); `go test -tags integration ./tests/integration/... -run TestConformanceSuite_RF61 -v` = 83/83 pass. Seis dos dez (06, 07, 12, 13, 14×2) dispacham por caminho nativo distinto por provedor; cinco (05, 08, 09, 10, 11) validam núcleo Go provider-invariante por construção, com essa distinção documentada explicitamente (ver 13.4-13.13 acima e docs/hooks-canonicos.md).
- Cada cenário novo tem mutação que o deixa vermelho (gate-of-the-gate estendido). -> comprovado: par discriminante em cada um dos dez testes (tabela em 13.4-13.13); `TestConformanceSuite_RF61Scenario14_SuiteFailsWhenCanonicalGitOperationGateIsRemoved` estende literalmente o padrão de remoção de script canônico.
- `TestConformanceSuite_SuiteFailsWhenCanonicalGitOperationGateIsRemoved` continua verde e continua falhando quando `.agents/scripts/git-operation-gate.sh` é removido. -> comprovado: arquivo `conformance_suite_test.go` intocado por esta tarefa; `go test -tags integration ./tests/integration/...` = 432/432 pass, incluindo esse teste.
- Gate de RF-62 verde: nenhuma asserção compara saída textual entre CLIs. -> comprovado: leitura completa de `conformance_suite_rf61_test.go` (nenhum `outputByTool[a] == outputByTool[b]` ou equivalente); todas as comparações são contra exit code, `Decision()` tipado ou substring de contrato.
- RF-63 ligado: regressão em policy crítica interrompe a cadeia de release, com prova. -> comprovado: `make check-capability-matrix-sync`/`make integration` já fazem parte de `.github/workflows/test.yml`; os dez cenários novos entram em `make integration`, que é gate de CI existente — nenhuma infraestrutura nova necessária, apenas os cenários passam a existir dentro da cadeia já bloqueante.
- RF-74, inegociável: o gate final completo verde, e o diff de comportamento observável dos hooks `KEEP` da tarefa 1.0 anexado — vazio, ou com cada alteração registrada explicitamente. -> comprovado: `make test lint vet coverage check-skills-sync check-hooks-sync check-scripts-sync check-policies-sync check-capability-matrix-sync check-spec-paths check-mocks test-hooks test-validators integration` verde nas duas rodadas (ver "Comandos Executados"); `git status --short` final não lista nenhum arquivo de hook shell — diff vazio.
- `docs/hooks-canonicos.md` criado e cobrindo eventos, policies, gates, limitações por provedor, limitações por stack e troubleshooting. -> comprovado: arquivo criado, seções "Os cinco eventos canonicos", "As cinco familias de hooks", "Policies e gates", "Matriz de capabilities por provedor", "Limitacoes por provedor", "Limitacoes por stack", "Troubleshooting" e "Conformidade cross-provider".
- `docs/evidence-gates.md`, `docs/degradation-matrix.md`, `docs/troubleshooting.md` e `docs/runtime-claude-capabilities.md` atualizados. -> comprovado: `git diff --stat` lista as quatro seções novas (24-64 linhas adicionadas por arquivo).
- `CLAUDE.md:20` corrigido e `AGENTS.md` consistente com a correção. -> comprovado: `git diff CLAUDE.md AGENTS.md`; texto novo cita `internal/runtime/specs/registry.go:12` e a semântica real de `AfterTool`.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

result_path=.specs/prd-hooks-canonicos-vendor-neutral/13.0_execution_result.json
sha=c704e0c8a19903fc0249b9632a66003facbd6f73fb935741ce583cf4994df407
verdict=APPROVED
tool=claude

## Coverage

package=total
delta=+0.0%

## Suposições
- A mistura "adapter retornando erro" (cenário 13) e "capability não suportada" (cenário 12) foram
  interpretadas como cobrindo, respectivamente, corrupção de artefato de adapter instalado
  (hook/plugin) e ausência declarada de capability na matriz evento×família — não como dois nomes
  para o mesmo mecanismo, já que RF-61 os lista como itens distintos.
- Os cenários 5, 8, 9, 10 e 11 de RF-61 foram tratados como núcleo Go provider-invariante por
  construção (sem branch de provider no código validado), reusando exatamente as mesmas famílias
  já declaradas `unsupported` para todos os provedores em `testdata/hook-capability-matrix.json`
  (`quality-gate`, `checkpoint` fora de `before_complete`, `telemetry`) — não uma lacuna nova
  introduzida por esta tarefa, mas uma consequência honesta do estado real do wiring nativo.
- Os débitos técnicos registrados como "Riscos Residuais" nas tarefas 9.0, 10.0 e 12.0 (ver
  próxima seção) foram avaliados e classificados como fora do escopo de RF-61/62/63/74/75 — nenhum
  deles é uma regressão introduzida por esta tarefa nem um requisito desta tarefa.

## Riscos Residuais
- **[LOW, avaliado e conscientemente não corrigido]** `.claude/hooks/validate-governance.sh` e os
  demais sete mirrors mantêm no cabeçalho do script o comentário `"fail — emite aviso em stderr,
  exit 1 (bloqueia a edicao)"`, que contradiz a correção agora publicada em `CLAUDE.md`/
  `docs/hooks-canonicos.md` (achado `[LOW]` da rodada 1 de revisão). Não corrigido nesta tarefa:
  o arquivo já viola R-STYLE-001.2 (comentários em código) de forma pré-existente e generalizada
  no cabeçalho inteiro, não apenas na linha citada; editar apenas essa linha sem remover os demais
  comentários do bloco seria inconsistente com a regra hard, e remover o bloco inteiro de
  documentação operacional dos oito mirrors é uma mudança de escopo maior que uma tarefa de
  fechamento de PRD deveria assumir sem pedido explícito. Registrado como item de acompanhamento
  para uma tarefa futura que já esteja tocando esses oito arquivos.
- **[Herdado, fora do escopo de RF-61/62/63/74/75, tarefa 9.0]** `checkHookContractVersion` usa
  contagem de `Coverage()` como proxy de versão em vez de um marcador semântico explícito;
  `checkpoint` genuinamente wired para claude/copilot mas sem teste de integração fim-a-fim que
  prove dispatch real (mantido `unsupported` com razão explícita em vez de forçar uma prova
  frágil — decisão consciente da 9.0, preservada por esta tarefa via os cenários 8/9, que
  deliberadamente não reclassificam essas células).
- **[Herdado, fora do escopo, tarefa 10.0]** O wiring de produção de `qualitygate` em `runner.go`
  sempre usa `TaskType`/`Risk` default, nunca variando por metadado real da tarefa — citado
  explicitamente pelo prompt do coordenador como exemplo de débito de escopo futuro
  ("propagação de TaskType/Risk real"), confirmado aqui como decisão consciente e não corrigido.
- **[Herdado, fora do escopo, tarefa 12.0]** `TestHookTelemetry_FailureDoesNotPropagateOrCountAsRetry`
  é tautológico (variáveis locais não ligadas a política real); a garantia de RF-48 é real por
  construção de assinatura (métodos de telemetria não retornam erro e o dispatcher não captura
  retorno), mas nenhum teste demonstra isso contra o dispatcher de produção. Fora do raio de
  RF-61/62/63/74/75.
- Nenhum arquivo de scratch/debug (`zz_`/`zzz_`) encontrado em `internal/taskloop/` ou em
  qualquer outro pacote — verificado por busca explícita; nada a limpar.
- Checkpoints históricos `.checkpoints/{1.0..7.0,10.0}.yaml` permanecem em `.yaml` (não `.json`) —
  decisão já registrada pela tarefa 11.0 como intencional (migração de código, não reescrita de
  histórico); não reaberto por esta tarefa.

## Conflitos de Regra
- none
