<!-- evidence-contract: v2 -->
# Generated: 2026-09-20T20:16:52Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.0
- Título: Contrato canônico de hooks em internal/hookcontract
- Arquivo: .specs/prd-hooks-canonicos-vendor-neutral/task-4.0-contrato-canonico-de-hooks-em-internal-hookcontract.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-hooks-canonicos-vendor-neutral/prd.md (RF-06, RF-07, RF-09 a RF-17)
- TechSpec: .specs/prd-hooks-canonicos-vendor-neutral/techspec.md (Interfaces Chave, Modelos de Dados)
- ADRs: adr-001-hook-contract-fonte-unica-projecoes.md, adr-002-resultado-tipado-traducao-exit-code.md
- Governança: `.claude/rules/code-style.md` (R-STYLE-001), `.claude/rules/governance.md` (R-GOV-001), skill `execute-task`, skill `go-implementation` (auto-carregada pela detecção de linguagem Go), skill `review` (auto-crítica inline, sem diff externo a carregar — pacotes novos)
- Referências de padrão: `internal/runtime/specs/enforcement.go:87-110` (NewPointCoverage), `internal/harness/contract.go`

## Comandos Executados
- `export AI_INVOCATION_DEPTH=0; source .agents/lib/check-invocation-depth.sh; export AI_PREFLIGHT_DONE=1` -> depth OK, preflight marcado (orquestrador já validou drift/skills-lock)
- `git status --short` (início) -> árvore limpa nos arquivos de código; residual de sessão anterior identificado separadamente (ver Riscos Residuais)
- `go build ./internal/hookcontract/... ./internal/hookaudit/...` -> build OK
- `gofmt -l -w internal/hookcontract internal/hookaudit` -> formatação aplicada, sem pendências
- `go vet ./internal/hookcontract/... ./internal/hookaudit/...` -> sem saída (OK)
- `go test ./internal/hookcontract/... ./internal/hookaudit/... -v` -> 57 testes, todos PASS
- Prova de vermelho deliberado (critério de sucesso explícito): inserido temporariamente `const debugClaudeProviderLeak = "claude"` em `event.go`, `go test ./internal/hookcontract/... -run TestHookContractHasNoProviderIdentifier -v` -> `--- FAIL: TestHookContractHasNoProviderIdentifier`, `no_provider_identifier_test.go:33: event.go contains forbidden provider identifier "claude" in "debugClaudeProviderLeak"`; linha revertida em seguida, reexecutado -> PASS
- `make lint` -> 1ª execução falhou por `SA1019` (`go/parser.ParseDir` deprecated); reescrito o gate para `os.ReadDir` + `parser.ParseFile` por arquivo, sem `ParseDir`; 2ª execução -> `0 issues.`
- `make vet` -> `go vet ./...` sem saída (OK)
- `make check-mocks` -> `Mocks: OK (sincronizados com mockery.yml)` (nenhuma interface nova exigiu mock nesta tarefa: `Decoder`, `CapabilityRegistry` e `ExitCodeTranslator` são consumidas apenas pelos próprios testes da tarefa via implementação concreta, sem necessidade de dublê)
- `make coverage` -> falhou por pré-existência: `internal/skills` `TestAllLangsAreExhaustivelyWired/dotnet` é vermelho por desenho (task 3.0, commit `55f9f13`, mensagem "(vermelho por design para dotnet, sucessor 7.0)"), não relacionado a esta tarefa
- `go tool cover -func=coverage.out` (profile gerado apesar da falha, pois o teste falha por `t.Fatalf` e não por pânico) -> total 71.6%
- Comparação de baseline: pacotes `internal/hookcontract`/`internal/hookaudit` movidos temporariamente para fora da árvore, `go test -coverprofile=... ./...` -> total 71.5% sem os pacotes novos, confirmando que o piso de 75% já estava furado antes desta tarefa (pré-existente) e que os pacotes novos são incremento líquido positivo (+0.1 p.p.)
- `bash scripts/check-package-coverage.sh 70` -> todos os pacotes críticos listados PASS (não inclui `hookcontract`/`hookaudit`, que não fazem parte da lista crítica declarada)
- `go build ./...` -> OK
- `go test $(go list ./... | grep -v '.../internal/skills$')` -> nenhum FAIL fora do pacote excluído
- `git diff --stat -- internal/hookcontract internal/hookaudit` (via `git add -N` seguido de `git reset`, sem staging real) -> 15 arquivos, 1208 inserções, 0 deleções — confirma pacotes 100% novos
- `git status --short` (final) -> nenhum arquivo `.go` preexistente modificado; único diff de código é a criação de `internal/hookcontract/` e `internal/hookaudit/`
- `rm -f coverage.out coverage_filtered.out` -> artefatos de cobertura removidos da árvore de trabalho

## Arquivos Alterados
**Criados (100% novos, zero arquivo preexistente tocado):**
- `internal/hookcontract/event.go`
- `internal/hookcontract/event_test.go`
- `internal/hookcontract/capability.go`
- `internal/hookcontract/capability_test.go`
- `internal/hookcontract/result.go`
- `internal/hookcontract/result_test.go`
- `internal/hookcontract/payload.go`
- `internal/hookcontract/payload_test.go`
- `internal/hookcontract/exitcode.go`
- `internal/hookcontract/exitcode_test.go`
- `internal/hookcontract/no_provider_identifier_test.go`
- `internal/hookaudit/entry.go`
- `internal/hookaudit/writer.go`
- `internal/hookaudit/reader.go`
- `internal/hookaudit/audit_test.go`

**Não modificados:** `mockery.yml` permaneceu intacto — nenhuma das três interfaces novas (`Decoder`, `CapabilityRegistry`, `ExitCodeTranslator`) tem consumidor externo ainda (a tarefa é puramente aditiva) nem foi mockada em teste desta tarefa; todos os testes exercitam as implementações concretas diretamente.

## Resultados de Validação
- Testes: pass — `go test ./internal/hookcontract/... ./internal/hookaudit/... -v` (57/57 PASS); suíte global `make test` falha apenas em `internal/skills` (pré-existente, fora de escopo, ver Riscos Residuais)
- Lint: pass — `make lint` → `0 issues.` (após correção de `SA1019` no próprio código desta tarefa)
- Vet: pass — `make vet` sem saída
- Check-mocks: pass — `make check-mocks` → `Mocks: OK`
- Coverage por pacote novo: `internal/hookcontract` 83.1%, `internal/hookaudit` 87.5% (ambos acima do piso de 70% por pacote crítico); coverage total do repositório 71.6% (abaixo do gate de 75%, mas pré-existente e não regressivo — baseline sem os pacotes novos é 71.5%)
- Veredito do Revisor: APPROVED (autorrevisão inline via skill `review`, pacotes 100% novos e sem consumidor; achados: nenhum)

## Critérios de Aceite
- Os cinco eventos canônicos existem em fonte única e `EventKind` é enum fechado (RF-06) -> comprovado: `internal/hookcontract/event.go:9-25` (`EventKind` `iota+1`, `allEventKinds` com exatamente 5 valores); `TestEventKind_ParseAndString` e `TestEventKinds_ReturnsAllFive` PASS
- `TestHookContractHasNoProviderIdentifier` passa e falha comprovadamente quando um nome de provedor é introduzido (RF-07) -> comprovado: execução vermelha registrada em "Comandos Executados" (`debugClaudeProviderLeak = "claude"` → FAIL com mensagem `contains forbidden provider identifier "claude"`), revertida e reexecutada verde; teste implementado em `internal/hookcontract/no_provider_identifier_test.go`
- `Envelope` rejeita campo desconhecido, versão não suportada e evento desconhecido com o erro sentinela correspondente (RF-09, RF-10, RF-17) -> comprovado: `internal/hookcontract/payload.go:52-71` (`ErrUnknownField`, `ErrSchemaVersion`, `ErrUnknownEvent` via `ParseEventKind`); testes `TestEnvelope_RejectsUnknownField`, `TestEnvelope_RejectsUnsupportedSchemaVersion`, `TestEnvelope_RejectsUnknownEvent` PASS; `FuzzDecode` executado dentro de `go test` (seed corpus, sem `-fuzz`) PASS
- `NewResult` recusa `DecisionBlock` sem `reason` e sem `policyID`/`gateID` (RF-12, RF-13) -> comprovado: `internal/hookcontract/result.go:53-68`; `TestResult_BlockRequiresReasonAndPolicy` (5 casos tabela) PASS
- `ToDecision` com `critical == true` e código não mapeado devolve `DecisionError` (RF-14) -> comprovado: `internal/hookcontract/exitcode.go:26-28,66-78` (`unmappedResult`); `TestExitCodeTranslator_CriticalUnmappedIsError` PASS
- `TestExitCodeTranslator_PreservesCurrentSemantics` passa com a fixture dos quatro códigos reais (RF-15) -> comprovado: `internal/hookcontract/exitcode_test.go:5-29`, tabela cita `git-operation-gate.sh:5` (exit 2), `validate-preload.sh:5` (exit 2), `validate-session-end.sh:4` (exit 2), `validate-governance.sh:45` (exit 1 literal); todos mapeados para `DecisionBlock`; PASS
- `.aispec/hook-decisions.jsonl` tem escritor e leitor, com round-trip provado (RF-16) -> comprovado: `internal/hookaudit/writer.go` (`Writer.Append`, `O_APPEND|O_CREATE`), `internal/hookaudit/reader.go` (`Reader.ReadAll`); `TestAuditLogRoundTrip` e `TestAuditLogAppend_DoesNotTruncatePriorEntries` PASS (verificação de append real, não rewrite — 5 chamadas a `Append` produzem exatamente 5 linhas no arquivo)
- `SupportUnsupported` é estado próprio, distinto de sucesso e de falha, provado por teste -> comprovado: `internal/hookcontract/capability.go:12-16`; `TestCapability_UnsupportedIsNotSuccess` e `TestSupportState_ThreeDistinctValues` PASS
- `SchemaVersion` com tabela de migração declarada e teste (RF-17) -> comprovado: `internal/hookcontract/payload.go:13-15` (`schemaVersionDescriptions`), `TestSchemaVersionMigration` PASS. Tabela de migração (única entrada nesta tarefa, aditiva): `v1` = schema inicial (`schema_version, event, provider, session_id, payload`); versão `2` (e demais) permanecem indefinidas e produzem `ErrSchemaVersion` — nenhuma migração automática silenciosa
- `internal/hookcontract` não contém identificador nem literal de provedor (RF-07, verificável por AST) -> comprovado: gate acima + `go build`/`go vet`/`make lint` verdes sobre o pacote final

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão (regressão pré-existente em `internal/skills` documentada e comprovadamente anterior a esta tarefa).
- [x] Estado de tasks.md sincronizado com este relatório (tarefa 4.0 marcada `done`, sem tocar a linha residual de 2.0 já pendente de outra sessão).

## Diff Reviewed

sha=4a380c4e78bfbc4b729687a5621bf25605fd36baa756b58df80956c6eb06ffbf
verdict=APPROVED
tool=claude
result_path=.specs/prd-hooks-canonicos-vendor-neutral/4.0_execution_result.json

Patch canônico (base 55f9f13ee5153df139c8783236b807421745444c, capturado com a árvore de
trabalho isolada de sujeira residual de outra tarefa — ver Riscos Residuais):
`evidence/task-4.0/4.0_execution_result.patch` (sha256 acima). Revisão de correção conduzida
inline via skill `review` (pacotes 100% novos, sem consumidor; achados: nenhum).

## Coverage

package=internal/hookcontract,internal/hookaudit
delta=+0.1%

## Suposições
- `CapabilityRegistry` e uma implementação concreta de `Capability` populada por provedor não foram entregues nesta tarefa: o VO `Capability` e a interface `CapabilityRegistry` estão prontos, mas o catálogo real (mapeando `claude`/`codex`/`copilot`/`opencode` × evento) é responsabilidade das tarefas 5.0+ — não invadir esse escopo mantém RF-07 verificável, pois nenhum nome de provedor pode existir no pacote `hookcontract`.
- `mockery.yml` não foi tocado: as três interfaces novas (`Decoder`, `CapabilityRegistry`, `ExitCodeTranslator`) não têm consumidor nesta tarefa nem foram mockadas em teste; se uma tarefa futura precisar dublê, deve declarar a interface em `mockery.yml` na própria tarefa que a consumir (conforme subtarefa 4.10 desta tarefa, que só exige o mock "se necessário").
- `internal/hookaudit` foi desenhado para não depender de `internal/fs.FileSystem`: como a interface `FileSystem` do repositório não expõe append real (apenas `WriteFile`/`WriteFileAtomic`), e a subtarefa 4.6 exige explicitamente "escritor append-real" (distinto do padrão de rewrite completo hoje usado em `internal/runtime/persistence/jsonl.go`), o pacote usa `os.OpenFile` com `O_APPEND` diretamente. Isso também evita modificar `internal/fs` (preexistente), preservando a prova de aditividade. Testes usam `t.TempDir()` (padrão já presente em outros ~20 pacotes do repositório para código que toca o FS real diretamente).
- Formato de `Entry` (`hookaudit`) usa campos exportados simples (não VO com construtor estritamente validador no sentido de R-DDD-001, que lista apenas `Result`, `Capability` e `Envelope`): necessário para serialização direta via `encoding/json` sem `MarshalJSON`/`UnmarshalJSON` customizado; `NewEntry` ainda valida campos obrigatórios (`event`, `hook`, `decision`, duração não-negativa).
- Decisão de representação textual persistida: `EventKind.String()` usa `snake_case` (`before_tool`) espelhando o exemplo de envelope em `techspec.md`; `Decision.String()` usa `UPPER_SNAKE_CASE` (`BLOCK`) espelhando o exemplo do log de decisão (`.aispec/hook-decisions.jsonl`) e a nomenclatura de RF-12 do PRD (`ALLOW`/`BLOCK`/`WARN`/`NOT_APPLICABLE`/`ERROR`). Ambas as convenções são as literalmente mostradas em `techspec.md` seção "Modelos de Dados", não uma escolha arbitrária.
- Testes de integração não se aplicam a esta tarefa (pacote aditivo, sem consumidor) — decisão consciente registrada conforme a própria task file (seção "Testes da Tarefa"), não uma lacuna.

## Riscos Residuais
- **Sujeira de sessão anterior identificada e preservada intocada**: no início da execução, `git status --short` revelou `.specs/prd-hooks-canonicos-vendor-neutral/tasks.md` e `2.0_execution_report.md` modificados (não commitados), além de artefatos não rastreados (`evidence/task-2.0/*`, `.checkpoints/2.0.yaml`, `2.0_execution_result.json`) que promovem a tarefa 2.0 de `pending` para `done` — em contradição com a afirmação do prompt de que 1.0/2.0/3.0 já estariam commitadas (o commit `55f9f13` de fato só contém 1.0 e 3.0; 2.0 permanece pendente em HEAD). Não fazem parte do escopo desta tarefa 4.0 e não foram tocados, revertidos nem incluídos no commit desta tarefa — reportado aqui para que a sessão responsável por 2.0 feche corretamente esse estado (ou para investigação de eventual execução concorrente).
- `make coverage` (total do repositório) permanece abaixo do gate de 75% — 71.6%, pré-existente à tarefa (baseline sem os pacotes novos: 71.5%), causado por `internal/skills` e por outros pacotes de baixa cobertura já existentes antes desta tarefa (ex.: `internal/reviewverdict` 13.2%). Fora do escopo de correção desta tarefa 4.0, que é estritamente aditiva ao contrato de hooks.
- `TestAllLangsAreExhaustivelyWired/dotnet` falha por desenho (task 3.0, "(vermelho por design para dotnet, sucessor 7.0)", commit `55f9f13`) — não é regressão desta tarefa; confirmado por `git status --short` sem diff em `internal/skills`, `internal/detect` ou `internal/contextgen`.
- `internal/hookaudit` ainda não substitui `.aispec/governance-escapes.log` (TSV) em produção — nenhum ponto de escrita real foi migrado nesta tarefa (isso é aditivo por desenho, conforme a task file: "os dois coexistem" até a tarefa de limpeza posterior). O log JSONL só existe hoje como artefato testável de biblioteca, sem escritor de produção plugado — a integração é trabalho das tarefas 10.0/11.0/12.0.

## Conflitos de Regra
- none
