# Generated: 2026-05-22T00:00:00Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 7.0
- Título: Paridade 4×4 + cross-project + invariante de fallback
- Arquivo: .specs/prd-fundacao-portatil/task-7.0-paridade-4x4-cross-project.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-fundacao-portatil/prd.md
- TechSpec: .specs/prd-fundacao-portatil/techspec.md
- Governança: agent-governance (auto-carregado), go-implementation (inferido do diff)

## Comandos Executados
- `go test ./internal/parity/...` -> PASS (coverage: 81.0%)
- `go test -tags=integration ./internal/parity/...` -> PASS
- `go test ./...` -> PASS (zero falhas em todos os pacotes)
- `make test` -> PASS
- `make integration` -> PASS
- `go vet ./internal/parity/...` -> clean
- `golangci-lint run ./internal/parity/...` -> 0 issues
- `golangci-lint run` (repo inteiro) -> 3 issues pré-existentes (store_test.go, engine_test.go, não tocados por esta tarefa)

## Arquivos Alterados
- `internal/parity/parity.go` — adicionado `invFB01FallbackLauncherChainDeclared` (RF-19) e registrado em `Invariants()`
- `internal/parity/parity_test.go` — adicionados testes 4x4 table-driven (RF-18), testes de fallback argv parity (RF-19), testes de FB01
- `internal/parity/e2e_parity_test.go` — novo arquivo com build tag `integration`: testes cross-project e fallback E2E

## Resultados de Validação
- Testes: pass
- Lint: pass
- Cobertura `internal/parity`: 81.0% (≥ 75% exigido)
- Veredito do Revisor: APPROVED

## Diff Reviewed

sha=HEAD
verdict=APPROVED
tool=claude

## Coverage

package=internal/parity
delta=81.0%

## Subtarefas

- [x] 7.1 Refatorar `parity_test.go` para gerar combinações 4×4 table-driven
  - `TestParity_Matrix4x4_AllSubsets` — 15 subconjuntos das 4 CLIs, paralelo
  - `TestParity_Matrix4x4_CodexSubsets_CompactProfile` — variações de profile
  - `TestParity_Matrix4x4_InvariantCoverage` — cobertura de grupos de invariantes
- [x] 7.2 Adicionar invariante de fallback launcher em `parity.go` (`Invariants()`)
  - `invFB01FallbackLauncherChainDeclared` — nível Common, verifica AGENTS.md referencia agent-governance
  - Registrado em `Invariants()` como último elemento
- [x] 7.3 Criar `internal/parity/e2e_parity_test.go` (build tag `integration`)
  - `TestE2EParity_CrossProject_InstallAndValidateInvariants` — install em t.TempDir() + invariantes
  - `TestE2EParity_CrossProject_AllTools` — todas as 4 CLIs cross-project
  - `TestE2EParity_CrossProject_Idempotency` — idempotência RF-09+RF-18
  - `TestE2EParity_Matrix4x4_AllSubsets_CrossProject` — 15 subconjuntos cross-project
- [x] 7.4 Validar argv/resultado idêntico via binário direto vs fallback para as 4 specs
  - `TestE2EParity_FallbackLauncher_ArgvIdentical` — RF-19 argv via LookPath fake
  - `TestE2EParity_FallbackLauncher_ResultIdenticalToDirectBinary` — direto vs fallback argv comparison
  - `TestE2EParity_FallbackLauncher_BothUnavailable` — error path para todas as 4 specs
  - `TestParity_FallbackArgvParity_AllSpecs` — unit test RF-19 (LookPath fake)
  - `TestParity_FallbackArgvParity_DirectBinaryWins` — ADR-017: canônico primeiro
- [x] 7.5 Garantir verde de toda a suíte de paridade (existentes + novos)
  - Todos os 39 testes unitários passam
  - Todos os 7 grupos de testes de integração passam

## Critérios de Sucesso

- [x] Matriz 4×4 cobre as 4 CLIs e subconjuntos; todas as invariantes aplicáveis passam
- [x] Teste cross-project instala em repo temporário e valida invariantes com sucesso
- [x] Invariante de fallback: com binário direto ausente, o fallback produz resultado idêntico (RF-19)
- [x] `make test` verde; `go test -tags=integration ./internal/parity/...` verde
- [x] Cobertura ≥ 75% no pacote `parity` (obtido: 81.0%)

## Suposições
- `invFB01FallbackLauncherChainDeclared` verifica presença de "agent-governance" em AGENTS.md como proxy estrutural para o fallback launcher (o conteúdo gerado não inclui "ACP"/"fallback" diretamente)
- Testes de integração E2E usam binários fake via LookPather — sem dependência de binário ACP real no PATH
- A suíte de integração do Makefile (`make integration`) não inclui `./internal/parity/...` explicitamente; os testes de integração de parity são executados separadamente via `go test -tags=integration ./internal/parity/...`

## Riscos Residuais
- Se o template de contextgen for alterado para remover "agent-governance" do AGENTS.md, FB01 quebrará (esperado — é o comportamento desejado de detecção de drift)
- Os 3 lint issues pré-existentes em `store_test.go` e `engine_test.go` permanecem; não são escopo desta tarefa

## Conflitos de Regra
- none
