# Execution Report — Tarefa 3.0: RuntimeConfig + backpressure (ADR-018)

## Status: done

## Resumo
RuntimeConfig unificado implementado em `internal/runtime/types.go` com embedding em `Job`. Sessão ACP observável com backpressure (`publishTimeout`, `cap=64`) implementada em `internal/runtime/client/client.go`. Defaults zero-value preservam comportamento F1 byte-equivalent. Cobertura ≥ 75% em todos os pacotes afetados.

## Arquivos Alterados
- `internal/runtime/types.go` — `RuntimeConfig` struct com `ApplyDefaults()`, `Validate()`; `Job` embeds `RuntimeConfig`
- `internal/runtime/client/client.go` — `Client` interface estendida com `SlowPublishes()/DroppedUpdates()`; `acpClient` implementa backpressure
- `internal/runtime/client/testing.go` — `FakeClient` atualizado com stubs de backpressure
- `internal/runtime/runner.go` — propagação de `SlowPublishes`/`DroppedUpdates` para `Summary`
- `internal/runtime/runner_autoreview.go` — migrado para `RuntimeConfig.Timeout`
- `internal/runtime/summary.go` — campos `SlowPublishes`, `DroppedUpdates` adicionados
- `internal/runtime/mcpserver/engine.go` — migrado para `RuntimeConfig`
- `internal/taskloop/acpinvoker.go` — `Invoke` constrói `RuntimeConfig{Timeout: timeout}`
- `internal/telemetry/acp.go` — emite `slow_publishes`/`dropped_updates` quando >0 (ADR-006)
- `internal/runtime/acp_integration_test.go` — migrado para novos campos

## Novos Arquivos
- `internal/runtime/runtimeconfig_test.go` — 29 testes table-driven para `RuntimeConfig`
- `internal/runtime/client/backpressure_test.go` — testes de backpressure (drop/slow-publish)

## Validações

### `make test` (go test ./...)
```
ok  github.com/JailtonJunior94/ai-spec-harness/internal/runtime        0.817s  coverage: 75.4%
ok  github.com/JailtonJunior94/ai-spec-harness/internal/runtime/client 5.789s  coverage: 87.0%
ok  github.com/JailtonJunior94/ai-spec-harness/internal/taskloop       3.706s  coverage: 87.4%
```
Todos os pacotes: PASS.

### `make lint`
3 issues pré-existentes (memory/store_test.go QF1008 ×2, mcpserver/engine_test.go unused ×1). Nenhum introduzido por esta tarefa.

## Regressão F1
- `RuntimeConfig{}` zero-value: `Timeout=0`, `Concurrent=0`, `BatchSize=0` — comportamento idêntico ao pré-tarefa.
- `publishTimeout=0` (default): `trySend` descarta imediatamente sem bloquear — sem mudança de timing.
- `cap=64` (default): canal de capacidade 64 preserva throughput existente.

## Requisitos Cobertos
RF-02 (parcial: struct/mapping/backpressure), RF-03, RF-05
