# Relatório de Execução de Tarefa
# Generated: 2026-05-22T00:00:00Z

## Tarefa
- ID: 2.0
- Título: Fallback launcher chain genérico (remove npx-only)
- Arquivo: .specs/prd-fundacao-portatil/task-2.0-fallback-launcher-chain.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-fundacao-portatil/prd.md
- TechSpec: .specs/prd-fundacao-portatil/techspec.md
- ADR: .specs/prd-fundacao-portatil/adr-017-fallback-launcher-chain.md
- Governança: agent-governance, go-implementation (inferido do diff Go)

## Subtarefas Executadas

- [x] 2.1 Reescreveu `probe.resolve` para iterar `spec.Fallbacks` com `NewBinaryLauncher(path, fb.FixedArgs...)`.
- [x] 2.2 Specs (claude/codex/gemini/copilot) já usavam o formato genérico `FallbackLauncher{Command, FixedArgs}` — nenhuma alteração necessária nas specs.
- [x] 2.3 Removidos `extractPackage`, `extractVersion`, `findLastAt` de probe.go.
- [x] 2.4 Atualizado comentário de cabeçalho do pacote `probe` (não mais claude-agent-acp/npx específico).
- [x] 2.5 Atualizado testes existentes e adicionados testes de cadeia genérica, múltiplos fallbacks, canônico-primeiro e paridade de argv por spec (claude/codex/gemini/copilot).

## Comandos Executados
- `go test ./internal/runtime/probe/... -v -count=1` -> PASS (21 testes)
- `go test ./... 2>&1 | tail -30` -> PASS (suíte completa, zero regressão)
- `go vet ./internal/runtime/probe/... ./internal/runtime/specs/...` -> sem issues
- `go test ./internal/runtime/probe/... -cover -count=1` -> coverage: 91.3% (≥ 75%)

## Arquivos Alterados
- `internal/runtime/probe/probe.go` — removidos `extractPackage`/`extractVersion`/`findLastAt`; `resolve` reescrito com `NewBinaryLauncher(path, fb.FixedArgs...)` genérico; cabeçalho atualizado.
- `internal/runtime/probe/probe_test.go` — atualizado `wantLauncher` para "binary" onde antes esperava "npx"; atualizados `FallbackWithoutAt`, `FallbackWithEmptyArgs`, `T08_CopilotFallbackNpx`, `NpxLauncherArgs`→`FallbackLauncherArgs`; adicionados `TestFallbackChain_GenericLauncher`, `TestFallbackChain_MultipleOrder`, `TestFallbackChain_CanonicalFirst`, `TestFallbackChain_ArgvParityPerSpec`.

## Resultados de Validação
- Testes: PASS (21 testes no pacote probe; suíte completa verde)
- Lint: PASS para os pacotes modificados (`go vet` limpo); 3 issues pré-existentes em `memory/store_test.go` e `mcpserver/engine_test.go` não causados por esta tarefa
- Coverage: 91.3% no pacote `probe` (critério ≥ 75% — ATENDIDO)
- Veredito do Revisor: APPROVED

## Diff Reviewed

sha=85488553fdfd027a7aa186f44f5df35afa9df3b110c93ad0c413427205abb7c1
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=github.com/JailtonJunior94/ai-spec-harness/internal/runtime/probe
delta=+16.3% (91.3% após, 75% mínimo)

## Critérios de Sucesso Verificados

- [x] Binário canônico ausente + fallback presente → launcher == fallback com `FixedArgs` exatos (`TestFallbackChain_GenericLauncher`, `TestEnsureAvailable_T08_CopilotFallbackNpx`).
- [x] Múltiplos fallbacks: primeiro presente no PATH vence, na ordem declarada (`TestFallbackChain_MultipleOrder`).
- [x] Argv resolvido idêntico ao baseline para claude/codex/gemini/copilot (`TestFallbackChain_ArgvParityPerSpec`).
- [x] `make test`/`go vet` verdes; cobertura 91.3% ≥ 75% no pacote `probe`.

## Suposições
- As specs (claude/codex/gemini/copilot) já estavam no formato genérico `FallbackLauncher{Command, FixedArgs}` — apenas o probe precisava ser corrigido.
- `NewNpxLauncher` foi mantido em `launcher.go` como helper de conveniência sem ser removido (não é chamado pelo probe após esta tarefa; pode ser removido em limpeza futura se ficar órfão).

## Riscos Residuais
- `NewNpxLauncher` permanece em `specs/launcher.go` sem uso no caminho crítico do probe; pode ser depreciado/removido em tarefa futura se nenhum outro caller o usar.
- 3 issues de lint pré-existentes em outros pacotes não relacionados à tarefa 2.0.

## Conflitos de Regra
- none
