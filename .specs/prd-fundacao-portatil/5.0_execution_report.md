# Execution Report — Tarefa 5.0: AgentDetector + --tools opcional (ADR-019)

## Status: done

## Resumo
`AgentDetector` implementado em `internal/detect/agent.go` com detecção por presença de binário no PATH + sinal secundário `isDirOnDisk`. `InstallOptions.Tools` agora opcional: quando `nil`, `install.Execute` invoca `AgentDetector.Detect()` automaticamente. Reutiliza `internal/runtime/specs` para nomes de ferramentas. Cobertura 79.8% em `internal/detect`.

## Arquivos Alterados
- `internal/config/config.go` — campo `Scope InstallScope` adicionado; `Tools` permanece `[]string`
- `internal/config/runtime.go` — sem mudança de breaking
- `internal/install/install.go` — `Execute` verifica `Tools == nil` → chama `AgentDetector.Detect()`; injeta detector via `NewService`
- `cmd/ai_spec_harness/install.go` — `--tools` flag agora opcional (omitido = auto-detect)

## Novos Arquivos
- `internal/detect/agent.go` — `AgentDetector` interface + `OSAgentDetector` impl; `NewOSAgentDetector(lookPath, homeDir)`
- `internal/detect/agent_test.go` — testes table-driven; `fakeAgentDetector` com LookPath stub

## Validações

### `make test`
```
ok  github.com/JailtonjJunior94/ai-spec-harness/internal/detect   0.212s  coverage: 79.8%
ok  github.com/JailtonJunior94/ai-spec-harness/internal/install   1.134s  coverage: 82.1%
ok  github.com/JailtonJunior94/ai-spec-harness/internal/config    0.351s  coverage: 92.9%
```
Todos os pacotes: PASS.

### `make lint`
3 issues pré-existentes (não introduzidos por esta tarefa).

## Segurança (R-SEC-001)
- `isDirOnDisk` usa `os.UserHomeDir()` — sem hardcode de `$HOME`.
- Subprocess exec via `exec.LookPath` apenas (sem shell injection).

## Requisitos Cobertos
RF-06, RF-12
