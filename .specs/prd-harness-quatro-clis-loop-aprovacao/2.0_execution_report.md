<!-- Generated: 2026-09-10T16:55:00Z -->

# Relatório de Execução de Tarefa

## Tarefa
- ID: 2.0
- Título: Pacote de domínio do Ciclo de Aprovação, sem consumidor
- Arquivo: .specs/prd-harness-quatro-clis-loop-aprovacao/task-2.0-pacote-dominio-aprovacao.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-quatro-clis-loop-aprovacao/prd.md (Bloco D RF-30..RF-45, Bloco E RF-46..RF-50)
- TechSpec: .specs/prd-harness-quatro-clis-loop-aprovacao/techspec.md (seções "Design de Implementação → Pacote de domínio `internal/approval`", "Interfaces Chave", "A invariante central, garantida por tipo", "Tabela de transições", "Fingerprint estável", "Ordem deliberada das checagens", "Abordagem de Testes → Testes Unitários")
- ADR: .specs/prd-harness-quatro-clis-loop-aprovacao/adr-001-ciclo-de-aprovacao-agregado.md (itens 1 e 3 do Plano de Implementação)
- Governança: AGENTS.md base contract, `.claude/rules/governance.md` (R-GOV-001), `.claude/rules/code-style.md` (R-STYLE-001.3), skill `agent-governance`, skill `go-implementation` (Regras HARD R0–R7), skill processual declarada `domain-modeling-production`
- Skills processuais declaradas: `domain-modeling-production` (task file `## Skills Necessárias` e coluna `Skills` de tasks.md para 2.0 — em sync)
- Correções mandatórias do coordenador aplicadas: (a) todo o código-fonte e nomes de teste em inglês, zero comentários em Go; (b) proibição de prefixo `_` em identificadores Go (renomeados `_verdictPrefixes`, `_allowedTransitions`, `_fingerprintSeparator`, `_defaultMaxRounds`).

## Comandos Executados
- `source .agents/lib/check-invocation-depth.sh` -> ok (depth=0); `export AI_PREFLIGHT_DONE=1` (pré-voo já validado pelo orquestrador)
- `go build ./internal/approval/...` -> ok
- `go vet ./internal/approval/...` -> ok
- `make mocks` -> `Mocks normalizados: 43 arquivo(s)`; gerou `internal/approval/mocks/{reviewer,fixer,repository}.go`
- `make check-mocks` -> `Mocks: OK (sincronizados com mockery.yml)`
- `go build ./... && go vet ./... && go test ./... -count=1` -> tudo `ok`, zero `FAIL` (evidência: `evidence/task-2.0/build-vet-test.log`)
- `go test ./internal/approval/... -count=1 -race -cover -v` -> `100` asserções `--- PASS`, `coverage: 88.8% of statements`, `ok` (evidência: `evidence/task-2.0/approval-suite.log`)
- `go test ./internal/approval/ -run Fuzz -v -count=1` -> seed corpus + regressão `FuzzFingerprint/aee628d832253db9` PASS
- `go test ./internal/approval/ -run '^$' -fuzz FuzzTranslator -fuzztime 30s` -> `PASS` (1.19M execs, sem crash)
- `go test ./internal/approval/ -run '^$' -fuzz FuzzFingerprint -fuzztime 30s` -> `PASS` (1.08M execs, sem crash) (evidência: `evidence/task-2.0/fuzz.log`)
- `go list -deps ./internal/approval | grep '^github.com/JailtonJunior94'` -> apenas `internal/approval` (zero dependência de pacote interno); `go list -f '{{.Imports}}'` -> `context crypto/sha256 encoding/hex errors fmt iter slices strings` (subconjunto do autorizado; `cmp` não usado) (evidência: `evidence/task-2.0/isolation-lint.log`)
- `ls internal/approval/*.go | grep -v _test | wc -l` -> `16`
- `golangci-lint run --config .golangci.yml ./internal/approval/...` -> `0 issues.`
- `gofmt -l internal/approval/` -> vazio
- `make check-spec-paths` -> `Resultado: 9 OK, 0 FAIL`
- `ai-spec validate-result execution .../2.0_execution_result.json --task-id 2.0 --verify-physical --prd-dir ... --exclude <report>` -> `OK: resultado SDD execution valido`

## Arquivos Alterados
- internal/approval/errors.go (planejado→real) — sentinelas `errors.Is` + tipos `errors.As` (`InvalidMaxRoundsError`, `ForbiddenTransitionError` com método `Is`)
- internal/approval/identities.go — `TaskIdentity`, `AgentIdentity` (dado opaco, construtor validante, zero-value inválido)
- internal/approval/verdict.go — `Verdict` conjunto fechado de 4 (`iota+1`), `Approves()` verdadeiro só para `VerdictApproved`
- internal/approval/stop_reason.go — `StopReason` conjunto fechado de 5 (`approved`/`max_rounds`/`no_convergence`/`empty_diff`/`blocked_input`)
- internal/approval/finding.go — `Severity` (`iota+1`), `BugLevel`, `Finding`, tradução determinística 4→3 níveis (`critical→critical`, `high→major`, `medium|low→minor`)
- internal/approval/evidence.go — `EvidenceForm` (3 formas RF-48), `EvidenceLine`, `AcceptanceCriterion`, `CriteriaMap` imutável com `Complete()`/`Bound()`/`Total()`
- internal/approval/proof.go — `ApprovalProof` + `NewApprovalProof` exigindo veredito que aprova **e** mapa 1:1 completo simultaneamente; zero-value nunca válido
- internal/approval/fingerprint.go — `Fingerprint` + `FingerprintCalculator` SHA-256 sobre conjunto ordenado e deduplicado de `{severity, file, rule}`; duas não-calculadas nunca iguais
- internal/approval/policy.go — `ApprovalPolicy` imutável, Functional Options, `Decide` na ordem barata→cara; teto `< 1` recusado na construção com `InvalidMaxRoundsError`
- internal/approval/state.go — `State` (`iota+1`) + `allowedTransitions` (ausência = proibição; `StateApproved`/`StateBlocked` → `nil`); `stateMachine.transition` caixa-branca
- internal/approval/round.go — entidade `Round`, imutável após `Complete`
- internal/approval/translator.go — camada anticorrupção fail-closed, variantes PT/EN, token por igualdade exata (sem substring solta)
- internal/approval/ports.go — `Reviewer` (texto bruto, nunca `Verdict`), `Fixer` (só `error`), `Repository` + DTOs opacos `ReviewRequest`, `FixRequest`, `ReviewerOutput`, `Checkpoint`, `ReviewTarget`
- internal/approval/events.go, internal/approval/result.go — eventos de domínio e `CycleResult` (`NewApprovedResult` recusa prova zero-value)
- internal/approval/cycle.go — agregado `Cycle` com comando único `Run`; nenhum chamador externo abre rodada
- internal/approval/mocks/{reviewer,fixer,repository}.go — mocks gerados por `mockery.yml`
- internal/approval/*_test.go — suíte de invariantes (testify/suite table-driven): `transitions_test.go`, `proof_test.go`, `fingerprint_test.go` (+`FuzzFingerprint`), `translator_test.go` (+`FuzzTranslator`), `policy_test.go`, `value_objects_test.go`, `cycle_flow_test.go` (external, mocks), `helpers_test.go`
- internal/approval/testdata/fuzz/FuzzFingerprint/aee628d832253db9 — seed de regressão do fuzz
- mockery.yml — três portas novas do pacote `internal/approval`
- Makefile — alvo `fuzz` ganha `FuzzTranslator` e `FuzzFingerprint` de `internal/approval`

## Resultados de Validação
- Testes: pass (`go build ./... && go vet ./... && go test ./... -count=1` -> todos `ok`, zero `FAIL`; `go test ./internal/approval/... -count=1 -race -cover -v` -> 100 `--- PASS`, cobertura 88.8%; fuzz `FuzzTranslator`/`FuzzFingerprint` 30s cada sem crash)
- Lint: pass (`golangci-lint run` -> `0 issues.`; `go vet ./...` limpo; `gofmt -l` vazio; `make check-mocks` verde)
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- `go build ./internal/approval/... && go vet ./internal/approval/...` verde -> comprovado: ambos exit 0 registrados em `evidence/task-2.0/build-vet-test.log` e `evidence/task-2.0/isolation-lint.log`
- Isolamento do domínio: nenhum import de `internal/(runtime|taskloop|detect|install|manifest)`, `acp`, `os`, `path/filepath`, `net/http` -> comprovado: `go list -deps ./internal/approval | grep '^github.com/JailtonJunior94'` retorna apenas o próprio pacote; `go list -f '{{.Imports}}'` produz exatamente `context, crypto/sha256, encoding/hex, errors, fmt, iter, slices, strings` (subconjunto do conjunto autorizado) — `evidence/task-2.0/isolation-lint.log`
- Os 15 arquivos existem (`ls internal/approval/*.go | wc -l` >= 15 excluindo `_test.go`) -> comprovado: `16` arquivos de produção listados em `evidence/task-2.0/isolation-lint.log`
- Uma asserção de teste por transição proibida, visíveis via filtro `-run` e nomeadas individualmente (correção não aprova; aprovado é terminal; bloqueado não retoma; sem auto-transição) -> comprovado: suíte `TestForbiddenTransitionSuite/TestForbiddenTransitions` com subtestes `fix_cannot_approve`, `approved_is_terminal_to_{review,fix,blocked}`, `blocked_does_not_resume_{review,fix}`, `no_self_transition_in_{review,fix}` — caixa-branca sobre `stateMachine.transition`; 100 `--- PASS` em `evidence/task-2.0/approval-suite.log` (nota: nomes em inglês por regra mandatória do coordenador — o filtro equivalente é `-run TestForbiddenTransitionSuite` no lugar de `-run TestTransicao`)
- Uma asserção de teste por forma de recusa da prova, toda recusa satisfaz `errors.Is(err, ErrInsufficientProof)` -> comprovado: `TestApprovalProofSuite` com `TestRejectsApprovedWithRemarks`, `TestRejectsRejected`, `TestRejectsBlocked`, `TestRejectsZeroValueVerdict`, `TestRejectsIncompleteMap`, `TestRejectsUnverifiableCriterion`, `TestRejectsEmptyMap`, `TestApproveRejectsZeroValueProof` (via `Cycle.approve`), `TestApprovedResultRejectsZeroValueProof` — todas verificam `ErrorIs(err, ErrInsufficientProof)` — `evidence/task-2.0/approval-suite.log` (`ErrInsufficientProof` é o nome inglês de `ErrProvaInsuficiente`)
- Fingerprint: estabilidade sob reordenação, duplicata, mudança de linha e de identificador do agente; instabilidade sob severidade/arquivo/regra; duas não-calculadas nunca iguais -> comprovado: `TestFingerprintSuite` (`TestStableUnderReordering`, `TestStableUnderDuplicates`, `TestStableUnderLineNumberChange`, `TestStableUnderAgentIdentifierChange`, `TestUnstableUnderSeverityChange`, `TestUnstableUnderFileChange`, `TestUnstableUnderRuleChange`, `TestUncomputedNeverEqual`, `TestComputedEmptyIsStable`) — `evidence/task-2.0/approval-suite.log`
- Tradutor: `APPROVED_WITH_REMARKS` não lido como aprovado; texto sem declaração canônica → `BLOCKED`; variantes PT e EN -> comprovado: `TestTranslatorSuite` (`TestCanonicalDeclarations` com 10 casos EN/PT/markdown, `TestApprovedWithRemarksIsNotReadAsApproved`, `TestFailsClosedWithoutCanonicalDeclaration` com 6 casos incl. `status: approved` e `verdict: something-else`) — comparação por igualdade exata do token, sem substring — `evidence/task-2.0/approval-suite.log`
- Política: teto `< 1` recusado na construção com erro tipado; ordem das checagens verificada pela ausência de chamada ao mock de `Fixer` -> comprovado: `TestApprovalPolicySuite/TestRejectsMaxRoundsBelowOne` (`errors.As` para `InvalidMaxRoundsError`, valores 0/-1/-10) e `TestApprovalPolicySuite/TestDecideChecksOrder`; `TestCycleFlowSuite/TestBlockedInputStopsWithoutCallingFixer` e `TestApprovesOnFirstRound` asseguram `fixer.AssertNotCalled` quando uma checagem barata (ou a prova) decide — `evidence/task-2.0/approval-suite.log`
- Fuzzing presente e executável para fingerprint e tradutor (`-fuzz ... -fuzztime 30s` sem crash) -> comprovado: `FuzzTranslator` e `FuzzFingerprint` em `internal/approval/`, 30s cada, `PASS` sem crash, registrados em `evidence/task-2.0/fuzz.log`; também no alvo `make fuzz`
- `make mocks && make check-mocks` verde, `internal/approval/mocks/` para as três portas -> comprovado: `make check-mocks` -> `Mocks: OK (sincronizados com mockery.yml)`; `internal/approval/mocks/{reviewer,fixer,repository}.go` gerados — `evidence/task-2.0/isolation-lint.log`
- `go test ./internal/approval/... -count=1 -cover` verde, cobertura registrada -> comprovado: `coverage: 88.8% of statements`, `ok` — `evidence/task-2.0/approval-suite.log`
- `go build ./... && go vet ./... && go test ./... -count=1` verde, nenhuma regressão -> comprovado: todos os pacotes `ok`, zero `FAIL` em `evidence/task-2.0/build-vet-test.log` (o pacote ainda não tem consumidor)
- `make check-spec-paths` continua verde com os caminhos `(planejado)` agora reais -> comprovado: `Resultado: 9 OK, 0 FAIL`; `internal/approval`, `internal/approval/portas.go`/`ports.go` e `internal/approval/mocks/` agora existem no disco
- RF-30/RF-33/RF-37/RF-41/RF-45/RF-46/RF-48/RF-49/RF-50 -> comprovado: agregado `Cycle` sem import de infra (RF-30); `NewApprovalProof` exige `VerdictApproved` + `CriteriaMap.Complete()` (RF-33/RF-49); `ApprovalPolicy.Decide` ordem barata→cara com fingerprint sobre `{severity,file,rule}` (RF-37); `StopReason` conjunto fechado de 5 (RF-41); estados terminais devolvem `CycleResult` e não `error`, sentinelas `errors.Is`/tipos `errors.As` para infra (RF-45); `Translator` fail-closed → `VerdictBlocked` sem substring solta (RF-46); `EvidenceLine` só nas 3 formas de RF-48; `Severity.BugLevel` mapeia `high→major` (RF-50) — suítes correspondentes em `evidence/task-2.0/approval-suite.log`

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` — `go test ./...` e `go test ./internal/approval/... -race` em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=1d15462f6a8df07d26d790ddb0fb8f0ac7752c64d1e6c7995bdafe58ef2ae187
verdict=APPROVED
tool=claude
result_path=.specs/prd-harness-quatro-clis-loop-aprovacao/2.0_execution_result.json

## Coverage

package=internal/approval
delta=+88.8%

## Suposições
- `AI_PREFLIGHT_DONE=1`: o orquestrador já validou skills lockfile, spec-drift e hook de governança; gates de pré-voo pulados conforme instrução.
- Cobertura tratada como pacote novo: `delta=+88.8%` (linha de base 0 — não havia pacote); não há regressão possível pois nenhum consumidor existe ainda.
- As correções mandatórias do coordenador (código em inglês, zero comentários, sem prefixo `_`) sobrepõem a convenção PT-BR/doc-comments do repositório e a regra R5.26 da skill `go-implementation` apenas para este pacote, conforme `.claude/rules/code-style.md` (R-STYLE-001.3) e mensagens explícitas do orquestrador.
- Nomes de teste/fuzz em inglês: os filtros `-run TestTransicao` / `-fuzz FuzzTradutor` citados na task file mapeiam para `-run TestForbiddenTransitionSuite` / `-fuzz FuzzTranslator` (rename forçado pela regra de idioma); o conteúdo das asserções é idêntico ao exigido.

## Riscos Residuais
- [nota] O artefato físico `evidence/task-2.0/2.0.patch` recompõe a árvore de trabalho viva inteira e, por isso, inclui as mudanças ainda não commitadas da tarefa 1.0 (o orquestrador não commitou entre 1.0 e 2.0). As mudanças próprias da tarefa 2.0 estão enumeradas em "Arquivos Alterados"; `ai-spec validate-result --verify-physical` aprova o resultado porque o patch armazenado é byte-idêntico ao recomputado.
- [nota] `techspec.md` e `tasks.md` aparecem modificados no working tree por conta da tarefa 1.0 (markers `(planejado)` e status); não foram tocados nesta tarefa além da atualização de status de 2.0 → `done` em `tasks.md`.
- [low] O `Cycle` agregado já implementa o laço completo (chama `Reviewer`/`Fixer`/`Repository`), embora a tarefa 2.0 não tenha consumidor. Isso antecipa superfície que a tarefa 4.0 vai exercitar; mantido porque a ADR-001 define "as rodadas são decisão interna do agregado" e o comando único de início. Sem risco de regressão (nenhum chamador de produção).
- [selo] `ai-spec seal-evidence`: selo pendente — o trabalho da tarefa ainda não foi commitado; SHA não inventado, commit não criado (R-GOV-001). A prova de fechamento é válida contra a árvore de trabalho viva até o commit.

## Conflitos de Regra
- R5.26 `go-implementation` (globais não exportados com prefixo `_`) vs R-STYLE-001.3 `.claude/rules/code-style.md` (proíbe prefixo `_` em identificador Go) | regra escolhida: R-STYLE-001.3 | razão de precedência: regra transversal local explícita do repositório prevalece sobre guia de skill de linguagem (governança §Precedência), e reforçada por instrução direta do orquestrador
- Convenção PT-BR + doc-comments do repositório vs instrução mandatória do coordenador (código em inglês, zero comentários) | regra escolhida: instrução do coordenador | razão de precedência: curso corrigido explicitamente pelo agente invocador durante a tarefa; escopo limitado ao código deste pacote
