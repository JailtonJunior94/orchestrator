# Generated: 2026-09-10T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.3
- Título: Extrator de critérios compartilhado e adaptador de portas em `internal/taskloop`
- Arquivo: .specs/prd-harness-quatro-clis-loop-aprovacao/task-4.3-extrator-criterios-e-adaptador-taskloop.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-quatro-clis-loop-aprovacao/prd.md (RF-34)
- TechSpec: .specs/prd-harness-quatro-clis-loop-aprovacao/techspec.md (fase F2b — Ciclo, ordem interna item 3; subseção "Integração do agregado nos três caminhos (Bloco D)": D-B1, D-B1-corolário, D-B3; subseção "Interfaces Chave")
- Governança: `.claude/rules/governance.md` (R-GOV-001), R-STYLE-001, `agent-governance`, `go-implementation`

## Comandos Executados
- `export AI_INVOCATION_DEPTH=0` + `source .agents/lib/check-invocation-depth.sh` + `export AI_PREFLIGHT_DONE=1` -> depth ok; gates de pré-voo pulados (orquestrador já validou)
- `go build ./... && go vet ./... && go test ./... -count=1` -> exit 0, 2811 passed em 71 pacotes, 0 FAIL (evidence/task-4.3/build-vet-test.log)
- `go test -tags=integration ./internal/taskloop/... -count=1` -> exit 0, 818 passed (evidence/task-4.3/integration.log)
- `go test ./internal/taskcriteria/... ./internal/taskloop/... -count=1 -v` -> exit 0, 806 pass (evidence/task-4.3/unit-tests.log)
- `make check-spec-paths check-skills-sync check-scripts-sync check-hooks-sync check-mocks` -> exit 0 (evidence/task-4.3/sync-gates.log; skills 80 em sync/0 drift; validadores 24/0; hooks 36/0; mocks OK)
- `grep -n 'approval\.' internal/taskloop/approval_adapters.go` -> 35 ocorrências (evidence/task-4.3/grep-approval.log)
- `grep -n 'Verdict' internal/taskloop/approval_adapters.go` -> exit 1, sem saída (evidence/task-4.3/grep-verdict.log)
- `git diff --stat -- internal/taskloop internal/taskcriteria` -> só arquivos novos + acceptance.go (2 ins / 54 del); runloop.go/taskloop.go/runner.go ausentes (evidence/task-4.3/diff-stat.log)
- subagent[nenhum] -> execução inline
- skill `review` invocada inline com prd.md + techspec.md como contexto -> veredito APPROVED; artefato em evidence/task-4.3/review.md (validador `validate-review-evidence.sh` -> exit 0)

## Arquivos Alterados
- internal/taskcriteria/extract.go (novo) — pacote-folha, imports só `strings`; `Extract(content []byte) []string` (itens de checklist sob `## Definition of Done` / `## Critérios de Sucesso` / `## Acceptance Criteria`, mesma detecção de seção antes embutida em `acceptance.go:144-150`) e `Pending(content []byte) []string` (subconjunto não marcado, preserva o cálculo de `missing`)
- internal/taskcriteria/extract_test.go (novo) — tabela: seções reconhecidas (EN/PT), itens marcados/não marcados/mistos, ausência de seção, encerramento por próxima seção, múltiplas seções equivalentes, texto vazio, checklist fora da seção
- internal/taskloop/acceptance.go — `parseCriteriaFromTaskFile` passa a delegar (`taskcriteria.Extract` + `taskcriteria.Pending`); `isAcceptanceSection` e o parsing inline removidos; comentários das linhas tocadas removidos (R-STYLE-001). Cálculo de `missing` e assinatura do método preservados
- internal/taskloop/approval_adapters.go (novo) — `reviewerPort` (`approval.Reviewer`) sobre `FinalReviewer`: devolve `FinalReviewResult.RawOutput` como texto bruto (nunca `ReviewVerdict`), traduz `taskloop.Finding` → `approval.Finding` com `file`/`rule` default quando ausentes, e constrói `CriteriaMap` a partir dos critérios do `ReviewRequest` vinculando cada um à evidência-sentinela de paridade `approval.NewCommandEvidence("parity-stage", "criteria gate deferred to task 5.0")` (`WithEvidence`) — shim confinado ao adaptador, `Cycle`/`NewApprovalProof` inalterados, removido em 5.0. `fixerPort` (`approval.Fixer`) sobre `BugfixInvoker`: devolve só `error`, extrai `Fail-before`/`Pass-after` reusando `extractBugfixEvidence` para um `*bugfixEvidenceRecorder` side-channel (uma entrada por `Fix`), fail-closed sem marcadores. `repositoryPort` (`approval.Repository`): `Checkpoint` via `git rev-parse HEAD`, `FullTarget`/`Delta` via `DiffCapturer.CaptureDiff`
- internal/taskloop/approval_adapters_test.go (novo) — testes de contrato: cada porta traduz a chamada e nada mais; nenhuma lê `Verdict`; `CriteriaMap` alcança `Complete()` e produz `ApprovalProof` válida no estágio de paridade; `fixerPort` fail-closed sem `Fail-before`/`Pass-after`; `repositoryPort.Checkpoint` sobre repo git real
- .specs/prd-harness-quatro-clis-loop-aprovacao/tasks.md — status da 4.3 → done

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- `go test ./internal/taskcriteria/... -count=1` verde -> comprovado: evidence/task-4.3/unit-tests.log (`go test ./internal/taskcriteria/... ./internal/taskloop/...` -> exit 0, 806 pass; `TestExtract`, `TestExtractIgnoresChecklistOutsideSection` PASS)
- `grep -n 'approval\.' internal/taskloop/approval_adapters.go` retorna ocorrências -> comprovado: evidence/task-4.3/grep-approval.log (35 ocorrências)
- `internal/taskloop/approval_adapters.go` não contém lógica de decisão de veredito; `grep -n 'Verdict'` não retorna leitura do campo -> comprovado: evidence/task-4.3/grep-verdict.log (`grep -n 'Verdict' internal/taskloop/approval_adapters.go` -> exit 1, saída vazia); `reviewerPort.Review` usa `result.RawOutput` e `result.Findings`, nunca `result.Verdict`
- `git diff --stat` mostra apenas arquivos novos + delegação em `internal/taskloop/acceptance.go`; `runloop.go`, `taskloop.go`, `internal/runtime/runner.go` intocados -> comprovado: evidence/task-4.3/diff-stat.log (internal/taskloop/acceptance.go | 56 +---; +2/-54; runloop.go/taskloop.go/runner.go ausentes do diff)
- `go test ./internal/taskloop/... -count=1` verde sem ajuste de asserção -> comprovado: evidence/task-4.3/unit-tests.log (`TestParseCriteriaFromTaskFile` e `TestAcceptanceGate_Verify` PASS; acceptance_test.go e bugfix_test.go fora do diff — evidence/task-4.3/4.3.patch)
- T-REV-01/02/04 verdes e sem alteração de asserção -> comprovado: internal/runtime/runner_autoreview_test.go ausente de evidence/task-4.3/4.3.patch; `go test ./...` -> exit 0 (evidence/task-4.3/build-vet-test.log)
- Estilo R-STYLE-001 no código novo/tocado -> comprovado: evidence/task-4.3/4.3.patch (extract.go e approval_adapters.go sem comentários; identificadores/erros/nomes de teste em inglês; sem prefixo `_` em identificador novo; comentários das linhas legadas tocadas em acceptance.go removidos); `go vet ./...` limpo (build-vet-test.log)
- `make check-spec-paths check-skills-sync check-scripts-sync` verde -> comprovado: evidence/task-4.3/sync-gates.log (spec-paths 9 OK/0 FAIL; skills 80 em sync/0 drift; validadores 24/0; hooks 36/0; mocks OK). mockery.yml não alterado — adaptadores são tipos não exportados; portas `approval.Reviewer/Fixer/Repository` já mockadas
- `go build ./... && go vet ./... && go test ./... -count=1` verde -> comprovado: evidence/task-4.3/build-vet-test.log (exit 0, 2811 passed em 71 pacotes)
- `go test -tags=integration ./internal/taskloop/... -count=1` verde -> comprovado: evidence/task-4.3/integration.log (exit 0, 818 passed)

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos correspondentes em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=93eaa90f70d6b33f8d9ff8b65e97cc60a2f1492c0b920d8d9166ef11315386a6
verdict=APPROVED
tool=claude
result_path=.specs/prd-harness-quatro-clis-loop-aprovacao/4.3_execution_result.json

## Coverage

package=internal/taskloop
delta=+0.0%

## Suposições
- A techspec manda "uma função exportada `Extract`"; foi adicionada uma segunda função `taskcriteria.Pending` para o cálculo de `missing` em `acceptance.go` sem reintroduzir a detecção de seção no consumidor — o objetivo da refatoração. `Extract` continua sendo a API consumida por `internal/runtime` na 4.7.
- `repositoryPort.Delta` devolve o diff completo do working tree (a porta `DiffCapturer` não recebe ponto de corte), preservando a semântica atual do `BugfixLoop`. A captura incremental por rodada do agregado entra na 4.6/5.0.
- A fiação das três portas aos call sites (`Service.Execute`, `RunLoop`, `ACPRunner`) pertence às fatias 4.4–4.7; esta fatia entrega só a infraestrutura comum. As portas ficam vivas pelos testes de contrato.

## Riscos Residuais
- Selo de evidência pendente: o trabalho ainda não foi commitado (R-GOV-001). `ai-spec seal-evidence` só após o commit da tarefa.
- Shim de evidência-sentinela de paridade em `parityCriteriaMap` é time-boxed: a tarefa 5.0 substitui pelo parsing da seção 1:1 real da saída da skill `review` e vira `translateReviewStatus` / o critério de encerramento.
- Segundo call site de `BugfixLoop.Run` em `applyImplementDecisions` (re-entrada `APPROVED_WITH_REMARKS` → Implement) permanece; colapso fora do estágio de paridade (registrado na techspec D-B3).

## Conflitos de Regra
- techspec "uma função exportada `Extract`" vs. necessidade de preservar `missing` sem duplicar lógica no consumidor | escolhido: adicionar `taskcriteria.Pending` | razão: R-GOV-001 política de evidência (alteração justificável por necessidade técnica demonstrável); a alternativa (reimplementar a varredura de seção em `acceptance.go`) contraria o objetivo declarado da fatia
