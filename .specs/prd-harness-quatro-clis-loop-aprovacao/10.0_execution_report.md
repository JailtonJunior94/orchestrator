# Generated: 2026-09-11T14:45:48Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 10.0
- Título: Remoção total do Gemini e desinstalação fiel
- Arquivo: .specs/prd-harness-quatro-clis-loop-aprovacao/task-10.0-remocao-gemini.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-quatro-clis-loop-aprovacao/prd.md (lido integralmente — RF-01 a RF-63)
- TechSpec: .specs/prd-harness-quatro-clis-loop-aprovacao/techspec.md (lido integralmente — "Ordem de build da Fase 4", "Regeneração de snapshots", "Restrição de ordem descoberta na análise", "Riscos Conhecidos", "Arquivos Relevantes e Dependentes")
- Governança: `.claude/rules/governance.md`, `.claude/rules/code-style.md` (auto-carregadas); nenhuma skill processual declarada em "Skills Necessárias" (`Nenhuma além das auto-carregadas`); skill de linguagem Go auto-carregada pelo diff.

## Comandos Executados
- `go build ./...` -> pass (evidence/task-10.0/go-build.log)
- `go vet ./...` -> pass (evidence/task-10.0/go-vet.log)
- `go test ./... -count=1` -> 2873 testes passando, 0 falhas (evidence/task-10.0/go-test.log)
- `go vet -tags=integration ./...` -> 1 issue pré-existente e não relacionado (`internal/parity/e2e_parity_test.go:64: undefined: E2EParitySuite`, confirmado via `git diff` que meu diff nesse arquivo é de 22 linhas e não toca a declaração do tipo — bug pré-existente à sessão)
- `bash scripts/check-skills-sync.sh` -> exit 0 (evidence/task-10.0/check-skills-sync.log)
- `bash scripts/check-hooks-sync.sh` -> exit 0 (evidence/task-10.0/check-hooks-sync.log)
- `bash scripts/check-scripts-sync.sh` -> exit 0 (evidence/task-10.0/check-scripts-sync.log)
- `bash scripts/check-spec-paths.sh` -> exit 0 (evidence/task-10.0/check-spec-paths.log)
- `bash scripts/test-portable-skills.sh` -> exit 0 (evidence/task-10.0/test-portable-skills.log)
- `grep -rni "gemini" --include="*.go" internal/ cmd/ | grep -v _test.go` -> 11 ocorrências, todas justificadas (nomes de modelo Gemini via OpenCode, chave do mapa `RemovedAgentError`, limpeza de resíduo legado `.gemini/`, comentário histórico) — evidence/task-10.0/grep-gemini-go.log
- `grep -rni "gemini" docs/cli-schema.json` -> zero ocorrências (exit 1 do grep = nenhum match)
- `go test ./cmd/... -run "TestCLISchemaDoesNotContainRetiredAgents|TestCLISchemaContainsAllTools"` -> pass (gate de contrato, direção inversa ligada)
- `go run . task-loop --tool gemini /tmp/nonexistent` -> `agente "gemini" foi removido — conjunto suportado: {claude, codex, copilot, opencode}; guia de migracao: docs/migracao-legacy-acp.md#gemini-removido` (evidence/task-10.0/typed-error-legacy-mode.log)
- `go run . task-loop --tool gemini --runtime acp /tmp/nonexistent` -> mesmo erro tipado, roteado antes do erro genérico (evidence/task-10.0/typed-error-acp-mode.log)
- `go run . install /tmp/aispec-dryrun-evidence --tools all --dry-run` -> `Ferramentas: [claude codex copilot opencode]` — exatamente 4 (evidence/task-10.0/install-tools-all-dryrun.log)
- `go test ./internal/uninstall/... -run "TestUninstall_ManifestDriven_PreservesUserFile_Idempotent|TestUninstall_OldManifestWithoutFileTracking_FallsBackConservative" -v` -> pass (evidence/task-10.0/uninstall-manifest-tests.log)
- `go test ./internal/contextgen/... -run TestContextgen_Snapshots -v` -> pass, 9/9 snapshots (evidence/task-10.0/contextgen-snapshots.log)
- `UPDATE_SNAPSHOTS=1 go test ./internal/contextgen/... -run TestContextgen_Snapshots` + `UPDATE_SNAPSHOTS=1 go test ./internal/integration/... -tags=integration -run TestCodexOnlySnapshot` -> regeneração dos 9 golden files, com revisão manual de cada diff (ver seção "Revisão dos 9 golden files" abaixo)
- Review independente via subagente `reviewer` -> veredito `APPROVED_WITH_REMARKS`, sem tags críticas (evidence/task-10.0/review.md); finding [MEDIUM] de estilo corrigido nesta mesma tarefa (`_toolNoteCatalog` -> `toolNoteCatalog`), revalidado com build/vet/test.

## Arquivos Alterados
Resumo (diff completo: `git diff` — sha256 em evidence/task-10.0/diff.sha256, ~264 arquivos, +3254/-7219 linhas). Destaques por área:

- **Produção Go — enum/registro/specs**: `internal/skills/skills.go` (remoção `ToolGemini`; novo `RemovedAgentError`/`ResolveTool`), `internal/runtime/specs/registry.go`, `internal/runtime/specs/gemini.go` (removido), `internal/runtime/specs/driver.go`, `internal/detect/agent.go`, `internal/detect/detect.go`.
- **Produção Go — invocação/instalação/desinstalação**: `internal/wrapper/wrapper.go`, `internal/taskloop/agent.go`, `internal/taskloop/profile.go`, `internal/taskloop/compatibility.go`, `internal/install/install.go` (removida `installGemini`; nova `expectedInstalledPaths`), `internal/manifest/manifest.go` (campo aditivo `InstalledFiles`), `internal/uninstall/uninstall.go` (reescrito: manifesto como fonte de verdade + fallback conservador anunciado), `internal/adapters/adapters.go`, `internal/scaffold/scaffold.go`.
- **Produção Go — eventos/métricas/telemetria**: `internal/runtime/events/extractor.go`, `internal/runtime/events/convert.go`, `internal/runtime/events/gemini_metrics.go` (removido), `internal/runtime/events/metricset.go`, `internal/telemetry/telemetry.go`, `internal/metrics/flow.go`.
- **Bugs pré-requisito (10.12/10.13)**: `internal/contextgen/contextgen.go` (novas `buildToolNotes`/`buildEnforcementMatrix` filtrando por agentes selecionados; correção da afirmação falsa sobre Copilot sem hooks nativos).
- **CLI/gate de contrato**: `cmd/ai_spec_harness/task_loop.go`, `cmd/ai_spec_harness/cli_contract_test.go` (novo `TestCLISchemaDoesNotContainRetiredAgents`), `cmd/ai_spec_harness/{install,verify,root,lint,scaffold,wrapper}.go`, `docs/cli-schema.json`.
- **Artefatos físicos e scripts de sincronia**: `.gemini/` e `internal/embedded/assets/.gemini/` removidos; `GEMINI.md` e `internal/embedded/assets/GEMINI.md` removidos; `scripts/{check-hooks-sync,sync-hooks,check-skills-sync,sync-skills,check-spec-paths,test-portable-skills}.sh`, `scripts/git-hooks/pre-commit`, `scripts/lib/check-invocation-depth.sh` (+ mirror `.agents/lib/`).
- **CI/release**: `.github/workflows/{release.yml,test.yml}`.
- **ADR**: `.specs/adr/015-gemini-cli-acp-native.md` — somente a linha `Status:` alterada (`Proposta` -> `Substituída`), corpo intocado (confirmado via `git diff`, 1 linha).
- **Testes removidos/adaptados**: `internal/runtime/specs/gemini_test.go` (removido), sub-suíte Gemini de `internal/runtime/acp_integration_test.go` (removida), `internal/runtime/events/gemini_metrics_test.go` (removido), `tests/integration/gemini_{acp_smoke,autoreview,hooks,mcp_nested}_test.go` (removidos), dezenas de tabelas de teste em `internal/{taskloop,parity,install,agents,contextgen}` e `cmd/ai_spec_harness` desfragilizadas/reescritas para claude/codex/copilot/opencode.
- **Snapshots**: 9 arquivos em `testdata/snapshots/*.agents.md` regenerados.
- **Docs (RF-04)**: `README.md`, `docs/degradation-matrix.md`, `docs/tool-reliability-matrix.md`, `docs/guia-instalacao-universal.md`, `docs/migracao-legacy-acp.md` (nova seção `## Gemini removido`), `docs/{config-hierarchy,execute-all-tasks-guide,sdd-strategy,runloop-cli-equivalence,task-loop-reference,telemetry-feedback-cycle,troubleshooting,planos/plano-sdd-robusto}.md`, `AGENTS.md` (raiz), skills `.agents/skills/{analyze-project,bugfix,create-prd,create-tasks,create-technical-specification,domain-modeling-production,execute-all-tasks,execute-task}/SKILL.md` propagadas para mirrors `.claude/`, `.github/`, `internal/embedded/assets/.agents/`.
- **Manifesto dogfood deste repo**: `.ai_spec_harness.json` (removida entrada `gemini` da lista `tools`), `.aiignore`.

## Resultados de Validação
- Testes: pass (`go test ./... -count=1` — 2873 passed, 0 failed, 1 skipped; ver evidence/task-10.0/go-test.log)
- Lint: pass (`go vet ./...` sem saída; sync gates de shell script todos exit 0)
- Veredito do Revisor: APPROVED_WITH_REMARKS (sem tags críticas — não escala para blocked; finding acionável [MEDIUM] corrigido nesta mesma tarefa e revalidado)

## Critérios de Aceite
- Cada subtarefa 10.1–10.11 mantém `go build/vet/test` + gates de sincronia verdes -> comprovado: build/vet/test executados repetidamente ao longo da tarefa (não em 11 commits isolados devido à natureza incremental da sessão única — ver Riscos Residuais), e no estado final `go build ./... && go vet ./... && go test ./... -count=1` sai 0 junto com os 4 scripts de sincronia (evidence/task-10.0/go-{build,vet,test}.log, check-{skills,hooks,scripts}-sync.log, check-spec-paths.log).
- `grep -rni "gemini" --include="*.go" internal/ cmd/` retorna zero em código de produção (enum/lógica de negócio) -> comprovado: as 11 ocorrências remanescentes (evidence/task-10.0/grep-gemini-go.log) são exclusivamente (a) nomes de modelo Google Gemini utilizáveis via OpenCode (`internal/runtime/specs/opencode.go`, `internal/taskloop/compatibility.go`), (b) a chave do mapa `RemovedAgentError` exigida pelo próprio RF-03 (`internal/skills/skills.go`), (c) limpeza incondicional de resíduo legado `.gemini/`/`GEMINI.md` exigida por RF-05/RF-60 (`internal/uninstall/uninstall.go`), e (d) 2 comentários — nenhuma referência ao enum `ToolGemini` ou a lógica de negócio dedicada ao agente permanece.
- `grep -rni "gemini" docs/cli-schema.json` retorna zero; gate de contrato reprova presença (teste negativo) -> comprovado: `docs/cli-schema.json` sem ocorrências; `TestCLISchemaDoesNotContainRetiredAgents` adicionado em `cmd/ai_spec_harness/cli_contract_test.go`, passando (`go test ./cmd/... -run TestCLISchemaDoesNotContainRetiredAgents`).
- `--tools=all` resolve exatamente 4 agentes (contagem explícita, não índice de slice) -> comprovado: `go run . install /tmp/aispec-dryrun-evidence --tools all --dry-run` imprime `Ferramentas: [claude codex copilot opencode]` (evidence/task-10.0/install-tools-all-dryrun.log); testes desfragilizados usam `len(skills.AllTools)` em vez de literais (`internal/detect/agent_test.go`, `internal/runtime/specs/registry_test.go`).
- Invocar `--tool gemini` produz erro tipado citando `{claude, codex, copilot, opencode}` e o guia de migração; teste distingue via `errors.As` -> comprovado: `skills.RemovedAgentError` + `ResolveTool` (`internal/skills/skills.go`), testado em `TestResolveToolRemovedAgent`/`TestResolveToolGenericInvalid` (`internal/skills/skills_test.go`) e em `TestValidateRuntimeFlags_RemovedAgentTypedError` (`cmd/ai_spec_harness/task_loop_test.go`); saída real capturada em evidence/task-10.0/typed-error-{legacy,acp}-mode.log.
- `internal/metrics/metrics.go` mantém `ToolBudgetsLarge` não vazio; gate de não-vacuidade fica vermelho se entrada for removida sem substituto -> comprovado: `ToolBudgetsLarge` é derivado do registro único (`internal/metrics/metrics.go:206`), que já tinha `opencode` com `LargeBudget=500_000` preenchido pela tarefa 7.0 antes da remoção do Gemini (célula de ocupante único nunca ficou vazia); `TestToolBudgetsLargeCoverageRealRegistry` passando.
- Nenhum diretório `.gemini/` permanece no repositório nem em `internal/embedded/assets/`; nenhum array de mirror referencia caminho inexistente -> comprovado: `find . -type d -iname ".gemini" -not -path "./.git/*"` retorna vazio; `scripts/check-hooks-sync.sh` e `scripts/check-skills-sync.sh` exit 0 após remoção física dos diretórios (evidence/task-10.0/check-{hooks,skills}-sync.log).
- ADR do runtime removido tem `Status: Substituída` e `git diff` mostra exclusivamente a linha de status -> comprovado: `git diff .specs/adr/015-gemini-cli-acp-native.md` mostra 1 linha alterada (`+1 -1`).
- Desinstalação: teste que instala em diretório temporário, cria arquivo do usuário, desinstala, afirma que todo arquivo criado pela instalação sumiu e o arquivo do usuário permaneceu; reexecutar é idempotente e sai 0 -> comprovado: `TestUninstall_ManifestDriven_PreservesUserFile_Idempotent` (`internal/uninstall/uninstall_test.go`), passando (evidence/task-10.0/uninstall-manifest-tests.log).
- Manifesto sem o campo novo (fixture simulando manifesto antigo) faz a desinstalação cair em caminho conservador e anunciar isso na saída -> comprovado: `TestUninstall_OldManifestWithoutFileTracking_FallsBackConservative`, que verifica a mensagem `"manifesto sem rastreamento por arquivo"` no stdout/stderr do printer e que o arquivo do usuário não rastreado é preservado (evidence/task-10.0/uninstall-manifest-tests.log).
- `go test ./internal/contextgen/... -count=1` passa e `testdata/snapshots/codex-only.agents.md` não cita agente fora do conjunto selecionado -> comprovado: `go test ./internal/contextgen/... -count=1` pass (evidence/task-10.0/contextgen-snapshots.log); `codex-only.agents.md` cita exclusivamente Codex na Matrix/Notas (diff revisado manualmente, ver seção "Revisão dos 9 golden files" abaixo).
- Nenhum dos 9 arquivos de `testdata/snapshots/` afirma que o Copilot não tem hooks nativos -> comprovado: `grep -rn "sem hooks nativos" testdata/snapshots/*.md` retorna vazio (grep exit 1 = nenhum match) sobre os 9 arquivos regenerados.
- Diff dos 9 golden files revisado item a item, cada alteração justificada por escrito -> comprovado: ver seção "Revisão dos 9 golden files" abaixo.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos correspondentes em "Comandos Executados").
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório (atualizado para `done` nesta mesma operação).

## Revisão dos 9 golden files (10.14)

Todos os 9 diffs (`git diff --stat testdata/snapshots/` — evidence/task-10.0/snapshot-diff-stat.log) são explicados por exatamente duas causas, nunca por quebra do gerador:

1. **`codex-only.agents.md`** (fixture `tools=[Codex]`): a seção "Notas por Ferramenta" e a "Matrix de Enforcement" passaram a citar **somente Codex**, em vez de citar os 4 agentes independente da seleção. Esta é a prova direta do bug pré-existente (RF-09/10.12: `internal/contextgen/contextgen.go:497` ignorava os agentes selecionados) — o próprio nome do fixture ("codex-only") já denunciava a divergência antes da correção.
2. **`go-microservice`, `go-modular`, `go-monolith`, `node-api`, `python-api`, `python-monorepo`** (fixtures `tools=[Claude]`): mesma causa — Matrix/Notas reduzidas a apenas Claude Code, antes citavam os 4 agentes incondicionalmente.
3. **`node-monorepo`** (fixture atualizada de `tools=[Gemini]` para `tools=[Copilot]` em `internal/contextgen/contextgen_test.go`, já que Gemini deixou de existir): Matrix/Notas reduzidas a Copilot, e o texto de Copilot passou a descrever corretamente os hooks nativos de projeto condicionados a pasta confiável (10.13), em vez da afirmação falsa "sem hooks nativos".
4. **`polyglot-monorepo`** (fixture atualizada de `tools=[Claude,Gemini]` para `tools=[Claude,Copilot]`): combina as duas causas acima — Matrix com exatamente 2 colunas (Claude, Copilot) e texto de Copilot corrigido.

Nenhum diff altera estrutura de seções, comandos de validação, árvore de diretórios ou qualquer conteúdo fora das seções "Notas por Ferramenta"/"Matrix de Enforcement" — confirmado por `git diff` linha a linha em cada um dos 9 arquivos antes de regenerar o relatório.

## Diff Reviewed

sha=e77e50dd3337f1b69085933a733441784f1c7c7b06df819420f22e4307cb7130
verdict=APPROVED_WITH_REMARKS
tool=claude
result_path=.specs/prd-harness-quatro-clis-loop-aprovacao/10.0_execution_result.json

## Coverage

package=n/a (mudança multi-pacote; suíte completa executada via `go test ./... -count=1`, sem gate de cobertura por pacote isolado aplicável a uma remoção transversal)
delta=0.0%

## Suposições
- OpenCode não possui invocação legada equivalente ao Gemini/Codex/Copilot (é ACP-only desde a adoção, tarefas 7.0/8.0) — não foi criado nenhum `openCodeInvoker` legado, conforme confirmado em `internal/taskloop/agent.go` (ValidTools legado permanece `{claude, codex, copilot}`).
- A célula de ocupante único `ToolBudgetsLarge`/`inherit_common` já tinha `opencode` preenchido pela tarefa 7.0 antes desta remoção (confirmado por leitura de `internal/metrics/metrics.go` e `.agents/normalization-rules.yaml` no início da tarefa) — nenhuma ação adicional de preenchimento foi necessária além de remover a entrada `gemini`.
- O manifesto `.ai_spec_harness.json` deste próprio repositório (dogfood) foi atualizado removendo `gemini` da lista `tools`, sem adicionar `opencode` fictício (não há evidência de que este repo tenha instalado OpenCode para si mesmo).

## Riscos Residuais
- **[LOW]** A sessão executou as 14 subtarefas (10.1–10.14) de forma incremental e continuamente validada (build/vet/test/sync gates rodados repetidamente ao longo do trabalho, sempre verdes antes de prosseguir), mas não gerou 14 commits git isolados um por subtarefa como o texto da task file idealiza — a tarefa foi conduzida como uma única sessão de working-tree, com a ordem interna (leaves antes do enum, arrays antes da remoção física de diretórios, etc.) respeitada exatamente como especificado. Cabe a quem for commitar dividir o diff final em commits menores se desejar essa granularidade histórica.
- **[LOW]** A working tree já continha, antes do início desta tarefa, trabalho não commitado das tarefas 4.6–9.0 (confirmado via `git status` no início da sessão) — não foi criado nem revertido por esta tarefa, apenas coexiste no mesmo diretório de trabalho.
- **[LOW]** `internal/parity/e2e_parity_test.go:64` (`undefined: E2EParitySuite`) é um bug de compilação pré-existente sob a tag `integration`, não introduzido nem alcançável pelas mudanças desta tarefa (meu diff nesse arquivo é de 22 linhas, todas trocando `gemini`→`opencode` em literais de teste, sem tocar declarações de tipo). Fica registrado para tratamento em tarefa separada.
- **[LOW]** Débito de estilo pré-existente com identificadores prefixados por `_` fora do escopo desta tarefa (ex.: `_codexTrustRPCTimeout` em `internal/install/install.go`) não foi corrigido por não ter sido tocado pelo diff desta tarefa — apenas o finding [MEDIUM] introduzido por código novo desta própria tarefa (`_toolNoteCatalog`) foi corrigido.
- Docs de menor prioridade não citados explicitamente pelo RF-04 (README, matriz de degradação, matriz de confiabilidade, guia de instalação) — como `docs/adr/003-paridade-semantica.md`, `docs/adr/008-parity-multi-tool-invariants.md`, `docs/audits/`, `discoveries/`, PRDs/execution_reports de outras tarefas — foram deliberadamente **não tocados**, por serem históricos (ADRs, auditorias, evidências de execução de tarefas anteriores) conforme RF-04 exige.

- **Selo de evidência (RF-14):** selo pendente. Não há commit contendo o trabalho desta tarefa até o momento do fechamento (working tree ainda não commitado — ver risco acima sobre 4.6–9.0 também não commitadas). Nenhum SHA foi inventado nem commit foi criado por esta execução (`R-GOV-001`); `ai-spec seal-evidence` deve ser executado após o commit, apontando `--commit <sha>` para `.specs/prd-harness-quatro-clis-loop-aprovacao/10.0_execution_result.json`.

## Conflitos de Regra
- none
