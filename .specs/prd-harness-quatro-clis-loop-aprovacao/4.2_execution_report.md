# Generated: 2026-09-10T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.2
- Título: Evidência por rodada, revisão por delta e reset de profundidade (defeito D2 + contratos órfãos)
- Arquivo: .specs/prd-harness-quatro-clis-loop-aprovacao/task-4.2-evidencia-rodada-delta-profundidade.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-quatro-clis-loop-aprovacao/prd.md (RF-38, RF-39, RF-42, RF-44, RF-58; V-20, V-23, V-25, V-26)
- TechSpec: .specs/prd-harness-quatro-clis-loop-aprovacao/techspec.md (fase F2b — Ciclo; tabela "Componentes modificados" → internal/runtime/runner_autoreview.go)
- Governança: `.claude/rules/governance.md` (R-GOV-001), R-STYLE-001, `agent-governance`, `go-implementation`
- ADR: `.specs/prd-harness-quatro-clis-loop-aprovacao/adr-001-ciclo-de-aprovacao-agregado.md` ("Contexto" — guarda de profundidade; "Plano de Implementação" itens 6–7)

## Comandos Executados
- `export AI_INVOCATION_DEPTH=0` + `source .agents/lib/check-invocation-depth.sh` + `export AI_PREFLIGHT_DONE=1` -> depth ok; gates de pré-voo pulados (orquestrador já validou)
- `go build ./... && go vet ./... && go test ./... -count=1` -> pass, 52 pacotes `ok`, 0 FAIL (evidence/task-4.2/build-vet-test.log)
- `go test -tags=integration ./internal/runtime/... -count=1` -> 613 passed (evidence/task-4.2/integration.log)
- `go test ./internal/runtime/ -run 'TestEvidence|TestReviewPriorSHA|TestReviewRoundResets' -v -count=1` -> 5/5 PASS (evidence/task-4.2/round-tests.log)
- `go test ./internal/invocation/ -run ResetDepth -v -count=1` -> 3/3 PASS (evidence/task-4.2/invocation-tests.log)
- `make check-spec-paths check-skills-sync check-scripts-sync check-hooks-sync` -> pass (evidence/task-4.2/sync-gates.log; skills 80 em sync / 0 drift)
- `git grep -n 'buildReviewPointer' internal/` -> sem resultado (exit 1) (evidence/task-4.2/d2-grep.log)
- `grep -n '_defaultMax = 2' internal/invocation/invocation.go` -> `12:\t_defaultMax = 2` (evidence/task-4.2/defaultmax-guard.log)
- subagent[nenhum] -> execução inline
- review skill invocada com prd.md + techspec.md como contexto -> veredito APPROVED

## Arquivos Alterados
- internal/runtime/runner_autoreview.go — `buildReviewPointer` e a sobrescrita de `<EvidenceDir>/review.md` via `os.WriteFile` removidas (D2/RF-58); `runAutoReview` delega a novo `runAutoReviewRound(ctx, j, round, priorSHA)`; endereço de evidência deriva do número da rodada (`roundReviewEvidenceDir` → `review/round-N/`); escrita exclusiva `O_CREATE|O_WRONLY|O_EXCL` com erro explícito em endereço existente (`writeRoundReviewEvidence`); persiste a saída real do revisor; `applyRoundReviewEnv` combina reset de profundidade + export de `AI_REVIEW_PRIOR_SHA` (só rodada N>1) com restauração
- internal/runtime/approval_adapters.go — `ReviewerAdapter` ganha `repo approval.Repository`; `Review` exporta `AI_REVIEW_PRIOR_SHA` por rodada (`priorCutPoint` — valor de `Repository.Checkpoint`, ausente na rodada 1) e reseta a profundidade em torno da sessão de revisão; child job continua com `AutoReview=false` (RF-44)
- internal/invocation/invocation.go — novo `Guard.ResetDepth() func()` (reset por rodada com restauração); `_defaultMax = 2` intocado (linha 12)
- internal/runtime/runner_autoreview_round_test.go (novo) — evidências de rodadas distintas coexistem e são imutáveis (`O_EXCL`); `runAutoReviewRound` numera o endereço e recusa reescrita; `AI_REVIEW_PRIOR_SHA` escopado à rodada e restaurado; valor vem de `RepositoryAdapter.Checkpoint`; ciclo de 3 rodadas abre com profundidade resetada a 0 sem child auto-review
- internal/invocation/invocation_test.go — testes de `ResetDepth` (restaura valor anterior; unset quando ausente; `_defaultMax` permanece 2)
- .specs/prd-harness-quatro-clis-loop-aprovacao/tasks.md — status da 4.2 → done

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- `grep -rn 'buildReviewPointer' internal/runtime/` não retorna escrita sobre o `review.md` da skill -> comprovado: evidence/task-4.2/d2-grep.log (git grep sem resultado, exit 1); `buildReviewPointer` e `os.WriteFile(reviewPath, ...)` removidos de runner_autoreview.go (evidence/task-4.2/4.2.patch)
- Teste D2: duas rodadas gravam evidências em endereços distintos, ambas legíveis; escrever em endereço já existente retorna erro -> comprovado: `TestEvidenceRoundAddressesAreDistinctAndImmutable` e `TestEvidenceRunAutoReviewRoundIsNumberedAndExclusive` PASS em evidence/task-4.2/round-tests.log (`go test ./internal/runtime/ -run TestEvidence`)
- `AI_REVIEW_PRIOR_SHA` presente no ambiente da sessão de revisão da rodada N>1 e ausente na rodada 1, valor igual ao ponto de corte de `Repository.CutPoint` -> comprovado: `TestReviewPriorSHAEnvIsScopedToRoundAndRestored` e `TestReviewPriorSHAValueComesFromRepositoryCutPoint` PASS em evidence/task-4.2/round-tests.log (inspeciona o ambiente lido dentro da função de sessão de revisão; round 2 == `NewRepositoryAdapter(dir).Checkpoint(ctx).String()`)
- Rodada 1 completa com `grep -n '_defaultMax = 2' internal/invocation/invocation.go` ainda na linha 12 -> comprovado: evidence/task-4.2/defaultmax-guard.log (`12:	_defaultMax = 2`); `TestReviewRoundResetsInvocationDepthWithoutRaisingLimit` roda 3 rodadas, cada uma abrindo com depth `0`, sem elevar `_defaultMax` (round-tests.log); `TestResetDepthDoesNotChangeDefaultMax` PASS (invocation-tests.log)
- RF-44: teste comprova que o `Job` filho da sessão de revisão não abre novo Ciclo -> comprovado: `TestReviewRoundResetsInvocationDepthWithoutRaisingLimit` assere `child.AutoReview == false` em cada rodada; adaptadores forçam `job.AutoReview = false` (evidence/task-4.2/4.2.patch); T-REV-04 permanece verde (build-vet-test.log)
- Estilo R-STYLE-001 no código novo/tocado -> comprovado: evidence/task-4.2/4.2.patch (código novo 100% inglês, zero comentários, sem prefixo `_` em identificador novo); `gofmt -l` limpo; `go vet ./...` limpo (build-vet-test.log)
- `make check-spec-paths check-skills-sync check-scripts-sync` verde (espelhos da skill `review` em sync) -> comprovado: evidence/task-4.2/sync-gates.log (spec-paths 9 OK/0 FAIL; skills 80 em sync/0 drift; validadores 24 em sync/0 drift; hooks 36 em sync). SKILL.md da review não foi tocada — o contrato de `:16` e `:70` já especificava `AI_REVIEW_PRIOR_SHA` e agora é honrado pelo Go
- `go build ./... && go vet ./... && go test ./... -count=1` verde -> comprovado: evidence/task-4.2/build-vet-test.log (52 pacotes `ok`, 0 FAIL)
- `go test -tags=integration ./internal/runtime/... -count=1` verde -> comprovado: evidence/task-4.2/integration.log (613 passed)

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos correspondentes em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=269238399b65028fff09f803c9eb98d5ba3ad0ecd7714eac755a0c6d747bf47a
verdict=APPROVED
tool=claude
result_path=.specs/prd-harness-quatro-clis-loop-aprovacao/4.2_execution_result.json

## Coverage

package=internal/runtime
delta=+0.0%

## Suposições
- A fiação do `Cycle` real aos três caminhos de produção e a passagem do ponto de corte por rodada pertencem à 4.3; a 4.2 entrega o primitivo por rodada e o aplica no caminho one-shot (rodada 1) e no `ReviewerAdapter`.
- O processo filho da sessão de revisão herda o ambiente do processo pai (nenhum `cmd.Env` explícito no runtime), portanto `os.Setenv`/`os.Unsetenv` em torno do spawn é o mecanismo de export por rodada.

## Riscos Residuais
- Selo de evidência pendente: o trabalho ainda não foi commitado (R-GOV-001). `ai-spec seal-evidence` só após o commit da tarefa.
- `ReviewerAdapter.priorCutPoint` usa `Repository.Checkpoint` (HEAD corrente) como ponto de corte; a 4.3 substituirá pela captura por rodada do agregado quando promover o loop.

## Conflitos de Regra
- none
