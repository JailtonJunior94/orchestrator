# Generated: 2026-09-10T19:10:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 6.0
- Título: Catálogo de Agentes como registro único
- Arquivo: task-6.0-catalogo-registro-unico.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-quatro-clis-loop-aprovacao/prd.md (RF-07, RF-22, RF-26)
- TechSpec: .specs/prd-harness-quatro-clis-loop-aprovacao/techspec.md (§Catálogo de Agentes, §Verificação de pré-condições, §Ordem de build da Fase 4 etapas 2–5)
- Governança: AGENTS.md base contract; agent-governance (auto); go-implementation (R0–R7); domain-modeling-production (declarada — princípios de VO/conjunto fechado/construtor validante aplicados); R-STYLE-001 / .claude/rules/code-style.md
- ADRs: adr-002-catalogo-de-agentes-registro-unico.md, adr-005-precondicoes-de-enforcement.md

## Comandos Executados
- `go build ./...` -> pass
- `go vet ./...` -> pass (0 issues)
- `go test ./... -count=1` -> pass (2780 testes, 70 pacotes)
- `go test -tags=integration ./internal/runtime/... ./internal/detect/... -count=1` -> pass (716 testes, 21 pacotes)
- `go test -race ./internal/runtime/specs/... -count=1` -> pass
- `go test ./internal/runtime/specs/... ./cmd/ai_spec_harness/... ./internal/skills/... ./internal/taskloop/... ./internal/detect/... -run 'Enforcement|Canonical|Agent|Matrix|Precondition|Registry|Order|Immutable|Literal|ResolveACPSpec|CatalogInSync|DetectionNeverExecutes|AllToolsDerives' -count=1 -v` -> pass (32 testes/subtests, 0 falhas)
- `go test -count=1 ./internal/parity/...` -> pass (86)
- `go test -count=1 ./internal/adapters/... ./internal/taskloop/... -run 'TestGenerate_executeTaskYAMLContract_allTools|TestE2EAgent_PromptContainsAgentBlocks'` -> pass
- `go test -tags=integration -run TestTokenBudget ./internal/integration/...` -> pass (make budget)
- `bash scripts/check-skills-sync.sh` -> exit 0
- `bash scripts/check-scripts-sync.sh` -> exit 0
- `bash scripts/check-hooks-sync.sh` -> exit 0
- `bash scripts/check-mocks.sh` -> exit 0
- `bash scripts/check-spec-paths.sh` -> exit 0
- `git status --porcelain | grep -E "assets/|golden|testdata"` -> vazio (nenhum golden/asset de governança alterado)
- `ai-spec validate-result execution ...6.0_execution_result.json --task-id 6.0 --verify-physical` -> OK
- `gofmt -l internal/ cmd/` -> limpo

## Arquivos Alterados
- internal/runtime/specs/registry.go (novo) — registro único: `Agent` (VO imutável), `AgentIdentity`, `DetectionSignals`, `agentRegistry` via inicializador de variável de pacote (ADR-002), `Registry()`, `CanonicalOrder()`, `AgentByID()`, `ACPSpecCatalog()`, `ResolveACPSpec()` (erro tipado), `VerifyCatalogSync()`, `SpecOf()`, sentinelas `ErrUnknownAgent`/`ErrToolNotInCatalog`/`ErrCatalogOutOfSync`. Strings de erro/panic em inglês; identificadores privados sem prefixo `_`; zero comentários.
- internal/runtime/specs/enforcement.go (novo) — `CanonicalPoint` (conjunto fechado iota+1, zero-value reservado), `PointCoverage`, `Enforcement` + `NewEnforcement` (RECUSA cobertura incompleta), `EnforcementPrecondition` + `NewEnforcementPrecondition` (remédio acionável obrigatório), `PreconditionState` (current/inert/unknown, `IsFailure()`), `PreconditionKind`, sentinelas. `canonicalPoints` sem prefixo `_`; strings em inglês; zero comentários.
- internal/runtime/specs/registry_test.go, enforcement_test.go, literal_scan_test.go (novos) — invariantes, matriz obrigatória, gate de ordem canônica única, scanner AST de literais de lista de agentes (`literalScanAllowlist`/`agentIDs` sem `_`; mensagens e justificativas em inglês)
- internal/runtime/specs/driver.go — `ParseDriverID` deriva de `CanonicalOrder()` (elimina 3ª enumeração implícita)
- internal/skills/skills.go — `AllTools` e `ParseTool` derivam de `specs.CanonicalOrder()` (ordem canônica única, RF-07)
- internal/skills/order_gate_test.go (novo) — gate de ordem canônica única
- cmd/ai_spec_harness/task_loop.go — `runtimeACPCatalog` (renomeado de `_runtimeACPCatalog` — linha de declaração alterada) = `specs.ACPSpecCatalog()` (fonte única)
- cmd/ai_spec_harness/cli_contract_test.go, task_loop_test.go — refs atualizadas para `runtimeACPCatalog`
- cmd/ai_spec_harness/verify.go — remove a 2ª ordem canônica fixa; consome `skills.AllTools`
- cmd/ai_spec_harness/catalog_sync_test.go (novo) — gate de sincronia do catálogo ACP do CLI (vermelho na divergência artificial)
- internal/taskloop/taskloop.go — `acpSpecCatalog` (renomeado de `_acpSpecCatalog`) = `specs.ACPSpecCatalog()`; `resolveACPSpec` retorna `(Spec, error)` — fallback silencioso para Claude REMOVIDO, substituído por `specs.ErrToolNotInCatalog`
- internal/taskloop/taskloop_test.go — `TestResolveACPSpec` reescrito (tool ausente ⇒ erro tipado, nunca Claude()); mensagens em inglês
- internal/taskloop/acp_catalog_sync_test.go (novo) — gate de sincronia do catálogo ACP interno
- internal/detect/agent.go — `allEntries` deriva do registro (`Registry()`, `Signals()`); detecção permanece pura
- internal/detect/no_exec_test.go (novo) — prova que internal/detect não contém `exec.Command`/`exec.CommandContext`
- internal/install/install.go — `specForTool` deriva de `specs.ResolveACPSpec`
- internal/runtime/mcpserver/engine.go — `resolveAgentSpec` deriva de `specs.ResolveACPSpec`
- internal/runtime/probe/probe.go — mapa `_adrByID` substituído por `adrByID(id)` derivado do registro (`ADRPath()`)
- internal/metrics/metrics.go — `ToolBudgets`/`ToolBudgetsLarge` derivados de `Agent.StandardBudget()`/`LargeBudget()`
- .specs/prd-harness-quatro-clis-loop-aprovacao/tasks.md — 6.0 -> done

## Resultados de Validação
- Testes: pass
- Lint: pass (`go vet ./...` 0 issues; golangci-lint ausente no ambiente — `gofmt -l` limpo)
- Veredito do Revisor: APPROVED_WITH_REMARKS

## Critérios de Aceite
- `go build ./... && go vet ./... && go test ./... -count=1` verde ao final -> comprovado: evidence/task-6.0/build-vet-test.log (2780 testes untagged + 716 integration-tagged; vet 0 issues)
- Teste de compilação do registro prova que construir um Agente com cobertura incompleta dos três pontos canônicos retorna erro — asserção por ponto faltante -> comprovado: `TestNewEnforcementRejectsIncompleteCoverage` em evidence/task-6.0/gate-tests.log (itera PointPreTool/PointPostTool/PointSessionEnd; cada faltante ⇒ `errors.Is(err, ErrIncompleteCoverage)`); registry.go faz `panic` no inicializador se `NewEnforcement` falhar
- Teste prova que o zero-value de Agente e de PontoCanonico é inválido e recusado pelos construtores -> comprovado: `TestAgentZeroValueIsInvalid` + `TestCanonicalPointIsClosedSet` em evidence/task-6.0/gate-tests.log (`Agent{}.Valid()==false`, `CanonicalPoint(0).Valid()==false`, `ParseCanonicalPoint("bogus")` ⇒ `ErrUnknownCanonicalPoint`)
- Teste prova imutabilidade do Agente: nenhum método com receptor ponteiro que mute campo; comparação por valor consistente -> comprovado: `TestAgentIsImmutableValueObject` (reflection: todo método de `*Agent` também existe em `Agent` valor) + `TestRegistryIsImmutable` (`Registry()[0]=Agent{}` não vaza; `AgentByID("codex").Equal` consistente) em evidence/task-6.0/gate-tests.log
- Existe uma única lista ordenada de agentes no código: o gate de ordem canônica falha se um segundo literal de ordem for introduzido -> comprovado: `TestAllToolsDerivesFromRegistry` + `TestCanonicalOrderIsSingleSource` + `TestNoAgentListLiteralOutsideRegistry` (scanner AST varre internal/ e cmd/; allowlist explícita e justificada em `literalScanAllowlist`) em evidence/task-6.0/gate-tests.log
- `verify .` e `install .` produzem os agentes na mesma ordem, verificável por diff das duas saídas -> comprovado: verify.go e install.go passam a consumir `skills.AllTools` = `specs.CanonicalOrder()` (`["claude","gemini","codex","copilot"]`); `TestAllToolsDerivesFromRegistry` prova a fonte única em evidence/task-6.0/gate-tests.log; suíte install/verify verde em build-vet-test.log
- Teste prova que resolver um tool ausente do catálogo ACP retorna erro tipado — e não Claude() -> comprovado: `TestResolveACPSpec` subtests `missing/unknown-tool` e `missing/` (`errors.Is(err, specs.ErrToolNotInCatalog)` e Spec zero-value) em evidence/task-6.0/gate-tests.log; falha se o fallback de taskloop.go for reintroduzido
- Gate de sincronia dos catálogos fica vermelho quando uma entrada é removida de apenas um dos dois pontos (verificado por teste que simula a divergência) -> comprovado: `TestRuntimeACPCatalogInSyncWithRegistry` e `TestACPSpecCatalogInSyncWithRegistry` (deletam uma entrada e assertam `errors.Is(err, specs.ErrCatalogOutOfSync)`) em evidence/task-6.0/gate-tests.log
- Matriz obrigatória (unitária): produto cartesiano agentes × três pontos canônicos, com verificação de que cada célula declara script canônico e de que nenhum agente tem cobertura menor que outro -> comprovado: `TestMandatoryMatrixAgentsByCanonicalPoints` (4 agentes × 3 pontos; `len(Coverage())==3` por agente; `NativeKey`/`ScriptPath` não-vazios) em evidence/task-6.0/gate-tests.log
- Nenhum script canônico é reimplementado por agente: gate de espelhamento verde -> comprovado: `TestMandatoryMatrixAgentsByCanonicalPoints` assert `scriptByPoint[point]` idêntico entre todos os agentes (todos apontam para `.agents/scripts/hook-prereq-gate.sh`, `.agents/hooks/post-execute-task.sh`, `.agents/hooks/subagent-stop-wrapper.sh`); `check-hooks-sync.sh` exit 0 em evidence/task-6.0/sync-gates.log
- Teste prova que internal/detect/ não executa nenhum binário: nenhuma chamada a exec.Command no pacote de detecção -> comprovado: `TestDetectionNeverExecutesBinaries` (varre .go não-teste do pacote por `exec.Command`/`exec.CommandContext`/`cmd.Run(`...) em evidence/task-6.0/gate-tests.log
- `git diff` dos golden files de governança gerada vazio — esta tarefa não muda conteúdo -> comprovado: evidence/task-6.0/golden-diff.log (vazio); ordem de `skills.AllTools` preservada (`claude, gemini, codex, copilot`); `check-skills-sync`/`check-scripts-sync`/`check-hooks-sync` exit 0 em evidence/task-6.0/sync-gates.log

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com `go test ./... -count=1` em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório (6.0 = done).
- [x] R-STYLE-001: código novo/tocado 100% inglês (identificadores, strings, arquivos, testes); zero comentários; identificadores privados sem prefixo `_`.

## Diff Reviewed

sha=4c5d2fd65b9c675e64ba7fb9cce5e5f8c69d9e0e2017585d4de6aac0f2d37e69
verdict=APPROVED_WITH_REMARKS
tool=claude

result_path=.specs/prd-harness-quatro-clis-loop-aprovacao/6.0_execution_result.json

## Coverage

package=internal/runtime/specs
delta=+0.0%

## Suposições
- Ordem canônica única adotada = ordem atual de `skills.AllTools` (`claude, gemini, codex, copilot`), escolhida para manter os golden files de governança byte-idênticos. `verify` passa a exibir nessa ordem (era `claude, codex, copilot, gemini`) — mudança de saída explicitamente mandada por RF-07/V-29 e sem teste que assertasse a ordem antiga.
- Chaves nativas de hook por CLI no registro (`PreToolUse`/`pre_tool_use`/`preToolUse`/...) refletem o nome de evento documentado; a validação de disparo real e a correção da "chave de evento inválida do Copilot" pertencem à Fase 5 (techspec §F5). A 6.0 é estrutural: a matriz assere presença de chave + script canônico compartilhado, não disparo.
- `scriptPostTool`/`scriptSessionEnd` apontam para `.agents/hooks/post-execute-task.sh` e `.agents/hooks/subagent-stop-wrapper.sh` (artefatos tool-neutros já existentes e espelhados). O gate de encerramento canônico dedicado da techspec §F5 substituirá o script de encerramento quando existir.
- Gemini permanece no catálogo (4 agentes atuais). OpenCode entra na 7.0; remoção do Gemini na 10.0. Nenhuma célula de ocupante único esvaziada.
- R-STYLE-001 aplicada a todo código novo/tocado por decisão do dono do repo: strings de erro/panic dos novos tipos traduzidas para inglês; identificadores privados sem `_` (`agentRegistry`, `canonicalPoints`, `scriptPreTool`, `runtimeACPCatalog`, `acpSpecCatalog`, `literalScanAllowlist`, `agentIDs`); legado não-tocado do repo permanece como está.

## Riscos Residuais
- [medium] Adoção do registro é parcial por escopo: `internal/metrics/flow.go` (`_flowBudgets` — matriz fina tool×governance×skill×flow), `internal/taskloop/{agent.go (legado --tool), compatibility.go (CompatibilityTable), profile.go (_providerByIDE + phase gate mapIDEToSpec)}`, `internal/wrapper/wrapper.go` e `internal/parity/parity.go` (paths de fixtures) ainda enumeram agentes por literal. São concerns adjacentes fora dos onze pontos que a 6.0 unifica; ficam em `literalScanAllowlist` com justificativa individual. Migração completa pertence a fases posteriores / F4. O gate de scanner protege os onze pontos migrados contra regressão.
- [low] `mapIDEToSpec` (perfil ACP) permanece restrito a `claude` (phase gate da Fase 5 do PRD original) — já delega a `specs.NewCatalog().Claude()`; ampliá-lo para os 4 agentes seria mudança de comportamento fora do escopo estrutural.
- [low] Chaves nativas de hook são placeholders coerentes até a validação de disparo real da Fase 5.
- [low] `panic` nos helpers de construção do registro é intencional (invariante de inicialização, análogo a `regexp.MustCompile`; ADR-002: "cobertura incompleta deixa de inicializar o registro"). Não há `panic` em caminho de runtime.
- [pré-existente] `TestExternalSkills_FrontmatterAndReferences` (`-tags=integration`) falha para as skills `domain-modeling-production` e `design-patterns-mandatory` (frontmatter sem `version`) — introduzidas no commit `ba80b56`, anteriores ao checkpoint `ef92812`; não relacionado à 6.0 (confirmado por `git stash` + rerun no baseline). Coordenador ciente; tratado em separado.
- [selo] Selo de evidência (Etapa 6) pendente: o harness não cria commits (R-GOV-001); o orquestrador commita a tarefa e então `ai-spec seal-evidence` pode ancorar `commit_sha`.

## Conflitos de Regra
- R-STYLE-001 (mensagens de erro em inglês) vs convenção pré-existente do pacote `specs` (PT-BR nas mensagens de erro do código legado) | escolhido: inglês em todo código novo/tocado da 6.0 (identificadores, strings, arquivos, testes), legado não-tocado mantido | razão de precedência: decisão explícita do dono do repo — R-STYLE-001 aplica-se a todo código novo/tocado deste PRD; legado não-tocado do repo fica como está.
