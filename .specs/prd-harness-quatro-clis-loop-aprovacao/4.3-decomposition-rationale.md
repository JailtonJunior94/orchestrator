# Generated: 2026-09-10T00:00:00Z

# Relatório de Execução — Tarefa 4.3 (Promoção do loop nos três caminhos)

## Status: needs_input

A fatia 4.3, mesmo já reduzida de 4.0, permanece grande demais para um único passo
verificável-verde. Conforme instrução explícita da tarefa ("Se descobrir que a fatia ainda é
grande demais para um passo verde, retorne `needs_input` com a sub-decomposição proposta em vez
de entregar incompleto") e R-GOV-001 (falhar de forma explícita; nenhuma aprovação sem
evidência), a execução pausa com proposta de sub-decomposição.

## Diagnóstico de raio de explosão

Escopo real confirmado por inspeção:

| Artefato | Tamanho | Papel na 4.3 |
|---|---|---|
| `internal/runtime/runner.go` | 25 KB | caminho 1 — troca `runAutoReview` único (l. 216-228) por condução do `Cycle` |
| `internal/taskloop/taskloop.go` | 36 KB | caminho 2 — `Service.Execute` (l. 244), caminho real de produção |
| `internal/taskloop/runloop.go` | 20 KB | caminho 3 — `RunLoop` delega a `BugfixLoop` só o que o agregado não absorve |
| `internal/taskloop/bugfix.go` | 8 KB | loop promovido ao agregado |
| `internal/taskloop/taskloop_test.go` | 163 KB | asserções de veredito leniente a ajustar cirurgicamente |
| `internal/taskloop/runloop_test.go` | 32 KB | idem + fluxos do terceiro caminho |
| `internal/taskloop/reviewer_test.go` | 34 KB | idem |
| `internal/runtime/runner_autoreview*_test.go` | 21 KB | T-REV-01/02/04 devem permanecer intocados |

Além disso a 4.3 exige, no mesmo passo: fechamento das 4 lacunas (RF-31), teste de paridade
entre os 3 caminhos, 5 fluxos E2E com servidor ACP falso in-process, golden files de governança
byte-idênticos e `go test ./...` + `-tags=integration` verdes.

Estado herdado (4.1/4.2): agregado `internal/approval` completo e testado; adaptadores
`ReviewerAdapter`/`FixerAdapter`/`RepositoryAdapter` em `internal/runtime/approval_adapters.go`
(porém `internal/approval` ainda **não é importado por nenhum dos três call sites** — só por
`runner_autoreview.go` via `translateReviewStatus`); `Guard.ResetDepth`; evidência de review por
rodada; `AI_REVIEW_PRIOR_SHA` por rodada.

Tensões que impedem um passo atômico verde:

1. **Adapters no pacote errado.** `ReviewerAdapter`/`FixerAdapter` vivem em `internal/runtime`,
   mas `Service.Execute` e `RunLoop` estão em `internal/taskloop` e hoje não dependem de
   `internal/runtime` para o fluxo de review (usam `FinalReviewer`/`BugfixInvoker`). Ligar
   `taskloop` ao agregado exige um adaptador `internal/taskloop` novo sobre as portas
   `approval.Reviewer/Fixer/Repository` — trabalho independente do caminho do runner.
2. **Paridade vs. preservação.** A 4.3 quer os 3 caminhos com veredito/motivo/evidência
   idênticos E T-REV-01/02/04 sem mudança de asserção E ajuste "cirúrgico" das asserções
   lenientes em ~230 KB de testes. Cada um desses é um eixo de risco isolável; juntos, um único
   `go test ./...` vermelho não permite bissecção.
3. **4 lacunas × 3 caminhos.** Fingerprint repetida sem gastar a rodada seguinte, aborto por
   ausência de mudança e ponto de corte por rodada já vivem no agregado (`cycle.go`), mas a
   fiação de cada caminho para observá-las (Summary, LoopReport, telemetria) é distinta por
   caminho.

## Sub-decomposição proposta (4 fatias, cada uma um passo verde)

- **4.3a — Adaptador de taskloop + caminho `RunLoop`.**
  Criar `internal/taskloop/approval_adapters.go` (portas `approval.Reviewer/Fixer/Repository`
  sobre `FinalReviewer`/`BugfixInvoker`/`DiffCapturer`). Migrar `RunLoop` (`runloop.go`) para
  conduzir o `Cycle`, delegando a `BugfixLoop` apenas evidência/telemetria. Ajuste cirúrgico
  só em `runloop_test.go`. Critério: `grep 'approval\.' internal/taskloop/runloop.go` retorna;
  `go test ./internal/taskloop/... -count=1` verde.

- **4.3b — Caminho `Service.Execute`.**
  Migrar `taskloop.go:244` ao mesmo adaptador de 4.3a. Concentra o grosso do ajuste cirúrgico
  em `taskloop_test.go`/`reviewer_test.go`, cada ajuste justificado por requisito. Critério:
  `grep 'approval\.' internal/taskloop/taskloop.go` retorna; suíte `internal/taskloop` verde.

- **4.3c — Caminho `ACPRunner` + 4 lacunas fiadas.**
  `runner.go:216-228` passa a conduzir o `Cycle` via adapters de `internal/runtime`. Fiar as 4
  lacunas em Summary/telemetria nos 3 caminhos. T-REV-01/02/04 intocados (manter
  `runAutoReviewRound` como implementação da porta `Reviewer`, não removê-la). Critério:
  `grep 'approval\.' internal/runtime/runner.go` retorna; `go test ./internal/runtime/ -run
  TestAutoReview -v` verde sem diff de asserção em T-REV-01/02/04.

- **4.3d — Prova de paridade + E2E.**
  Teste de paridade entre os 3 caminhos + 5 fluxos E2E com servidor ACP falso in-process
  (aprova rodada 1; aprova rodada 3 após 2 correções; aborta por fingerprint repetida sem
  gastar rodada; aborta por ausência de mudança; remediação sem diff) + teste dedicado de
  `APPROVED_WITH_REMARKS` realimentando. Critério: `go test -tags=integration
  ./internal/runtime/... ./internal/taskloop/... -count=1` verde; golden files byte-idênticos.

Dependências: 4.3a → 4.3b (mesmo adaptador); 4.3a/4.3b/4.3c → 4.3d. 4.3c independe de 4.3a/b e
pode ser paralelizado.

## Validações executadas

Nenhuma alteração de código foi feita. Working tree permanece limpo em `c92c9a3`.

- Inspeção: `internal/approval/cycle.go`, `internal/runtime/approval_adapters.go`,
  `internal/runtime/runner_autoreview.go`, `internal/taskloop/bugfix.go`,
  `internal/runtime/runner.go:190-260`, `internal/taskloop/taskloop.go:210-300`,
  `internal/taskloop/runloop.go` (grep estrutural).
- `grep -rn 'internal/approval' --include=*.go` (produção): apenas `runner_autoreview.go` e
  `approval_adapters.go` — confirma que nenhum dos 3 call sites consome o agregado hoje.

## Riscos residuais

- Selo de evidência: não aplicável (sem commit; sem `4.3_execution_result.json` selável).
- A sub-decomposição assume que o adaptador de `internal/taskloop` é aceitável
  arquiteturalmente (portas declaradas no consumidor — coerente com techspec "três portas
  declaradas no consumidor"). Confirmar antes de 4.3a.

## Conflitos de Regra

Nenhum ajuste de asserção foi aplicado (execução pausada antes da implementação).

## Decisão requerida do orquestrador

Aprovar a sub-decomposição 4.3a–4.3d (ou variação) e reescrever `tasks.md`, ou instruir
execução monolítica assumindo o risco de não-bissecção.
