# Generated: 2026-09-10T17:09:12Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 3.0
- Título: Mapa 1:1 critério-evidência como dado verificável
- Arquivo: .specs/prd-harness-quatro-clis-loop-aprovacao/task-3.0-mapa-criterio-evidencia.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-quatro-clis-loop-aprovacao/prd.md
- TechSpec: .specs/prd-harness-quatro-clis-loop-aprovacao/techspec.md
- Governança: execute-task, agent-governance (transversal), go-implementation (diff Go em `internal/evidence/`), `.claude/rules/governance.md`, `.claude/rules/code-style.md` (R-STYLE-001)
- Skills processuais declaradas: `Nenhuma além das auto-carregadas (governance + linguagem).`

## Comandos Executados
- `go build ./... && go vet ./... && go test ./... -count=1` -> BUILD OK, VET OK, 2759 testes em 70 pacotes, zero FAIL (`evidence/task-3.0/build-vet-test.log`)
- `go test ./internal/evidence/... -count=1 -v -run 'Review|CriteriaMap'` -> 21 subtestes PASS, `ok internal/evidence` (`evidence/task-3.0/evidence-go.log`)
- `bash scripts/test-validators.sh` -> `Passaram: 28 | Falharam: 0` (`evidence/task-3.0/validators.log`)
- `make check-skills-sync check-scripts-sync check-spec-paths` -> mirrors sincronizados, `Validadores em sync: 24 / Drift 0`, `check-spec-paths` `9 OK, 0 FAIL` (`evidence/task-3.0/sync-gates.log`)
- `grep -c 'não verificável' .agents/skills/review/assets/review-report-template.md` -> `3`
- `grep -nE '\[[^]]*[^\x00-\x7F][^]]*\]' .agents/scripts/validate-review-evidence.sh` -> vazio (nenhuma classe de colchete multibyte)
- `ai-spec validate-result execution .../3.0_execution_result.json --task-id 3.0 --verify-physical --prd-dir ... --exclude .../3.0_execution_report.md` -> `OK: resultado SDD execution valido`

## Arquivos Alterados
- `.agents/skills/review/assets/review-report-template.md` — nova seção `## Mapa de Critérios de Aceite` (RF-47), formato literal da linha e regras de RF-48/RF-49/RF-51
- `.claude/skills/review/assets/review-report-template.md`, `.github/skills/review/assets/review-report-template.md`, `internal/embedded/assets/.agents/skills/review/assets/review-report-template.md` — espelhos (subtarefa 3.2)
- `.agents/scripts/validate-review-evidence.sh` — asserção fail-closed do mapa 1:1: seção ausente, critério sem evidência, marcador `não verificável`, marcador inválido e linha de evidência fora das três formas de RF-48 falham; expressões com alternação, sem classe de colchete multibyte (RF-54)
- `.claude/scripts/validate-review-evidence.sh`, `internal/embedded/assets/.claude/scripts/validate-review-evidence.sh`, `internal/embedded/assets/.agents/scripts/validate-review-evidence.sh` — espelhos (subtarefa 3.5)
- `.agents/scripts/validate-task-evidence.sh` — RF-53: relatório que declara `## Critérios de Aceite` sem task file resolvível (ou task sem seção de critérios) falha mesmo com `AI_SDD_STRICT_EVIDENCE=0`; o opt-out legado só reabre o escopo pré-existente
- `.claude/scripts/validate-task-evidence.sh`, `internal/embedded/assets/.claude/scripts/validate-task-evidence.sh`, `internal/embedded/assets/.agents/scripts/validate-task-evidence.sh` — espelhos
- `internal/evidence/evidence.go` — `KindReview`, despacho em `Validator.Validate`, `func (r1 *Validator) validateReview`, `func (r1 *Validator) validateCriteriaMap` (RF-52)
- `internal/evidence/evidence_test.go` — tabela `TestValidateReview_CriteriaMap` (5 cenários), `TestValidateReview_ParityWithShellValidator` (paridade Go↔shell sob `LC_ALL=C`), `TestValidateReview_KindPreserved`
- `scripts/test-validators.sh` — casos h1..h6 (mapa ausente, incompleto, não verificável, evidência inválida, válido, `AI_SDD_STRICT_EVIDENCE=0` não reabre) cada um sob `LC_ALL=C`; caso i (RF-53 no validador de tarefa); fixtures e/f/g atualizadas com mapa 1:1 válido
- `.agents/skills/review/SKILL.md` (+ 3 espelhos) — Etapa 6 alinhada ao template novo; versão 1.3.0 -> 1.4.0

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- `grep -c 'não verificável' .agents/skills/review/assets/review-report-template.md` retorna > 0 -> comprovado: comando retornou `3` (era `0`); registrado em `## Comandos Executados`
- Os 4 arquivos de template contêm a seção nova (`atendido` presente), incluindo o espelho embutido -> comprovado: `make check-skills-sync` verde em `evidence/task-3.0/sync-gates.log` e loop `for f in .agents .claude .github` sem saída de falta; `grep -q atendido internal/embedded/assets/.agents/skills/review/assets/review-report-template.md` OK
- `bash validate-review-evidence.sh <relatório sem seção de mapa>` sai `1` e nomeia a seção ausente -> comprovado: caso `h1-ausente` em `evidence/task-3.0/validators.log` (`FALTANDO: seção 'Mapa de Criterios de Aceite' ... RF-47/RF-51`), exit=1
- `bash validate-review-evidence.sh <relatório com critério sem evidência>` sai `1` -> comprovado: caso `h2-incompleto` em `evidence/task-3.0/validators.log`, exit=1 (`FALTANDO: criterio sem linha de evidencia no mapa 1:1`)
- `bash validate-review-evidence.sh <relatório com critério "não verificável">` sai `1` -> comprovado: caso `h3-nverif` em `evidence/task-3.0/validators.log`, exit=1 (`FALTANDO: criterio marcado 'nao verificavel' proibe APPROVED (RF-49)`)
- `bash validate-review-evidence.sh <relatório completo e válido>` sai `0` -> comprovado: casos `h5-ok` e `e` em `evidence/task-3.0/validators.log`, exit=0
- Cada comando produz exit code idêntico com prefixo `LC_ALL=C` (RF-54) -> comprovado: cada caso h1..h5 em `evidence/task-3.0/validators.log` executa a variante `sob LC_ALL=C` com o mesmo exit esperado; `TestValidateReview_ParityWithShellValidator` roda o validador shell com `LC_ALL=C` e casa com o Go
- `grep -nE '\[[^]]*[^\x00-\x7F][^]]*\]' .agents/scripts/validate-review-evidence.sh` não retorna classe de colchete não-ASCII -> comprovado: comando retornou vazio (registrado em `## Comandos Executados`); expressões novas usam alternação `(e|é)` e `case` em vez de `[...]`
- `AI_SDD_STRICT_EVIDENCE=0 bash validate-review-evidence.sh <mapa incompleto>` sai `1` e não contém `gate de aceite ignorado` (RF-53) -> comprovado: caso `h6` em `evidence/task-3.0/validators.log` (`opt-out legado não faz o mapa incompleto passar` exit=1; `mapa 1:1 permanece fail-closed`); o validador de review não lê a variável
- `bash scripts/test-validators.sh` passa e nomeia os casos novos, inclusive variantes `LC_ALL=C` e o escape legado -> comprovado: `Passaram: 28 | Falharam: 0`, casos `h1..h6` e `i` nomeados em `evidence/task-3.0/validators.log`
- `grep -n 'func (r1 \*Validator) validateReview' internal/evidence/evidence.go` retorna a rotina; `go test ./internal/evidence/... -count=1 -v` passa e nomeia os casos de mapa -> comprovado: `evidence.go:250 func (r1 *Validator) validateReview(text string) []Finding`; `evidence/task-3.0/evidence-go.log` lista `TestValidateReview_CriteriaMap/*` PASS
- Paridade Go↔shell comprovada para os cinco cenários -> comprovado: `TestValidateReview_ParityWithShellValidator` (5 subtestes PASS em `evidence/task-3.0/evidence-go.log`) e `evidence/task-3.0/parity-go-shell.log` (`match=YES` nas 5 linhas: absent/incomplete/nverif/badev FAIL↔FAIL, valid PASS↔PASS)
- `make check-skills-sync check-scripts-sync` verde -> comprovado: `evidence/task-3.0/sync-gates.log` (`Todos os mirrors sincronizados`; `Validadores em sync: 24 / Drift 0`)
- `make check-spec-paths` verde -> comprovado: `evidence/task-3.0/sync-gates.log` (`Nenhum PRD sob gestao SDD encontrado` + `check-spec-paths_test.sh` `9 OK, 0 FAIL`)
- `go build ./... && go vet ./... && go test ./... -count=1` verde -> comprovado: `evidence/task-3.0/build-vet-test.log` (`BUILD OK`, `VET OK`, 2759 testes, zero FAIL)

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com `bash scripts/test-validators.sh` e `go test ./...` em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório (3.0 -> `done`).

## Rastreabilidade de Requisitos
- RF-47: seção `## Mapa de Critérios de Aceite` obrigatória para toda tarefa ativa, uma linha por critério.
- RF-48: linha de evidência válida só nas três formas (comando com saída, `arquivo:linha`, teste com resultado); validada no shell e no Go.
- RF-49: critério `não verificável` reprova (proíbe `APPROVED`).
- RF-51: validadores canônicos fail-closed — seção ausente ou mapa incompleto falham com exit não-zero.
- RF-52: `validateReview` em `internal/evidence/evidence.go` restaura a paridade Go↔shell.
- RF-53: `AI_SDD_STRICT_EVIDENCE` não alcança o mapa 1:1 nem o veredito `APPROVED` estrito.
- RF-54: expressões com alternação, sem classe de colchete multibyte; invariante travada por caso sob `LC_ALL=C`.

## Diff Reviewed

sha=da98d34f773d400626d8aeded2ede389ffd8b9bf333883790e732468a77d58ac
verdict=APPROVED
tool=claude
result_path=.specs/prd-harness-quatro-clis-loop-aprovacao/3.0_execution_result.json

## Coverage

package=internal/evidence
delta=+0.0%

## Suposições
- `AI_PREFLIGHT_DONE=1`: o orquestrador já validou skills lock, spec-drift e hook de governança; gates de pré-voo pulados conforme instrução.
- `AI_INVOCATION_DEPTH=0` exportada e `check-invocation-depth.sh` resolvido em `.agents/lib/` conforme instrução do invocador.
- Skills processuais: task file e coluna `Skills` de `tasks.md` ambas vazias (`—`) — nenhuma skill processual extra carregada (retrocompatível).
- R-STYLE-001 (código em inglês, zero comentários no código novo, sem prefixo `_`) prevalece sobre a convenção PT-BR/doc-comments do repositório para as linhas de diff em Go e shell criadas nesta tarefa. Strings de diagnóstico em PT-BR nos scripts shell foram mantidas por serem contrato de saída já publicado dos validadores (paridade com as mensagens `FALTANDO:` existentes).
- Heading do mapa: `## Mapa de Critérios de Aceite` (distinto de `## Critérios de Aceite` do relatório de execução de tarefa, que tem contrato próprio em `validate-task-evidence.sh`).
- `delta=+0.0%`: `internal/evidence` ganhou rotina e testes na mesma proporção; sem regressão de cobertura possível (nenhuma linha coberta removida).

## Riscos Residuais
- [baixo] O reconhecimento das três formas de RF-48 no validador shell e no Go é heurístico (tokens de comando + marcador de saída, `arquivo:linha`, `teste`+resultado). Aceita formas ambíguas como `12:34`; rejeita corretamente prosa sem token. Alinhado ao mesmo grau de tolerância de `EvidenceLine` em `internal/approval` (tarefa 2.0).
- [nota] `evidence/task-3.0/3.0.patch` recompõe a árvore de trabalho viva inteira e inclui mudanças não commitadas das tarefas 1.0 e 2.0 (`internal/approval/`, `.claude/rules/code-style.md`, relatórios 1.0/2.0) — o orquestrador não commitou entre as tarefas. As mudanças próprias da 3.0 estão em `## Arquivos Alterados`; `ai-spec validate-result --verify-physical` aprova porque o patch armazenado é byte-idêntico ao recomputado.
- [selo] `ai-spec seal-evidence`: selo pendente — o trabalho da tarefa ainda não foi commitado; SHA não inventado, commit não criado (R-GOV-001).

## Veredito do Revisor
- Veredito do revisor: APPROVED
- Escopo: diff das subtarefas 3.1–3.10 (template + 3 espelhos, validador de review + 3 espelhos, validador de tarefa + 3 espelhos, `internal/evidence`, `scripts/test-validators.sh`, `review/SKILL.md`).
- Base: revisão própria como dono do código contra `prd.md` (RF-47..RF-54) e `techspec.md` (fase F2a). Sem achados `critical`/`high`. Mapa 1:1 abaixo.

## Mapa de Critérios de Aceite
- [atendido] Template com seção de mapa (`grep -c 'não verificável'` > 0) -> `evidence/task-3.0/sync-gates.log` (check-skills-sync verde) e comando `grep -c` -> 3 registrado em Comandos Executados
- [atendido] Validador shell fail-closed (seção ausente, sem evidência, não verificável, inválida) -> `scripts/test-validators.sh:h1..h4` PASS em `evidence/task-3.0/validators.log`
- [atendido] Validador Go `validateReview` com paridade -> `internal/evidence/evidence_test.go` `TestValidateReview_ParityWithShellValidator` PASS em `evidence/task-3.0/evidence-go.log`
- [atendido] `AI_SDD_STRICT_EVIDENCE=0` não reabre o mapa 1:1 -> `scripts/test-validators.sh:h6` PASS em `evidence/task-3.0/validators.log`
- [atendido] Sem classe de colchete multibyte, invariante sob `LC_ALL=C` -> `grep -nE` vazio (Comandos Executados) e variantes `sob LC_ALL=C` de h1..h5 em `evidence/task-3.0/validators.log`
- [atendido] Suíte completa e gates de sincronia verdes -> `evidence/task-3.0/build-vet-test.log` e `evidence/task-3.0/sync-gates.log`
