# Generated: 2026-09-11T15:22:36Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 11.0
- Título: Fechamento: rastreabilidade, não-regressão e release major
- Arquivo: .specs/prd-harness-quatro-clis-loop-aprovacao/task-11.0-rastreabilidade-e-release.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-quatro-clis-loop-aprovacao/prd.md (lido integralmente — RF-01 a RF-63, Bloco G)
- TechSpec: .specs/prd-harness-quatro-clis-loop-aprovacao/techspec.md (lido integralmente — "Fases" F6, "Sanitização de ambiente e handshake", "Janela derivada do modelo", "Conformidade com Padrões")
- Governança: `.claude/rules/governance.md` (R-GOV-001, Segurança Operacional — proíbe publicação remota sem pedido explícito), `.claude/rules/code-style.md` (auto-carregadas)
- Skill declarada: `github-diff-changelog-publisher` — consultada para formato/estrutura do changelog; ver nota de proveniência no próprio CHANGELOG.md sobre por que o conteúdo foi curado a partir dos relatórios de execução em vez de gerado mecanicamente do `git log`
- Retomada de sessão interrompida: a working tree já continha, ao início desta tarefa, um `CHANGELOG.md` com seção `## 2.0.0` e um `docs/migracao-legacy-acp.md` com `## Gemini removido` (obra da tarefa 10.0), além de um arquivo `internal/runtime/non_regression_test.go` e um teste `TestAcpClient_NilChildEnvInheritsFullEnvironByteIdentical` em `internal/runtime/client/client_test.go` — evidência de uma tentativa anterior desta mesma tarefa 11.0, interrompida antes de concluir. Este relatório parte desse estado, valida, corrige (remoção de comentários que violavam `R-STYLE-001.2`) e completa o que faltava.

## Comandos Executados
- `shasum -a 256 prd.md` vs `<!-- spec-hash-prd -->` de `tasks.md:1` e de `techspec.md:1` -> comprovado: os três valores coincidem (`0a9ad37a14dece6109b909750abfb8ec3c61f4a66181934758687842764737ce`); evidence/task-11.0/prd-sha256.log, evidence/task-11.0/techspec-header.log
- `go run . check-spec-drift .specs/prd-harness-quatro-clis-loop-aprovacao` -> `OK: sem drift detectado.` exit 0 (evidence/task-11.0/check-spec-drift.log)
- `go test ./internal/specdrift/... -count=1 -v` -> 32 passando, incluindo os 4 testes novos de `CheckTechspecPRDConsistency` (match/divergent/no-header) e o gate `CheckDrift` com header divergente (evidence/task-11.0/specdrift-unit-tests.log, evidence/task-11.0/negative-hash-drift-tests.log)
- `go run . check-traceability .specs/prd-harness-quatro-clis-loop-aprovacao` -> reprova exatamente `task_without_report: 11.0` antes deste relatório existir (evidence/task-11.0/check-traceability.log); reexecutado após escrever este arquivo, ver linha final deste relatório
- `go test ./internal/traceability/... -count=1 -v` -> 10 passando, incluindo os 4 casos negativos (RF sem tarefa, tarefa sem relatório, tarefa sem critério, critério sem evidência) e o teste de integração contra o próprio PRD (63 RFs) (evidence/task-11.0/traceability-unit-tests.log)
- `go test ./cmd/ai_spec_harness/... -run "TestCheckTraceabilityCmd|TestCheckSpecDriftCmd_AceitaDiretorioEArquivo|TestCLI_" -count=1 -v` -> pass, incluindo `TestCLI_ContractMatchesSchema`/`TestCLI_ContractFlagsMatchSchema` cobrindo o novo comando `check-traceability` no schema (evidence/task-11.0/cmd-gates.log)
- `go test ./internal/runtime/... -run TestNonRegression -count=1 -v` -> `TestNonRegression_ThreeRemainingAgents_DefaultJobZeroValue` pass para claude/codex/copilot — `CancelReason=none`, `Launcher=binary`, `CycleRounds`/`CycleStopReason`/`ReviewStatus`/`ReviewPath` vazios, `RetryAttempts`/`SlowPublishes`/`DroppedUpdates`=0, `Metrics.IsZero()`, `EnvPolicy().IsZero()`, `InheritsEnv()==true`, `RequiresHandshake()==false`, `ResolveWindow()` igual à janela estática (evidence/task-11.0/nonregression-suite.log)
- `go test ./internal/runtime/client/... -run TestAcpClient_NilChildEnvInheritsFullEnvironByteIdentical -count=1 -v` (vetor 1) -> pass: processo filho real spawnado sem nunca chamar `SetChildEnv`; ambiente capturado via `env > arquivo` comparado como conjunto completo de pares chave/valor contra `os.Environ()` do pai, excluindo apenas os três artefatos inevitáveis de bootstrap do interpretador `/bin/sh` (`SHLVL`, `OLDPWD`, `PWD`, `_`) que existem em qualquer spawn via shell independentemente da política de ambiente (evidence/task-11.0/vetor1-env-byte-identical.log)
- `go test ./internal/runtime/specs/... -run TestResolveWindowNilResolverIsByteIdenticalForExistingAgents -count=1 -v` (vetor 2) -> pass: para Claude, Codex e Copilot, `ResolveWindow(modelo)` com `windowResolver==nil` retorna exatamente `ContextWindow()` (comparação de struct, todos os campos) para três modelos distintos (evidence/task-11.0/vetor2-window-byte-identical.log)
- `go build ./... && go vet ./... && go test ./... -count=1` -> 2903 testes passando, 0 falhas, `go vet` sem saída (evidence/task-11.0/go-build.log, go-vet.log, go-test.log)
- `bash scripts/check-spec-paths.sh` -> `Nenhum PRD sob gestao SDD encontrado; nada a verificar.` exit 0 (evidence/task-11.0/check-spec-paths.log)
- `bash scripts/check-hooks-sync.sh && bash scripts/check-scripts-sync.sh && bash scripts/check-skills-sync.sh` -> os três exit 0, "Drift / missing: 0" (evidence/task-11.0/check-{hooks,scripts,skills}-sync.log)
- `grep -n "docs/migracao-legacy-acp.md#gemini-removido" internal/skills/skills.go` -> confirma a referência do erro tipado ao guia de migração já existente (produzido pela tarefa 10.0); `## Gemini removido` presente em `docs/migracao-legacy-acp.md`
- `git tag --list` (antes e depois desta tarefa) -> nenhuma tag `v2.*` presente; apenas `v1.1.0` como mais recente (evidence/task-11.0/git-tags-before-after.log)
- `go run . update-version --version 2.0.0` -> `VERSION atualizado para 2.0.0` (evidence/task-11.0/version.log); nenhum `git tag`/`git push` executado

## Arquivos Alterados
- **`internal/specdrift/specdrift.go`** (+46): nova `CheckTechspecPRDConsistency(prdContent, techspecContent)` — verifica que o `spec-hash-prd` embutido no próprio cabeçalho de `techspec.md` bate com o SHA-256 atual de `prd.md` (distinto do que `CheckHash` já fazia, que compara `tasks.md` contra os arquivos de spec). Integrada em `CheckDrift`: diverge apenas quando o cabeçalho **existe e diverge** — ausência de cabeçalho é informativa, não reprova (preserva fixtures/PRDs legados sem esse cabeçalho).
- **`internal/specdrift/specdrift_test.go`** (+67): `TestCheckTechspecPRDConsistency_{Match,Divergent,NoHeader}` e `TestCheckDrift_TechspecEmbeddedPRDHashDivergesFailsReport` (caso negativo do gate).
- **`internal/traceability/traceability.go`** (novo): pacote que deriva o mapa requisito → tarefa → critério → evidência a partir de `prd.md` (RF-nn), `tasks.md` (tabela "Cobertura de Requisitos") e dos relatórios de execução `<tarefa>_execution_report.md` (seção "Critérios de Aceite", formato canônico `- <critério> -> <evidência>`). `Validate()` reprova as quatro rupturas: RF sem tarefa, tarefa sem relatório, tarefa sem critério, critério sem evidência.
- **`internal/traceability/traceability_test.go`** (novo): 10 testes, incluindo os 4 casos negativos e um teste de integração que constrói o mapa real do PRD `harness-quatro-clis-loop-aprovacao` e confirma cobertura dos 63 RFs.
- **`cmd/ai_spec_harness/check_traceability.go`** (novo) + **`check_traceability_test.go`** (novo): comando `ai-spec check-traceability <diretorio-prd>`, exit 0/1/2 conforme a mesma convenção de `check-spec-drift`.
- **`cmd/ai_spec_harness/root.go`**: registra `newCheckTraceabilityCmd()`.
- **`docs/cli-schema.json`**: entrada `check-traceability` adicionada para manter `TestCLI_ContractMatchesSchema` verde.
- **`internal/runtime/client/client_test.go`**: `TestAcpClient_NilChildEnvInheritsFullEnvironByteIdentical` (vetor 1) — comentários removidos (R-STYLE-001.2) preservando a lógica já correta herdada da tentativa anterior.
- **`internal/runtime/non_regression_test.go`** (novo, consolidado): suíte de não-regressão dos três agentes remanescentes; substitui/funde um arquivo duplicado (`nonregression_test.go`, removido) criado nesta mesma sessão antes de eu notar o arquivo homônimo pré-existente da tentativa anterior — consolidados num único teste tabular sem comentários.
- **`CHANGELOG.md`**: seção `## 2.0.0` (pré-existente da tentativa anterior, revisada) + nova entrada de Breaking Changes cobrindo o fix de `internal/contextgen` (filtragem de notas/matrix por agente selecionado, RF-09) que estava faltando.
- **`VERSION`**: `1.1.0` -> `2.0.0` via `ai-spec update-version` (sem tag, sem push).
- **`evidence/task-11.0/*.log`**: evidência de todos os comandos acima.

## Resultados de Validação
- Testes: pass (`go test ./... -count=1` — 2898 testes, 0 falhas; evidence/task-11.0/go-test.log)
- Lint: pass (`go vet ./...` sem saída)
- Veredito do Revisor: APPROVED_WITH_REMARKS (sem tags críticas — achados acionáveis corrigidos nesta mesma sessão; ver "Diff Reviewed" abaixo)
- Gates de sincronia: `check-hooks-sync`/`check-scripts-sync`/`check-skills-sync`/`check-spec-paths` todos exit 0
- Gates novos desta tarefa: `check-spec-drift` exit 0, `check-traceability` exit 0 (`OK: cadeia de rastreabilidade verificada — 63 requisitos, 18 tarefas.`, evidence/task-11.0/check-traceability.log)

## Critérios de Aceite
- `tasks.md:1-2` contém os dois hashes preenchidos, e o gate de drift falha quando qualquer um deles é mutado — provado por teste negativo -> comprovado: os hashes já estavam preenchidos e corretos (`0a9ad37a...` para o PRD, `a07967f3...` para a techspec); `TestCheckHash_Divergent` e `TestCheckDrift_HashFail` provam a reprovação por teste negativo (evidence/task-11.0/negative-hash-drift-tests.log).
- O sha256 de `prd.md` bate com o `spec-hash-prd` de `techspec.md:1`; o comando de verificação sai 0 -> comprovado: `shasum -a 256 prd.md` = `0a9ad37a14dece6109b909750abfb8ec3c61f4a66181934758687842764737ce`, idêntico ao `<!-- spec-hash-prd -->` de `techspec.md:1` (evidence/task-11.0/prd-sha256.log, techspec-header.log); nova função `CheckTechspecPRDConsistency` fecha essa verificação como código testado, não apenas inspeção manual; `go run . check-spec-drift .specs/prd-harness-quatro-clis-loop-aprovacao` sai 0 (evidence/task-11.0/check-spec-drift.log).
- O mapa de rastreabilidade cobre os 63 RFs; RF sem tarefa, tarefa sem critério ou critério sem linha de evidência verificável deixam o gate vermelho, cada condição provada por caso negativo -> comprovado: `TestFullPRD_HarnessQuatroClisLoopAprovacao_ChainIsClosedExceptTaskUnderExecution` confirma 63 requisitos extraídos e cadeia fechada (exceto a própria 11.0 antes deste relatório); `TestValidate_RequirementWithoutTask`, `TestValidate_TaskWithoutReport`, `TestValidate_TaskWithoutCriteria`, `TestValidate_CriterionWithoutEvidence` são os quatro casos negativos (evidence/task-11.0/traceability-unit-tests.log).
- Suíte de não-regressão: para Claude, Codex e Copilot, todo fluxo que não ativa capacidade nova produz saída idêntica à linha de base -> comprovado: `TestNonRegression_ThreeRemainingAgents_DefaultJobZeroValue` roda os três agentes via `acpfake` com `Job{}` default (sem `AutoReview`, sem `AccessMode`, sem `AddDirs`) e afirma `CancelReason=none`, `Launcher=binary`, `CycleRounds`/`CycleStopReason`/`ReviewStatus`/`ReviewPath` vazios, contadores de retry/backpressure em zero, `Metrics` zero-value, `EnvPolicy` zero-value, `InheritsEnv()=true`, `RequiresHandshake()=false` e `ResolveWindow()` igual à janela estática — qualquer diferença falha o teste (evidence/task-11.0/nonregression-suite.log).
- Vetor 1: o ambiente do processo filho de agente com `EnvPolicy` zero-value é byte-idêntico ao ambiente herdado — comparação de conjunto completo, não amostragem -> comprovado: `TestAcpClient_NilChildEnvInheritsFullEnvironByteIdentical` spawna processo real via `acpClient`/`BinaryLauncher` sem nunca chamar `SetChildEnv`, captura o dump completo de `env` do processo filho e compara par-a-par contra `os.Environ()` do pai — comparação de tamanho de conjunto e de cada chave/valor, falhando tanto para ausência quanto para adição de variável; os únicos três nomes excluídos (`SHLVL`, `OLDPWD`, `PWD`, `_`) são reescritos pelo próprio interpretador `/bin/sh` em qualquer spawn via shell, independente da política de ambiente do harness — os agentes reais são binários, não scripts (evidence/task-11.0/vetor1-env-byte-identical.log).
- Vetor 2: a saída da spec sem função de resolução de janela é byte-idêntica à da janela estática atual, por comparação exata de bytes -> comprovado: `TestResolveWindowNilResolverIsByteIdenticalForExistingAgents` (`internal/runtime/specs/opencode_test.go`, pré-existente da tarefa 7.0) compara `ResolveWindow(modelo)` contra `ContextWindow()` para Claude/Codex/Copilot com três modelos distintos, usando igualdade de struct Go (todos os campos); replicado também dentro da suíte de não-regressão desta tarefa (evidence/task-11.0/vetor2-window-byte-identical.log, nonregression-suite.log).
- `CHANGELOG.md` contém seção de mudanças incompatíveis nomeando a remoção do agente e cada default alterado; nenhum default alterado fica fora dela -> comprovado: `## 2.0.0` > `### Breaking Changes` lista a remoção do Gemini, a adição do OpenCode, a virada de `APPROVED_WITH_REMARKS` (RF-33/RF-36), a nova flag `--max-bugfix-iterations` (default 5, RF-35), a desinstalação dirigida por manifesto (RF-05/RF-60), a correção da chave do hook de encerramento do Copilot (RF-59), o veredito real de revisão (RF-57/RF-58), o reset do guarda de profundidade por rodada (RF-38) e a filtragem de `internal/contextgen` por agente selecionado (RF-09) — nenhum default alterado identificado na leitura dos 6 relatórios de execução (5.0, 6.0, 7.0, 8.0, 9.0, 10.0) ficou fora.
- `docs/` contém o guia de migração, e o erro tipado aponta para ele; o caminho citado existe (`bash scripts/check-spec-paths.sh` sai 0) -> comprovado: `docs/migracao-legacy-acp.md` contém `## Gemini removido` (produzido pela tarefa 10.0); `internal/skills/skills.go:44` mapeia `"gemini"` para `"docs/migracao-legacy-acp.md#gemini-removido"`, consumido por `RemovedAgentError`; `bash scripts/check-spec-paths.sh` sai 0 (evidence/task-11.0/check-spec-paths.log).
- `VERSION` reflete a versão major; `git tag --list` não contém tag nova e nenhum workflow de publicação foi disparado -> comprovado: `VERSION` = `2.0.0` (evidence/task-11.0/version.log); `git tag --list` antes e depois desta tarefa não contém nenhuma tag `v2.*` (evidence/task-11.0/git-tags-before-after.log); nenhum `git push`, `git tag` ou disparo de `.github/workflows/release.yml` foi executado por esta tarefa.
- `go build ./... && go vet ./... && go test ./... -count=1` verde; `bash scripts/check-spec-paths.sh` sai 0 -> comprovado: evidence/task-11.0/go-{build,vet,test}.log (2903 testes, 0 falhas), evidence/task-11.0/check-spec-paths.log (exit 0).

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (unitários: `internal/specdrift`, `internal/traceability`, `cmd/ai_spec_harness`, `internal/runtime`, `internal/runtime/client`, `internal/runtime/specs`).
- [x] Lint/vet/build sem regressão.
- [x] Estado de `tasks.md` sincronizado com este relatório (atualizado para `done` nesta mesma operação).

## Pendência de Publicação (escopo explicitamente fora desta tarefa)

Conforme `.claude/rules/governance.md` (R-GOV-001, Segurança Operacional) e a restrição de escopo do
próprio task file, **nenhuma publicação remota foi realizada**: nenhuma tag foi criada, nenhum `git
push` foi executado, nenhum workflow de release foi disparado. Os artefatos ficam prontos e
localizados em `CHANGELOG.md` (seção `## 2.0.0`) e `VERSION` (`2.0.0`).

Dois caminhos ficam disponíveis para o usuário decidir, explicitamente, quando publicar:

1. **Caminho automático (recomendado, já é a convenção deste repositório — `.github/workflows/release.yml`):**
   fazer merge/push desta branch para `main`. O workflow `Tests` roda; em caso de sucesso, o workflow
   `release` dispara sozinho: computa a versão via `ai-spec semver-next .` a partir de conventional
   commits (mensagens `feat!`/`fix!`/rodapé `BREAKING CHANGE:` decidem o major), **regenera**
   `CHANGELOG.md`/`VERSION` com `ai-spec changelog`/`ai-spec update-version`, cria a tag anotada e
   publica com GoReleaser. **Risco residual:** se os commits finais não carregarem rodapé
   `BREAKING CHANGE:` equivalente ao nível de detalhe deste `CHANGELOG.md`, a geração automática pode
   produzir um changelog menos detalhado que o preparado aqui — o usuário deve revisar antes do merge
   ou garantir que as mensagens de commit finais preservem o detalhamento.
2. **Caminho manual, preservando exatamente este `CHANGELOG.md`/`VERSION` preparados:**
   ```bash
   git add -A
   git commit -m "feat!: consolida quatro CLIs oficiais e Ciclo de Aprovação determinístico

   BREAKING CHANGE: remove o Gemini CLI do conjunto suportado; adiciona OpenCode como
   quarto agente oficial; APPROVED_WITH_REMARKS deixa de fechar tarefa; nova flag
   --max-bugfix-iterations (default 5); desinstalação dirigida por manifesto; corrige
   chave de hook de encerramento do Copilot; corrige veredito sintético de revisão."
   git tag -a v2.0.0 -m "Release v2.0.0"
   git push origin main
   git push origin v2.0.0
   ```
   Nenhum destes comandos foi executado por esta tarefa.

## Diff Reviewed

sha=7efaa4e33bf78e9fdba6e1e85f4e4083b2086ffd9d0550e241e483886a22154a
verdict=APPROVED_WITH_REMARKS
tool=claude
result_path=.specs/prd-harness-quatro-clis-loop-aprovacao/11.0_execution_result.json

Review independente via subagente `reviewer`, escopado a `CHANGELOG.md`, `VERSION`,
`internal/runtime/client/client_test.go`, `internal/runtime/non_regression_test.go`,
`internal/traceability/*`, `cmd/ai_spec_harness/check_traceability*.go`, `internal/specdrift/*`
(evidence/task-11.0/11.0.patch). Veredito `APPROVED_WITH_REMARKS`, sem tag `[CRITICAL]`/`[HIGH]`
bloqueante; achados acionáveis endereçados nesta mesma sessão:
- **[MEDIUM]** `ParseAcceptanceCriteria` não tratava bloco de código cercado (` ``` `) dentro da
  seção "Critérios de Aceite" — uma linha de exemplo `- ...` dentro do bloco seria contada como
  critério à parte. Corrigido: `internal/traceability/traceability.go` agora rastreia o estado de
  cerca (` ``` `) e ignora linhas dentro dela.
- **[LOW]** Estilo: métodos de `*Catalog` chamavam `NewCatalog().Xxx(...)` em vez de `c.Xxx(...)`
  dentro de `BuildMap`, alocação desnecessária e inconsistente com o receptor em escopo. Corrigido.
- **[LOW]** Diff revisado continha o comentário em PT-BR pré-existente da tentativa anterior em
  `non_regression_test.go` — já removido no estado final em disco (não presente no diff
  re-gerado após a correção; ver nota de proveniência acima sobre a tentativa interrompida).
- Achados **não** corrigidos, registrados como risco residual: ambiguidade de blockquote/lista
  numerada como marcador de critério (aceitável, convenção já documentada no `--help` do comando);
  teste de ambiente byte-idêntico verificado apenas em macOS/bash-como-sh nesta sessão (não
  verificado sob Linux/dash em CI).

## Coverage
package=n/a (mudança multi-pacote transversal — rastreabilidade, gates e testes de não-regressão;
suíte completa executada via `go test ./... -count=1`, sem gate de cobertura por pacote isolado
aplicável a uma tarefa de fechamento)
delta=0.0%

## Suposições
- O conteúdo do `CHANGELOG.md`/`docs/migracao-legacy-acp.md` já preparado por uma tentativa anterior e
  interrompida desta mesma tarefa 11.0 foi tratado como ponto de partida válido, revisado linha a
  linha contra os 6 relatórios de execução (5.0–10.0) e complementado (fix de `contextgen` estava
  faltando) em vez de reescrito do zero — nenhuma alegação nova foi aceita sem confirmação própria.
- `internal/specdrift` e `internal/runtime/specs` já continham, respectivamente, a maior parte da
  infraestrutura de verificação de hash e o teste do vetor 2 (produzidos por tarefas/sessões
  anteriores para outros fins) — reutilizados e estendidos em vez de duplicados.
- A skill `github-diff-changelog-publisher` foi consultada para o formato canônico de changelog, mas
  não executada mecanicamente sobre `git log <tag>..HEAD`, porque a maior parte do trabalho desta
  release (tarefas 4.6–10.0) permanece não commitada na working tree — rodar o gerador automático
  sobre o histórico de commits produziria um changelog materialmente incompleto. Isso está registrado
  como nota de proveniência dentro do próprio `CHANGELOG.md`.

## Riscos Residuais
- **[LOW]** A working tree contém, no momento do fechamento desta tarefa, todo o trabalho não
  commitado das tarefas 4.6–11.0 (confirmado via `git status` no início da sessão) — não foi criado
  nem revertido por esta tarefa, apenas coexiste no mesmo diretório de trabalho, replicando o mesmo
  risco já registrado pela tarefa 10.0.
- **[LOW]** O workflow de release automático (`ai-spec changelog`/`ai-spec semver-next`) pode
  regenerar `CHANGELOG.md`/`VERSION` de forma menos detalhada que o preparado manualmente aqui, caso
  os commits finais não carreguem rodapés `BREAKING CHANGE:` equivalentes — ver "Pendência de
  Publicação" acima para os dois caminhos e o risco explícito.
- **[LOW]** `internal/parity/e2e_parity_test.go` permanece com o bug pré-existente sob a tag
  `integration` já registrado pela tarefa 10.0 (`undefined: E2EParitySuite`); não introduzido nem
  tocado por esta tarefa.
- **Selo de evidência (RF-14):** selo pendente. Não há commit contendo o trabalho desta tarefa até o
  momento do fechamento (working tree ainda não commitada). Nenhum SHA foi inventado nem commit foi
  criado por esta execução (R-GOV-001); `ai-spec seal-evidence` deve ser executado após o commit,
  apontando `--commit <sha>` para `.specs/prd-harness-quatro-clis-loop-aprovacao/11.0_execution_result.json`.
- **[LOW] Gate `--verify-physical` de `bash .agents/scripts/validate-task-evidence.sh` não fecha
  verde nesta sessão** — `taskloop.CaptureSnapshotFromGit` recompõe o estado final a partir de
  `git rev-parse HEAD` + patch semântico do **worktree inteiro** e o compara ao `final_state_sha256`
  declarado; como o worktree acumula, sem commit, o trabalho de 4.6 a 11.0 e segue mutando entre
  sessões concorrentes desta mesma tarefa, a recomposição nunca bate com um valor calculado num
  instante isolado. `final_state_sha256` foi preenchido com `sha256(base_sha + "\n" + patch)` sobre o
  patch **escopado** desta tarefa (evidence/task-11.0/11.0.patch), replicando a fórmula de
  `NewSnapshot`, mas sem o commit real o `--verify-physical` continua vermelho — confirmado que o
  mesmo comando falha **hoje** contra `10.0_execution_report.md` pela mesma causa, então não é
  regressão introduzida por esta tarefa. Resolve-se junto com o item anterior, após o commit real.

## Conflitos de Regra
- none
