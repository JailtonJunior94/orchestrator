# Execution Report — Task 7.0

**Task:** Aviso único de depreciação no copilotInvoker legado (sync.Once)  
**Status:** done  
**Date:** 2026-05-21  
**Recovery note:** Subagent ran `review` skill instead of `execute-task`, returning a review verdict without creating this report or updating tasks.md. Implementation verified correct by orchestrator; report written as contract recovery.

## Changes

### `internal/taskloop/agent.go`
- Added `copilotLegacyDeprecationMsg` const with required literals: "modo legado", "--runtime=acp", "ADR-012"
- Added `warnOnce sync.Once` field to `copilotInvoker` struct for per-instance single emission
- Added `warnWriter io.Writer` field for test injection (defaults to `os.Stderr` at call time)
- Warning emitted via `fmt.Fprintln` (stderr, not stdout)
- Args pipeline (`--autopilot --yolo -p <prompt>`) unchanged

### `internal/taskloop/agent_test.go`
- T-17: first invocation emits warning containing all three required literals
- T-18: second invocation in same instance stays silent
- `TestCopilotInvokerArgsUnchanged`: confirms legacy args pipeline preserved

## Validations

- `go test ./internal/taskloop/... -v` — all PASS
- `go build ./internal/taskloop/...` — clean
- Diff zero in `internal/runtime/` (this task only touched `internal/taskloop/`)

## Requirements Covered

- RF-09: `copilotInvoker` emits WARNING on first invocation per process execution announcing deprecation and pointing to ADR-012

## Test Cases Satisfied

- T-17: deprecation warning emitted once on first call
- T-18: no warning emitted on subsequent calls (sync.Once)

## Residual Risks

- `vX.Y.Z` placeholder in warning message requires filling with actual version before merge
- `sync.Once` is per-instance; one invoker per task run means effectively once per task execution (acceptable per D-05)
