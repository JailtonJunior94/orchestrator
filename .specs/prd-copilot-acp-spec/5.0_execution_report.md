# Relatório de Execução de Tarefa
# Generated: 2026-05-21T00:00:00Z

## Tarefa
- ID: 5.0
- Título: Tabela runtimeACPCatalog em cmd/ai_spec_harness/task_loop.go
- Arquivo: .specs/prd-copilot-acp-spec/task-5.0-runtime-acp-catalog-cli.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-copilot-acp-spec/prd.md
- TechSpec: .specs/prd-copilot-acp-spec/techspec.md
- Governança: agent-governance, go-implementation, AGENTS.md

## Comandos Executados
- `go test ./cmd/ai_spec_harness/... -v` -> PASS (todos os testes verdes incluindo T-13, T-14, T-15)
- `go vet ./...` -> sem warnings

## Arquivos Alterados
- `cmd/ai_spec_harness/task_loop.go` — adicionada `runtimeACPCatalog`, atualizada validação `--runtime=acp`, atualizado help text do flag `--runtime`
- `cmd/ai_spec_harness/task_loop_test.go` — adicionados T-13, T-14, T-15 em `TestRuntimeACPCatalog_T13_T14_T15`; atualizado `validateRuntimeFlags` para usar catálogo; adicionado import `sort`
- `docs/cli-schema.json` — atualizada descrição do flag `--runtime` para refletir tools suportados (claude, copilot)

## Resultados de Validação
- Testes: pass — `go test ./cmd/ai_spec_harness/...` verde; T-13/T-14/T-15 todos passando
- Lint: pass — `go vet ./...` sem warnings
- Veredito do Revisor: APPROVED

## Diff Reviewed

sha=n/a
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=cmd/ai_spec_harness
delta=+4 casos de teste novos (T-13, T-14, T-15 + catálogo + construtores)

## Requisitos Cobertos
- RF-06: `cmd/ai_spec_harness/task_loop.go` aceita `--tool copilot --runtime acp` via tabela `runtimeACPCatalog`
- RF-07: mensagem de erro lista tools suportados em ordem lexicográfica `[claude copilot]` quando tool fora do catálogo

## Critérios de Sucesso Verificados
- `--tool copilot --runtime acp` passa pela validação CLI (T-13 verde)
- `--tool gemini --runtime acp` falha com mensagem contendo `[claude copilot]` ordenado (T-14 verde)
- `--tool claude --runtime acp` continua funcionando idêntico (T-15 verde — regressão)
- Catálogo não redeclarado em `internal/taskloop/` (D-04 preservado)
- Diff zero em `internal/runtime/persistence/` e `internal/runtime/watchdog.go`

## Suposições
- `internal/runtime/specs/copilot.go` já estava implementado (Tarefa 2.0: done) com construtor `specs.Copilot()` correto
- A tabela em `cmd/ai_spec_harness/` é a única fonte de verdade para roteamento ACP (D-04 — responsabilidade do CLI)
- Tarefa 6.0 (wiring `taskloop.Service.Execute`) consumirá o catálogo do CLI ou por parâmetro/pacote exposto sem redeclarar

## Riscos Residuais
- Leve: `runtimeACPCatalog` é `var` (mutável em testes) — risco aceitável; catálogo é package-level e testes não o modificam
- Leve: se `specs.Copilot()` ou `specs.Claude()` mudarem de assinatura, o catálogo compila mas runtime pode diferir — coberto por testes de integração da Tarefa 8.0
- Baixo: `--tool gemini` sem `--runtime=acp` ainda passa validação do catálogo (não entra no branch ACP) — comportamento correto e esperado

## Conflitos de Regra
- none
