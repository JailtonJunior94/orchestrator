# Execution Report — Tarefa 4.0: Generalizar probe error template + tabela adrByID

**Status:** done  
**Data:** 2026-05-21  
**Branch:** feat/acp-runtime-claude

## Resumo

Generalização de `internal/runtime/probe/probe.go:69-82` para eliminar dependência hardcoded em `specs.ClaudeNpmPackage` no template de erro. `errMsgTemplate` (string literal) substituído por função `formatLauncherUnavailable(spec specs.Spec, adrPath string) string`. Tabela `adrByID` criada no package `probe` mapeando `spec.ID` → path do ADR. Cache keyed por `spec.ID` permanece inalterado.

## Arquivos Alterados

- `internal/runtime/probe/probe.go` — substituiu `errMsgTemplate` por `formatLauncherUnavailable()` + tabela `adrByID`; `resolve()` atualizado para lookup `adrByID[spec.ID]` com fallback `".specs/adr/"`
- `internal/runtime/probe/probe_test.go` — atualizado testes existentes (referência ADR via fallback) + adicionados T-06, T-07, T-08, T-20, T-21, Regressão Claude
- `internal/runtime/specs/copilot.go` — verificado existente (criado em tarefa 2.0)
- `.specs/prd-copilot-acp-spec/tasks.md` — status da tarefa 4.0 atualizado para `done`

## Arquivos Inalterados (invariante verificada)

- `internal/runtime/persistence/` — diff zero confirmado
- `internal/runtime/watchdog.go` — diff zero confirmado

## Testes Executados

```
go test ./internal/runtime/probe/... -v
```

**Resultado:** PASS — 100% verde

Testes novos adicionados:
- `TestEnsureAvailable_T06_CopilotNoBinaryNoNpx` — erro Copilot contém binário, npm@pin, fallback, ADR path
- `TestEnsureAvailable_T07_CopilotBinaryPresent` — BinaryLauncher retornado para Copilot com binary
- `TestEnsureAvailable_T08_CopilotFallbackNpx` — NpxLauncher retornado com CopilotNpmPackage/Version
- `TestAdrByID_T20_KnownIDs/claude_adr009` — `adrByID["claude"]` → `.specs/adr/009-acp-protocol-adoption.md`
- `TestAdrByID_T20_KnownIDs/copilot_adr012` — `adrByID["copilot"]` → `.specs/adr/012-copilot-cli-acp-native.md`
- `TestAdrByID_T21_UnknownIDFallback` — ID desconhecido → fallback `".specs/adr/"`
- `TestEnsureAvailable_ClaudeRegressionErrorMessage` — regressão Claude com fallback gracioso

Suíte completa `internal/runtime/...`:
```
ok  github.com/JailtonJunior94/ai-spec-harness/internal/runtime         0.434s
ok  github.com/JailtonJunior94/ai-spec-harness/internal/runtime/probe   0.391s
ok  github.com/JailtonJunior94/ai-spec-harness/internal/runtime/specs   (cached)
... (todos os subpacotes: PASS)
```

## Definition of Done — Checklist

- [x] `errMsgTemplate` substituído por `formatLauncherUnavailable(spec, adrPath)`
- [x] `adrByID` declarado como `var` no package `probe`; mapping inclui `"claude"` → ADR-009 e `"copilot"` → ADR-012
- [x] `resolve()` usa `adrByID[spec.ID]` com fallback `".specs/adr/"` para IDs desconhecidos
- [x] Mensagem Copilot contém binário (`copilot`), npm@pin (`@github/copilot@...`), fallback `--runtime=legacy`, path ADR
- [x] Mensagem Claude continua com estrutura idêntica (verificada via T-20/regressão)
- [x] Cache (`sync.Map`) inalterado em comportamento — `TestEnsureAvailable_Cache` verde
- [x] T-06/T-07/T-08/T-20/T-21 verdes em `probe_test.go`
- [x] `go test ./internal/runtime/probe/...` 100% verde
- [x] Diff zero em `internal/runtime/persistence/` e `internal/runtime/watchdog.go`

## Riscos Residuais

- `CopilotMinCLIVersion = "X.Y.Z"` em `specs/copilot.go` é placeholder (Q1 do PRD — herdado de tarefa 2.0). Não bloqueia merge desta tarefa; impacta documentação.
- Testes T-20 usam `probe.ResetCache()` → não marcados como `t.Parallel()` para evitar race conditions em estado global do cache.
