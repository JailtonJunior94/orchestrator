# Execution Report — Task 1.0

**Task:** Estender Spec value object com metadata SDK/NPM  
**Status:** done  
**Date:** 2026-05-21  
**Recovery note:** Subagent ran `review` skill and produced a review verdict rather than returning canonical YAML. Implementation was verified correct by orchestrator; report written by orchestrator as contract recovery.

## Changes

### `internal/runtime/specs/spec.go`
- Added three private fields to `Spec` struct: `sdkVersion`, `npmVersion`, `npmPackage`
- Added three public value-receiver accessors: `SDKVersion()`, `NPMVersion()`, `NPMPackage()`
- Extended `newSpec` internal constructor with three new parameters (at the end, preserving order)

### `internal/runtime/specs/claude.go`
- Passed `ClaudeSDKVersion`, `ClaudeNpmVersion`, `ClaudeNpmPackage` to updated `newSpec` call

### `internal/runtime/specs/spec_test.go` (new)
- Accessor round-trip tests (T-19): non-empty guards via `Claude()` exerciser

## Validations

- `go test ./internal/runtime/specs/... -v` — PASS (R-01 gate: 7/7 tests)
- `go vet ./internal/runtime/specs/...` — clean
- `go build ./...` — clean
- `git diff --stat internal/runtime/persistence/ internal/runtime/watchdog.go` — empty (R-08 gate: diff zero)

## Requirements Covered

- RF-04 (preparação: `Spec` expõe versões SDK/npm para `runtime_init` e `probe`)
- A5, A6 preserved (private fields, public accessors only)

## Test Cases Satisfied

- T-05: Claude regression (all existing specs tests pass)
- T-19: Accessor round-trip and non-empty guards
