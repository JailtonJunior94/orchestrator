# Generated: 2026-09-18T00:00:00Z
<!-- evidence-contract: v2 -->
# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.0
- Título: Canonicalização de policies com par dedicado de espelhamento
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-4.0-canonicalizacao-de-policies-com-par-dedicado-de-espelhamento.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (Bloco B, RF-09..RF-11, RF-16; Fatos Verificados V-01, V-02, V-18, V-21; decisão D-19)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (seções "Bloco B — canonicalização", "Gate do gate — item 1 `policies-sync`", "Registro obrigatório — anti-gate-órfão", "P3 — Canonicalização de policies" em Sequenciamento de Desenvolvimento, tabela "Rastreabilidade — RF × nível × arquivo")
- Tasks: .specs/prd-harness-portatil-vendor-neutral/tasks.md (dependência 2.0 confirmada `done` antes de iniciar; 1.0/3.0/7.0/9.0/12.0 também `done`)
- Inventário (Tarefa 2.0, entrada obrigatória desta tarefa): .specs/prd-harness-portatil-vendor-neutral/inventario-regras.md (subconjunto `universal` fechado: exatamente R-GOV-001 `.claude/rules/governance.md` e R-STYLE-001 `.claude/rules/code-style.md`)
- Governança: `.agents/skills/agent-governance/SKILL.md`, `.agents/skills/go-implementation/SKILL.md` (diff é majoritariamente shell + um arquivo de teste Go de integração; nenhuma skill processual declarada na task file — "Nenhuma além das auto-carregadas")
- Código de referência lido antes de codar: `scripts/sync-skills.sh`, `scripts/check-skills-sync.sh`, `scripts/check-scripts-sync.sh` (padrão dos três pares existentes, apenas leitura — D-13 proíbe generalizá-los), `tests/integration/sync_gates_guard_test.go` (estrutura do guardião: `syncGateMirrorDirs`, `syncGateScripts`, `cloneRepoForSyncGate`, `replaceInClone`, `runSyncGate`, os testes de mutação existentes), `Makefile`, `.github/workflows/test.yml`

## Comandos Executados
- `diff .claude/rules/governance.md .agents/policies/governance.md` -> idêntico
- `diff .claude/rules/code-style.md .agents/policies/code-style.md` -> idêntico
- `git diff v2.0.1 -- .claude/rules/governance.md .claude/rules/code-style.md` -> vazio (confirma que os dois arquivos já eram byte-idênticos à tag `v2.0.1` antes da migração, condição prévia da prova de identidade)
- `chmod +x scripts/sync-policies.sh scripts/check-policies-sync.sh` -> aplicado
- `bash scripts/check-policies-sync.sh` -> pass (`Policies em sync: 2`, `Drift / missing: 0`) (evidence/task-4.0-harness-portatil-vendor-neutral/check-policies-sync.log)
- Mutações manuais em clone temporário (`/tmp`, fora do working tree) de `check-policies-sync.sh`: apagar `governance.md` canônico -> exit 1; apagar `code-style.md` do espelho -> exit 1; editar só o espelho -> exit 1; editar só a origem -> exit 1; apagar `.agents/policies/` inteiro -> exit 1; clone intocado -> exit 0 (as 5 mutações exigidas pela techspec, seção "Gate do gate — item 1 policies-sync")
- `bash scripts/sync-policies.sh` em clone com drift manual -> regenera o espelho e `check-policies-sync.sh` volta a exit 0 (prova de que o par gera, não só detecta)
- `shellcheck --severity=warning scripts/sync-policies.sh scripts/check-policies-sync.sh` -> exit 0, sem achados (evidence/task-4.0-harness-portatil-vendor-neutral/shellcheck.log)
- `go build ./...` -> pass (evidence/task-4.0-harness-portatil-vendor-neutral/build.log)
- `go vet ./...` -> pass (evidence/task-4.0-harness-portatil-vendor-neutral/vet.log)
- `make lint` -> `0 issues.` (evidence/task-4.0-harness-portatil-vendor-neutral/lint.log)
- `go test ./...` -> pass, todos os pacotes `ok` (evidence/task-4.0-harness-portatil-vendor-neutral/unit-tests.log)
- `go test -v -tags=integration ./tests/integration/...` -> pass, 105 subtestes, 0 falhas (inclui os quatro novos: `TestPoliciesSyncGateFailsOnMirrorOnlyDeletion`, `TestPoliciesSyncGateFailsOnMirrorOnlyEdit`, `TestPoliciesSyncGateFailsOnCanonicalOnlyEdit`, `TestClaudeRulesMirrorMatchesPreMigrationBaseline`) (evidence/task-4.0-harness-portatil-vendor-neutral/integration-tests.log)
- `go test -tags=integration ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/runtime/memory/durable/... ./cmd/ai_spec_harness/...` -> pass (escopo completo do alvo `make integration`, sem regressão)
- `bash scripts/check-skills-sync.sh` -> pass, inalterado (evidence/task-4.0-harness-portatil-vendor-neutral/check-skills-sync.log)
- `bash scripts/check-hooks-sync.sh` -> pass, inalterado (evidence/task-4.0-harness-portatil-vendor-neutral/check-hooks-sync.log)
- `bash scripts/check-scripts-sync.sh` -> pass, inalterado (evidence/task-4.0-harness-portatil-vendor-neutral/check-scripts-sync.log)
- Snapshot final capturado por chamada direta a `internal/taskloop.Orchestrator.captureFinalSnapshot` a partir de um teste Go temporário (`internal/taskloop/zzdebug_test.go`, criado e removido em seguida — mesmo padrão das tarefas 2.0/3.0/7.0, desta vez usando a própria função de produção em vez de reimplementá-la em Python, para eliminar divergência de exclusões: uma primeira tentativa em Python não excluiu `.specs/prd-harness-portatil-vendor-neutral/.checkpoints/**`, que `buildExclusions` exclui incondicionalmente — `go test -run TestZZDebugSnapshot` expôs a divergência e a corrigiu) -> `base_sha`, `patch_sha256` e `final_state_sha256` gravados em `4.0_execution_result.json`; patch físico salvo em `evidence/task-4.0-harness-portatil-vendor-neutral/4.0.patch`; `go build ./...`/`go vet ./...` reconfirmados limpos após remover o teste temporário
- `ai-spec validate-result execution .specs/prd-harness-portatil-vendor-neutral/4.0_execution_result.json --task-id 4.0 --verify-physical --prd-dir .specs/prd-harness-portatil-vendor-neutral --exclude .specs/prd-harness-portatil-vendor-neutral/4.0_execution_report.md` -> `OK: resultado SDD execution valido`
- `bash .agents/scripts/validate-task-evidence.sh .specs/prd-harness-portatil-vendor-neutral/4.0_execution_report.md` -> validação final do pacote de evidências

## Arquivos Alterados
- `.agents/policies/governance.md` (novo) — cópia byte-a-byte de `.claude/rules/governance.md` (R-GOV-001), origem canônica única (RF-09)
- `.agents/policies/code-style.md` (novo) — cópia byte-a-byte de `.claude/rules/code-style.md` (R-STYLE-001), origem canônica única (RF-09)
- `.claude/rules/governance.md`, `.claude/rules/code-style.md` — inalterados em caminho e conteúdo; passam a ser conceitualmente derivados de `.agents/policies/` (RF-10), verificado por `check-policies-sync.sh`
- `scripts/sync-policies.sh` (novo) — gera `.claude/rules/*.md` a partir de `.agents/policies/*.md`, lista declarada (`required_policies`), nunca glob
- `scripts/check-policies-sync.sh` (novo) — par dedicado de verificação (RF-11): falha se canônico ausente (arquivo ou diretório inteiro), espelho ausente, conteúdo divergente, ou arquivo órfão no espelho fora da lista declarada
- `Makefile` (editado) — alvo `check-policies-sync` + entrada em `.PHONY` (registro anti-gate-órfão #1)
- `.github/workflows/test.yml` (editado) — step "Policies sync drift gate" (`make check-policies-sync`) no job `lint`, que roda em todo push/PR (registro anti-gate-órfão #2)
- `tests/integration/sync_gates_guard_test.go` (editado, apenas adições) — `"scripts/check-policies-sync.sh"` em `syncGateScripts`; `.agents/policies/governance.md` e `.agents/policies/code-style.md` em `TestSyncGatesFailWhenCentralArtifactIsDeleted`; `.agents/policies` em `TestSyncGatesFailWhenCanonicalDirectoryIsDeleted`; `"make check-policies-sync"` em `TestValidatorSuitesRunInCI` (registro anti-gate-órfão #3 e #4 — as duas listas hardcoded do guardião); quatro funções de teste novas: `TestPoliciesSyncGateFailsOnMirrorOnlyDeletion`, `TestPoliciesSyncGateFailsOnMirrorOnlyEdit`, `TestPoliciesSyncGateFailsOnCanonicalOnlyEdit`, `TestClaudeRulesMirrorMatchesPreMigrationBaseline`
- `.specs/prd-harness-portatil-vendor-neutral/tasks.md` (editado) — linha da Tarefa 4.0 marcada `done`
- `.specs/prd-harness-portatil-vendor-neutral/4.0_execution_result.json` (criado)
- `evidence/task-4.0-harness-portatil-vendor-neutral/build.log`, `vet.log`, `unit-tests.log`, `integration-tests.log`, `lint.log`, `check-policies-sync.log`, `check-skills-sync.log`, `check-hooks-sync.log`, `check-scripts-sync.log`, `shellcheck.log` (criados)
- `evidence/task-4.0-harness-portatil-vendor-neutral/4.0.patch` (criado)

## Resultados de Validação
- Testes: pass (`go test ./...` — todos os pacotes ok; `go test -v -tags=integration ./tests/integration/...` — 105 subtestes, 0 falhas; `go test -tags=integration ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/runtime/memory/durable/... ./cmd/ai_spec_harness/...` — pass, escopo completo de `make integration`)
- Lint: pass (`make lint` — 0 achados; `go vet ./...` — 0 achados; `shellcheck --severity=warning scripts/sync-policies.sh scripts/check-policies-sync.sh` — 0 achados)
- Veredito do Revisor: APPROVED_WITH_REMARKS (sem achado `[HIGH]`/`[CRITICAL]` — dois achados `[LOW]` registrados em Riscos Residuais, RF-33)

## Critérios de Aceite
- O gate falha ao editar o espelho E ao apagar o canônico -> comprovado: `tests/integration/sync_gates_guard_test.go::TestPoliciesSyncGateFailsOnMirrorOnlyEdit` e `TestPoliciesSyncGateFailsOnCanonicalOnlyEdit` (editar apenas espelho ou apenas origem faz `check-policies-sync.sh` sair com código != 0); `TestSyncGatesFailWhenCentralArtifactIsDeleted` cobre `.agents/policies/governance.md` e `.agents/policies/code-style.md` (apagar o canônico); `TestPoliciesSyncGateFailsOnMirrorOnlyDeletion` cobre apagar só o espelho; `TestSyncGatesFailWhenCanonicalDirectoryIsDeleted` cobre `.agents/policies` (apagar o diretório canônico inteiro) — as 5 mutações da techspec, seção "Gate do gate — item 1 policies-sync", todas verdes em `go test -v -tags=integration ./tests/integration/... -run TestPolicies\|TestSyncGates` (evidence/task-4.0-harness-portatil-vendor-neutral/integration-tests.log)
- Conteúdo carregado pelo Claude Code é byte-idêntico ao anterior à migração -> comprovado: `TestClaudeRulesMirrorMatchesPreMigrationBaseline` compara `.claude/rules/governance.md` e `.claude/rules/code-style.md` atuais contra o conteúdo desses mesmos caminhos na tag git `v2.0.1` via `git show v2.0.1:<path>`, exigindo `bytes.Equal`; confirmado manualmente com `git diff v2.0.1 -- .claude/rules/governance.md .claude/rules/code-style.md` (diff vazio)
- Os quatro registros anti-gate-órfão estão presentes e o gate roda em todo push e PR -> comprovado: (1) alvo `make check-policies-sync` em `Makefile` (+ `.PHONY`); (2) step "Policies sync drift gate" em `.github/workflows/test.yml`, job `lint`, que roda em `on: push (main)` e `on: pull_request (main)` (topo do arquivo, `on:` inalterado por esta tarefa); (3) `"scripts/check-policies-sync.sh"` em `syncGateScripts` e as entradas em `TestSyncGatesFailWhenCentralArtifactIsDeleted`/`TestSyncGatesFailWhenCanonicalDirectoryIsDeleted` (lista de scripts e diretórios do guardião); (4) `"make check-policies-sync"` na lista de `TestValidatorSuitesRunInCI` (lista de alvos que rodam em CI) — `go test -v -tags=integration ./tests/integration/... -run TestValidatorSuitesRunInCI` pass
- Os três gates de sincronia existentes permanecem verdes, evidência de que a decisão de não generalizar foi respeitada -> comprovado: `bash scripts/check-skills-sync.sh`, `bash scripts/check-hooks-sync.sh`, `bash scripts/check-scripts-sync.sh` todos exit 0 (evidence/task-4.0-harness-portatil-vendor-neutral/check-skills-sync.log, check-hooks-sync.log, check-scripts-sync.log); `git diff --stat scripts/sync-skills.sh scripts/check-skills-sync.sh scripts/check-hooks-sync.sh scripts/check-scripts-sync.sh` mostra zero linhas alteradas por esta tarefa (a única diferença pré-existente em `scripts/check-scripts-sync.sh`/`scripts/sync-skills.sh` pertence à Tarefa 9.0, já `done`, não a esta)

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=8ca2393a2c35cc2fd1371f0d8fa7d351cd7bcd34c219fc10f4545632b887baff
verdict=APPROVED_WITH_REMARKS
tool=claude

## Coverage

package=N/A
delta=+0.0%

Nota: esta tarefa altera dois scripts shell novos (`scripts/sync-policies.sh`, `scripts/check-policies-sync.sh`),
dois arquivos de policy Markdown (`.agents/policies/*.md`), `Makefile`, `.github/workflows/test.yml` e um
arquivo de teste sob build tag `integration` (`tests/integration/sync_gates_guard_test.go`). Nenhum desses
artefatos é medido por `make coverage`/`scripts/check-package-coverage.sh` (que cobrem apenas pacotes Go sem
a tag `integration`); a cobertura funcional do par novo está inteiramente nos testes de integração listados em
"Comandos Executados" (105 subtestes, 0 falhas) e nas mutações manuais registradas acima. `go test ./...`
(cobertura total do repositório, pacotes não-integration) não sofreu regressão.

result_path=.specs/prd-harness-portatil-vendor-neutral/4.0_execution_result.json

## Suposições
- O "Registro obrigatório — anti-gate-órfão" da techspec cita linhas específicas de `tests/integration/sync_gates_guard_test.go` (`:424-437` para a "lista de alvos que rodam em CI") que, no estado atual do arquivo (pós-tarefas 1.0/3.0/7.0/9.0/12.0), correspondem a `TestValidatorSuitesRunInCI` — uma lista de strings `make <alvo>` verificadas contra `test.yml`, não uma lista específica de gates de sincronia. Interpretei "as duas listas hardcoded" da task file (4.5) como `syncGateScripts`/as listas de artefatos e diretórios do guardião (uma) e `TestValidatorSuitesRunInCI` (a outra), e adicionei `check-policies-sync.sh` a ambas — decisão registrada aqui porque a numeração de linha da técspec ficou desatualizada em relação ao arquivo real (o arquivo cresceu com tarefas anteriores).
- Não editei `CODEX.md:119`/`COPILOT.md:97` (que referenciam `.claude/rules/governance.md` em prosa) nem `.github/copilot-instructions.md` (duplicata de prosa de R-STYLE-001, item 2 do inventário). O próprio inventário da Tarefa 2.0 registra explicitamente que essas duplicações ficam "fora do escopo de ação de RF-11/RF-13 nesta entrega" (P3/P4 fecham só as regras #1 e #2 do inventário, migração de arquivo, não as referências de prosa nos adapters) — nenhuma subtarefa de 4.1..4.5 pede edição desses arquivos.
- O campo `review_verdict` do contrato JSON (`internal/sdd/execution-result.schema.json`) usa vocabulário reduzido de 3 valores (`approved | changes_requested | needs_input`), distinto do vocabulário de 4 valores da skill `review` (`APPROVED | APPROVED_WITH_REMARKS | REJECTED | BLOCKED`). O veredito humano desta tarefa é `APPROVED_WITH_REMARKS` (achados apenas `low`, nenhum `high`/`critical`) — que RF-33 trata como fechamento válido do ciclo — por isso `4.0_execution_result.json` registra `review_verdict: "approved"`, sem perda de informação: os achados `low` continuam declarados nesta seção e na seção "Riscos Residuais", nunca apagados. Mesmo precedente já registrado em `12.0_execution_report.md`.
- Não toquei `internal/embedded/assets/.claude/rules/` (onde `governance.md` já está embarcado e `code-style.md` está ausente, V-01). A distribuição para projetos consumidores via `ai-spec install`/`upgrade` (RF-12, os quatro pontos hardcoded em `internal/install/install.go`, `internal/upgrade/upgrade.go`, `internal/uninstall/uninstall.go`) é escopo explícito da Tarefa 5.0 ("Distribuição de R-STYLE-001"), que depende desta (4.0 precede 5.0 segundo `tasks.md`).

## Riscos Residuais
- **[LOW]** (achado do revisor) `scripts/sync-policies.sh` aplica `chmod -R u+w` recursivamente sobre `.agents/policies/` e `.claude/rules/` inteiros, não escopado às duas entradas declaradas em `required_policies`. Hoje é inofensivo porque os dois diretórios só contêm as duas policies; se um arquivo não declarado for adicionado a `.claude/rules/` no futuro, o script alteraria sua permissão sem necessidade. Dívida aceita nesta entrega — não é regressão de comportamento observável, apenas superfície de blast radius do `chmod`.
- **[LOW]** (achado do revisor) `scripts/check-policies-sync.sh` varre órfãos apenas do lado do espelho (`"$mirror"/*.md`), nunca do lado canônico (`.agents/policies/`). Um arquivo novo adicionado a `.agents/policies/` sem entrada em `required_policies` nos dois scripts não gera drift algum — consistente com o padrão já existente em `check-scripts-sync.sh` (também lista declarada sem checagem de órfão do lado canônico), portanto não é regressão introduzida por esta tarefa, mas fica registrado como risco a lembrar quando uma terceira policy for adicionada.
- Selo pendente: `4.0_execution_result.json` foi produzido e validado fisicamente (`ai-spec validate-result execution ... --verify-physical`), mas o selo da Etapa 6 (`ai-spec seal-evidence`) exige commit prévio, que o harness não cria por si (R-GOV-001). Selo fica pendente até alguém commitar o trabalho desta tarefa e rodar `ai-spec seal-evidence .specs/prd-harness-portatil-vendor-neutral/4.0_execution_result.json --prd-dir .specs/prd-harness-portatil-vendor-neutral --commit <sha>`.
- `patch_sha256`/`final_state_sha256` de `4.0_execution_result.json` refletem a árvore de trabalho inteira no momento da captura (múltiplas tarefas do PRD em andamento sem commit intermediário nesta orquestração), não apenas o diff isolado da Tarefa 4.0; a seção "Arquivos Alterados" acima é a fonte de verdade do escopo real desta tarefa.
- Mesmo precedente das tarefas 2.0/3.0: `tasks.md` é marcado `done` antes de `validate-task-evidence.sh` ser a última chamada, porque o próprio validador compara `Estado:` deste relatório contra a coluna Status de `tasks.md` e falha em caso de divergência.

## Conflitos de Regra
- none
