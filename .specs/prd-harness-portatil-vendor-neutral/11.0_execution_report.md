<!-- evidence-contract: v2 -->
# Generated: 2026-09-18T22:29:23Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 11.0
- Título: Telemetria comparavel, ablation e doctor multi-provider
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-11.0-telemetria-comparavel-ablation-e-doctor-multi-provider.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (seção Fatos Verificados: V-09, V-10, V-29, V-34, V-37; Requisitos Funcionais RF-30..RF-34, RF-43, RF-43.1, RF-44, RF-45, RF-46, RF-47)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (seções "Monitoramento e Observabilidade", "Abordagem de Testes" Bloco E/G, riscos R-12/R-13, "Arquivos Relevantes e Dependentes")
- Governança: AGENTS.md (contrato base), .agents/skills/agent-governance/SKILL.md (via skill execute-task), .agents/skills/go-implementation/SKILL.md (linguagem do diff), .claude/rules/code-style.md (R-STYLE-001, hard), .claude/rules/governance.md (R-GOV-001)

## Comandos Executados
- `go build ./...` -> exit 0, sem output (evidence/task-11.0-harness-portatil-vendor-neutral/build.log)
- `go vet ./...` -> exit 0, sem output (evidence/task-11.0-harness-portatil-vendor-neutral/vet.log)
- `go test -count=1 ./...` -> 4014 testes passando em 85 pacotes, 0 falhas (evidence/task-11.0-harness-portatil-vendor-neutral/test.log)
- `GOGC=20 golangci-lint run --config .golangci.yml --timeout 10m` -> 0 issues (evidence/task-11.0-harness-portatil-vendor-neutral/lint.log)
- `go test -tags=integration -count=1 ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/parity/...` -> 4 pacotes de integração passando (evidence/task-11.0-harness-portatil-vendor-neutral/integration-test.log)
- `bash scripts/check-scripts-sync.sh` / `check-hooks-sync.sh` / `check-skills-sync.sh` / `check-mocks.sh` -> todos verdes, 0 drift (evidence/task-11.0-harness-portatil-vendor-neutral/check-*.log)
- `go run . skills --verify .` -> "Integridade das skills OK (versao + hash SHA-256)." — mesmo mecanismo consumido por `internal/doctor.checkSkillIntegrity` (RF-60, subtarefa 11.8, não reimplementado) (evidence/task-11.0-harness-portatil-vendor-neutral/skills-lock.log)
- `go run . doctor .` neste repositório (self-dogfooding) -> exit 1 honesto: Core com 9 checks (contrato, integridade de skills, sincronia canônica/derivados) + blocos Claude/Codex/Copilot (os 3 provedores efetivamente instalados neste projeto, GitHub Copilot com aviso de config parcial, Claude com falha real de `.claude/settings.json` ausente); nenhuma chamada de LLM (evidence/task-11.0-harness-portatil-vendor-neutral/doctor-smoke.log)
- `go run . doctor . --codex-trust` -> bloco Codex ganha check adicional "Trust de hooks do Codex" via RPC read-only `hooks/list` (opt-in explícito, sem execução de LLM) (evidence/task-11.0-harness-portatil-vendor-neutral/doctor-codex-trust-smoke.log)
- `go run . telemetry report/summary --trend` contra log manual isolado em `/tmp` (nunca o `.agents/telemetry.log` real do repositório) -> seção "Métricas RF-44" presente em texto e JSON de `report`, em `summary` e em `--trend` (coluna "Metricas RF-44"), confirmando o critério de sucesso "chave nova aparece em relatório, resumo e tendência"
- `subagent[reviewer, rodada 1]` -> Verdict: REJECTED (achado [HIGH]: violação pervasiva de R-STYLE-001.2 — comentários em quase todo arquivo tocado/criado; [MEDIUM]: colisão de nome `token`/`tokens_in`/`tokens_out` em `internal/telemetry/sensitive.go`; 2x [LOW])
- bugfix no escopo: removidos todos os comentários das linhas adicionadas/criadas nos 17 arquivos do diff; corrigida a colisão MEDIUM via `rf44MetricLogKeyAllowlist`
- `subagent[reviewer, rodada 2]` -> Verdict: APPROVED_WITH_REMARKS ([MEDIUM] residual trivial em `internal/telemetry/trend.go:15-16` — comentários remanescentes em linhas realinhadas por `gofmt`; 2x [LOW] aceitos como dívida declarada)
- correção do achado MEDIUM residual: removidos os 2 comentários de `WeeklyBucket` em `trend.go`; suíte completa re-executada (4014 testes, 0 falhas) após a correção
- `ai-spec validate-result execution .specs/prd-harness-portatil-vendor-neutral/11.0_execution_result.json --task-id 11.0` -> "OK: resultado SDD execution valido"
- Delta de cobertura dos pacotes tocados: `git worktree add --detach /tmp/base-cov-11 a8b77217cd713b4ef629d324e63934e05e589864` + `go test -coverprofile=... ./internal/telemetry/... ./internal/doctor/...` no worktree da base (81.4%) vs. na árvore atual (82.2%) -> delta +0.80% (ver seção Coverage)

## Arquivos Alterados
- internal/doctor/doctor.go (reescrito: Core + bloco por provedor instalado, `FailureLayer` tipada)
- internal/doctor/doctor_test.go (ajuste de chamada: `runChecks` -> `runCoreChecks`)
- internal/doctor/multiprovider_test.go (novo)
- internal/runtime/runner.go (novo método `SpecID()`)
- internal/taskloop/acpinvoker.go (mede duração, provider, final_status; passa a `telemetry.ACPSessionEvent`)
- internal/telemetry/acp.go (`ACPSessionEvent` estendido: Provider, Model, Duration, ToolCallsTotal, HasToolCallsTotal, FinalStatus)
- internal/telemetry/parser.go (reescrito: reconhece as 6 chaves RF-44 com escritor de produção via tabela de alias, mantém skill=/ref= e ignora chave desconhecida)
- internal/telemetry/metrics_rf44.go (novo: `summarizeRF44Metrics`, `MetricSummary`)
- internal/telemetry/report.go, summary.go, trend.go (expõem métricas RF-44 observadas)
- internal/telemetry/ablation.go (novo: `AblationBaseline`, `CompareAblation`, decisão KEEP/ON-DEMAND/REMOVE)
- internal/telemetry/sensitive.go (novo: tabela de campos sensíveis + allowlist RF-44)
- internal/telemetry/rf44_test.go, ablation_test.go, sensitive_test.go (novos)
- cmd/ai_spec_harness/verify_contract_test.go (novo: trava o contrato de `verify`)

## Resultados de Validação
- Testes: pass (`go test -count=1 ./...` -> 4014 testes, 85 pacotes, 0 falhas)
- Lint: pass (`golangci-lint`, 0 issues)
- Veredito do Revisor: APPROVED_WITH_REMARKS (rodada 2, após bugfix do REJECTED da rodada 1; zero achados HIGH/CRITICAL)

## Critérios de Aceite
- Chave nova aparece em relatório, resumo e tendência — presença no arquivo de log não basta -> comprovado: `internal/telemetry/rf44_test.go:TestRF44_ReportSummaryTrendSurfaceNewKey` e smoke manual (`telemetry report`/`summary`/`report --trend` mostrando seção "Métricas RF-44" com `provider`, `model`, `duration`, `tool_calls`, `final_status`, `retries`).
- Nenhum leitor existente quebra e chave desconhecida continua ignorada em vez de falhar -> comprovado: `internal/telemetry/parser_test.go` (suíte pré-existente inalterada, 100% verde) + `internal/telemetry/rf44_test.go:TestRF44_UnknownRawKeyNeverResolvesAsMetric` (chave crua homônima sem escritor, ex. `tokens_in=999`, nunca resolve como métrica).
- Zero métrica inventada, inferida ou preenchida com zero -> comprovado: `internal/telemetry/rf44_test.go:TestRF44_AbsentMetricIsNeverZero` (campos opcionais omitidos nunca aparecem em `logEntry.Metric` nem em `ReportData.Metrics`) e `internal/telemetry/ablation_test.go:TestBuildAblationBaseline_NeverIncludesMetricWithoutWriter` (baseline de ablation nunca inclui métrica sem escritor de produção identificado, mesmo com chave crua homônima forjada).
- Doctor reporta os quatro provedores, com somente verificações estáticas e sem chamada paga a LLM -> comprovado: `internal/doctor/multiprovider_test.go:TestBuildProviderBlocks_AllFourProvidersWhenInstalled` (mecanismo suporta os 4 quando instalados) e smoke real neste repositório (3 provedores efetivamente instalados: Claude/Codex/Copilot); `checkCodexTrustedHash` é RPC read-only opt-in (`hooks/list`), nunca LLM; `detect`/agentDetect não é usado por doctor — a lista de "instalado" deriva do manifesto via `install.Service.Verify`, sem executar nenhum binário além do LookPath já existente em `probeBinaryAvailable`.
- RF-60 consumido, não reimplementado (subtarefa 11.8) -> comprovado: `internal/doctor/doctor.go:checkSkillIntegrity` chama `skillscheck.Service.Verify` (mesmo `Service` usado por `ai-spec skills --verify` e `make check-skills-lock`); `internal/doctor/multiprovider_test.go:TestCheckSkillIntegrity_ConsumesRF60LockMismatch` prova hash mismatch real detectado sem recálculo próprio.
- `verify` preserva integralmente contrato, flags, saída e códigos de saída (subtarefa 11.9) -> comprovado: `git diff -- cmd/ai_spec_harness/verify.go` vazio (arquivo não tocado) + `cmd/ai_spec_harness/verify_contract_test.go:TestVerifyCommand_ContractPreserved` (trava `Use` e as 6 flags existentes, falha se qualquer flag nova for adicionada).
- Nenhuma chave secreta, credencial ou conteúdo sensível é persistido pela telemetria; existe teste que falha se um campo sensível conhecido for emitido (RF-46) -> comprovado: `internal/telemetry/sensitive_test.go:TestFindSensitiveField_Mutation` (mutation-proof: dispara para `api_key`/`auth_token`/`client_secret`/`password`/`cookie`, case-insensitive, e não dispara em linha limpa) e `TestNoSensitiveFieldsEmittedByProductionWriters` (varre a saída real de todos os escritores públicos do pacote).
- Mecanismo e baseline de ablation; campanhas ficam sob demanda e não são gate (RF-47) -> comprovado: `internal/telemetry/ablation.go` (`BuildAblationBaseline`, `CompareAblation`) é biblioteca pura sem novo comando CLI; `internal/telemetry/ablation_test.go` cobre amostra insuficiente, nenhuma métrica comparável, custo melhora/piora/dentro da tolerância — todas as decisões (KEEP/ON-DEMAND/REMOVE) sustentadas por evidência numérica registrada em `AblationResult.Evidence`, nunca por avaliação subjetiva.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=8163b73b8d345b3f0d77f240244794117299e0e2e2c28a54ab968cca71212858
verdict=APPROVED_WITH_REMARKS
tool=claude

## Coverage

package=internal/telemetry,internal/doctor
delta=+0.80%

## Suposições
- A skill `.agents/skills/execute-task` invoca `review` via subagente; nesta execução, o veredito foi obtido por dois subagentes `reviewer` sucessivos (Ciclo de Aprovação review -> bugfix -> review, RF-38 do processo), a mesma máquina que a skill exige.
- RF-44 declara 15 nomes de métrica; apenas 6 (provider, model, duration, tool_calls, final_status, retries) têm escritor de produção identificado nesta entrega. Os 9 restantes (task_type, risk, tokens_in, tokens_out, context_loaded, skills_loaded, review_rounds, tests_passed, human_intervention) permanecem deliberadamente ausentes — nenhum ponto de produção hoje tem essa informação disponível sem instrumentação adicional fora do escopo declarado de "mecanismo e baseline" desta tarefa. Registrado explicitamente em `internal/telemetry/parser.go` (`RF44MetricsWithProductionWriter`) e testado em `TestRF44_MetricsWithProductionWriterCoverage`.
- `retries` (nome RF-44) é servido pela chave de log já existente `retry_attempts` (ADR-018/RF-04), evitando duplicar uma chave para o mesmo conceito — RF-43 exige apenas que chaves sejam aditivas, não que cada nome RF-44 vire uma chave literal nova.
- O selo de evidência (`ai-spec seal-evidence` / Etapa 6 de `execute-task`) fica pendente por natureza: não há commit desta tarefa ainda. Registrado explicitamente conforme instrução do orquestrador.
- Este relatório NÃO inclui a linha `result_path = <arquivo>` que `.agents/scripts/validate-task-evidence.sh` procura para disparar `validate-result execution ... --verify-physical` internamente. Decisão deliberada, por instrução explícita do orquestrador ("SEM tentar --verify-physical/seal-evidence completo"): tentei reproduzir o mecanismo real (`taskloop.Orchestrator.captureFinalSnapshot`, via teste local descartável, mesmo padrão da tarefa 1.0) e o resultado se mostrou não confiável neste contexto de lote — com 1.0..10.0 e 12.0 já aplicados e não commitados simultaneamente, o `git diff HEAD -- .` subjacente arrasta os arquivos de patch de evidência de tarefas anteriores (não excluídos por padrão, ex.: `evidence/task-10.0-harness-portatil-vendor-neutral/10.0.patch` com 365MB) para dentro do "patch semântico" desta tarefa, produzindo um artefato de ~750MB sem relação com o diff real de 11.0. Reproduzir esse caminho exigiria alterar exclusões de outras tarefas (fora do escopo declarado desta execução) ou aceitar um artefato fisicamente inconsistente. A prova de fechamento válida para este modo de execução em lote é a validação de schema já executada com sucesso (`ai-spec validate-result execution ... --task-id 11.0`, sem `--verify-physical`), consistente com a instrução explícita do orquestrador.

## Riscos Residuais
- [LOW] `internal/taskloop/acpinvoker.go` (`finalStatus`) é método de `*Catalog` sem uso do receptor — aceito na 2ª rodada de revisão como consistente com o padrão já existente no mesmo arquivo (`MapExitCode`, `deriveEvidenceDir`).
- [LOW] `internal/telemetry/acp.go` (`provider=%s`/`model=%s`) não faz escaping de espaço no valor. Risco baixo: os valores atuais vêm de `ACPRunner.SpecID()` (id canônico sem espaço) e do parâmetro `model` já usado sem sanitização em todo o pipeline; aceito como risco residual documentado na 2ª rodada de revisão.
- [LOW] Cobertura RF-44 permanece em 6/15 campos com escritor de produção identificado (ver Suposições); os 9 campos remanescentes exigem instrumentação em pontos de produção fora do escopo desta tarefa (ex.: contagem de tokens do provedor, resultado de testes do task-executor) — reabertura explícita se e quando esses pontos de produção existirem.
- [LOW] `ablationCostToleranceRatio` (10%) é decisão de projeto sem lastro experimental documentado além da analogia com a convenção de margem de RF-41; deve ser revisitado quando campanhas reais de ablation rodarem (RF-47 declara execução de campanha como sob demanda, fora do escopo desta entrega).
- Selo de evidência: pendente (sem commit desta tarefa ainda) — ver Suposições.

## Conflitos de Regra
- none
