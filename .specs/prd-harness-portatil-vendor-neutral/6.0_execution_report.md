<!-- evidence-contract: v2 -->
# Generated: 2026-09-18T18:13:09Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 6.0
- Título: Saneamento dos invariantes e capability matrix gerada
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-6.0-saneamento-dos-invariantes-e-capability-matrix-gerada.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (Fatos Verificados V-24..V-28; RF-15, RF-17, RF-18, RF-18.1, RF-19, RF-20, RF-20.1, RF-21)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (Bloco C — capability matrix RF-15..RF-21; Sequenciamento P5/P6; tabela de rastreabilidade RF-17/18/18.1/21 "Unitário + golden")
- ADR: .specs/prd-harness-portatil-vendor-neutral/adr-003-capability-matrix-gerada.md (contexto V-25/V-26/V-27, decisão, três alternativas rejeitadas, plano de implementação em 8 passos)
- Governança: AGENTS.md, .claude/rules/code-style.md (R-STYLE-001), .claude/rules/governance.md (R-GOV-001), .agents/skills/agent-governance/SKILL.md, .agents/skills/go-implementation/SKILL.md (skill de linguagem inferida do diff — 100% Go). Skills Necessárias da task file: "Nenhuma além das auto-carregadas (governance + linguagem)."
- Dependências 1.0 e 3.0 confirmadas `done` em tasks.md antes do início.

## Comandos Executados
- `go build ./...` -> sem erros (evidence/task-6.0-harness-portatil-vendor-neutral/build.log)
- `go vet ./...` -> sem erros (evidence/task-6.0-harness-portatil-vendor-neutral/vet.log)
- `go test -coverprofile=... ./...` -> todos os pacotes `ok`, exit 0 (evidence/task-6.0-harness-portatil-vendor-neutral/test.log); cobertura total (excluindo mocks) 82.4%; `internal/capability` 87.3%, `internal/parity` 84.8%, `internal/runtime/specs` 86.3%
- `go test -v ./internal/capability/...` -> 15 testes, todos PASS (evidence/task-6.0-harness-portatil-vendor-neutral/capability-test-verbose.log)
- `go test -v -tags=integration ./internal/parity/... ./tests/integration/...` -> 366 subtestes PASS, 0 FAIL, exit 0 (evidence/task-6.0-harness-portatil-vendor-neutral/integration-test-final.log) — inclui os E2E de `internal/parity/e2e_parity_test.go` (RF-20.1, já entregue e verificado nominalmente no log)
- `go test -v -tags=integration ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/parity/...` -> 174 PASS, 0 FAIL, exit 0 (evidence/task-6.0-harness-portatil-vendor-neutral/integration-test.log) — replica exatamente o comando do job `integration` do CI
- `GOGC=20 golangci-lint run --config .golangci.yml --timeout 5m ./...` -> "0 issues." (evidence/task-6.0-harness-portatil-vendor-neutral/lint-full.log)
- `make check-capability-matrix-sync` -> `go test ./internal/capability/...` `ok` (gate novo, registrado no Makefile)
- `bash scripts/check-skills-sync.sh` -> "Todos os mirrors sincronizados" (evidence/task-6.0-harness-portatil-vendor-neutral/check-skills-sync.log) — confirma que a Tarefa 6.0 não introduziu drift nos mirrors de skills/hooks apesar do aviso de colisão de arquivo com 5.0/9.0
- `bash scripts/check-scripts-sync.sh` -> "Todos os validadores de evidencia sincronizados." (evidence/task-6.0-harness-portatil-vendor-neutral/check-scripts-sync.log)
- `bash scripts/check-hooks-sync.sh` -> "Todos os hooks orquestrador sincronizados." (evidence/task-6.0-harness-portatil-vendor-neutral/check-hooks-sync.log)
- `go test -v -tags=integration ./tests/integration/... -run TestValidatorSuitesRunInCI` -> PASS (evidence/task-6.0-harness-portatil-vendor-neutral/guard-test.log) — confirma `make check-capability-matrix-sync` presente no gate de PR
- `make check-spec-paths` -> "Resultado: 12 OK, 0 FAIL" (evidence/task-6.0-harness-portatil-vendor-neutral/check-spec-paths.log) — este PRD ainda não está sob `sdd-state.json`, gate roda sobre o PRD ativo declarado em `SPEC_PATH_TARGETS`, sem regressão
- Skill `review` (auto-invocada nesta execução, escopo restrito ao diff da Tarefa 6.0 + prd.md/techspec.md) -> ver seção "Diff Reviewed"
- `python3 <script de replicação do algoritmo de captureFinalSnapshot>` (mesmo padrão usado em 2.0/3.0/4.0/7.0/9.0/12.0 para captura fora do `ai-spec task-loop` orquestrado) -> `base_sha`, `patch_sha256` e `final_state_sha256` recomputados e gravados em `6.0_execution_result.json`; patch físico salvo em `evidence/task-6.0-harness-portatil-vendor-neutral/6.0.patch`
- `go build -o ./ai-spec .` -> binário local construído a partir do HEAD atual para satisfazer o toolchain mínimo (`ai-spec-harness >= 2.0.0`) exigido por `validate-task-evidence.sh`
- `AI_SPEC_BIN=./ai-spec bash .agents/scripts/validate-task-evidence.sh .specs/prd-harness-portatil-vendor-neutral/6.0_execution_report.md` -> aprovado (ver saída no encerramento da Etapa 5)

## Arquivos Alterados
- `internal/parity/parity.go` (editado) — RF-18.1: campo `SelfSatisfiedStubPath` em `Invariant`; `InvariantScope`/`Scope()` derivando escopo de `AppliesTo` nunca de `Level` (V-26); `EvidenceInvalid()`; `CanonicalProviders()` (RF-15, ordem estável); `selfSatisfiedInvariantStubPaths()` como fonte única dos stubs que `Generate` injeta, substituindo os dois blocos inline anteriores; `CL03..CL08` e `X03` marcados com `SelfSatisfiedStubPath` (V-25)
- `internal/parity/sanitization_test.go` (novo) — `TestSelfSatisfiedInvariantsMatchInjectedStubs` (amarra bidirecional stub-injetado ↔ invariante-marcado, subtarefa 6.2/6.7 gate "stub novo não classificado falha o build"), `TestSelfSatisfiedInvariantIDs`, `TestScopeDerivesFromAppliesToNotLevel` (regressão direta de V-26 para CL01/CL02/CP01/CD01/CD02), `TestScopeUniversalWhenAppliesToNilOrCoversAllProviders`
- `internal/capability/capability.go` (novo) — modelo em memória (`State`, `Cell`, `Matrix`), `Generate()` (fonte única: invariantes de `internal/parity`, RF-18), `resolveProviders`/`buildCell` (estados honestos: supported/unsupported/provider capability/unknown), `sortCells`/`providerOrder` (ordenação canônica determinística), `RenderJSON`/`RenderMarkdown`/`RenderBoth` (dois emissores, mesma execução, RF-18/6.3)
- `internal/capability/evidence.go` (novo) — `EvidenceCells` (ponte Matrix → `specs.CapabilityCell`), `ParityTestMethodExists`/`DispatchProvenFromParityTests` (verificação AST de que o teste de evidência declarado existe de fato em `internal/parity/parity_test.go`, não apenas menção textual)
- `internal/capability/golden_test.go` (novo) — golden-file no padrão de `internal/contextgen/contextgen_test.go` (`UPDATE_SNAPSHOTS=1`), `goldenMatches` (comparação pura, sem side effect em `*testing.T`, usada pelos testes de gate-do-gate), determinismo, "nenhuma célula suportada sem teste", "toda célula desconhecida é justificada", "toda célula não suportada carrega razão" (RF-17)
- `internal/capability/gate_test.go` (novo) — gate do gate (subtarefa 6.6): matriz vazia, célula suportada sem teste, célula não suportada nunca exige prova, matriz real limpa, drift somente no JSON, drift somente no Markdown, aprovação em correspondência exata
- `internal/runtime/specs/parity_gate.go` (editado, apenas adições) — `CapabilityCell`, `CapabilityDispatchProofFunc`, `ValidateCapabilityMatrixEvidence` (RF-20: estende o arquivo que já contém `ValidateParityMatrix`, reaproveita o tipo `ParityViolation` e preserva a literal `"no dispatch proof test associated"`)
- `internal/runtime/specs/parity_gate_capability_test.go` (novo) — testes unitários de `ValidateCapabilityMatrixEvidence` no próprio pacote `specs_test` (matriz vazia, sem prova de disparo, fail-closed com `nil`, prova resolvida)
- `Makefile` (editado) — alvo `check-capability-matrix-sync: go test ./internal/capability/...` + entrada em `.PHONY` (registro anti-gate-órfão #1)
- `.github/workflows/test.yml` (editado) — step "Capability matrix sync gate" (`make check-capability-matrix-sync`) no job `lint` (registro anti-gate-órfão #2)
- `tests/integration/sync_gates_guard_test.go` (editado, uma linha) — `"make check-capability-matrix-sync"` adicionado à lista de `TestValidatorSuitesRunInCI` (registro anti-gate-órfão #3)
- `testdata/capability-matrix.json` (novo, gerado) — artefato que o gate compara (RF-18/RF-19)
- `docs/capability-matrix.md` (novo, gerado) — artefato que o mantenedor lê (RF-18)
- `.specs/prd-harness-portatil-vendor-neutral/tasks.md` (editado) — linha da Tarefa 6.0 marcada `done`
- `.specs/prd-harness-portatil-vendor-neutral/6.0_execution_result.json` (criado)
- `.specs/prd-harness-portatil-vendor-neutral/.checkpoints/6.0.yaml` (criado)
- `evidence/task-6.0-harness-portatil-vendor-neutral/*.log`, `6.0.patch` (criados)

## Resultados de Validação
- Testes: pass (`go test ./...` exit 0; `go test -tags=integration ...` exit 0; 15/15 em `internal/capability`, 4/4 novos em `internal/runtime/specs`, 4/4 novos em `internal/parity`)
- Lint: pass (`golangci-lint run ./...` — 0 issues)
- Veredito do Revisor: APPROVED_WITH_REMARKS (sem achados `critical`/`high` em aberto; um achado `high` foi identificado e corrigido durante a própria execução — ver "Diff Reviewed"; achados remanescentes são `medium`/`low`, registrados como dívida declarada em "Riscos Residuais")

## Critérios de Aceite
- Contagem de células suportadas sem teste resolvido é ZERO -> comprovado: `TestCapabilityMatrix_NoSupportedCellWithoutResolvedTest` e `TestCapabilityMatrix_RealMatrixEvidenceGateIsClean` (`go test -v ./internal/capability/...`, ambos PASS, evidence/task-6.0-harness-portatil-vendor-neutral/capability-test-verbose.log); `ValidateCapabilityMatrixEvidence(EvidenceCells(m), DispatchProvenFromParityTests(source))` retorna 0 violações sobre a matriz real
- Células desconhecidas são rastreadas e justificadas uma a uma -> comprovado: `TestCapabilityMatrix_UnknownCellsAreJustified` (PASS); as 10 células `unknown` em `testdata/capability-matrix.json` (CL03..CL08 para claude + X03 para os 4 provedores) carregam `reason` citando o caminho de stub e V-25 (`grep -c '"state": "unknown"' testdata/capability-matrix.json` = 10)
- Adulterar qualquer um dos dois artefatos derruba o CI -> comprovado: `TestCapabilityMatrix_GoldenGateDetectsJSONOnlyDrift` e `TestCapabilityMatrix_GoldenGateDetectsMarkdownOnlyDrift` (PASS, ambos provam que `goldenMatches` reprova conteúdo adulterado); o gate real está registrado em `make check-capability-matrix-sync` e no step "Capability matrix sync gate" do job `lint`, confirmado presente por `TestValidatorSuitesRunInCI`
- Reajuste de largura de coluna no Markdown regenerado NÃO produz falso positivo -> comprovado: por desenho, `RenderMarkdown` não alinha/preenche colunas (nenhum `padding`/`Sprintf` com largura fixa) — cada linha é emitida independentemente das demais, então o crescimento de uma célula nunca reformata linhas vizinhas; o artefato que o gate compara byte a byte é o JSON (`testdata/capability-matrix.json`), nunca a prosa formatada — exatamente a decisão registrada na ADR-003 contra a alternativa (c)
- Teste amarra o conjunto de stubs injetados ao conjunto marcado como evidência inválida -> comprovado: `TestSelfSatisfiedInvariantsMatchInjectedStubs` (`go test -v ./internal/parity/... -run TestSelfSatisfied`, PASS) — falha nos dois sentidos: stub novo em `selfSatisfiedInvariantStubPaths` sem invariante marcado, ou invariante marcado apontando para caminho fora da lista de stubs

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

result_path=.specs/prd-harness-portatil-vendor-neutral/6.0_execution_result.json

Revisão autoconduzida via skill `review`, escopo restrito ao diff desta tarefa (`internal/parity/parity.go`, `internal/parity/sanitization_test.go`, `internal/capability/*.go`, `internal/runtime/specs/parity_gate.go`, `internal/runtime/specs/parity_gate_capability_test.go`, `Makefile`, `.github/workflows/test.yml`, `tests/integration/sync_gates_guard_test.go`, `docs/capability-matrix.md`, `testdata/capability-matrix.json`), confrontado contra `prd.md`/`techspec.md`/`adr-003-capability-matrix-gerada.md`.

Achado `high` identificado e corrigido nesta mesma execução: a primeira versão gerava os dois artefatos (JSON/Markdown) a partir de duas chamadas independentes de `Generate()` em testes separados — funcionalmente seguro pela determinicidade comprovada (`TestCapabilityMatrix_DeterministicGeneration`), mas violava literalmente a subtarefa 6.3/RF-18 ("dois emissores na MESMA execução") e o mecanismo de mitigação do risco "Divergência entre JSON e Markdown por caminho de geração separado" da ADR-003. Corrigido introduzindo `RenderBoth(m Matrix)` e um único teste (`TestCapabilityMatrix_GoldenJSONAndMarkdownFromSameGeneration`) que chama `Generate()` uma vez e deriva os dois artefatos da mesma instância de `Matrix`.

Achados `medium`/`low` remanescentes (dívida declarada, não bloqueante):
- `[medium]` `internal/parity/parity.go` (invariantes INV-30, INV-32, X02, FB01): estas checagens retornam `pass()` por vacuidade quando a fixture/condição de contorno está ausente do snapshot sintético de `Checker.Generate` (ex.: `tests/fixtures/parity/*.jsonl` não existem no `FakeFileSystem`), o que é epistemicamente análogo ao problema que V-25 descreve para CL03..CL08/X03 — mas V-25, tal como registrado em `prd.md`/`adr-003`, nomeia explicitamente apenas `CL03..CL08` e `X03`. Optei por não expandir o saneamento de RF-18.1 além do conjunto literal documentado, para não introduzir escopo não especificado; o efeito prático hoje é que essas 4 células aparecem como `provider capability`/`supported` no lugar de `unknown`. Registrado aqui para decisão consciente em revisão futura da ADR-003 (a própria ADR já lista `EvaluateHandshake` como gatilho de revisão semelhante).
- `[low]` `internal/runtime/specs/parity_gate.go`: `ValidateCapabilityMatrixEvidence` é uma função nova adicionada ao mesmo arquivo de `ValidateParityMatrix`, reaproveitando `ParityViolation` e a literal de falha, em vez de uma alteração de assinatura da função existente — os tipos de `ValidateParityMatrix` (`CanonicalPoint`, `PointCoverage`) são de um eixo diferente (pontos canônicos de hook) do eixo de invariantes de `internal/parity`, e forçar o mesmo tipo produziria acoplamento artificial. Interpretação registrada como decisão, não como lacuna.
- `[low]` Subtarefa 6.7 "quatro pontos": os dois primeiros (alvo `Makefile`, step de CI) e o terceiro (`TestValidatorSuitesRunInCI`) seguem o padrão já usado em 4.0/9.0. O quarto ponto não tem lista-guardiã hardcoded natural aplicável (o mecanismo de `syncGateScripts`/`syncGateMirrorDirs` em `sync_gates_guard_test.go` é específico de gates de espelhamento canônico↔mirror, que não é o caso de um gate golden-file gerado↔commitado) — adaptei o quarto ponto para o próprio "gate do gate" em `internal/capability/gate_test.go`, seguindo o precedente de 4.0 de documentar a interpretação quando a numeração/mecanismo da técspec não mapeia 1:1 para o estado atual do repositório.
- `[low]` Documentação de operação (`docs/evidence-gates.md`, `docs/degradation-matrix.md`, `docs/troubleshooting.md`) listada no plano da ADR-003 como impacto de documentação não foi tocada nesta tarefa — está alocada à Tarefa 13.0 ("Fechamento... documentação reconciliada"), que roda depois e agrega toda a PRD; tocar esses documentos aqui seria antecipar escopo de outra tarefa.

Nenhum achado `critical` em aberto.

sha=0a43c5f1ab0bac885bef556141d3c15ceb91229c1a6fe2e8b6d4d9722bcd2db4
verdict=APPROVED_WITH_REMARKS
tool=claude

## Coverage

package=internal/capability
delta=+87.3%

Pacote novo (87.3% de cobertura na primeira entrega). `internal/parity` permanece em 84.8% e
`internal/runtime/specs` em 86.3%, ambos sem regressão frente ao estado anterior à Tarefa 6.0
(evidence/task-6.0-harness-portatil-vendor-neutral/test.log).

## Suposições
- RF-20 ("estender `specs.ValidateParityMatrix`... preservando a resolução via `dispatchProven` e a literal") foi interpretado como estender o **arquivo** `parity_gate.go` com uma função nova que reaproveita `ParityViolation` e a literal de falha, e não como forçar o eixo de `CanonicalPoint`/`PointCoverage` (específico de pontos canônicos de hook) sobre o eixo de invariantes de `internal/parity`, que são conceitualmente distintos.
- "Teste que sustenta" cada célula `supported`/`provider capability` foi resolvido para o único teste que já exercita a totalidade dos 26 invariantes contra o snapshot com os 4 provedores: `TestParitySuite/TestParity_AllTools`. A existência real desse método é verificada via `go/ast` (`ParityTestMethodExists`), não apenas menção textual.
- O 4º ponto do "registro anti-gate-órfão" (subtarefa 6.7) foi interpretado como o "gate do gate" da subtarefa 6.6 (testes que provam que o gate detecta matriz vazia/drift/célula sem teste), dado que as duas listas hardcoded do guardião em `sync_gates_guard_test.go` são específicas do mecanismo de espelhamento canônico↔mirror, inaplicável a um gate golden-file gerado↔commitado. Precedente: 4.0 documentou interpretação equivalente quando a numeração da técspec não mapeou 1:1 ao estado real do arquivo.
- RF-17 foi tratado como já estruturalmente satisfeito por mecanismo pré-existente (invariantes `Common`/`ToolSpecific` que falham já derrubam `TestParity_AllTools` e portanto o step "Parity gate" do CI, anterior a esta tarefa) mais a visibilidade nova que a matriz commitada acrescenta (uma célula `unsupported` aparece no diff do PR); nenhum mecanismo de enforcement novo foi criado para RF-17 especificamente, por não haver Fato Verificado (V-XX) nem subtarefa que apontasse lacuna nesse mecanismo.

## Riscos Residuais
- Invariantes INV-30, INV-32, X02 e FB01 podem passar por vacuidade no snapshot sintético de `Checker.Generate` (ausência de fixtures/condição de contorno), produzindo células `supported`/`provider capability` que não são, em rigor, mais fortes que uma ausência de evidência — mesma classe de risco que V-25, mas fora do conjunto literal que V-25/RF-18.1 nomeiam. Ver achado `medium` acima.
- `EvaluateHandshake` (`internal/runtime/precondition/precondition.go`) permanece stub; por decisão de manter RF-18 com fonte única em `internal/parity` (não introduzir um segundo eixo de dados no gerador), a capability matrix desta entrega não inclui uma célula `unknown` para `handshake`/`opencode` como a ADR-003 sugere a título de exemplo. As 10 células `unknown` hoje vêm inteiramente de V-25 (CL03..CL08 + X03), o que já satisfaz o critério de sucesso "células desconhecidas são rastreadas e justificadas", mas o exemplo específico da ADR não está representado.
- Documentação operacional (`docs/evidence-gates.md`, `docs/degradation-matrix.md`, `docs/troubleshooting.md`) listada no plano de impacto da ADR-003 fica para a Tarefa 13.0.
- Selo pendente (RF-14 da skill `execute-task`, Etapa 6): não há commit contendo esta tarefa ainda — nenhum commit foi criado por esta execução, por restrição de governança (o harness não commita). `ai-spec seal-evidence` deve ser invocado após o commit desta tarefa (por `semantic-commit` ou pelo humano), apontando para `.specs/prd-harness-portatil-vendor-neutral/6.0_execution_result.json`.

## Conflitos de Regra
- none
