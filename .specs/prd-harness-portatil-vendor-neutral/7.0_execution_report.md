<!-- evidence-contract: v2 -->
# Generated: 2026-09-18T00:00:00Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 7.0
- Título: Fundacao do manifesto: path para checksum e hash fail-closed
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-7.0-fundacao-do-manifesto-path-para-checksum-e-hash-fail-closed.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (Fatos Verificados V-08, V-08b, V-08c; RF-24, RF-26, RF-27)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (Modelos de Dados; Sequenciamento de Desenvolvimento P7; Bloco D — conflito e transação)
- Governança: AGENTS.md, .claude/rules/code-style.md (R-STYLE-001), .claude/rules/governance.md (R-GOV-001), .agents/skills/agent-governance/SKILL.md, .agents/skills/go-implementation/SKILL.md (skill de linguagem inferida do diff Go). Skills Necessárias da task file: "Nenhuma além das auto-carregadas (governance + linguagem)."

## Comandos Executados
- `go build ./...` -> sem erros (build limpo, repositório inteiro)
- `go vet ./...` -> sem erros (repositório inteiro)
- `go test ./internal/tracking/...` -> 10 passed (pacote novo)
- `go test ./internal/manifest/...` -> 21 passed
- `go test ./internal/install/...` -> 120 passed
- `go test ./internal/upgrade/...` -> 43 passed
- `go test ./internal/contextgen/...` -> 43 passed
- `go test ./internal/parity/...` -> 98 passed
- `go test ./...` -> 3869 passed em 81 packages (suíte completa, sem regressão)
- `go test -tags=integration ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/runtime/memory/durable/... ./cmd/ai_spec_harness/...` -> 1116 passed em 7 packages
- `go test -bench=. -benchmem -run '^$' ./internal/tracking/...` -> BenchmarkFakeFileSystem_WriteFile_Baseline 435.2 ns/op (269 B/op, 3 allocs/op); BenchmarkTracker_WriteFile_WithChecksum 1231 ns/op (711 B/op, 6 allocs/op) — overhead de ~800ns por escrita para SHA-256 de arquivo pequeno, irrelevante frente ao I/O real de disco do `CopyDir`/`WriteFile` em produção
- `GOGC=20 golangci-lint run --config .golangci.yml --timeout 5m ./internal/tracking/... ./internal/install/... ./internal/upgrade/... ./internal/manifest/... ./internal/contextgen/...` -> "golangci-lint: No issues found" (após correção de 2 achados QF1008/staticcheck)
- `bash scripts/check-mocks.sh` -> "Mocks: OK (sincronizados com mockery.yml)" (nenhuma interface exportada nova foi introduzida; subtarefa 7.4 vacuamente satisfeita)
- `gofmt -l internal/manifest internal/tracking internal/install internal/upgrade internal/contextgen` -> sem saída (formatação ok)
- Revisão via subagente `reviewer` (rodada 1): veredito REJECTED, achado [HIGH] em `internal/tracking/tracker.go` (`recordCopiedTree` fail-open em falha de `ReadDir`)
- Correção aplicada (`return fmt.Errorf(...)` em vez de `return nil`) + teste de regressão `TestTracker_CopyDirPropagatesReadDirFailureNeverSucceedsSilently`
- Revisão via subagente `reviewer` (rodada 2): veredito APPROVED, achado [HIGH] confirmado corrigido; 1 observação [LOW] não bloqueante (dívida pré-existente de idioma em mensagens de teste)
- Replicação local (script Python temporário, criado sob `evidence/task-7.0-harness-portatil-vendor-neutral/` e removido em seguida, mesmo padrão de 2.0/3.0) do algoritmo de `internal/taskloop.Orchestrator.captureFinalSnapshot` (`git diff --binary HEAD -- . <exclusões>` + diff de arquivos não rastreados via `git diff --binary --no-index -- /dev/null <path>` + SHA-256) -> `base_sha`, `patch_sha256` e `final_state_sha256` recomputados e gravados em `7.0_execution_result.json`; patch físico salvo em `evidence/task-7.0-harness-portatil-vendor-neutral/7.0.patch`
- `ai-spec validate-result execution .specs/prd-harness-portatil-vendor-neutral/7.0_execution_result.json --task-id 7.0 --verify-physical --prd-dir .specs/prd-harness-portatil-vendor-neutral --exclude <abs 7.0_execution_report.md>` -> `OK: resultado SDD execution valido`

## Arquivos Alterados
- internal/manifest/manifest.go (campo aditivo `FileChecksums map[string]string` `json:"file_checksums,omitempty"`)
- internal/manifest/task_7_0_test.go (novo)
- internal/tracking/tracker.go (novo pacote; substitui internal/install/write_tracker.go)
- internal/tracking/tracker_test.go (novo)
- internal/tracking/tracker_bench_test.go (novo)
- internal/install/install.go (usa tracking.Tracker; grava FileChecksums no manifesto; trackInstalled/trackMerged retornam error)
- internal/install/write_tracker.go (removido — lógica migrada para internal/tracking)
- internal/install/write_tracker_test.go (estendido com TestManifestFileChecksumsMatchRealDiskContent e TestManifestLegacyWithoutFileChecksumsNeverDiverges)
- internal/upgrade/upgrade.go (upgrade passa a usar tracking.Tracker; mergeFileTracking une checksum/tracking novo com o manifesto anterior; correção fail-closed em refsChangedFiles)
- internal/upgrade/upgrade_test.go (sem mudança de teste pré-existente; apenas consumido pelos novos testes do pacote)
- internal/upgrade/task_7_0_test.go (novo)
- internal/contextgen/claude_merge.go (interface mergeRecorder.MarkMerged retorna error; recordMerge propaga)
- internal/contextgen/claude_merge_test.go (test double recordingFakeFS.MarkMerged atualizado para nova assinatura)
- internal/contextgen/contextgen.go (call site de recordMerge propaga erro)
- .specs/prd-harness-portatil-vendor-neutral/tasks.md (editado) — linha da Tarefa 7.0 marcada `done`
- .specs/prd-harness-portatil-vendor-neutral/7.0_execution_result.json (criado)
- evidence/task-7.0-harness-portatil-vendor-neutral/build.log, vet.log, test.log, integration-test.log, lint.log, check-mocks.log, bench.log (criados)
- evidence/task-7.0-harness-portatil-vendor-neutral/7.0.patch (criado)

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- Manifesto legado e novo convivem sem regressao, pelo caminho de `HasFileTracking()` -> comprovado: `internal/manifest/task_7_0_test.go::TestManifest_FileChecksums_Retrocompatibility` (deserializa manifesto real da v2.0.1 com `installed_files`/`merged_files` e sem `file_checksums`, confirma `HasFileTracking() == true` e `FileChecksums == nil`); `internal/install/write_tracker_test.go::TestManifestLegacyWithoutFileChecksumsNeverDiverges`; `internal/upgrade/task_7_0_test.go::TestUpgrade_LegacyManifestWithoutTrackingStaysUntrackedOnVersionOnlyBump` (upgrade real com manifesto legado + bump de versão sem escrita mantém `HasFileTracking() == false`)
- Manifesto legado sem o campo significa ausencia de rastreio e NUNCA divergencia — senao todo projeto instalado antes desta entrega classificaria tudo como conflito -> comprovado: `internal/manifest/task_7_0_test.go::TestManifest_FileChecksums_LegacyManifestWithoutAnyTrackingNeverDiverges` (acesso a `FileChecksums["qualquer.md"]` em manifesto pré-RF-05 retorna `ok == false`, nunca um valor "divergente"); `mergeFileTracking` (internal/upgrade/upgrade.go) preserva `nil` explicitamente via as flags `trackedInstalled`/`trackedMerged`/`trackedChecksums`, testado em `TestUpgrade_LegacyManifestWithoutTrackingStaysUntrackedOnVersionOnlyBump`
- Nenhuma falha de leitura de hash produz resultado de sucesso -> comprovado: `internal/tracking/tracker_test.go::TestTracker_HashReadFailurePropagatesErrorNeverSucceedsSilently`, `TestTracker_WriteFileErrorsWhenPostWriteHashFails`, `TestTracker_CopyDirPropagatesReadDirFailureNeverSucceedsSilently` (achado [HIGH] da revisão, corrigido: `recordCopiedTree` agora propaga erro de `ReadDir` em vez de `return nil`); `internal/upgrade/task_7_0_test.go::TestUpgrade_RefsChangedFilesNeverSilentlyIgnoresHashReadFailure` (fix do fail-open pré-existente em `refsChangedFiles`, onde `err1 != nil || err2 != nil` antes resultava em nenhuma mudança reportada e agora é tratado como divergência)
- Benchmark antes e depois sem regressao relevante -> comprovado: `go test -bench=. -benchmem -run '^$' ./internal/tracking/...` — `BenchmarkFakeFileSystem_WriteFile_Baseline` 435.2 ns/op vs `BenchmarkTracker_WriteFile_WithChecksum` 1231 ns/op; overhead de ~800ns/op (uma passada SHA-256 sobre um arquivo pequeno em memória), ordens de magnitude abaixo do custo de I/O real de disco (`CopyDir`/`WriteFile` via `OSFileSystem`) que já domina o tempo de install/upgrade em produção

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=ee4dd644f7528c218c46f9ff796f48e114871494d14ea729e95ba9b1b4a757d8
verdict=APPROVED
tool=claude

## Coverage

package=internal/tracking,internal/manifest,internal/install,internal/upgrade,internal/contextgen
delta=+85.2%

result_path=.specs/prd-harness-portatil-vendor-neutral/7.0_execution_result.json

## Suposições
- Subtarefa 7.4 ("Declarar as interfaces novas em mockery.yml no mesmo commit") é condicional: nenhuma interface exportada nova foi introduzida por esta tarefa (`tracking.Tracker` é struct concreto que embute `fs.FileSystem`, já mockado; `mergeRecorder` em `internal/contextgen` permanece não-exportado e não listado em mockery.yml). Confirmado com `bash scripts/check-mocks.sh` (sem drift).
- A "leitura de produção" exigida pela subtarefa 7.3 foi implementada em `mergeFileTracking` (internal/upgrade/upgrade.go): ao salvar o manifesto após upgrade parcial, o `FileChecksums`/`InstalledFiles`/`MergedFiles` previamente persistidos são lidos do manifesto carregado e unidos com o que o tracker desta rodada capturou, evitando repetir o padrão write-only já criticado em V-08 para o campo `Checksums` por-skill.
- Symlink apontando para diretório (modo de instalação `--link-mode symlink`, majoritário para skills) não recebe checksum de arquivo — FileHash sobre um diretório sempre falha (io.Copy retorna erro de leitura); o path ainda é rastreado em InstalledFiles/MergedFiles, apenas sem entrada em FileChecksums. Task 8.0 (conflito) precisará tratar ausência de checksum para um path tracked como "sem decisão possível", não como conflito automático.
- Task 8.0 (categoria `conflict`, `FileOutcome`, transação em lote, aliases init/sync) está fora do escopo desta tarefa e não foi implementada — apenas a fundação (campo + escrita + leitura de produção + hash fail-closed) que ela exige como pré-requisito duro (D-a da techspec).

## Riscos Residuais
- [LOW, dívida declarada pelo revisor, não bloqueante] Mensagens de asserção em `internal/tracking/tracker_test.go` estão em PT-BR, seguindo o padrão já presente no arquivo `internal/install/write_tracker_test.go` anterior a esta tarefa. R-STYLE-001.1 pede inglês para mensagens de erro/log/strings internas; texto de asserção de teste é uma zona cinzenta e não foi corrigido nesta tarefa por já ser convenção pré-existente no pacote-irmão, não uma regressão introduzida por este diff.
- [LOW] `mergeFileTracking` (internal/upgrade/upgrade.go) nunca purga entradas de `FileChecksums`/`InstalledFiles`/`MergedFiles` para paths que existiam no manifesto anterior mas deixaram de existir fisicamente (arquivo removido/renomeado numa skill atualizada, sem recriação com o mesmo nome). Fica para a task 8.0 tratar "path rastreado mas ausente em disco" como categoria própria (missing), não como falso conflito — registrado explicitamente na techspec como escopo de P8, não de P7.
- `s.manifest.Save(projectDir, mf)` em internal/upgrade/upgrade.go continua descartando erro de gravação (`_ = ...`), padrão pré-existente ao diff desta tarefa, não introduzido por ela.
- Selo pendente: `7.0_execution_result.json` foi produzido e validado fisicamente (`ai-spec validate-result execution ... --verify-physical`), mas o selo da Etapa 6 (`ai-spec seal-evidence`) exige commit prévio, que o harness não cria por si (R-GOV-001). Selo fica pendente até alguém commitar o trabalho desta tarefa e rodar `ai-spec seal-evidence .specs/prd-harness-portatil-vendor-neutral/7.0_execution_result.json --prd-dir .specs/prd-harness-portatil-vendor-neutral --commit <sha>`.
- `patch_sha256`/`final_state_sha256` de `7.0_execution_result.json` refletem a árvore de trabalho inteira no momento da captura (as Tarefas 1.0, 2.0 e 3.0 do mesmo PRD estão concluídas mas ainda não commitadas), não apenas o diff isolado da Tarefa 7.0 — a seção "Arquivos Alterados" acima é a fonte de verdade do escopo real desta tarefa.

## Conflitos de Regra
- none
