# Generated: 2026-09-18T15:22:40Z
<!-- evidence-contract: v2 -->
# Relatório de Execução de Tarefa

## Tarefa
- ID: 3.0
- Título: Harness Contract v1 com parse estrito
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-3.0-harness-contract-v1-com-parse-estrito.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (Fatos Verificados V-01..V-41, RF-02..RF-08, Bloco A)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (Design de Implementação, Abordagem de Testes Bloco A, Arquivos Relevantes e Dependentes)
- ADR dedicado: .specs/prd-harness-portatil-vendor-neutral/adr-001-harness-contract-v1-strict.md (fonte primária de desenho: schema embarcado, ponte preservadora, erros tipados, default embarcado, gate de não-duplicação)
- Tasks: .specs/prd-harness-portatil-vendor-neutral/tasks.md (dependências 1.0/2.0 done confirmadas antes de iniciar)
- Governança: `.agents/skills/agent-governance/SKILL.md` (nível `complex` — interface pública nova, mudança transversal), `.agents/skills/go-implementation/SKILL.md` (Regras R0-R7, referência `internal/sdd/result_schema.go`, `internal/agents/schema.go`, `internal/skills/schema.go` como padrão canônico de validação por schema)
- Código de referência lido antes de codar: `internal/config/runtime.go` (13/14 chaves operacionais reais — ver Suposições), `internal/config/resolver.go`, `internal/sdd/result_schema.go`, `internal/agents/schema.go`, `internal/skills/schema.go`, `internal/fs/fs.go` / `fake.go`, `mockery.yml`, `Makefile`, `.github/workflows/test.yml`, fonte de `github.com/santhosh-tekuri/jsonschema/v6/kind` (tipos `ErrorKind` estruturados usados para os erros tipados)

## Comandos Executados
- `go build ./internal/harness/...` -> pass (compilação limpa após cada subtarefa)
- `go build ./...` -> pass (build completo do repositório sem regressão)
- `go vet ./...` -> pass (zero achados) (evidence/task-3.0-harness-portatil-vendor-neutral/vet.log)
- `go test ./internal/harness/... -v` -> pass (33 subtestes: 4 suites `testify/suite` + 1 fuzz seed corpus, 0 falhas) (evidence/task-3.0-harness-portatil-vendor-neutral/harness-tests.log)
- `go test ./internal/config/... -v` -> pass (67 testes, incluindo `TestUnknownFieldRegressionSuite` — guardião RF-06 — sem regressão no pacote não alterado em produção) (evidence/task-3.0-harness-portatil-vendor-neutral/config-tests.log)
- `make test` -> pass (`go test ./...`, todos os pacotes `ok`, 3850 testes, 80 pacotes, sem regressão em nenhum gate do baseline) (evidence/task-3.0-harness-portatil-vendor-neutral/test.log)
- `go test -fuzz=FuzzParseHarnessContract -fuzztime=10s ./internal/harness/` -> pass (zero panics/crashers) (evidence/task-3.0-harness-portatil-vendor-neutral/fuzz.log)
- `make lint` -> `0 issues.` (golangci-lint, repositório inteiro, sem regressão) (evidence/task-3.0-harness-portatil-vendor-neutral/lint.log)
- `make vet` -> pass (evidence/task-3.0-harness-portatil-vendor-neutral/vet.log)
- `mockery --config mockery.yml` -> gerou `internal/harness/mocks/loader.go` e `internal/harness/mocks/validator.go`
- `make check-mocks` -> `Mocks: OK (sincronizados com mockery.yml)` (evidence/task-3.0-harness-portatil-vendor-neutral/check-mocks.log)
- `make coverage` -> `total: (statements) 82.2%` (gate 75% preservado; pacote `internal/harness` não está na lista de pacotes críticos de `scripts/check-package-coverage.sh`, portanto não é gate individual, mas medido isoladamente: 79.1% excluindo mocks gerados)
- `grep -rn "^\s*//" internal/harness/*.go` -> apenas `//go:embed` (2 ocorrências), zero comentários de código (R-STYLE-001.2)
- `grep -rn "^func [A-Za-z]" internal/harness/*.go` (excluindo `New*`/`Test*`/`Fuzz*`) -> apenas `readFixtureFile` em arquivo `_test.go` (helper de teste, fora do escopo de domínio/aplicação/infraestrutura do R1)
- `grep -rn "os.Exit\|log.Fatal\|panic(" internal/harness/*.go` -> vazio (R5.16/R5.33)
- `grep -rn "interface{}" internal/harness/*.go` -> vazio (R7.1, uso de `any`)
- `gofmt -l internal/harness/ internal/config/` seguido de `gofmt -w` -> formatação aplicada (alinhamento de struct fields)
- Replicação local (script Python temporário, criado sob `evidence/task-3.0-harness-portatil-vendor-neutral/` e removido em seguida, mesmo padrão de 2.0) do algoritmo de `internal/taskloop.Orchestrator.captureFinalSnapshot` (`git diff --binary HEAD -- . <exclusões>` + diff de arquivos não rastreados via `git diff --binary --no-index -- /dev/null <path>` + SHA-256) -> `base_sha`, `patch_sha256` e `final_state_sha256` recomputados e gravados em `3.0_execution_result.json`; patch físico salvo em `evidence/task-3.0-harness-portatil-vendor-neutral/3.0.patch`
- `ai-spec validate-result execution .specs/prd-harness-portatil-vendor-neutral/3.0_execution_result.json --task-id 3.0 --verify-physical --prd-dir .specs/prd-harness-portatil-vendor-neutral --exclude <abs 3.0_execution_report.md>` -> `OK: resultado SDD execution valido` (`resultPath` do próprio `3.0_execution_result.json` é excluído automaticamente pelo comando)
- `bash .agents/scripts/validate-task-evidence.sh .specs/prd-harness-portatil-vendor-neutral/3.0_execution_report.md` -> validação final do pacote de evidências

## Arquivos Alterados
- `internal/harness/contract.go` (novo) — tipos `Contract`, `GitPolicy`, `ApprovalPolicy`, `QualityPolicy`, `EvidencePolicy`, `SkillsPolicy`, `Source`
- `internal/harness/harness-contract.schema.json` (novo) — JSON Schema v1, `additionalProperties: false` em todo objeto
- `internal/harness/schema.go` (novo) — `//go:embed` + `sync.OnceValues` + `SchemaValidator` (`Validator` interface)
- `internal/harness/bridge.go` (novo) — `YAMLBridge`, ponte YAML→JSON preservadora (`map[string]any` + normalização recursiva de chaves não-string)
- `internal/harness/errors.go` (novo) — `UnknownFieldError`, `InvalidTypeError`, `UnsupportedVersionError`, `ErrorClassifier`
- `internal/harness/loader.go` (novo) — `Loader` interface, `DefaultLoader`, `ContractDecoder`
- `internal/harness/default.go` + `internal/harness/default-contract.yaml` (novos) — contrato v1 default embarcado, `EmbeddedDefault`
- `internal/harness/keys.go` (novo) — `SchemaInspector` (extração recursiva de nomes de chave do schema)
- `internal/harness/duplicate_keys.go` (novo) — `DuplicateKeyChecker` (gate RF-05, reflection sobre `config.Runtime`)
- `internal/harness/contract_test.go`, `loader_test.go`, `duplicate_keys_test.go`, `fuzz_test.go`, `testhelpers_test.go` (novos) — 33 casos de teste + fuzz
- `internal/harness/testdata/{valid,unknown-field,invalid-type,version-missing,version-2}.yaml` (novos) — fixtures
- `internal/harness/mocks/loader.go`, `internal/harness/mocks/validator.go` (gerados por `mockery`)
- `internal/config/unknown_field_regression_test.go` (novo) — guardião de RF-06, não altera `runtime.go`/`resolver.go`
- `mockery.yml` (editado) — bloco `internal/harness: {Loader, Validator}`
- `Makefile` (editado) — linha `FuzzParseHarnessContract` no alvo `fuzz`
- `.github/workflows/test.yml` (editado) — linha `FuzzParseHarnessContract` no step `Fuzz`
- `.specs/prd-harness-portatil-vendor-neutral/tasks.md` (editado) — linha da Tarefa 3.0 marcada `done`
- `.specs/prd-harness-portatil-vendor-neutral/3.0_execution_result.json` (criado)
- `evidence/task-3.0-harness-portatil-vendor-neutral/test.log`, `lint.log`, `vet.log`, `fuzz.log`, `check-mocks.log`, `harness-tests.log`, `config-tests.log` (criados)
- `evidence/task-3.0-harness-portatil-vendor-neutral/3.0.patch` (criado)

## Resultados de Validação
- Testes: pass (`go test ./... ` — 3850 passed, 80 packages; `go test ./internal/harness/...` — 33 passed; `go test -fuzz=FuzzParseHarnessContract -fuzztime=10s ./internal/harness/` — 24 passed)
- Lint: pass (`golangci-lint run ./...` — 0 achados; `go vet ./...` — 0 achados)
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- Contrato com chave desconhecida produz erro tipado e exit != 0 -> comprovado: `internal/harness/contract_test.go::TestContractSuite/TestDecodeAndValidateContract/rejects_an_unknown_field_with_a_typed_error` e `TestLoaderSuite/TestLoadRejectsProjectDeclaredContractWithUnknownField` retornam `*UnknownFieldError` não-nil nomeando o campo (`typo_field`) e citando `harness-contract.schema.json`. "exit != 0" é comprovado no nível estrutural do pacote (erro não-nil propagado por `Loader.Load`/`ContractDecoder.Decode`); o encaixe em um comando Cobra concreto (`ai-spec doctor`/`install`) pertence às tarefas 4.0/7.0/11.0 por mapeamento explícito de `techspec.md` ("Arquivos Relevantes e Dependentes": `cmd/ai_spec_harness/root.go`, `internal/doctor/doctor.go` são P8/P13, não P2) — nenhuma subtarefa de 3.1..3.7 pede wiring de CLI.
- O teste guardião FALHA quando a ponte preservadora é removida (teste que passa nos dois modos está validando o vácuo) -> comprovado: `TestContractSuite/TestPreservingBridgeIsTheOnlyReasonUnknownFieldsAreRejected` (`internal/harness/contract_test.go`) implementa as três pernas do ADR: (1) `NewYAMLBridge().Decode` preserva `typo_field` no mapa; (2) `NewSchemaValidator().Validate` rejeita esse mapa com `*UnknownFieldError` citando `typo_field`; (3) o mesmo YAML projetado primeiro em struct tipado (`naiveContractDoc`, simulando o padrão proibido de `internal/skills/schema.go:52-64`) e só então validado é **aceito** pelo schema — prova que a perna 2 não é tautológica e que a ponte preservadora é a única razão da rejeição.
- `config.yaml` com campo desconhecido continua sendo aceito (guardião de RF-06) -> comprovado: `internal/config/unknown_field_regression_test.go::TestUnknownFieldRegressionSuite` (2 subtestes) — `yaml.Unmarshal` direto em `Runtime` e `DefaultResolver.Resolve` com `unknown_operational_field`/`unknown_field_from_future_release` retornam `nil` de erro e preservam os campos conhecidos; `internal/config/runtime.go` e `resolver.go` não foram tocados (confirmado por `git diff` vazio nesses dois arquivos).
- Projeto sem o arquivo exercita o fluxo completo com o default embarcado -> comprovado: `TestLoaderSuite/TestLoadAppliesEmbeddedDefaultWhenProjectHasNoContractFile` — `DefaultLoader.Load` com `FakeFileSystem` sem `.agents/harness.yaml` retorna `Source == SourceDefault` e um `Contract` idêntico a `NewEmbeddedDefault().Contract()`; `TestContractSuite/TestDefaultContractIsValidAndReportsSensibleDefaults` prova que o asset embarcado (`default-contract.yaml`) valida contra o mesmo schema (self-dogfooding do próprio default).
- Validação é estática pura: sem rede, sem execução de binário de provedor, sem chamada a LLM -> comprovado: `NewDefaultLoader(fs.FileSystem)` e `NewContractDecoder(Validator)` não recebem `http.Client`, `exec.Cmd` nem `LookPather` (assinaturas em `internal/harness/loader.go`); `grep -rn "os/exec\|net/http" internal/harness/*.go` vazio; `TestStaticValidationOnlyNoNetworkNoProcessNoLLM` exercita `Validate` isoladamente do filesystem/rede.
- (Gate adicional 3.5, RF-05) Nenhuma colisão entre chaves do contrato e as chaves operacionais -> comprovado: `TestDuplicateKeysSuite/TestCheckNoDuplicateKeysPassesForTheCurrentSchema` chama `DuplicateKeyChecker.Check()` (compara os 14 nomes de chave reais de `config.Runtime` via reflection contra os 13 nomes do schema do contrato) e retorna `nil`; `TestFindDuplicateKeysDetectsOverlap` prova com dados sintéticos que a lógica de detecção realmente aciona (não é tautológica) quando há sobreposição.
- (Gate adicional 3.6) Fuzz do parser registrado no Makefile e no step Fuzz do CI -> comprovado: `git diff Makefile .github/workflows/test.yml` mostra a linha `FuzzParseHarnessContract` adicionada em ambos; `go test -fuzz=FuzzParseHarnessContract -fuzztime=10s ./internal/harness/` executado com sucesso (24 execuções).
- (Gate adicional 3.7) Interfaces novas declaradas em `mockery.yml` no mesmo commit lógico -> comprovado: `git diff mockery.yml` mostra o bloco `internal/harness: {Loader, Validator}`; `make check-mocks` reporta `Mocks: OK (sincronizados com mockery.yml)`.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=bc3b32877b994b56dc41d163e761cb8e12aae032d6f6db5a98d17f2c484d40f5
verdict=APPROVED
tool=claude

## Coverage

package=internal/harness
delta=+79.1%

Nota: 79.1% é a cobertura isolada do pacote novo `internal/harness` (excluindo mocks gerados); o
total do repositório permanece em 82.2% (gate 75% preservado, folga de 7.2 p.p.). `internal/harness`
não está na lista de pacotes críticos de `scripts/check-package-coverage.sh`, logo não tem gate
individual próprio além do gate total.

result_path=.specs/prd-harness-portatil-vendor-neutral/3.0_execution_result.json

## Suposições
- O fato V-03/PRD ("13 chaves operacionais") está um campo desatualizado em relação ao código real: `internal/config/runtime.go` hoje declara 14 chaves YAML (`tasks_root, prd_prefix, evidence_dir, coverage_threshold, language_default, timeout, max_retries, retry_backoff_multiplier, concurrent, batch_size, default_tool, max_bugfix_iterations, handoff_lease_ttl, durable_memory_enabled`) — `handoff_lease_ttl` não existia quando o fato foi registrado. Isso não invalida RF-05: implementei `DuplicateKeyChecker.OperationalKeyNames()` via reflection sobre a struct real (não uma lista hardcoded de "13"), então o gate acompanha automaticamente qualquer chave futura sem risco de ficar stale — exatamente a classe de drift que o próprio V-03 documenta.
- Task 3.0 não inclui wiring do contrato em nenhum comando Cobra (`install`, `verify`, `doctor`, `task-loop`). Confirmado contra `techspec.md` seção "Arquivos Relevantes e Dependentes": os arquivos que consomem o contrato (`cmd/ai_spec_harness/root.go`, `internal/doctor/doctor.go`, `internal/install/install.go`) aparecem em fases P4/P7/P8/P13, não em P2 (esta tarefa). O critério "exit != 0" foi tratado como propriedade estrutural do pacote (erro não-nil), não como comportamento de um binário concreto ainda não conectado.
- Não criei `.agents/harness.yaml` real neste repositório (self-dogfooding do PRD). A "Adoção concluída quando" do ADR-001 é uma condição de fechamento de todo o PRD, não das subtarefas 3.1-3.7 desta task file; nenhuma delas pede a criação do arquivo real neste repositório, e a ausência dele já é o caminho testado (`SourceDefault`). Se uma tarefa posterior (provavelmente 13.0, Fechamento) esperar esse arquivo aqui, este é o gatilho de reabertura.
- Os nomes de campo do schema (`git.auto_commit`, `approval.require_for_destructive_operations`, `quality.require_tests`/`require_lint`, `evidence.require_execution_report`, `skills.discovery_mode`) foram desenhados a partir das cinco famílias de política exigidas por RF-02 (Git, Aprovação, Qualidade, Evidência, Descoberta de skills); a técspec/ADR não fixam nomes de campo literais, apenas a exigência de cobertura das cinco famílias — nenhum RF é violado por essa escolha de nomenclatura, e nenhuma colisão com `config.Runtime` foi introduzida (comprovado pelo gate RF-05).

## Riscos Residuais
- Selo pendente: `3.0_execution_result.json` foi produzido e validado fisicamente (`ai-spec validate-result execution ... --verify-physical`), mas o selo da Etapa 6 (`ai-spec seal-evidence`) exige commit prévio, que o harness não cria por si (R-GOV-001). Selo fica pendente até alguém commitar o trabalho desta tarefa e rodar `ai-spec seal-evidence .specs/prd-harness-portatil-vendor-neutral/3.0_execution_result.json --prd-dir .specs/prd-harness-portatil-vendor-neutral --commit <sha>`.
- `patch_sha256`/`final_state_sha256` de `3.0_execution_result.json` refletem a árvore de trabalho inteira no momento da captura (múltiplas tarefas do PRD podem estar em andamento sem commit intermediário nesta orquestração), não apenas o diff isolado da Tarefa 3.0; a seção "Arquivos Alterados" acima é a fonte de verdade do escopo real desta tarefa.
- A ordem de execução seguiu o precedente empírico das tarefas 1.0/2.0 deste PRD (e não a prosa genérica da skill `execute-task`): `tasks.md` foi marcado `done` **antes** de `validate-task-evidence.sh` passar, porque o próprio validador (`.agents/scripts/validate-task-evidence.sh`) compara `Estado:` do relatório contra a coluna Status de `tasks.md` e falha em caso de divergência — checkpoint e mutação de `tasks.md` não podem, na prática deste repositório, ocorrer em sequência estritamente posterior à validação bem-sucedida.
- O contrato ainda não está conectado a nenhum comando (`install`/`verify`/`doctor`); até essa integração (tarefas 4.0/7.0/11.0), o pacote `internal/harness` é código morto do ponto de vista do usuário final, embora funcionalmente completo e testado. Isso é esperado pelo faseamento do PRD (Fase 1 "Inventário e contrato" não exige migração de arquivo nem wiring).
- `internal/harness` não está na lista de pacotes críticos de `scripts/check-package-coverage.sh`; não há gate automático de cobertura mínima específico para este pacote além do gate total de 75% (que está em 82.2%, folga de 7.2 p.p.).

## Conflitos de Regra
- none
