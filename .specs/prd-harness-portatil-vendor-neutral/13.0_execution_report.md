<!-- evidence-contract: v2 -->
# Generated: 2026-09-18T23:00:00Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 13.0
- Título: Fechamento: nao-regressao, portabilidade, release e documentacao
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-13.0-fechamento-nao-regressao-portabilidade-release-e-documentaca.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (Fatos Verificados V-01..V-41, com foco em V-40; RF-48..RF-57, RF-63)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (seção Design de Implementação — Bloco H, tabela de rastreabilidade RF x arquivo, item "Não-regressão do baseline")
- Tasks: .specs/prd-harness-portatil-vendor-neutral/tasks.md — dependências 1.0..12.0 confirmadas `done` antes de iniciar
- Governança: AGENTS.md, .claude/rules/code-style.md (R-STYLE-001), .claude/rules/governance.md (R-GOV-001), .agents/skills/agent-governance/SKILL.md, .agents/skills/go-implementation/SKILL.md (skill de linguagem inferida do diff Go — o restante do diff é Makefile/CHANGELOG/docs). Skills Necessárias da task file: "Nenhuma além das auto-carregadas (governance + linguagem)."

## Comandos Executados
- `go build ./...` -> exit 0 (evidence/task-13.0-harness-portatil-vendor-neutral/build.log)
- `go vet ./...` -> exit 0 (evidence/task-13.0-harness-portatil-vendor-neutral/vet.log)
- `gofmt -l .` (excluindo vendor/) -> 0 arquivos com formatação pendente (evidence/task-13.0-harness-portatil-vendor-neutral/gofmt-check.log)
- `make test` (go test ./...) -> todos os pacotes `ok`, sem `--- FAIL` (evidence/task-13.0-harness-portatil-vendor-neutral/test.log)
- `make integration` -> `ok` nos 5 pacotes (`internal/integration`, `internal/skills`, `tests/integration`, `tests/integration/hooks_live`, `internal/runtime/memory/durable`, `cmd/ai_spec_harness`), 61.2s (evidence/task-13.0-harness-portatil-vendor-neutral/integration.log)
- `make lint` -> golangci-lint v2.13.2, "0 issues." (evidence/task-13.0-harness-portatil-vendor-neutral/lint.log)
- `make coverage` -> cobertura total 82.2% (gate 75%) (evidence/task-13.0-harness-portatil-vendor-neutral/coverage.log)
- `make coverage-packages` -> 9/9 pacotes críticos PASS, todos ≥ 70% (evidence/task-13.0-harness-portatil-vendor-neutral/coverage-packages.log)
- `make check-skills-sync` -> "Skills em sync: 87 / Skills com drift: 0"; "Gate de encerramento em paridade nos 4 agentes oficiais: 7 mirrors" (evidence/task-13.0-harness-portatil-vendor-neutral/check-skills-sync.log) — confere com V-40
- `make check-hooks-sync` -> "Hooks em sync: 28 / Drift / missing: 0"; "Gate de encerramento em sync: 7/7 mirrors" (evidence/task-13.0-harness-portatil-vendor-neutral/check-hooks-sync.log) — números idênticos a V-40
- `make check-scripts-sync` -> "Validadores em sync: 32 / Drift / missing: 0" (evidence/task-13.0-harness-portatil-vendor-neutral/check-scripts-sync.log) — V-40 registrou 29; ver seção "Confronto com V-40" abaixo (crescimento explicado, não regressão)
- `make check-mocks` -> "Mocks: OK (sincronizados com mockery.yml)" (evidence/task-13.0-harness-portatil-vendor-neutral/check-mocks.log)
- `make test-hooks` -> "Resultado: 57 asserts OK, 0 asserts FAIL" (evidence/task-13.0-harness-portatil-vendor-neutral/test-hooks.log)
- `make check-spec-paths` (após corrigir `SPEC_PATH_TARGETS` no Makefile para incluir este PRD) -> "Referencias de caminho: 42 verificadas, todas resolvem (2 PRD sob gestao SDD)"; suite de meta-teste "12 OK, 0 FAIL" (evidence/task-13.0-harness-portatil-vendor-neutral/check-spec-paths.log)
- `make test-validators` -> "Resultado: 60 passaram, 0 falharam" + demais suítes de validador OK (evidence/task-13.0-harness-portatil-vendor-neutral/test-validators.log)
- `make test-portable-skills` -> "portable skills contracts: OK" (evidence/task-13.0-harness-portatil-vendor-neutral/test-portable-skills.log)
- `make budget` -> `ok` (evidence/task-13.0-harness-portatil-vendor-neutral/budget.log)
- `make test-sdd-evals` -> `escape_rate=0.0, false_negative_rate=0.0, false_positive_rate=0.0, quality_rate=1.0` (evidence/task-13.0-harness-portatil-vendor-neutral/test-sdd-evals.log)
- `make check-capability-matrix-sync` -> `ok` (evidence/task-13.0-harness-portatil-vendor-neutral/check-capability-matrix-sync.log) — gate adicional de task 6.0, também verificado por completude
- `make check-policies-sync` -> "Policies em sync: 2 / Drift / missing: 0" (evidence/task-13.0-harness-portatil-vendor-neutral/check-policies-sync.log) — gate adicional de task 4.0
- `make check-skills-lock` -> "Integridade das skills OK (versao + hash SHA-256)" (evidence/task-13.0-harness-portatil-vendor-neutral/check-skills-lock.log) — gate adicional de task 1.0/7.0 (RF-60, fecha V-34)
- `go test -tags=integration -run 'TestLegacyV201Project|TestPortability_CLISwitchWithoutReinstall|TestPortability_NoDaemonRequired|TestNoImplicitGitOperations_InstallUpgradeDoctor|TestReleasePolicy_DeterministicScenariosGateMergeLiveScenariosDoNot|TestOpenCodeGovernancePluginBlocksUnsolicitedGitCommitAndPushEvenWhenPreloadConfirmed' -v ./tests/integration/...` -> 7/7 PASS (evidence/task-13.0-harness-portatil-vendor-neutral/rf50-rf52-rf53-rf55-rf56-new-tests.log)

## Confronto com V-40 (subtarefa 13.1)
V-40 registrou: `go build ./...` exit 0; skills drift 0; hooks 28 em sync, drift 0; validadores 29 em
sync, drift 0; gate de encerramento 7/7 mirrors. Reexecução nesta tarefa:
- `go build ./...`: exit 0 -> **idêntico**.
- Skills drift: 0 -> **idêntico** (87 skills em sync; V-40 não fixou o total de skills, só o drift).
- Hooks: 28 em sync, drift 0 -> **idêntico**, número exato preservado.
- Gate de encerramento: 7/7 mirrors -> **idêntico**.
- Validadores: 32 em sync, drift 0 (V-40 registrou 29). Investigado: `check-scripts-sync.sh` conta
  `len(EVIDENCE_VALIDATORS) x len(mirror_dirs) + len(session_end_extra_mirrors)`. Hoje
  `EVIDENCE_VALIDATORS` tem 10 entradas (`validate-task-evidence.sh`, `validate-bugfix-evidence.sh`,
  `validate-refactor-evidence.sh`, `validate-review-evidence.sh`, `validate-session-end.sh`,
  `hook-prereq-gate.sh`, `resolve-references.sh`, `validate-skill-prerequisites.sh`,
  `validate-governance-references.sh`, `git-operation-gate.sh`) x 3 mirrors + 2 extras = 32.
  `git diff a8b7721 -- scripts/check-scripts-sync.sh` confirma que a única mudança nesse arquivo desde
  o baseline foi a adição de `git-operation-gate.sh` à lista (task 9.0, RF-40.1/RF-40.2 — criação do
  gate canônico de operação Git). Sem essa entrada a conta seria 9x3+2=29, batendo exatamente com
  V-40. **Crescimento explicado por entrega legítima desta mesma PRD, não regressão** — drift
  permanece 0 nas duas medições.

## Arquivos Alterados
- Makefile — `SPEC_PATH_TARGETS` passa a incluir `.specs/prd-harness-portatil-vendor-neutral` além de
  `.specs/prd-harness-quatro-clis-loop-aprovacao` (que segue `blocked` na tarefa 11.0, não concluído).
  Sem esta correção o gate `check-spec-paths` (RF-51) nunca cobria os artefatos desta própria entrega —
  inconsistência entre tarefas encontrada e corrigida nesta tarefa de fechamento, conforme instrução
  explícita de revisar consistência entre 1.0..12.0 antes de reportar `done`.
- CHANGELOG.md — nova seção "Unreleased (proxima minor, PRD harness-portatil-vendor-neutral)"
  declarando nominalmente a distribuição incondicional de `R-STYLE-001` aos projetos consumidores
  (RF-12), com nota de migração (remoção manual local de `.claude/rules/code-style.md` e mirrors
  equivalentes caso o consumidor não queira aplicá-la), e os demais itens aditivos desta entrega
  (harness contract opcional, `.agents/policies/`, `file_checksums` no manifesto, gate de operação
  Git). Seção "Breaking Changes: Nenhuma" declarada explicitamente (RF-49).
- docs/guia-instalacao-universal.md — seções novas "Uso Multi-CLI Simultâneo", "Contrato do Harness
  (`.agents/harness.yaml`)", "Políticas Canônicas (`.agents/policies/`)", "Capability Matrix" e
  "Diagnóstico Multi-Provedor (`doctor`)" adicionadas após "Atualização Após `git pull`" (RF-51).
  Nenhum conteúdo existente foi reescrito — apenas adição, conforme RF-51 explícito ("documentação
  existente que descreve comportamento preservado não é reescrita").
- tests/integration/legacy_v2_0_1_fixture_test.go (novo) — `TestLegacyV201Project_DoctorWorksWithoutAnyUserAction`
  e `TestLegacyV201Project_UpgradeSucceedsWithZeroFlags` (RF-50): fixture real (fs.NewOSFileSystem,
  t.TempDir()) construída por uma instalação real via assets embarcados, envelhecida para o formato
  exato da v2.0.1 (version="2.0.1", `file_checksums` removido — único campo que `git diff a8b7721 --
  internal/manifest/manifest.go` mostra ter sido adicionado desde aquele release). `doctor` funciona
  sem nenhuma ação do usuário (contrato ausente cai no default embutido, RF-07, e aparece como aviso,
  nunca falha); `upgrade` com zero flags migra e backfilla `file_checksums`. Também inclui
  `TestNoImplicitGitOperations_InstallUpgradeDoctor` (RF-55): install+upgrade+doctor num repo git real
  não criam commit nem remote.
- tests/integration/portability_test.go (editado) — `TestPortability_CLISwitchWithoutReinstall`
  (RF-53): instalação única com os 4 provedores, `Verify()` confirma os 4 como current a partir do
  mesmo diretório, e o gate de descoberta dispara idêntico para claude/codex/copilot a partir da MESMA
  árvore sem reinstalar; `TestPortability_NoDaemonRequired` (RF-52): varre a árvore instalada com os 4
  provedores e assevera ausência de artefato de daemon/supervisor e que todo script `.sh` tem shebang
  one-shot.
- tests/integration/release_policy_config_test.go (novo) — `TestReleasePolicy_DeterministicScenariosGateMergeLiveScenariosDoNot`
  (RF-56): parseia `.github/workflows/{test,hooks-live,acp-live}.yml` como YAML e verifica por
  configuração (não por convenção) que `test.yml` dispara em `pull_request` e roda os comandos
  determinísticos reais do baseline, e que os workflows live não disparam em `pull_request` nem
  duplicam a suíte determinística.
- evidence/task-13.0-harness-portatil-vendor-neutral/ (novo) — logs de todos os comandos acima,
  patch físico (`13.0.patch`) e resultado SDD.
- .specs/prd-harness-portatil-vendor-neutral/13.0_execution_result.json,
  .specs/prd-harness-portatil-vendor-neutral/.checkpoints/13.0.yaml — contrato SDD v2 desta tarefa.

## RF-63 (subtarefa 13.8) — revisão de capacidade herdada falsamente
Confrontado cada um de V-33, V-34 e V-37 contra o estado atual do código e da prosa dos artefatos:
- V-33 (passo de CI de snapshot nunca executava teste algum): `.github/workflows/test.yml` linha 54
  hoje roda `go test -v ./internal/contextgen/... -run TestContextgen_Snapshots`, batendo com o nome
  real do teste (`TestContextgen_Snapshots`, `internal/contextgen/contextgen_test.go:238`). **Corrigido
  de fato, não apenas declarado.**
- V-34 (nenhum gate recomputava os hashes de `skills-lock.json`): `make check-skills-lock` (`go run .
  skills --verify .`) está wireado em `.github/workflows/test.yml` linha 102, executado nesta tarefa
  com saída "Integridade das skills OK". **Corrigido de fato.**
- V-37 (`Report.Flows` criado vazio e nunca populado): confirmado que `internal/metrics/flow.go`
  continua sem chamador de produção (`grep` por `Flows[` permanece vazio). Nenhum PRD/techspec desta
  entrega afirma que isso foi corrigido — a mitigação registrada em techspec (R-13, linha ~874) foi
  explicitamente "criar por requisito explícito OU declarar escopo reduzido", e o RF-47 (mecanismo de
  ablation) foi implementado sobre `internal/telemetry` (métricas RF-43/44/45, com escritor de
  produção real), **não** sobre `internal/metrics.Report.Flows` — um caminho de medição
  independente e genuíno, não uma reafirmação da capacidade quebrada. Buscado em todo `docs/*.md` e
  em `.specs/prd-harness-portatil-vendor-neutral/*.md` por menção a "Report.Flows"/"flow.go" fora das
  três entradas que já descrevem o defeito (V-37, R-13, RF-63) — nenhuma ocorrência adicional
  encontrada. **Nenhuma afirmação de capacidade herdada falsa identificada.**

## Resultados de Validação
- Testes: pass (`make test` exit 0, `make integration` exit 0, mais 7 testes novos/reexercidos exit 0)
- Lint: pass (`make lint` exit 0, "0 issues.")
- Veredito do Revisor: APPROVED (autorrevisão de fechamento — ver seção "Riscos Residuais"; task file
  não declara skill de revisão adicional além da governança base)

## Critérios de Aceite
- Todos os gates do baseline verdes, com os números de V-40 preservados -> comprovado: os 16 alvos
  do Bloco H (`test`, `integration`, `lint`, `vet`, `coverage`, `coverage-packages`,
  `check-skills-sync`, `check-hooks-sync`, `check-scripts-sync`, `check-mocks`, `test-hooks`,
  `test-validators`, `check-spec-paths`, `test-portable-skills`, `budget`, `test-sdd-evals`) mais
  `check-capability-matrix-sync`, `check-policies-sync` e `check-skills-lock` (gates adicionais
  entregues por esta mesma PRD) rodaram com exit 0 — evidence/task-13.0-harness-portatil-vendor-neutral/*.log.
  Números de V-40 preservados exatamente (build exit 0, skills drift 0, hooks 28/0, gate de
  encerramento 7/7); o único número que mudou (validadores 29->32) tem causa raiz identificada e
  legítima (seção "Confronto com V-40" acima), com drift permanecendo 0.
- Zero mudança de default não declarada no changelog -> comprovado: evidence/task-13.0-harness-portatil-vendor-neutral/changelog-unreleased-section.txt
  (cópia da seção "Unreleased" de CHANGELOG.md) declara nominalmente a única mudança de comportamento
  percebida pelo consumidor (RF-12, distribuição de R-STYLE-001) com nota de migração; seção "Breaking
  Changes: Nenhuma" explícita; demais itens descritos são estritamente aditivos (harness.yaml opcional,
  policies/ com fallback, file_checksums aditivo no manifesto, gate de git como reforço de segurança).
- Projeto instalado na versão anterior funciona sem nenhuma ação do usuário -> comprovado: `TestLegacyV201Project_DoctorWorksWithoutAnyUserAction`
  e `TestLegacyV201Project_UpgradeSucceedsWithZeroFlags`, PASS —
  evidence/task-13.0-harness-portatil-vendor-neutral/rf50-rf52-rf53-rf55-rf56-new-tests.log.
- Evidências persistidas por tarefa conforme o padrão vigente (RF-57) -> comprovado: evidence/task-13.0-harness-portatil-vendor-neutral/test-validators.log
  ("Resultado: 60 passaram, 0 falharam"); este relatório, `13.0_execution_result.json`
  (schema_version 2) e `.checkpoints/13.0.yaml` seguem o mesmo formato de
  `9.0_execution_result.json`/`12.0_execution_result.json`, e `bash .agents/scripts/validate-task-evidence.sh`
  aprovou este relatório nesta mesma execução.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comandos correspondentes em Comandos
      Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=0440e07b2e8829e6ad3fac94a08f0bc91d1876820711da2d87165cb4250fc9c4
verdict=APPROVED
tool=claude

## Coverage

package=n/a (tarefa de fechamento; nenhum pacote de produção alterado — apenas Makefile, CHANGELOG,
docs e testes de integração novos sob build tag `integration`, que não entram em `make coverage`)
delta=0.0%

result_path=.specs/prd-harness-portatil-vendor-neutral/13.0_execution_result.json

## Suposições
- `SPEC_PATH_TARGETS` no Makefile é o mecanismo declarado de "PRD ativo" para `check-spec-paths.sh`
  (nenhum PRD deste repositório usa `sdd-state.json`); adicionar este PRD à lista, mantendo o PRD
  anterior (ainda não concluído, task 11.0 `blocked`), segue a regra documentada no próprio comentário
  do Makefile ("PRD concluído sai da lista; PRD novo entra") sem remover cobertura de nenhum PRD ativo.
- A Etapa 6 (selo de evidência via `ai-spec seal-evidence`) é deferida para depois do commit humano,
  conforme instrução explícita do invocador; `13.0_execution_result.json` foi produzido no mesmo
  formato usado por 9.0/12.0 mas não foi selado (`--verify-physical` não executado).
- O veredito "APPROVED" desta tarefa é autorrevisão de fechamento (a task file não declara skill de
  revisão adicional além da governança base); não houve invocação do subagente `reviewer` dedicado,
  já que trata-se de tarefa de fechamento/consolidação e não de implementação de funcionalidade nova
  isolada.

## Riscos Residuais
- Selo de evidência (`ai-spec seal-evidence`) pendente até existir um commit contendo este trabalho —
  esperado e documentado (Etapa 6 da skill `execute-task`, deferida por instrução explícita).
- `internal/metrics/flow.go` (V-37) permanece sem chamador de produção; esta tarefa confirmou que
  nenhuma afirmação de "corrigido" existe sobre isso (RF-63 satisfeito), mas a limitação em si não foi
  criada nem removida por este PRD — decisão de escopo já registrada em techspec (R-13/D-16), não uma
  lacuna nova introduzida aqui.
- `V-24`/divergência residual entre `Makefile:integration` (5 pacotes) e o step `Integration tests` de
  `test.yml` (4 pacotes, sem `internal/runtime/memory/durable` nem `cmd/ai_spec_harness`) permanece:
  não é regressão desta tarefa (já existia no baseline) e está fora do escopo declarado de RF-48 (que
  lista os alvos `make`, não exige paridade byte-a-byte entre `Makefile` e `test.yml`); registrado aqui
  para transparência, não corrigido por não ser critério de sucesso desta tarefa nem RF explícito.

## Conflitos de Regra
- none
