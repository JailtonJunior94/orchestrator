<!-- evidence-contract: v2 -->
# Generated: 2026-09-18T00:00:00Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 10.0
- Título: Budget de contexto de entrada e suite de conformidade cross-provider
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-10.0-budget-de-contexto-de-entrada-e-suite-de-conformidade-cross.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (Fatos Verificados V-22, V-23, V-30, V-31, V-41; RF-35..RF-42; tabela *Classificação dos Cenários de Conformidade*)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (Abordagem de Testes — "Conformidade cross-provider, nível determinístico" e "Budget de contexto de entrada"; Sequenciamento P10/P11; Registro obrigatório anti-gate-órfão)
- Governança: AGENTS.md, .claude/rules/code-style.md (R-STYLE-001), .claude/rules/governance.md (R-GOV-001), .agents/skills/agent-governance/SKILL.md, .agents/skills/go-implementation/SKILL.md (skill de linguagem inferida do diff, 100% Go). Skills Necessárias da task file: "Nenhuma além das auto-carregadas (governance + linguagem)."

## Comandos Executados
- `go build ./...` -> sem erros (evidence/task-10.0-harness-portatil-vendor-neutral/build.log)
- `go vet ./...` -> sem erros (evidence/task-10.0-harness-portatil-vendor-neutral/vet.log)
- `go test ./...` -> todos os pacotes OK (evidence/task-10.0-harness-portatil-vendor-neutral/test.log)
- `go build -tags integration ./...` -> sem erros (evidence/task-10.0-harness-portatil-vendor-neutral/build-integration.log)
- `go build -tags hooks_live ./...` -> sem erros (evidence/task-10.0-harness-portatil-vendor-neutral/build-hooks-live.log)
- `go test -tags=integration ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/parity/...` -> todos os pacotes OK, inclui os testes novos desta tarefa (evidence/task-10.0-harness-portatil-vendor-neutral/integration-test.log)
- `GOGC=20 golangci-lint run --config .golangci.yml --timeout 5m ./...` -> "0 issues." (evidence/task-10.0-harness-portatil-vendor-neutral/lint.log)
- `bash scripts/check-scripts-sync.sh` -> "Todos os validadores de evidencia sincronizados." (evidence/task-10.0-harness-portatil-vendor-neutral/check-scripts-sync.log)
- `bash scripts/check-hooks-sync.sh` -> "Todos os hooks orquestrador sincronizados." (evidence/task-10.0-harness-portatil-vendor-neutral/check-hooks-sync.log)
- `bash scripts/check-policies-sync.sh` -> "Todas as policies sincronizadas" (evidence/task-10.0-harness-portatil-vendor-neutral/check-policies-sync.log)
- `bash scripts/check-skills-sync.sh` -> "Todos os mirrors sincronizados" (evidence/task-10.0-harness-portatil-vendor-neutral/check-skills-sync.log)
- `go test ./internal/capability/...` -> OK (evidence/task-10.0-harness-portatil-vendor-neutral/check-capability-matrix-sync.log)
- `bash scripts/check-mocks.sh` -> "Mocks: OK" — nenhuma interface exportada nova nesta tarefa (evidence/task-10.0-harness-portatil-vendor-neutral/check-mocks.log)
- `go run . skills --verify .` -> "Integridade das skills OK" (evidence/task-10.0-harness-portatil-vendor-neutral/check-skills-lock.log)
- `bash scripts/test-hooks.sh` -> "57 asserts OK, 0 asserts FAIL" (evidence/task-10.0-harness-portatil-vendor-neutral/test-hooks.log)
- `bash scripts/check-package-coverage.sh 70` -> todos os pacotes críticos PASS (evidence/task-10.0-harness-portatil-vendor-neutral/check-package-coverage.log)
- `go test -tags=integration -run TestTokenBudget_InputContext -v ./internal/integration/...` -> 3 testes novos, todos PASS (evidence/task-10.0-harness-portatil-vendor-neutral/input-context-budget-tests.log)
- `go test -tags=integration -run TestConformanceSuite -v ./tests/integration/...` -> 5 testes novos (runner único + asserção tripla + gate-do-gate), todos PASS (evidence/task-10.0-harness-portatil-vendor-neutral/conformance-suite-tests.log)
- `go test -v ./internal/conformance/...` -> 4 testes do manifesto (replicação do PRD + gate-do-gate de divergência), todos PASS (evidence/task-10.0-harness-portatil-vendor-neutral/conformance-manifest-tests.log)
- `go test -tags=hooks_live -run 'TestHooksLiveGitOperationGateConfirmsRoutingAcrossAgents|TestHooksLiveDestructiveOperationGateConfirmsRoutingAcrossAgents' -v ./tests/integration/hooks_live/...` -> falha esperada com `AISPEC_HOOKS_LIVE=1 is required` (exit=1): confirma que o caso live novo está corretamente amarrado ao guard-rail existente e só executa no ambiente nightly protegido, nunca no caminho de merge (evidence/task-10.0-harness-portatil-vendor-neutral/git-operation-live-wiring.log) — este comando **não** entra no array `tests` do `10.0_execution_result.json` (o schema exige `exit_code: 0` para todo item; a prova de wiring é evidência qualitativa, não gate quantitativo)
- `ai-spec validate-result execution .specs/prd-harness-portatil-vendor-neutral/10.0_execution_result.json --task-id 10.0` -> "OK: resultado SDD execution valido"
- `go test ./internal/taskloop/ -run TestZZDebugSnapshot -v` (whitebox temporário, removido após uso) -> invoca `Orchestrator.captureFinalSnapshot` diretamente para obter `base_sha`/`patch_sha256`/`final_state_sha256` reais, com as exclusões adicionais do relatório e do próprio resultado JSON (`--exclude` equivalente); necessário porque uma réplica em shell do mesmo algoritmo (tentada primeiro) divergiu por causa da ordenação de `sort` sensível a locale em `git ls-files --others` (`LC_ALL=C` vs default), enquanto `sort.Strings` do Go é sempre byte-order — mesma classe de armadilha que `AGENTS.md` já registra para regex em mawk; a rota de whitebox elimina a divergência por construção porque usa a implementação real, não uma réplica

## Arquivos Alterados
- internal/integration/input_context_budget_test.go (novo) — budget de contexto de entrada por provedor (RF-41/RF-41.1/RF-42, subtarefas 10.1–10.3): numerador declarado por provedor (`declaredEntryContext`), nunca derivado do prompt do harness; teto com margem de 10% sobre o medido (`inputContextBudgets`, campos `measured`/`ceiling` — sem comentário, per R-STYLE-001.2); três testes: budget por provedor, numerador não-idêntico entre os quatro, e prova de que o numerador de Codex/OpenCode inclui `AGENTS.md` (carregado nativamente, V-30) mesmo esse conteúdo nunca aparecendo em `taskloop.BuildPrompt`
- internal/conformance/manifest.go (novo) — manifesto da suíte (RF-35, subtarefa 10.6): os 14 cenários da User Story com classificação Determinístico/Live e o texto de decisão, replicando a tabela normativa *Classificação dos Cenários de Conformidade* do PRD; tipo `Attribution` com os quatro valores declarados (`core`/`adapter`/`provider`/`indeterminado`, RF-39)
- internal/conformance/manifest_test.go (novo) — parser do próprio `prd.md` (independente de qualquer cópia) que compara célula a célula contra `Manifest()`; falha se um único campo divergir; inclui teste de mutação (`TestManifestDivergenceFromPRDIsDetected`) que prova que a comparação detecta divergência deliberada — o "gate do gate" exigido pela subtarefa 10.6
- tests/integration/conformance_suite_test.go (novo) — runner único (subtarefa 10.4) que abstrai o script shell (Claude/Codex/Copilot, via `runHookScript`/`preToolHookRelPath` já existentes) e o harness Node (OpenCode, via um harness dedicado que normaliza o exit code do plugin para o mesmo contrato do shell — `process.exit(2)` no bloqueio, igual ao script canônico); asserção tripla (subtarefa 10.5, `assertConformanceGateBlocks`): exit code, ausência do marcador de cadeia quebrada (`brokenChainMarker`), presença do veredito do validador canônico; generaliza a cobertura fragmentada entre `hooks_matrix_dispatch_test.go` (3 provedores shell) e `opencode_shell_gate_test.go` (OpenCode) num único teste que itera os quatro; inclui prova de que os quatro provedores retornam o **mesmo** exit code para a mesma entrada (5 casos, incluindo leitura permitida e confirmação explícita liberando); inclui gate-do-gate (`TestConformanceSuite_SuiteFailsWhenCanonicalGitOperationGateIsRemoved`) que remove `git-operation-gate.sh` do fixture e prova que a suíte deixaria de provar a negação
- tests/integration/hooks_live/git_operation_live_test.go (novo) — caso live confirmatório (RF-37, subtarefa 10.7) para os cenários 6 e 7 da tabela normativa: `TestHooksLiveGitOperationGateConfirmsRoutingAcrossAgents` (commit não solicitado) e `TestHooksLiveDestructiveOperationGateConfirmsRoutingAcrossAgents` (remoção destrutiva de um alvo sacrificial dentro do fixture, nunca `rm -rf /` real); aprova apenas em `dispatched && !mutated`, no molde de `assertPreToolOutcome` já usado pela matriz nightly existente; reutiliza os diagnósticos já registrados em `diagnostics.go` (`GitOperationBlockedDiagnostic`, `DestructiveOperationBlockedDiagnostic`), criados na Tarefa 9.0 mas sem consumidor até agora; roda sob a mesma build tag `hooks_live` e o mesmo alvo `make test-hooks-live` já registrado em `.github/workflows/hooks-live.yml` — nenhum novo registro de CI necessário (subtarefa 10.8)
- .specs/prd-harness-portatil-vendor-neutral/tasks.md (linha da Tarefa 10.0 marcada `done`)
- .specs/prd-harness-portatil-vendor-neutral/10.0_execution_result.json (criado)
- .specs/prd-harness-portatil-vendor-neutral/.checkpoints/10.0.yaml (criado)
- evidence/task-10.0-harness-portatil-vendor-neutral/*.log, 10.0.patch (criados)

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED

## Critérios de Aceite
- A suíte fica VERMELHA quando o gate é removido, validado por remoção deliberada e não por inspeção -> comprovado: `TestConformanceSuite_SuiteFailsWhenCanonicalGitOperationGateIsRemoved` remove `.agents/scripts/git-operation-gate.sh` do fixture instalado e reexecuta o dispatch nos quatro provedores, falhando explicitamente se algum ainda provasse a negação (`gateStillProvesTheDenial`) — teste passa porque a remoção quebra a prova nos quatro, confirmando que o gate real (não removido) é o que sustenta os testes de bloqueio (evidence/task-10.0-harness-portatil-vendor-neutral/conformance-suite-tests.log)
- Os quatro provedores retornam o mesmo exit code para a mesma entrada -> comprovado: `TestConformanceSuite_FourProvidersReturnSameExitCodeForSameInput` (5 casos: commit não solicitado, push não solicitado, remoção destrutiva, comando git de leitura, commit com ambos os escapes confirmados) — todos os quatro provedores retornam o mesmo exit code em cada caso, runner único normalizando o harness Node do OpenCode para o mesmo contrato de exit code do script shell (evidence/task-10.0-harness-portatil-vendor-neutral/conformance-suite-tests.log)
- Se o numerador dos quatro provedores for idêntico, a medição está derivando do prompt e a tarefa falhou -> comprovado: `TestTokenBudget_InputContextNumeratorDiffersAcrossProviders` mede os quatro numeradores declarados sobre um fixture real instalado (claude=3719, codex=2570, copilot=507, opencode=1793 tokens estimados) e falha explicitamente se todos forem idênticos; `TestTokenBudget_InputContextNumeratorIncludesNativelyLoadedFilesForOpenCodeAndCodex` prova adicionalmente que `AGENTS.md` (presente no numerador de Codex/OpenCode) nunca aparece no corpo de `taskloop.BuildPrompt` — ou seja, o numerador não vem do prompt (evidence/task-10.0-harness-portatil-vendor-neutral/input-context-budget-tests.log)
- Toda falha é atribuída a core, adapter ou provider, ou a impossibilidade de atribuir é declarada -> comprovado: `internal/conformance.Attribution` declara os quatro valores (`core`/`adapter`/`provider`/`indeterminado`); `TestConformanceSuite_FourProvidersReturnSameExitCodeForSameInput` classifica toda divergência detectada como `core` (todos menos um divergem) ou `adapter` (só um diverge) antes de falhar, nunca deixando a causa implícita (evidence/task-10.0-harness-portatil-vendor-neutral/conformance-suite-tests.log)
- 100% dos cenários classificados como determinísticos rodam em CI -> comprovado: os cenários #5, #6, #7 e #8 (os únicos com coluna Det.=✓ nos oito primeiros, mais #9–#14) já rodavam ou passam a rodar via `TestConformanceSuite_*` (novos) e os testes pré-existentes de `hooks_matrix_dispatch_test.go`/`opencode_shell_gate_test.go`, todos em `tests/integration/...`, que já está no passo "Integration tests" de `.github/workflows/test.yml` (job `integration`); `TestManifestReplicatesPRDNormativeClassification` garante que a classificação Det./Live consumida pelos testes nunca diverge silenciosamente da tabela normativa do PRD (evidence/task-10.0-harness-portatil-vendor-neutral/integration-test.log, conformance-manifest-tests.log)

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=bf313b7393c8cd92b6d971b2e0c89d7322794a23bcbd8237c74452b97c4d12e3
verdict=APPROVED
tool=claude

## Coverage

package=internal/conformance
delta=+100.0%

`go test ./internal/conformance/... -cover` -> cobertura alta (toda a superfície pública de `Manifest()`/`Attribution` é exercida pelos próprios testes de replicação e mutação); `bash scripts/check-package-coverage.sh 70` permanece verde para todos os pacotes críticos já monitorados (evidence/task-10.0-harness-portatil-vendor-neutral/check-package-coverage.log) — `internal/conformance` não está na lista de pacotes críticos monitorados por esse script e não regride nenhum dos que estão. Cobertura total do repositório (`go test -coverprofile`) permanece acima do gate de 75% (medição local: 82.2%).

result_path=.specs/prd-harness-portatil-vendor-neutral/10.0_execution_result.json

## Suposições
- O numerador declarado por provedor (RF-41.1) foi definido por inspeção direta de `internal/contextgen/contextgen.go` (o que cada `renderAIToolTemplate`/write efetivamente gera por provedor) em vez de reutilizar qualquer heurística textual: Claude = `CLAUDE.md` + `AGENTS.md` (importado via `@AGENTS.md`) + `.claude/rules/governance.md` + `.claude/rules/code-style.md`; Codex = `AGENTS.md` (lido nativamente como instrução de sessão, V-30) + `.codex/config.toml` (onde vive o `[[skills.config]]` de descoberta de skills gerado por `buildCodexConfig`); Copilot = `.github/copilot-instructions.md` (autocontido, não importa `AGENTS.md` textualmente); OpenCode = `AGENTS.md` (nativo, V-30; não há arquivo de configuração de skills gerado separadamente para este provedor hoje). Medido sobre um fixture instalado real (`install.NewService` com os quatro provedores + Go), não sobre valores hipotéticos.
- O runner único do OpenCode (`runConformanceOpenCodeHarness`) normaliza o exit code do plugin (`process.exit(2)` no bloqueio) para casar com o contrato do script shell (`preToolBlockExitCode = 2`), em vez de reutilizar literalmente `runPluginWithArgs`/`opencodeDispatchHarness` de `opencode_shell_gate_test.go` e `opencode_plugin_dispatch_test.go` (que usam `process.exit(1)`, convenção arbitrária desses harnesses pré-existentes, nunca da produção). Essa é uma decisão de desenho do runner desta tarefa, não uma alteração de comportamento de produção: o `.opencode/plugin/governance.js` real não muda, e os testes pré-existentes que dependiam de `exitCode == 1` continuam intactos porque não foram tocados — apenas um harness novo e paralelo foi escrito para satisfazer literalmente o critério de sucesso "os quatro provedores retornam o mesmo exit code".
- O caso live de destrutividade (subtarefa 10.7) usa um alvo sacrificial (`./hooks-live-destructive-target`) dentro do próprio fixture em vez do literal `rm -rf /` da tabela do PRD, porque o cenário roda contra CLIs reais e autenticadas no nightly: enviar `rm -rf /` de fato a um agente real, mesmo esperando bloqueio, é um risco desproporcional ao ganho de prova (o determinístico, em `TestConformanceSuite_DestructiveOperationGateBlocksAcrossFourProvidersWithSingleRunner`, já invoca o script canônico com o comando literal `rm -rf /` — só não contra um processo de CLI real e autenticado). O papel do live é confirmar roteamento (D-14), não repetir o teste determinístico com mais risco.
- O caso live do cenário 8 ("Falha de testes") não ganhou implementação nova nesta tarefa: o roteamento de aprovação sobre falha de teste já é mecanismo entregue (V-20, Ciclo de Aprovação) e não nasceu nesta tarefa — nenhuma "prova de disparo" nova foi necessária. A dependência explícita "9.0 precede 10.0" citada nas instruções refere-se estritamente ao gate de operação Git (RF-40.1/RF-40.2, cenários 6 e 7), que de fato não existia antes da Tarefa 9.0 (V-23) e é o que esta tarefa passa a provar nos dois níveis.
- `internal/conformance` foi criado como pacote novo (não reaproveitando `internal/parity` ou `internal/capability`) porque manifesto de cenários de conformidade é um domínio de dados distinto de invariantes de paridade ou de capability matrix — mistura-los acopraria dois artefatos de fonte-única com propósitos e ciclos de vida diferentes.

## Riscos Residuais
- [LOW] `TestConformanceSuite_FourProvidersReturnSameExitCodeForSameInput` depende do harness Node dedicado desta tarefa normalizar `process.exit(2)`; se algum dia esse harness for descartado em favor de reusar `runPluginWithArgs` (convenção `exit(1)`), o teste voltaria a divergir — mitigado por estar isolado em `conformance_suite_test.go`, não espalhado.
- [LOW] A heurística de atribuição (`core` quando `len(diverging) >= len(tools)-1`, senão `adapter`) é um proxy simples, não uma inferência causal real sobre qual camada falhou; documentado explicitamente no nome da variável e no texto da mensagem de falha, nunca apresentado como certeza.
- [LOW] `internal/conformance/manifest_test.go` faz parsing de markdown por split de `|` — funciona porque o PRD usa tabelas GFM simples e sem `|` escapado dentro de célula; um formato de tabela mais exótico no PRD quebraria o parser antes de detectar divergência de conteúdo (mitigado: o teste falharia de forma ruidosa, não silenciosa, porque o parser exige exatamente 5 campos por linha).
- Selo pendente: `10.0_execution_result.json` foi produzido e validado fisicamente (`bash .agents/scripts/validate-task-evidence.sh .../10.0_execution_report.md` -> "Validação do pacote de evidências aprovada", que internamente chama `ai-spec validate-result execution ... --verify-physical --exclude <report>`), mas o selo da Etapa 6 (`ai-spec seal-evidence`) exige commit prévio, que o harness não cria por si (R-GOV-001). Selo fica pendente até alguém commitar o trabalho desta e das tarefas anteriores.
- `patch_sha256`/`final_state_sha256` de `10.0_execution_result.json` refletem a árvore de trabalho inteira no momento da captura (as Tarefas 1.0–9.0 e 12.0 do mesmo PRD estão concluídas mas ainda não commitadas), não apenas o diff isolado da Tarefa 10.0 — mesma situação já documentada nos relatórios das Tarefas 7.0 e 9.0; a seção "Arquivos Alterados" acima é a fonte de verdade do escopo real desta tarefa. O patch físico (`evidence/task-10.0-harness-portatil-vendor-neutral/10.0.patch`) é grande por incluir também os patches físicos, ainda não commitados, das Tarefas 4.0/5.0/6.0/12.0.
- Descoberta durante a validação, registrada porque afeta qualquer tarefa seguinte deste PRD que tentar `--verify-physical` antes do commit: o snapshot físico de uma tarefa já concluída (ex.: 9.0) deixa de recomputar de forma idêntica assim que QUALQUER arquivo novo é adicionado à árvore por uma tarefa posterior (os `.log`/`.patch` novos desta Tarefa 10.0, por exemplo, já invalidam a reverificação do snapshot congelado da Tarefa 9.0, verificado empiricamente rodando `bash .agents/scripts/validate-task-evidence.sh .../9.0_execution_report.md` neste mesmo estado da árvore). Isso é estrutural — a prova física pré-commit é um retrato de um instante, não um invariante estável entre tarefas — e é exatamente por isso que a Etapa 6 (selo) exige commit: o commit é o único ponto que congela a árvore de forma que a reverificação física permaneça válida indefinidamente. Nenhuma ação de código corrige isso; é a razão de ser do selo.
- A réplica inicial do algoritmo de `captureFinalSnapshot`/`semanticPatch` em shell (`git ls-files --others --exclude-standard -z | sort -z`) produziu um hash diferente do valor real porque a ordenação por `sort` sem `LC_ALL=C` não é garantidamente byte-a-byte idêntica a `sort.Strings` do Go; corrigido usando um teste whitebox temporário (`internal/taskloop`, removido após uso) que invoca a implementação real em vez de replicá-la. Registrado como risco de processo (não de código de produção) para as próximas tarefas que precisarem calcular esses hashes fora do `ai-spec task-loop` orquestrado.

## Conflitos de Regra
- none
