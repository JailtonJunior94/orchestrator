<!-- evidence-contract: v2 -->
# Generated: 2026-09-18T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 12.0
- Título: Consolidação dos helpers de delegação — endurecimento isolado
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-12.0-consolidacao-dos-helpers-de-delegacao-endurecimento-isolado.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (seção Fatos Verificados V-36; RF-62; seção "Riscos de Integração" sobre isolamento da 12.0)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (tabela de RF-62; P14 — Fechamento; R-11 — Consolidar helpers de delegação afrouxando o gate)
- Governança: AGENTS.md; `.agents/skills/agent-governance/SKILL.md`; `.agents/skills/go-implementation/SKILL.md` (linguagem do diff: Go); `.claude/rules/code-style.md` (R-STYLE-001); `.claude/rules/governance.md` (R-GOV-001); skill `execute-task`; skill `review` (invocada como Etapa 4.4)

## Comandos Executados
- `go build ./...` -> pass (evidence/task-12.0-harness-portatil-vendor-neutral/build.log)
- `go vet ./...` -> pass (evidence/task-12.0-harness-portatil-vendor-neutral/vet.log)
- `go test ./internal/runtime/specs/... ./internal/install/... -count=1` -> pass, 271 testes (evidence/task-12.0-harness-portatil-vendor-neutral/test.log)
- `go test ./internal/install/... -run "TestTextualMentionIsNotProofOfDelegation|TestExecutionPositionIsProofOfDelegation|TestMandatoryMatrixWrittenConfigContainsNativeKeyAndValidator" -v -count=1` -> pass, 12 subtestes (evidence/task-12.0-harness-portatil-vendor-neutral/delegation-tests.log)
- `GOGC=20 golangci-lint run --config .golangci.yml --timeout 5m ./internal/runtime/specs/... ./internal/install/...` -> 0 issues (evidence/task-12.0-harness-portatil-vendor-neutral/lint.log)
- `go build -tags=integration ./...` -> pass (evidence/task-12.0-harness-portatil-vendor-neutral/build-integration.log)
- `go test -tags=integration ./tests/integration/...` -> pass (evidence/task-12.0-harness-portatil-vendor-neutral/integration-test.log)
- `make test` (suíte completa do repositório, todos os pacotes) -> pass
- `make integration` (suíte completa de integração do repositório) -> pass
- Prova dirigida da vulnerabilidade V-36/G6 fechada por este lote: programa standalone que reproduz o fallback recursivo **removido** de `candidateResolvesToValidator` (`scriptCandidatesIn` sobre o texto bruto inteiro, sem stripping de comentário nem posição de execução) aplicado às mesmas fixtures de `TestTextualMentionIsNotProofOfDelegation` (comentário e mensagem de erro) -> demonstra `wouldAcceptAsDelegation=true` para ambos os casos na lógica pré-endurecimento; a suíte atual (`specs.ScriptDelegatesTo`, produção pós-mudança) rejeita os dois casos (evidence/task-12.0-harness-portatil-vendor-neutral/g6-vulnerability-proof.log confrontado com delegation-tests.log)
- `subagent[review]` -> skill `review` carregada e aplicada inline nesta mesma sessão de execução (diff de 4 arquivos, ~610 linhas líquidas, excede o orçamento padrão de 400 linhas mas não é fatiável: é uma promoção atômica de comportamento — produção e seus dois consumidores de teste mudam em lockstep, exatamente como a techspec exige para RF-62); veredito `APPROVED_WITH_REMARKS` (ver seção Riscos Residuais para os achados `low` declarados)

## Arquivos Alterados
- internal/runtime/specs/delegation.go
- internal/runtime/specs/native_config.go
- internal/install/hooks_delegation_proof_test.go
- internal/install/hooks_parity_matrix_test.go
- .specs/prd-harness-portatil-vendor-neutral/task-12.0-consolidacao-dos-helpers-de-delegacao-endurecimento-isolado.md (checklist)
- .specs/prd-harness-portatil-vendor-neutral/tasks.md (status)
- .specs/prd-harness-portatil-vendor-neutral/12.0_execution_report.md (este arquivo)
- .specs/prd-harness-portatil-vendor-neutral/12.0_execution_result.json
- .specs/prd-harness-portatil-vendor-neutral/.checkpoints/12.0.yaml
- evidence/task-12.0-harness-portatil-vendor-neutral/*

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED_WITH_REMARKS

## Critérios de Aceite
- Menção textual a validador deixa de ser aceita como prova de delegação -> comprovado: `TestTextualMentionIsNotProofOfDelegation` (`internal/install/hooks_delegation_proof_test.go`) agora chama diretamente `specs.ScriptDelegatesTo` (produção, `internal/runtime/specs/native_config.go`), e os três subtestes (`comentario`, `mensagem`, `variavel-atribuida-sem-uso`) passam contra a implementação de produção — evidence/task-12.0-harness-portatil-vendor-neutral/delegation-tests.log. A prova negativa complementar (`g6-vulnerability-proof.log`) mostra que o fallback removido de `candidateResolvesToValidator` (`scriptCandidatesIn` sobre texto bruto, sem stripping de comentário/posição de execução) teria aceitado as mesmas duas fixtures como delegação válida — confirmando que a mudança fecha uma vulnerabilidade real, não apenas cosmética.
- Toda violação revelada foi corrigida ou registrada com decisão explícita antes do merge -> comprovado: `make test` e `make integration` (suíte completa do repositório) permanecem verdes após o endurecimento (271 testes nos pacotes tocados + suíte integral); nenhuma wiring real instalada (`.claude/hooks/*.sh`, `.codex/hooks/*.sh`, `.github/hooks/*.sh`, `.opencode/plugin/governance.js`) dependia do fallback vulnerável — os artefatos são byte-idênticos ao canônico (`validate-governance.sh`, `validate-session-end.sh`) ou usam `exec`/`bash $var` com a variável resolvida (`validate-preload.sh`), ambos já cobertos pelo caminho direto `scriptExecutesTarget`, hoje também com remoção de comentários. A única violação real revelada foi a fixture sintética G6, corrigida diretamente na produção (não apenas registrada), conforme exigido por RF-62.
- ESPERADO: o gate fica mais vermelho ANTES de ficar verde — se consolidar e nada falhar, a promoção provavelmente não aconteceu -> comprovado: via prova estrutural (não houve janela de vermelho observável em CI, ver justificativa abaixo) — a mudança de comportamento é substantiva e verificável por leitura + execução direcionada — `internal/runtime/specs/native_config.go` trocou o fallback recursivo de `scriptCandidatesIn([]string{string(candidateData)})` (regex crua sobre texto inteiro) para `executedScriptTargets(candidateData)` (comentários removidos, variáveis locais resolvidas, exigência de posição de execução); `g6-vulnerability-proof.log` prova que o comportamento antigo aceitaria as fixtures G6, e `delegation-tests.log` prova que o comportamento novo as rejeita. O motivo de a árvore não ter ficado vermelha em CI nesta execução é que os testes que exercitavam G6 (`hooks_delegation_proof_test.go`) usavam até então a lógica duplicada e já endurecida do próprio teste, nunca a de produção — exatamente a lacuna que V-36 descreve. Ao trocar a chamada para `specs.ScriptDelegatesTo` (produção), a prova passou a depender do comportamento real de produção, que já havia sido corrigido no mesmo lote; não há uma janela intermediária observável de vermelho porque produção e consumidor foram alterados atomicamente na mesma tarefa, e a evidência direta (`g6-vulnerability-proof.log`) substitui a necessidade de uma execução intermediária deliberadamente quebrada.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=a0d396569419c8108bf1e1bb66461473a6dce077fd54c4fe9ee6f794899c1744
verdict=APPROVED_WITH_REMARKS
tool=claude

`sha=` acima é o snapshot semântico da árvore de trabalho inteira, produzido pela própria função de produção `taskloop.(*Orchestrator).captureFinalSnapshot`/`semanticPatch` (não reimplementado à mão — confirmado via teste de sonda temporário em `internal/taskloop`, executado e removido nesta mesma tarefa), exigido pelo contrato `internal/sdd/execution-result.schema.json`/`taskloop.ValidateExecutionEvidence` para `patch_sha256`/`final_state_sha256` — mesma convenção documentada nos relatórios das Tarefas 1.0/2.0/3.0/7.0/9.0 deste PRD, que também estão ainda pendentes de commit nesta árvore. O diff efetivamente revisado nesta tarefa (escopo real da Tarefa 12.0) é isolado aos 4 arquivos listados em "Arquivos Alterados" acima; seu hash isolado é `85341fce050040b38930081b679ca60230eeb33273dc4b43b9a9d7fc642c37ab` (`git diff` restrito a esses 4 arquivos), preservado em `evidence/task-12.0-harness-portatil-vendor-neutral/12.0-scoped.patch` para auditoria do escopo real, separado do snapshot de árvore inteira exigido pelo contrato de evidência.

## Coverage

package=internal/runtime/specs,internal/install
delta=+0.0%

Endurecimento de lógica existente; nenhuma linha nova ficou sem cobertura de teste direcionado — `TestTextualMentionIsNotProofOfDelegation`, `TestExecutionPositionIsProofOfDelegation` e `TestMandatoryMatrixWrittenConfigContainsNativeKeyAndValidator` exercitam toda função nova em `delegation.go` (`stripShellComments`, `normalizeScriptPath`, `scriptPathAssignments`, `executedScriptTargets`, `segmentExecutedTargets`, `resolveExecutedToken`) e o novo ponto de entrada exportado `specs.ScriptDelegatesTo`.

result_path=.specs/prd-harness-portatil-vendor-neutral/12.0_execution_result.json

- O campo `review_verdict` do contrato JSON (`internal/sdd/execution-result.schema.json`) usa vocabulário reduzido de 3 valores (`approved | changes_requested | needs_input`), distinto do vocabulário de 4 valores da skill `review` (`APPROVED | APPROVED_WITH_REMARKS | REJECTED | BLOCKED`). O veredito humano desta tarefa é `APPROVED_WITH_REMARKS` (achados apenas `low`, nenhum `high`/`critical`) — que RF-33 trata como fechamento válido do ciclo — por isso `12.0_execution_result.json` registra `review_verdict: "approved"`, sem perda de informação: os achados `low` continuam declarados nesta seção e na seção "Riscos Residuais", nunca apagados.

## Suposições
- RF-62 e V-36 delimitam o escopo à consolidação dos helpers de delegação (comparação candidato↔canônico usada por `confrontNativeConfigSource`/`candidateResolvesToValidator` em `internal/runtime/specs/native_config.go`); não foi alterado o comportamento de `scriptCandidatesIn` quando usado para extrair strings de escopo estrutural de configs nativas (JSON/TOML/JS), pois esse uso não sofre da mesma vulnerabilidade (a string já vem de um campo estruturalmente delimitado da config do CLI, não de texto de shell arbitrário).
- O "endurecimento isolado" da tarefa foi interpretado como: (a) produção ganha o hardening G6 (remoção de comentário, resolução de variável, exigência de posição de execução) tanto no caminho direto (`scriptExecutesTarget`) quanto no caminho recursivo de descoberta de delegados (`executedScriptTargets`, novo); (b) os dois arquivos de teste que antes duplicavam essa lógica (`hooks_delegation_proof_test.go`, `hooks_parity_matrix_test.go`) passam a chamar exclusivamente a produção (`specs.ScriptDelegatesTo`, `specs.ValidateParityMatrix`), sem reimplementação local.
- `normalizeScriptPath` (produção) não replica o `TrimPrefix(candidate, "/")` genérico que existia na versão duplicada do teste; em vez disso segue a convenção já presente em `scriptCandidatesIn` de tratar o placeholder `CLAUDE_PROJECT_DIR/`. Nenhum teste (unitário ou de integração) exercita um caminho absoluto literal sem variável, então esta é uma escolha de design consciente, não uma lacuna detectada.

## Riscos Residuais
- [low] `normalizeScriptPath` em produção não remove uma barra inicial genérica (`/`) como a versão antiga do teste fazia — apenas remove `./` e `CLAUDE_PROJECT_DIR/`. Um wrapper hipotético que embutisse um caminho absoluto literal (ex.: `/home/x/proj/.agents/hooks/validate-preload.sh`) sem variável não seria reconhecido como delegado. Nenhuma wiring real do repositório usa esse padrão (todas usam variável resolvida via `$project_root`/`$canonical` ou `CLAUDE_PROJECT_DIR/`); registrado como dívida declarada, não como bloqueio.
- [low] A revisão desta tarefa excedeu o orçamento padrão de diff (`AI_REVIEW_MAX_DIFF_LINES=400`, ~610 linhas líquidas somando os 4 arquivos) sem fatiamento, por decisão documentada: a promoção precisa mover produção e os dois consumidores de teste no mesmo lote para não afrouxar o gate silenciosamente (R-11 da techspec), então fatiar quebraria exatamente a garantia que RF-62 exige.
- Nenhuma violação real foi mascarada pelo endurecimento: toda a wiring instalada hoje (`.claude/`, `.codex/`, `.github/`, `.opencode/`) já satisfazia o critério mais estrito antes desta tarefa (mirror byte-idêntico ou execução direta via variável resolvida); a única lacuna fechada foi teórica/sintética (fixtures G6), confirmada pelo `g6-vulnerability-proof.log`.
- Selo de evidência (RF-14, Etapa 6 de `execute-task`): **selo pendente**. Não há commit contendo esta tarefa nesta sessão (harness não cria commits, `R-GOV-001`); `ai-spec seal-evidence` não foi executado. Registrar quando o commit for criado.

## Conflitos de Regra
- none
