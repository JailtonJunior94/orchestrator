<!-- evidence-contract: v2 -->
<!-- Generated: 2026-09-18T14:37:19Z -->
# Relatório de Execução de Tarefa

## Tarefa
- ID: 1.0
- Título: Reparo da instrumentação de CI e base probatória de filesystem
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-1.0-reparo-da-instrumentacao-de-ci-e-base-probatoria-de-filesyst.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (seção Fatos Verificados: V-24, V-32, V-33, V-34, V-38; Requisitos Funcionais RF-20.1, RF-58, RF-59, RF-60, RF-63)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (seções "Sequenciamento de Desenvolvimento" P0/P0.1, "Abordagem de Testes" Bloco D e "Prova de transação", "Arquivos Relevantes e Dependentes")
- Governança: AGENTS.md (contrato base), .agents/skills/agent-governance/SKILL.md (via skill execute-task), .agents/skills/go-implementation/SKILL.md (linguagem do diff), .claude/rules/code-style.md (R-STYLE-001, hard), .claude/rules/governance.md (R-GOV-001)

## Comandos Executados
- `go build ./...` -> exit 0, sem output (evidence/task-1.0-harness-portatil-vendor-neutral/build.log)
- `go vet ./...` -> exit 0, sem output (evidence/task-1.0-harness-portatil-vendor-neutral/vet.log)
- `go test ./... -count=1` -> 3814 testes passando em 78 pacotes (evidence/task-1.0-harness-portatil-vendor-neutral/test.log)
- `go test -v -count=1 ./internal/fs/... ./internal/upgrade/...` -> 98 testes passando em 3 pacotes, decorador de falha (RF-58) e regressão do BUG-T1.0-2 visíveis nominalmente (evidence/task-1.0-harness-portatil-vendor-neutral/fs-upgrade-test.log)
- `go test -v -tags=integration -count=1 ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/parity/...` -> 840 testes passando em 5 pacotes, comando idêntico ao novo step `integration` de test.yml; nomes de `TestE2EParitySuite/...` e dos 5 mutation/registration tests aparecem nominalmente (evidence/task-1.0-harness-portatil-vendor-neutral/integration-test.log)
- `go test -v ./internal/contextgen/... -run TestContextgen_Snapshots -count=1` -> 9 testes passando, comando idêntico ao step corrigido "Verificar snapshots atualizados" (evidence/task-1.0-harness-portatil-vendor-neutral/snapshot-gate.log)
- `go run . skills --verify .` (mesmo comando de `make check-skills-lock`) -> "Integridade das skills OK (versao + hash SHA-256)." (evidence/task-1.0-harness-portatil-vendor-neutral/skills-lock.log)
- `bash scripts/check-skills-sync.sh` -> 87 skills em sync, 0 drift (evidence/task-1.0-harness-portatil-vendor-neutral/check-skills-sync.log)
- `bash scripts/check-hooks-sync.sh` -> 28 hooks em sync, 0 drift (evidence/task-1.0-harness-portatil-vendor-neutral/check-hooks-sync.log)
- `bash scripts/check-scripts-sync.sh` -> 29 validadores em sync, 0 drift (evidence/task-1.0-harness-portatil-vendor-neutral/check-scripts-sync.log; baseline V-40 preservado nos três)
- `bash scripts/check-mocks.sh` -> "Mocks: OK (sincronizados com mockery.yml)" (evidence/task-1.0-harness-portatil-vendor-neutral/check-mocks.log)
- `make check-spec-paths` -> 16 referências verificadas + 12 OK no self-test do validador
- `make test-validators` -> 60 cenários passaram, 0 falharam
- `make test-portable-skills` -> "portable skills contracts: OK"
- `make budget` -> `ok internal/integration 0.227s`
- `golangci-lint run --config .golangci.yml --timeout 5m ./internal/fs/... ./internal/upgrade/...` -> "No issues found" (evidence/task-1.0-harness-portatil-vendor-neutral/lint.log)
- `golangci-lint run --config .golangci.yml --timeout 5m --build-tags=integration ./tests/integration/...` -> "No issues found"
- `go test -coverprofile=... ./... -count=1` + `go tool cover -func` -> cobertura total do repositório 82.2% (gate: 75%) (evidence/task-1.0-harness-portatil-vendor-neutral/coverage.log)
- `bash scripts/check-package-coverage.sh 70` -> todos os pacotes críticos ≥70% (evidence/task-1.0-harness-portatil-vendor-neutral/package-coverage.log)
- Delta de cobertura por pacote tocado: `git worktree add --detach /tmp/base-cov a8b77217` + `go test -coverprofile=... ./internal/fs/... ./internal/upgrade/...` no worktree da base (50.9%) vs. na árvore atual (51.1%) -> delta +0.20% (ver seção Coverage)
- `go test -tags=integration -run 'TestCIWorkflowRegistersRepairedGates|TestSnapshotGateFailsOnDeliberateDrift|TestSnapshotGateIsGreenOnUntouchedClone|TestSkillsLockGateFailsWhenLockedSkillDrifts|TestSkillsLockGateIsGreenOnUntouchedClone' -v ./tests/integration/...` -> 5/5 mutation/registration tests passando (prova por execução real, clone completo hardlinkado do repositório)
- Mutação manual de controle: reverti temporariamente a correção de `checkSkills` (references/) e confirmei que `TestUpgrade_CheckSkills_SourceRefsIOErrorNeverYieldsOK` falha sem ela; restaurei a correção e reconfirmei verde — prova de que o teste de regressão é mutation-proof
- subagent[revisão independente, rodada 1] -> Verdict: REJECTED (BUG-T1.0-1 comentário em linha tocada; BUG-T1.0-2 fail-open em erro de IO de `DirHash` em `references/`)
- subagent[revisão independente, rodada 2, após bugfix] -> Verdict: APPROVED_WITH_REMARKS (zero achados HIGH/CRITICAL; 2 achados LOW registrados como dívida abaixo)
- `go run . skills --verify . --task-id 1.0` (via `internal/taskloop.Orchestrator.captureFinalSnapshot`, invocado por teste local descartável e removido em seguida) -> base_sha, patch_sha256 e final_state_sha256 recomputados e gravados em `1.0_execution_result.json`; patch físico salvo em `evidence/task-1.0-harness-portatil-vendor-neutral/1.0.patch`
- `ai-spec validate-result execution .specs/prd-harness-portatil-vendor-neutral/1.0_execution_result.json --task-id 1.0 --verify-physical --prd-dir .specs/prd-harness-portatil-vendor-neutral --exclude <abs path do 1.0_execution_report.md>` -> "OK: resultado SDD execution valido"
- `bash .agents/scripts/validate-task-evidence.sh .specs/prd-harness-portatil-vendor-neutral/1.0_execution_report.md` -> "Validação do pacote de evidências aprovada"

## Arquivos Alterados
- .github/workflows/test.yml
- Makefile
- internal/fs/fs.go
- internal/fs/fake.go
- internal/fs/fake_test.go
- internal/fs/os_test.go
- internal/fs/failing_fake_test.go (novo)
- internal/upgrade/upgrade.go
- internal/upgrade/upgrade_test.go
- tests/integration/ci_repair_gates_test.go (novo)

## Resultados de Validação
- Testes: pass
- Lint: pass
- Veredito do Revisor: APPROVED_WITH_REMARKS

## Critérios de Aceite
- Os nomes dos testes de `internal/parity/e2e_parity_test.go` aparecem NOMINALMENTE no log do job de CI -> comprovado: `.github/workflows/test.yml` job `integration` agora roda `go test -v -tags=integration ... ./internal/parity/...`; execução local com `-v` via `rtk proxy` mostrou `=== RUN TestE2EParitySuite/...` para cada subteste (ex.: `TestE2EParity_CrossProject_AllTools/claude`).
- Cada gate reparado falha sob injeção deliberada de divergência, provado por execução e não por inspeção visual -> comprovado: `tests/integration/ci_repair_gates_test.go` (`TestSnapshotGateFailsOnDeliberateDrift`, `TestSkillsLockGateFailsWhenLockedSkillDrifts`) clona o repositório inteiro por hardlink e executa de fato `go test`/`go run` dentro do clone mutado, ambos passando (exit≠0 do comando sob teste é o que faz o teste Go passar).
- Editar um `SKILL.md` sem regerar `skills-lock.json` derruba o CI -> comprovado: `make check-skills-lock` adicionado ao job `lint` de `test.yml`; `TestSkillsLockGateFailsWhenLockedSkillDrifts` edita `semantic-commit/SKILL.md` num clone e confirma `go run . skills --verify .` retorna exit≠0 nomeando a skill divergente.
- Teste que injeta falha no meio de uma sequência de escrita observa erro propagado -> comprovado: `internal/fs/failing_fake_test.go:TestFailingFileSystem_PropagatesErrorMidSequence` (decorador local ao teste, `FakeFileSystem` intocado por padrão, confirmado por `TestFailingFileSystem_DefaultFakeStillNeverFails`).
- Nenhum caminho produz sucesso a partir de duas leituras de hash falhas -> comprovado: `internal/upgrade/upgrade.go` (`checkSkills`) — comparação de `SKILL.md` marca `StatusContentDivergent` se qualquer um dos dois `FileHash` falhar; comparação de `references/` marca `StatusRefsDivergent` para qualquer erro de `DirHash` do lado fonte que não seja `os.ErrNotExist` (ausência legítima). Regressão coberta por `TestUpgrade_CheckSkills_SourceRefsIOErrorNeverYieldsOK`, confirmada mutation-proof por reversão manual controlada.

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=61ed21593ff35709b7e93a69f876be0e5af0633b54003234fbc8ff398d24c897
verdict=APPROVED_WITH_REMARKS
tool=claude

result_path=.specs/prd-harness-portatil-vendor-neutral/1.0_execution_result.json

## Coverage

package=internal/fs,internal/upgrade
delta=+0.20%

## Suposições
- `check-skills-lock` roda `go run . skills --verify .` (mesmo mecanismo de `internal/skillscheck`, já testado unitariamente) em vez de exigir o binário `ai-spec` pré-compilado, mantendo paridade com o padrão já usado por outros alvos do Makefile que também usam `go run`/`go test` diretos.
- A subtarefa 1.1 (RF-20.1) exigia apenas incluir `./internal/parity/...` com `-tags=integration` no job `integration`; a divergência mais ampla de V-41 entre `Makefile` (5 pacotes) e `test.yml` (agora 4, antes 3) não foi resolvida integralmente nesta tarefa por estar fora do escopo textual da subtarefa 1.1 e do requisito RF-20.1, que menciona nominalmente apenas o pacote de paridade.
- `skills-lock.json` e `audit/skill-upgrade-lock-hash-resync-2026-09-18.md` aparecem no `git status` do repositório mas não foram alterados por esta execução — são artefato de uma resincronização de pré-condição feita pelo orquestrador `execute-all-tasks` (registrado no próprio arquivo de audit), anterior e independente desta tarefa.

## Riscos Residuais
- [LOW] `OSFileSystem.DirHash` retorna um erro genérico (`"%s is not a directory"`) para o caso "path existe mas não é diretório", sem sentinel error dedicado; o chamador em `upgrade.go` trata isso da mesma forma que qualquer erro não-`ErrNotExist` (fail-closed para `StatusRefsDivergent`), então não há lacuna de comportamento, apenas menor granularidade diagnóstica. Aceito conforme avaliação da 2ª rodada de revisão.
- [LOW] Débito de estilo pré-existente fora do escopo do diff: `internal/fs/fake.go` mantém uma mensagem de erro em PT-BR em `FileHash` (não tocada por esta tarefa), identificado pela 1ª rodada de revisão como fora de escopo.
- [selo pendente] `.specs/prd-harness-portatil-vendor-neutral/1.0_execution_result.json` foi produzido e validado fisicamente (`ai-spec validate-result execution ... --verify-physical`), mas o selo da Etapa 6 (`ai-spec seal-evidence`) exige commit prévio, que o harness não cria por si (R-GOV-001). Selo fica pendente até alguém commitar o trabalho desta tarefa e rodar `ai-spec seal-evidence .specs/prd-harness-portatil-vendor-neutral/1.0_execution_result.json --prd-dir .specs/prd-harness-portatil-vendor-neutral --commit <sha>`.
- `patch_sha256`/`final_state_sha256` de `1.0_execution_result.json` refletem a árvore de trabalho inteira no momento da captura (múltiplas tarefas do PRD podem estar em andamento sem commit intermediário nesta orquestração), não apenas o diff isolado da Tarefa 1.0; a seção "Arquivos Alterados" acima é a fonte de verdade do escopo real desta tarefa.
- V-41 (divergência de pacotes de integração entre `Makefile` e `test.yml`) permanece parcialmente aberta para os pacotes `internal/runtime/memory/durable` e `cmd/ai_spec_harness`, fora do escopo textual desta tarefa (ver Suposições).

## Conflitos de Regra
- none
