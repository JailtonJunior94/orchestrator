# Generated: 2026-09-18T19:45:52Z
<!-- evidence-contract: v2 -->
# Relatório de Execução de Tarefa

## Tarefa
- ID: 5.0
- Título: Distribuição de R-STYLE-001 e fonte única de lista install/uninstall
- Arquivo: .specs/prd-harness-portatil-vendor-neutral/task-5.0-distribuicao-de-r-style-001-e-fonte-unica-de-lista-install-u.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-harness-portatil-vendor-neutral/prd.md (Fatos Verificados V-01, V-35; RF-12, RF-13, RF-61)
- TechSpec: .specs/prd-harness-portatil-vendor-neutral/techspec.md (seção "P4 — Distribuição de R-STYLE-001" em Sequenciamento de Desenvolvimento; tabela "Rastreabilidade — RF × nível × arquivo", linhas RF-12/RF-61)
- Tasks: .specs/prd-harness-portatil-vendor-neutral/tasks.md (dependência 4.0 confirmada `done` antes de iniciar; 1.0/2.0/3.0/6.0/7.0/8.0/9.0/12.0 também `done`)
- Inventário (Tarefa 2.0): .specs/prd-harness-portatil-vendor-neutral/inventario-regras.md (item 2, R-STYLE-001: apenas itens 4 e 8 ficam explicitamente fora do escopo de RF-11/RF-13; item 2 é fechado por P3/P4)
- Relatório da Tarefa 4.0: .specs/prd-harness-portatil-vendor-neutral/4.0_execution_report.md (confirma que `.agents/policies/{governance,code-style}.md` já existem e espelham `.claude/rules/`; confirma explicitamente que `internal/embedded/assets/.claude/rules/` e os quatro pontos hardcoded ficaram para esta tarefa)
- Governança: `.agents/skills/agent-governance/SKILL.md`, `.agents/skills/go-implementation/SKILL.md` (diff é Go puro; nenhuma skill processual declarada na task file — "Nenhuma além das auto-carregadas")
- Código de referência lido antes de codar: `internal/install/install.go` (installClaude, CopyFile hardcoded), `internal/upgrade/upgrade.go` (regenerateAdapters, syncFileIfPresent), `internal/uninstall/uninstall.go` (legacyStaticRemoval, lista fixa), `internal/parity/parity.go` (invariante CL05, `SelfSatisfiedStubPath`, `selfSatisfiedInvariantStubPaths`, `Generate`), `internal/capability/capability.go` (consumidor de `SelfSatisfiedStubPath`), `internal/uninstall/roundtrip_test.go` (round-trip real via `fs.NewOSFileSystem()` e `internal/embedded` extraído para tempdir) — confirmado que `internal/install`, `internal/upgrade` e `internal/uninstall` já foram tocados pelas Tarefas 6.0/7.0/8.0/9.0 (camada transacional `tracking.Tracker`, `batchreport`, `manifest.FileChecksums`), lido o estado ATUAL antes de editar, sem assumir o estado pré-8.0

## Comandos Executados
- `diff .agents/policies/code-style.md .claude/rules/code-style.md` -> idêntico (pré-condição da Tarefa 4.0, confirmada antes de iniciar)
- `grep -rn "SelfSatisfiedStubPath" --include="*.go" .` -> localizados 3 arquivos (`internal/parity/parity.go`, `internal/parity/sanitization_test.go`, `internal/capability/capability.go`) para o refactor do campo (single path -> slice), necessário para CL05 cobrir os dois arquivos sem quebrar a bijeção 1:1 provada por `TestSelfSatisfiedInvariantsMatchInjectedStubs` (V-25)
- `cp .agents/policies/code-style.md internal/embedded/assets/.claude/rules/code-style.md` -> asset embarcado criado (fecha V-01; `//go:embed all:assets` em `internal/embedded/embedded.go` cobre o arquivo automaticamente)
- `go build ./...` -> pass (evidence/task-5.0-harness-portatil-vendor-neutral/build.log, vazio = sucesso)
- `go vet ./...` -> pass (evidence/task-5.0-harness-portatil-vendor-neutral/vet.log, vazio = sucesso)
- `go test ./...` -> pass, `Go test: 3972 passed in 84 packages` (evidence/task-5.0-harness-portatil-vendor-neutral/unit-tests.log) — baseline anterior era 3969 testes; +3 novos testes em `internal/skills/rules_test.go`
- `go test ./internal/parity/... -run TestParitySuite -v` -> pass, incluindo `TestParity_NewArtifacts_Claude_Absent/CL05` e `TestParity_NewArtifacts_Claude_Absent/CL05#01` (governance.md e code-style.md ausentes isoladamente, cada um falha CL05 isoladamente)
- `UPDATE_SNAPSHOTS=1 go test ./internal/capability/...` -> regenerou `testdata/capability-matrix.json` e `docs/capability-matrix.md` com a nova Description/Reason de CL05 (dois arquivos, `SelfSatisfiedStubPaths` unidos por `strings.Join`)
- `go test ./internal/capability/...` -> pass após regeneração, `Go test: 15 passed in 1 packages` (evidence/task-5.0-harness-portatil-vendor-neutral/capability-matrix-sync.log)
- `go test -tags=integration ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/runtime/memory/durable/... ./cmd/ai_spec_harness/...` -> pass, `Go test: 1219 passed in 7 packages` (evidence/task-5.0-harness-portatil-vendor-neutral/integration-tests.log), incluindo `TestSyncGatesFailWhenCentralArtifactIsDeleted/.agents/policies/code-style.md` e `TestSyncGatesAreGreenOnUntouchedClone`
- `go test ./internal/uninstall/... -run TestRoundtrip -v` -> pass, `Go test: 15 passed in 1 packages` — round-trip real (OS filesystem, extração de `internal/embedded/assets`) cobrindo os cinco cenários (claude/codex/copilot/opencode/all), árvore idêntica à original após instalar+desinstalar duas vezes
- `make lint` -> `0 issues.` (evidence/task-5.0-harness-portatil-vendor-neutral/lint.log)
- `bash scripts/check-policies-sync.sh` -> pass, `Policies em sync: 2`, `Drift / missing: 0` (evidence/task-5.0-harness-portatil-vendor-neutral/check-policies-sync.log)
- `bash scripts/check-skills-sync.sh` -> pass, inalterado (evidence/task-5.0-harness-portatil-vendor-neutral/check-skills-sync.log)
- `bash scripts/check-hooks-sync.sh` -> pass, inalterado (evidence/task-5.0-harness-portatil-vendor-neutral/check-hooks-sync.log)
- Auditoria dos 26 call sites de `.claude/rules/` (subtarefa 5.4): `grep -rn "\.claude/rules" --include="*.go" --include="*.md" --include="*.sh"` em todo o repositório, excluindo `.specs/*` (artefatos históricos de PRD, não editáveis nesta tarefa). Os quatro call sites Go que quebrariam teste (install/upgrade/uninstall/parity) foram corrigidos nesta tarefa. Nenhum call site remanescente em `AGENTS.md`, `CLAUDE.md`, `CODEX.md`, `COPILOT.md`, `.github/copilot-instructions.md`, `docs/*.md` afirma `.claude/rules/` como origem editável — todos usam linguagem de referência/carregamento ("ver", "carrega", "define"), nunca "editar aqui"; `.github/copilot-instructions.md:11` já foi corrigido por trabalho anterior (fora desta tarefa) de "duplicata de prosa divergente" para referência direta a `.claude/rules/code-style.md`
- Subagente `reviewer` invocado com prd.md + techspec.md como contexto -> veredito `APPROVED_WITH_REMARKS`, dois achados `[LOW]` (robustez de evidência de regeneração de golden file; ruído fora de escopo no patch consolidado), nenhum achado `[HIGH]`/`[CRITICAL]` — RF-33 permite encerramento
- `ai-spec validate-result execution .specs/prd-harness-portatil-vendor-neutral/5.0_execution_result.json --task-id 5.0` -> `OK: resultado SDD execution valido`
- `bash .agents/scripts/validate-task-evidence.sh .specs/prd-harness-portatil-vendor-neutral/5.0_execution_report.md` -> validação final do pacote de evidências

## Arquivos Alterados
- `internal/skills/rules.go` (novo) — `var UniversalRuleFiles = []string{"governance.md", "code-style.md"}`, fonte única compartilhada entre install e uninstall (RF-61)
- `internal/skills/rules_test.go` (novo) — testes unitários da lista (conteúdo esperado, ausência de duplicatas)
- `internal/embedded/assets/.claude/rules/code-style.md` (novo) — cópia byte-a-byte de `.agents/policies/code-style.md`; fecha V-01 (asset embarcado incompleto)
- `internal/install/install.go` (editado) — `installClaude` itera `skills.UniversalRuleFiles` para copiar cada regra em vez do `CopyFile` hardcoded só para `governance.md` (ponto hardcoded 1/4); dry-run também iterado
- `internal/install/install_test.go` (editado) — fixture e asserção para `code-style.md` em `TestInstall_CopyMode` (RF-12, teste escrito cobrindo o comportamento antes de a duplicata ser removida, conforme ordem interna de RF-13)
- `internal/upgrade/upgrade.go` (editado) — `regenerateAdapters` itera `skills.UniversalRuleFiles` em `syncFileIfPresent` em vez do par hardcoded só para `governance.md` (ponto hardcoded 2/4)
- `internal/upgrade/upgrade_test.go` (editado) — fixture e asserção para `code-style.md` sincronizado em `TestUpgrade_RegeneratesAdaptersAndSupportFiles`
- `internal/uninstall/uninstall.go` (editado) — `legacyStaticRemoval` itera `skills.UniversalRuleFiles` em vez da lista fixa de um único arquivo (ponto hardcoded 3/4, V-35); import de `internal/skills` adicionado
- `internal/uninstall/uninstall_test.go` (editado) — fixture e asserção de que ambos os arquivos são removidos e `.claude/rules` é podado (prune) quando ambos desaparecem
- `internal/parity/parity.go` (editado) — invariante CL05 (`invCL05ClaudeRulesGovernancePresent`) agora verifica os dois arquivos de `skills.UniversalRuleFiles` (ponto hardcoded 4/4); campo `Invariant.SelfSatisfiedStubPath string` generalizado para `SelfSatisfiedStubPaths []string` (permite múltiplos paths de stub por invariante, necessário para CL05 cobrir dois arquivos sem quebrar a bijeção 1:1 entre paths injetados e invariantes marcados); `selfSatisfiedInvariantStubPaths` e `Generate` atualizados para injetar `code-style.md` como stub; nova função `universalRuleStubPaths()`
- `internal/parity/parity_test.go` (editado) — nova asserção de ausência isolada de `code-style.md` em `TestParity_NewArtifacts_Claude_Absent`
- `internal/parity/sanitization_test.go` (editado) — `TestSelfSatisfiedInvariantsMatchInjectedStubs` adaptado ao campo slice, iterando todos os paths marcados por cada invariante
- `internal/capability/capability.go` (editado) — `buildCell` usa `strings.Join(inv.SelfSatisfiedStubPaths, ", ")` no lugar do path único no `Reason` da célula
- `testdata/capability-matrix.json`, `docs/capability-matrix.md` (regenerados via `UPDATE_SNAPSHOTS=1 go test ./internal/capability/...`) — refletem a nova Description/Reason de CL05
- `.specs/prd-harness-portatil-vendor-neutral/tasks.md` (editado) — linha da Tarefa 5.0 marcada `done`
- `.specs/prd-harness-portatil-vendor-neutral/5.0_execution_result.json` (criado)
- `evidence/task-5.0-harness-portatil-vendor-neutral/build.log`, `vet.log`, `unit-tests.log`, `integration-tests.log`, `lint.log`, `check-policies-sync.log`, `check-skills-sync.log`, `check-hooks-sync.log`, `capability-matrix-sync.log` (criados)
- `evidence/task-5.0-harness-portatil-vendor-neutral/5.0.patch` (criado)

## Resultados de Validação
- Testes: pass (`go test ./...` — 3972 testes, 84 pacotes, todos `ok`; `go test -tags=integration ./internal/integration/... ./internal/skills/... ./tests/integration/... ./internal/runtime/memory/durable/... ./cmd/ai_spec_harness/...` — 1219 testes, 7 pacotes, todos `ok`; `go test ./internal/uninstall/... -run TestRoundtrip -v` — 15 subtestes, round-trip real via OS filesystem)
- Lint: pass (`make lint` — 0 achados; `go vet ./...` — 0 achados)
- Veredito do Revisor: APPROVED_WITH_REMARKS (dois achados `[LOW]`, nenhum achado `[HIGH]`/`[CRITICAL]` — RF-33 permite encerramento; achados registrados em Riscos Residuais, nunca apagados)

## Critérios de Aceite
- Instalação limpa entrega `governance.md` E `code-style.md`, verificado em install e em upgrade -> comprovado: `internal/install/install_test.go::TestInstall_CopyMode` (asserções `/project/.claude/rules/governance.md` e `/project/.claude/rules/code-style.md`) e `internal/upgrade/upgrade_test.go::TestUpgrade_RegeneratesAdaptersAndSupportFiles` (asserção de conteúdo sincronizado de ambos os arquivos), ambos `ok` em `unit-tests.log`; round-trip real (`internal/uninstall/roundtrip_test.go`) confirma que o arquivo instalado vem do asset embarcado real, não de fixture
- Round-trip instala/desinstala deixa a árvore limpa, sem arquivo órfão e sem impedir o prune do diretório -> comprovado: `internal/uninstall/uninstall_test.go::TestUninstall_RemovesSkills` assevera explicitamente ausência de `governance.md` e `code-style.md` e que `.claude/rules` deixa de ser diretório (prune, V-35 fechado); `internal/uninstall/roundtrip_test.go::TestRoundtrip_InstallUninstall_LeavesOnlyUserFiles` (OS filesystem real, 5 combinações de ferramentas) confirma árvore idêntica à original após instalar+desinstalar (2x, incluindo idempotência)
- CL05 cobre as duas regras -> comprovado: `internal/parity/parity.go:533-547` (`invCL05ClaudeRulesGovernancePresent.Check` itera `skills.UniversalRuleFiles`); `internal/parity/parity_test.go` cobre presença (`TestParity_NewArtifacts_Claude_Present`) e ausência isolada de cada um dos dois arquivos (`TestParity_NewArtifacts_Claude_Absent/CL05` e `/CL05#01`); `testdata/capability-matrix.json`/`docs/capability-matrix.md` regenerados confirmam a Description dupla e o Reason com os dois paths
- Nenhum call site aponta para `.claude/rules/` como origem editável -> comprovado: auditoria por `grep` de todos os call sites de `.claude/rules/` fora de `.specs/*` (histórico de PRD, não tocável); nenhum usa linguagem de "origem editável" — todos são referência de carregamento/consulta; os quatro pontos Go que quebrariam teste (install, upgrade, uninstall, parity) foram corrigidos nesta tarefa e cobertos por teste

## Definition of Done (DoD)
- [x] Todos os critérios de aceite acima comprovados com evidência física.
- [x] Testes da tarefa criados e executados (`Testes: pass` com comando correspondente em Comandos Executados).
- [x] Lint/vet/build sem regressão.
- [x] Estado de tasks.md sincronizado com este relatório.

## Diff Reviewed

sha=818b9969125b77ddd7bd0406cc5741ab1826e04a86dca6fcc6ed03ec3a6a2b8a
verdict=APPROVED_WITH_REMARKS
tool=claude

## Coverage

package=internal/parity,internal/install,internal/upgrade,internal/uninstall,internal/skills,internal/capability
delta=+0.0%

Nota: nenhum pacote novo foi criado (exceto `internal/skills/rules.go`, coberto por `internal/skills/rules_test.go`,
pacote já medido por `make coverage`). Sem regressão observável em `go test ./...` (3972 testes, todos `ok`) nem em
`make lint`/`go vet`.

result_path=.specs/prd-harness-portatil-vendor-neutral/5.0_execution_result.json

## Suposições
- Interpretei a subtarefa 5.4 ("Atualizar os 26 call sites de `.claude/rules/`, começando pelos Go que quebram teste") como: (1) corrigir os quatro pontos Go hardcoded que de fato quebrariam round-trip/teste se `code-style.md` fosse distribuído sem eles — feito; (2) auditar os call sites de prosa restantes contra o critério de sucesso "nenhum aponta para `.claude/rules/` como origem editável" — nenhum viola, todos são referência de carregamento (`AGENTS.md`, `CLAUDE.md`, `CODEX.md`, `COPILOT.md`, `docs/*.md`). Não editei `.specs/prd-harness-portatil-vendor-neutral/*.md` (prd.md, techspec.md, ADR-002, inventário, task file) nem execution reports de outras tarefas/PRDs — são artefatos de planejamento/evidência histórica, fora do escopo de uma tarefa de implementação; reescrevê-los seria alterar o plano em vez de executá-lo. `.github/copilot-instructions.md:11` já havia sido corrigido por trabalho de uma tarefa anterior (fora do diff desta tarefa) de "duplicata de prosa divergente" para referência direta — confirmado por leitura, não por edição minha.
- `SelfSatisfiedStubPath string` foi generalizado para `SelfSatisfiedStubPaths []string` em vez de manter CL05 com um único path "representativo". Optei pelo slice porque a alternativa (manter singular e criar um invariante CL09 novo só para `code-style.md`) contradiria literalmente o critério de sucesso "CL05 cobre as duas regras" e fragmentaria a evidência de uma regra `universal` única em dois IDs de invariante. O refactor tem escopo contido (3 arquivos de produção: `parity.go`, `capability.go`, e os testes do próprio pacote `parity`) e é coberto pelo teste de bijeção já existente (`TestSelfSatisfiedInvariantsMatchInjectedStubs`), sem alterar seu comportamento de guarda (V-25).
- `internal/embedded/assets/.claude/rules/` não tinha gate de sincronia dedicado antes desta tarefa (a lacuna que causou V-01: nenhum script cobre esse mirror). Optei por não estender `scripts/check-policies-sync.sh` para incluir esse terceiro braço nesta tarefa: o traço de rastreabilidade da techspec associa RF-12 ao teste unitário `internal/install/install_test.go`, não a um gate shell novo, e a Tarefa 4.0 já registrou (nos seus próprios Riscos Residuais) que estender esse par para um terceiro alvo não fazia parte do seu escopo. A cobertura de regressão para este ponto específico fica em `internal/install/install_test.go::TestInstall_CopyMode` (fixture explícita de `code-style.md`) e no round-trip real de `internal/uninstall/roundtrip_test.go`, que exercita a extração real do embedded asset. Registrado aqui como decisão, não como lacuna oculta.
- Reaproveitei o diretório de evidência `evidence/task-5.0-harness-portatil-vendor-neutral/` (em vez de `evidence/task-5.0/`, que já existe no repositório mas pertence à Tarefa 5.0 de um PRD diferente — `prd-harness-quatro-clis-loop-aprovacao`) para não colidir com evidência de outra entrega já commitada.
- `base_sha`/`patch_sha256`/`final_state_sha256` de `5.0_execution_result.json`: capturados via chamada direta a `internal/taskloop.Orchestrator.captureFinalSnapshot` a partir de um teste Go temporário (`internal/taskloop/zzdebug_test.go`, criado e removido em seguida — mesmo padrão das Tarefas 2.0/3.0/4.0/7.0), usando a própria função de produção em vez de reimplementá-la, para que `bash .agents/scripts/validate-task-evidence.sh` (que invoca `ValidateExecutionEvidence` e recompõe o snapshot canônico da árvore de trabalho) bata byte a byte com o valor registrado. Como o PRD inteiro está em execução sem commits intermediários, `patch_sha256`/`final_state_sha256` refletem a árvore de trabalho inteira no momento da captura (múltiplas tarefas em andamento), não apenas o diff isolado da Tarefa 5.0 — mesmo precedente das Tarefas 4.0/9.0; a seção "Arquivos Alterados" acima é a fonte de verdade do escopo real desta tarefa. `go build ./...`/`go vet ./...` reconfirmados limpos após remover o teste temporário. Nenhum destes valores é verificado fisicamente via `ai-spec validate-result ... --verify-physical` nesta rodada (explicitamente não solicitado pelo orquestrador — Etapa 6 fica deferida até haver commit humano, R-GOV-001); o gate local `validate-task-evidence.sh`, que recomputa e compara o mesmo snapshot, foi executado e passou (é gate mandatório da Etapa 5 da skill, distinto do `--verify-physical` do `ai-spec` CLI).
- Mesmo precedente das tarefas 2.0/3.0/4.0: `tasks.md` é marcado `done` antes de `validate-task-evidence.sh` ser a última chamada, porque o próprio validador compara `Estado:` deste relatório contra a coluna Status de `tasks.md` e falha em caso de divergência.

## Riscos Residuais
- **[LOW]** (achado do revisor) `evidence/.../capability-matrix-sync.log` mostra apenas o resultado do gate pós-regeneração (`go test ./internal/capability/...`, cached), sem capturar separadamente a saída do passo `UPDATE_SNAPSHOTS=1`. Mitigado nesta revisão por conferência manual do revisor de que `testdata/capability-matrix.json`/`docs/capability-matrix.md` refletem corretamente CL05; para próximas rodadas, registrar também `git diff --stat` desses dois arquivos como prova complementar.
- **[LOW]** (achado do revisor) O patch de evidência consolidado (`5.0.patch`) foi gerado com `git diff -- <paths>` escopado aos arquivos desta tarefa (não ao diff integral da árvore, que inclui ruído de outras tarefas paralelas já `done`) — decisão tomada precisamente para atender ao `fix_hint` do revisor durante a própria revisão.
- `_upgradePlanningSkills` em `internal/upgrade/upgrade.go` continua com prefixo `_` (viola R-STYLE-001.3); linha pré-existente, não tocada por esta tarefa — débito técnico já registrado, fora do escopo de correção aqui (renomear ao tocar, não antes).
- Selo pendente: `5.0_execution_result.json` foi produzido e validado via `ai-spec validate-result execution ... --task-id 5.0` (sem `--verify-physical`, por instrução explícita do orquestrador). O selo da Etapa 6 (`ai-spec seal-evidence`) exige commit prévio, que o harness não cria por si (R-GOV-001); fica pendente até alguém commitar o trabalho desta tarefa e rodar `ai-spec seal-evidence .specs/prd-harness-portatil-vendor-neutral/5.0_execution_result.json --prd-dir .specs/prd-harness-portatil-vendor-neutral --commit <sha>`.
- `internal/embedded/assets/.claude/rules/` não tem gate de sincronia dedicado (ver Suposições) — se um terceiro arquivo de policy for adicionado no futuro sem tocar esse mirror, nenhum script falha automaticamente; hoje mitigado apenas por teste unitário (`install_test.go`) e round-trip real. Risco a lembrar quando uma terceira policy universal for introduzida.

## Conflitos de Regra
- none
