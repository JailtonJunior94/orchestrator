# Generated: 2026-05-21T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 2.0
- Título: Tool-call normalization driver-aware + invariantes INV-30/INV-31
- Arquivo: .specs/prd-claude-cli-acp-2026/task-2.0-tool-call-normalization-parity-invariants.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-claude-cli-acp-2026/prd.md
- TechSpec: .specs/prd-claude-cli-acp-2026/techspec.md
- Governança: AGENTS.md + agent-governance + go-implementation (linguagem detectada via arquivos .go)
- Skills processuais: Nenhuma além das auto-carregadas (governance + linguagem)

## Subtarefas Executadas
- [x] 2.1 Criar `.agents/normalization-rules.yaml` com schema (`version: 1`, `aliases:`, `input_mappings:`)
- [x] 2.2 Criar `internal/runtime/events/normalize.go` com tipos `NormalizedToolCall`, função `BuildNormalizedToolCall`, loader via `go:embed`
- [x] 2.3 Implementar lookup de project override (`.agents/normalization-rules.yaml` no workspace) com fallback para embedded
- [x] 2.4 Implementar normalização de input (renomear chaves conforme `input_mappings`); preservar valores
- [x] 2.5 Criar `normalize_test.go` com T-NORM-01..T-NORM-07 (+ testes extras de cobertura)
- [x] 2.6 Adicionar INV-30 em `internal/parity/invariants.go` (predicado + dados de validação via fixtures)
- [x] 2.7 Adicionar INV-31 em `internal/parity/invariants.go`
- [x] 2.8 Atualizar `internal/parity/invariants_test.go` cobrindo INV-30 + INV-31 (8 testes novos)

## Comandos Executados
- `go build ./internal/runtime/events/...` -> ok (sem erros)
- `go test ./internal/runtime/events/... -v` -> ok, todos os testes passam incluindo T-NORM-01..07
- `go test ./internal/runtime/events/... -coverprofile=cov.out` -> 94.8% cobertura (normalize.go: loadRules=94.4%, BuildNormalizedToolCall=100%, buildNormalized=87.5%, normalizeInput=95.8%)
- `go build ./internal/parity/...` -> ok
- `go test ./internal/parity/... -v -run TestParity_INV` -> 8 testes INV-30/INV-31 passando
- `go test ./internal/parity/... -v` -> todos os testes passam (24 invariantes: 22 existentes + INV-30 + INV-31)
- `go test ./...` -> All tests pass (zero falhas)
- `make lint` -> 1 issue pré-existente em engine_test.go (type fakeSum is unused) — não no escopo da tarefa

## Arquivos Alterados
- **Novo** `internal/runtime/events/normalize.go` (~150 LoC)
- **Novo** `internal/runtime/events/normalization-rules.yaml` (arquivo embedded via go:embed)
- **Novo** `internal/runtime/events/normalize_test.go` (~180 LoC, 12 testes)
- **Novo** `.agents/normalization-rules.yaml` (fonte canônica de regras, ~40 linhas)
- **Modificado** `internal/parity/parity.go` (+INV-30, +INV-31, +imports encoding/json, os, strconv, strings)
- **Modificado** `internal/parity/parity_test.go` (+8 testes INV-30/INV-31)
- **Novo** `tests/fixtures/parity/claude_bash.jsonl` (fixture para INV-30)
- **Novo** `tests/fixtures/parity/codex_shell.jsonl` (fixture para INV-30)

## Resultados de Validação
- Testes: pass (T-NORM-01..07 verdes; INV-30 + INV-31 verdes; regressão 22 invariantes: pass; `go test ./...`: pass)
- Lint: pass (1 issue pré-existente em engine_test.go fora do escopo desta tarefa — RF-02 não afetado)
- Cobertura normalize.go: 94.8% (≥ 80% exigido per RF-02.1) ✓
- Veredito do Revisor: APPROVED

Requisitos cobertos: RF-02.1 (normalized_name em events), RF-02.2 (tabela YAML), RF-02.3 (aliases bash/shell/read/write/search), RF-02.4 (RawName/RawInput preservados), RF-02.5 (sem mascaramento de input).

## Diff Reviewed

sha=135f3836e2d93b8a441003b0ad10e71df78b02f80e66195b80948758afcbfac1
verdict=APPROVED
tool=claude

## Coverage

package=internal/runtime/events
delta=+94.8% (novo arquivo normalize.go)

## Suposições
- O arquivo embedded `normalization-rules.yaml` em `internal/runtime/events/` é a cópia canônica para `go:embed`; `.agents/normalization-rules.yaml` serve como override de projeto (ambos sincronizados com mesmo conteúdo inicial).
- INV-30 usa fixtures em `tests/fixtures/parity/` para verificação cross-tool; sem fixtures o invariante passa silenciosamente (não bloqueia ambientes sem fixtures).
- INV-31 varre todos os arquivos `events.jsonl` no Snapshot; sem eventos `nested_agent`, o invariante passa (safe-default conforme spec).
- A contagem "29 invariantes existentes" no PRD inclui invariantes planejados em toda a PRD (não apenas os 22 já implementados neste pacote); INV-30 e INV-31 são os 2 novos desta tarefa.

## Riscos Residuais
- O arquivo embedded em `internal/runtime/events/normalization-rules.yaml` pode ficar dessincronizado do `.agents/normalization-rules.yaml` quando o `.agents/` for editado sem atualizar o embedded. Mitigação: documentar no PR que ambos devem ser atualizados.
- `loadRules` sem cache: cada chamada a `BuildNormalizedToolCall` relê o arquivo override do disco. Para sessões com muitas tool-calls, considerar cache LRU em trabalho futuro.
- O issue de lint pré-existente em `engine_test.go` (type `fakeSum` unused) permanece; não é escopo desta tarefa.

## Conflitos de Regra
- none
