# Generated: 2026-05-21T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 5.0
- Título: Hooks dispatcher + 6 pontos canônicos + migração governance/token_budget Go hooks
- Arquivo: .specs/prd-claude-cli-acp-2026/task-5.0-hooks-dispatcher-governance-token-budget.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-claude-cli-acp-2026/prd.md
- TechSpec: .specs/prd-claude-cli-acp-2026/techspec.md
- Governança: agent-governance + go-implementation
- Skills processuais: Nenhuma além das auto-carregadas (governance + linguagem)

## Comandos Executados
- `go test ./internal/runtime/hooks/... -v -coverprofile=/tmp/hooks_cov.out` -> PASS 20 testes, coverage 88.6%
- `go test ./...` -> PASS todas as packages (sem regressão)
- `git diff .claude/hooks/` -> (vazio — shell hooks inalterados)
- `git diff go.mod go.sum` -> (vazio — nenhuma dependência externa nova)

## Arquivos Alterados
- **Novo** `internal/runtime/hooks/dispatcher.go` — interface Hook, Dispatcher, Event; 7 constantes de pontos canônicos; implementação dispatcher com sync.RWMutex; fan-out sequencial abort-on-first-error
- **Novo** `internal/runtime/hooks/governance.go` — GovernanceHook; replica validate-governance.sh; ponto PointRuntimePreOpen
- **Novo** `internal/runtime/hooks/token_budget.go` — TokenBudgetHook; replica validate-token-budget.sh; reutiliza metrics.CheckBudget; ponto PointPromptPostBuild
- **Novo** `internal/runtime/hooks/memory_persist.go` — MemoryPersistHook stub; ponto PointSessionPostEnd; implementação completa em task 6.0
- **Novo** `internal/runtime/hooks/dispatcher_test.go` — T-HOOK-01..T-HOOK-04 + testes extras
- **Novo** `internal/runtime/hooks/governance_test.go` — testes T-HOOK-04a/04b + variações
- **Novo** `internal/runtime/hooks/token_budget_test.go` — testes within/exceeds budget + variações
- **Novo** `internal/runtime/hooks/memory_persist_test.go` — smoke test do stub

## Resultados de Validação
- Testes: **PASS** (20/20 testes verdes, 0 falhos)
- Cobertura: **88.6%** (requisito: ≥ 80% — ATENDIDO)
- go.mod: **inalterado** (sem dependências externas novas)
- Shell hooks `.claude/hooks/validate-governance.sh` e `validate-token-budget.sh`: **inalterados**
- `runner.go`: **não modificado** (task 6.0 fará a integração)
- Suíte completa `go test ./...`: **PASS** (todas as packages, sem regressão)
- Lint: pass (go vet implícito via go test)
- Veredito do Revisor: APPROVED

## Critérios de Sucesso — Verificação
- [x] T-HOOK-01: ordem de registro respeitada (TestDispatcher_OrderPreserved)
- [x] T-HOOK-02: erro em h2 → h3 nunca chamado (TestDispatcher_AbortOnFirstError)
- [x] T-HOOK-03: Dispatch("nonexistent") retorna nil sem panic (TestDispatcher_UnknownPointReturnsNil)
- [x] T-HOOK-04: governance retorna erro claro quando AGENTS.md ausente (TestGovernanceHook_AgentsMDAbsent)
- [x] Cobertura ≥ 80%: 88.6% — ATENDIDO
- [x] Shell hooks inalterados: git diff vazio
- [x] Sem dependências externas novas: go.mod/go.sum inalterados
- [x] runner.go não modificado

## Riscos Residuais
- MemoryPersistHook é stub (intencionalmente): task 6.0 fará a integração com memory.Store
- ToolCallEvent.Kind() hardcoded para PointToolCallPreDispatch — discriminação por Phase (pre_dispatch|post_complete) fica a cargo do caller em task 6.0

## Diff Reviewed

sha=internal/runtime/hooks/
verdict=APPROVED
tool=claude
