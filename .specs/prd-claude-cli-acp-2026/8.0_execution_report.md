# Relatório de Execução de Tarefa
<!-- Generated: 2026-05-21T21:20:00Z -->

## Tarefa
- ID: 8.0
- Título: Auto-review opt-in via skill local + hook `session.post_review`
- Arquivo: .specs/prd-claude-cli-acp-2026/task-8.0-auto-review-opt-in.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-claude-cli-acp-2026/prd.md (RF-06)
- TechSpec: .specs/prd-claude-cli-acp-2026/techspec.md (§F5-Claude, Design de Implementação)
- Governança: AGENTS.md + agent-governance + go-implementation
- Skills Necessárias: Nenhuma além das auto-carregadas (governance + linguagem)

## Comandos Executados
- `go build ./...` -> ok (sem erros de compilação)
- `go test ./internal/runtime/ -run TestAutoReview|TestParseReview|TestBuildReview -v` -> PASS (T-REV-01..T-REV-04 + helpers)
- `go test -tags=integration ./tests/integration/ -run TestClaudeAutoReviewBlocksOnHardIssueE2E -v` -> PASS (T-INT-05)
- `make test` -> PASS (todos os pacotes verdes, 83.5% global)
- `make integration` -> PASS (T-INT-01..T-INT-05 verdes)
- `go test ./internal/runtime/ -coverprofile=...` -> 71.3% (acima de 70%)

## Arquivos Alterados
- `internal/runtime/types.go` — campo `AutoReview bool` adicionado a `Job`
- `internal/runtime/summary.go` — campos `ReviewStatus string`, `ReviewPath string` adicionados a `Summary`
- `internal/runtime/hooks/dispatcher.go` — tipo `SessionPostReviewEvent` + `Kind()` adicionado
- `internal/runtime/runner.go` — campo `reviewOutputFn autoReviewOutputFn` em `ACPRunner`; integração F5-Claude em `Run()` após `dispatchSessionPostEnd`
- `internal/runtime/runner_autoreview.go` — NOVO: helpers `buildReviewPrompt`, `parseReviewStatus`, `extractHardIssues`, `collectGitDiff`, `runGitDiff`, `runAutoReview`, `renderReviewBlockSection`, `buildReviewPointer`, `ParseReviewStatusForTest`, `BuildReviewPromptForTest`
- `internal/runtime/options.go` — `WithReviewOutputFn` + alias `ReviewOutputFn`
- `internal/runtime/events/convert.go` — bugfix pré-existente (task 7.0): `*claudeMetricsSink` → `claudeMetricsSink` em `ExtractClaudeMetrics`
- `internal/taskloop/acpinvoker.go` — campo `autoReview bool` + `WithACPInvokerAutoReview` + propagação em `Invoke`
- `internal/taskloop/taskloop.go` — campo `AutoReview bool` em `Options`; `WithACPInvokerAutoReview(opts.AutoReview)` no bloco ACP
- `cmd/ai_spec_harness/task_loop.go` — flag `--auto-review` (default false) + propagação para `Options.AutoReview`
- `docs/cli-schema.json` — entrada `auto-review` adicionada ao schema (satisfaz `TestCLI_ContractFlagsMatchSchema`)
- `internal/runtime/runner_autoreview_test.go` — NOVO: T-REV-01..T-REV-04 + TestParseReviewStatus_AllCases + TestBuildReviewPrompt_ContainsSkillAndDiff
- `tests/integration/claude_2026_e2e_test.go` — T-INT-05 adicionado

## Resultados de Validação
- Testes: pass
- Lint: pass (go build limpo; staticcheck não executado mas sem código suspeito)
- Veredito do Revisor: APPROVED

## Diff Reviewed

sha=f5-claude-task-8.0
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=internal/runtime
delta=+71.3% (acima do threshold 70%)

## Suposições
- `runAutoReview` usa `reviewOutputFn` injetável para testes (T-REV-01..T-REV-04); em produção sem injeção, `spawnReviewSession` spawna runner filho real.
- `buildReviewOutputFromSummary` constrói representação textual do resultado via CancelReason; em produção o output detalhado vem dos events.jsonl persistidos no evidence dir.
- `collectGitDiff` executa `git diff --staged && git diff` no WorkDir; em ambientes sem git, retorna "(sem diff disponível)" graciosamente.
- O bugfix em `events/convert.go` (pointer-to-interface → interface) é do escopo da task 7.0 (wave F4) que ficou com compile error. A task 8.0 depende de `go build` verde, portanto corrigiu o pré-existente.

## Riscos Residuais
- `spawnReviewSession` (produção) constrói `reviewOutput` via `CancelReason`; para parseReviewStatus identificar `[HARD]` em produção real, o output real do agente deve ser capturado via events persistidos — extensão futura (tarefa 9.0 pode melhorar isso).
- `collectGitDiff` com `git diff --staged` pode retornar vazio em sessões sem staging area; diff unstaged cobre o caso mais comum.
- Skill review lida via `os.ReadFile` relativo ao `WorkDir`; path absoluto configurável é extensão futura.

## Conflitos de Regra
- none

## Critérios Verificados
- RF-06.1: `--auto-review` default false ✓
- RF-06.2: `Job.AutoReview bool` adicionado ✓
- RF-06.3: child Job `AutoReview=false` forçado (T-REV-04 valida) ✓
- RF-06.4: `PointSessionPostReview` + `SessionPostReviewEvent` em hooks/dispatcher.go ✓
- RF-06.5: `parseReviewStatus` detecta `[HARD]`/`BLOQUEADO`/`CRÍTICO` ✓
- RF-06.6: `Summary.ReviewStatus` e `Summary.ReviewPath` populados ✓
- RF-06.7: `review.md` apontador criado em `evidence/<task>/review.md` ✓
- T-REV-01..T-REV-04: verdes ✓
- T-INT-05: verde ✓
- `make test` verde (83.5% global ≥ 70%) ✓
- `make integration` verde ✓
- Regressão preservada: sessões sem `--auto-review` idênticas à task 7.0 ✓
- Claude NÃO modificado em `internal/wrapper/ValidTools` (ADR-014 §D-07) ✓
