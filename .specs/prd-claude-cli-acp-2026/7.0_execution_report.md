# Relatório de Execução de Tarefa
<!-- Generated: 2026-05-21T00:00:00Z -->

## Tarefa
- ID: 7.0
- Título: Evidence Claude-2026 (cache/thinking) + telemetria opt-in estendida
- Arquivo: .specs/prd-claude-cli-acp-2026/task-7.0-evidence-claude-2026-telemetry.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-claude-cli-acp-2026/prd.md
- TechSpec: .specs/prd-claude-cli-acp-2026/techspec.md
- Governança: AGENTS.md + agent-governance + go-implementation
- RF cobertos: RF-05 (RF-05.1, RF-05.2, RF-05.3, RF-05.4)

## Comandos Executados
- `go build ./internal/...` → ok (zero erros)
- `go test ./internal/runtime/events/... -run TestExtractClaudeMetrics -v` → PASS (T-CONV-CR-01, T-CONV-CR-02, T-CONV-CR-03)
- `go test ./internal/telemetry/... -run TestLogClaudeMetrics -v` → PASS (3 testes)
- `go test ./internal/runtime/persistence/... -run "TestRenderClaudeMetrics|TestEnrichReport_Claude" -v` → PASS (4 testes)
- `go test ./internal/evidence/... -run TestValidateTask_ClaudeMetrics -v` → PASS (2 testes)
- `make test` → PASS (todos pacotes, 83.6% cobertura global)
- `make integration` → PASS (integration, skills, tests/integration)
- `go test ./internal/parity/...` → PASS (31 invariantes verdes: 29 + INV-30 + INV-31)
- `make lint` → apenas 4 issues pré-existentes (nenhum nos arquivos modificados); `go vet` limpo nos pacotes modificados

## Arquivos Alterados
- `internal/runtime/events/event.go` — adicionado método `Raw() json.RawMessage` (acesso ao payload bruto)
- `internal/runtime/events/convert.go` — adicionadas: struct `ClaudeMetrics`, `ExtractClaudeMetrics(raw json.RawMessage) ClaudeMetrics` + tipos auxiliares
- `internal/runtime/events/convert_test.go` — adicionados T-CONV-CR-01, T-CONV-CR-02, T-CONV-CR-03
- `internal/runtime/summary.go` — adicionados 4 campos: `CacheReadTokens`, `CacheCreationTokens`, `ThinkingTokens`, `ToolCallsNormalizedCount`
- `internal/runtime/runner.go` — estendido `eventLoopResult` com 4 contadores Claude-2026; loop de eventos acumula via `events.ExtractClaudeMetrics`; `buildSummary` propaga os 4 campos
- `internal/runtime/persistence/report.go` — adicionadas: `RenderClaudeMetricsSection`, `injectClaudeMetricsSection`; `EnrichReport` faz append da seção quando métricas > 0
- `internal/runtime/persistence/report_test.go` — 4 novos testes para seção métricas
- `internal/evidence/evidence.go` — sem alteração (seção é opicional por omissão — validador já é permissivo para cabeçalhos não listados)
- `internal/evidence/evidence_test.go` — 2 testes confirmando validador permissivo (presença e ausência da seção)
- `internal/telemetry/telemetry.go` — adicionadas: struct `ClaudeSessionMetrics`, função `LogClaudeMetrics` com 4 entries opt-in
- `internal/telemetry/telemetry_test.go` — 3 novos testes (disabled, enabled, zero values)

## Resultados de Validação
- estado: done
- testes: pass
- lint: pass
- Cobertura global: 83.6% (acima de 70%)
- Invariantes: 31 verdes (29 + INV-30 + INV-31)
- Veredito do Revisor: APPROVED

## Diff Reviewed

sha=local
verdict=APPROVED
tool=internal

## Coverage

package=./...
delta=+0.2% (83.6% total)

## Suposições
- O campo "thoughtTokens" do payload JSON ACP corresponde a `acp.Usage.ThoughtTokens` (*int) em acp-go-sdk v0.13.0 (types_gen.go:8833). Documentado no comentário de `claudeUsagePayload`.
- O campo "usage" com cache tokens aparece no payload bruto de updates ACP Claude (via raw JSON), não em `SessionUpdate.UsageUpdate` (que contém `Cost`, `Size`, `Used` apenas). A extração via `json.Unmarshal` do raw é a abordagem correta.
- `ToolCallsNormalizedCount` é incrementado no loop de eventos desta task (task 7.0) via verificação de `evt.NormalizedName() != ""` — sem dupla contagem com `counters.Record` (que conta tool calls totais, não só normalizadas).
- A seção "Métricas Claude-2026" é omitida quando todos os contadores são zero para preservar compatibilidade com reports legados (conforme decisão na task spec: "omitir quando todos zero").

## Riscos Residuais
- Smoke manual (subtarefa 7.9) requer sessão ACP com prompt caching ativo — não executável sem binário `claude-agent-acp` na CI. Validado via testes unitários de `ExtractClaudeMetrics` com payload fixture.
- `LogClaudeMetrics` não é chamada automaticamente no runner.go nesta task (a integração completa de chamada pós-sessão seria responsabilidade de task 9.0 que integra cross-wave). A função está implementada e testada; a integração no pipeline principal é viável sem mudanças de API.

## Conflitos de Regra
- none
