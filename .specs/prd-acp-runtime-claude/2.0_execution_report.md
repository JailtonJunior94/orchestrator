# Generated: 2026-05-20T00:00:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 2.0
- Título: Domain Specs (Claude) + Sync Script
- Arquivo: .specs/prd-acp-runtime-claude/task-2.0-domain-specs-claude.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-acp-runtime-claude/prd.md
- TechSpec: .specs/prd-acp-runtime-claude/techspec.md
- ADR: .specs/adr/009-acp-protocol-adoption.md, .specs/prd-acp-runtime-claude/adr-010-event-tagged-union.md
- Governança: AGENTS.md, agent-governance (implícito), go-implementation (inferido por diff Go)
- Skills Necessárias: Nenhuma além das auto-carregadas (governance + linguagem)

## Comandos Executados
- `go test ./internal/runtime/specs/... -count=1 -race -cover -v` -> PASS, coverage: 100.0% of statements
- `golangci-lint run ./internal/runtime/specs/...` -> 0 issues
- `shellcheck scripts/sync-acp-sdk-version.sh` -> sem warnings (exit 0)
- `bash scripts/sync-acp-sdk-version.sh` (sem acp-go-sdk em go.mod) -> exit 1 (comportamento correto per spec — pacote ausente = erro)
- `GOMOD_PATH=<tmp> CLAUDE_GO_PATH=<tmp> bash scripts/sync-acp-sdk-version.sh` (go.mod com v0.1.5) -> atualiza ClaudeSDKVersion de v0.0.0 para v0.1.5, exit 0 (idempotência confirmada)
- `grep coder/acp-go-sdk internal/runtime/specs/*.go` -> apenas comentário, sem import real (OK)

## Arquivos Alterados
- `internal/runtime/specs/launcher.go` (novo) — VO `Launcher` imutável com kind, `NewBinaryLauncher`, `NewNpxLauncher`, `Kind()`, `Command()`
- `internal/runtime/specs/spec.go` (novo) — struct `Spec`, struct `FallbackLauncher`, construtor interno `newSpec`
- `internal/runtime/specs/claude.go` (novo) — constantes `ClaudeNpmPackage`, `ClaudeNpmVersion`, `ClaudeSDKVersion`; função pública `Claude()`
- `internal/runtime/specs/claude_test.go` (novo) — snapshot test `TestClaudeSnapshot`, `TestClaudeStability`, `TestLauncherKind`, `TestLauncherCommand`, `TestClaudeNpmFallbackFormat`
- `scripts/sync-acp-sdk-version.sh` (novo, executável) — lê go.mod, extrai versão coder/acp-go-sdk, atualiza ClaudeSDKVersion idempotente
- `Makefile` (modificado) — target `sync-acp-sdk-version` adicionado ao `.PHONY` e implementado

## Resultados de Validação
- Testes: pass (100.0% cobertura de statements, -race, -count=1) — 5 funções de teste, 11 sub-testes
- Lint: pass (golangci-lint 0 issues)
- Shellcheck: pass (0 warnings)
- No coder/acp-go-sdk import: confirmado (apenas comentário referencial)
- Sync script "sem mudança": exit 1 quando pacote ausente em go.mod (esperado — task 3.0 adiciona dependência)
- Sync script "mudança aplicada": ClaudeSDKVersion atualizada corretamente para v0.1.5, exit 0
- Sync script idempotência: confirmada (segunda execução sem diff retorna exit 0)
- Constante ClaudeNpmVersion documentada com comentário citando ADR-009 e PRD §"Restrições Técnicas"
- Veredito do Revisor: APPROVED (todas as validações automáticas passaram)

## Diff Reviewed

sha=untracked+modified
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=github.com/JailtonJunior94/ai-spec-harness/internal/runtime/specs
delta=+100.0% (pacote novo)

## Suposições
- `ClaudeNpmVersion = "0.1.0"` pinada como versão estável publicada no momento da implementação (ADR-009 §"Decisão")
- `ClaudeSDKVersion = "v0.0.0"` como placeholder correto para esta task — task 3.0 adicionará coder/acp-go-sdk ao go.mod e sync-acp-sdk-version.sh atualizará automaticamente
- O comportamento de exit 1 do sync script quando o pacote não existe em go.mod é correto per spec (cenário de erro explícito)

## Riscos Residuais
- `ClaudeSDKVersion = "v0.0.0"` permanece como placeholder até task 3.0 adicionar `github.com/coder/acp-go-sdk` ao go.mod — nenhum risco funcional, pois o valor não é consumido nesta task

## Conflitos de Regra
- none
