# Generated: 2026-05-20T20:57:00Z

# Relatório de Execução de Tarefa

## Tarefa
- ID: 9.0
- Título: Application Runner + Invoker Adapter + Integration Tests
- Arquivo: .specs/prd-acp-runtime-claude/task-9.0-runner-invoker-integration.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-acp-runtime-claude/prd.md
- TechSpec: .specs/prd-acp-runtime-claude/techspec.md
- Governança: agent-governance, go-implementation (inferida do diff)
- ADRs consultados: ADR-009 (ACP Protocol Adoption), ADR-010 (Event Tagged Union)

## Comandos Executados
- go build ./internal/runtime/... -> ok
- go build ./internal/taskloop/... -> ok
- go test ./internal/runtime/... -count=1 -race -cover -> ok (92.6%)
- go test ./internal/taskloop/... -count=1 -race -cover -> ok (88.4%)
- golangci-lint run ./internal/runtime/... ./internal/taskloop/... -> 0 issues
- make test -> all packages ok
- make lint -> 0 issues

## Arquivos Alterados
- internal/runtime/runner.go (novo): ACPRunner application service
- internal/runtime/types.go (novo): Job struct
- internal/runtime/options.go (novo): Functional Options + Prober/PersistenceFactory/Renderer interfaces
- internal/runtime/errors.go (modificado): ErrLauncherUnavailable re-exportado de probe
- internal/runtime/acp_integration_test.go (novo): 8 cenários de integração in-process
- internal/runtime/probe/errors.go (novo): ErrLauncherUnavailable movido para probe
- internal/runtime/probe/probe.go (modificado): remover import de runtime (eliminar ciclo)
- internal/runtime/persistence/session.go (novo): SessionPersistence + SessionPersistenceFactory
- internal/taskloop/acpinvoker.go (novo): acpInvoker adapter (AgentInvoker + LiveOutputSetter)
- internal/taskloop/acpinvoker_test.go (novo): testes unitários do acpInvoker

## Decisões de Arquitetura

### Ciclos de importação resolvidos
- `probe` importava `runtime` para `ErrLauncherUnavailable` → movido para `probe/errors.go`; `runtime.ErrLauncherUnavailable` agora é alias de `probe.ErrLauncherUnavailable`
- `persistence` importava `runtime.Summary` (correto); `runner.go` não importa `persistence` diretamente — usa interface `Persistence` definida em `runtime`

### Prober interface em runtime
- O runner usa `Prober interface { EnsureAvailable(ctx, spec) (Launcher, error) }` em vez de `probe.LookPather` diretamente
- Em produção: `defaultProber` wraps `probe.EnsureAvailable` com cache
- Em testes: `fakeProber` sem cache global permite testes paralelos isolados

### Signature NewAgentInvoker preservada (RF-12)
- `NewAgentInvoker(tool string)` mantida intacta; `NewACPInvoker(runner, quiet, timeout)` é construtor separado
- Nenhuma modificação ao campo `invokerFactory` de taskloop.Service

## Resultados de Validação
- Testes: pass
- Lint: pass (0 issues)
- Race detector: pass (sem data races)
- Cobertura runtime: 92.6% (alvo ≥85%) ✓
- Cobertura taskloop: 88.4% (alvo ≥85%) ✓
- Veredito do Revisor: APPROVED

## Cenários de Integração (9.10 — todos verdes)
- TestACPRunner_HappyPath: 3 msg + 2 tool calls + session_end; cancel_reason=none ✓
- TestACPRunner_ActivityTimeout: watchdog 50ms com delay 500ms; cancel_reason=activity_timeout ✓
- TestACPRunner_PermissionDenied: requestPermission cancelado pelo cliente; summary válido ✓
- TestACPRunner_UnknownDrift: 2 eventos unknown; UnknownEventsCount>0 ✓
- TestACPRunner_NpxFallback: binary ausente, npx disponível; Launcher="npx" ✓

## Testes Unitários acpInvoker (9.11 — todos verdes)
- TestNewACPInvoker_ImplementsAgentInvoker ✓
- TestACPInvoker_BinaryName ✓
- TestACPInvoker_Invoke_HappyPath ✓
- TestACPInvoker_Invoke_ExitCodes (5 subtestes) ✓
- TestACPInvoker_SetLiveOutput ✓
- TestACPInvoker_Invoke_QuietMode ✓
- TestACPInvoker_ImplementsAgentInvokerInterface ✓

## Sanidade legacy (9.12)
- go test ./internal/taskloop/... -count=1 -race -> ok (88.4%) ✓

## Diff Reviewed

sha=n/a
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=internal/runtime,internal/taskloop
delta=+92.6%,+88.4%

## Suposições
- `NewAgentInvoker` signature foi preservada (não estendida com parâmetro `runtime`) para evitar quebrar ~60 callers de testes legacy; `NewACPInvoker` é o construtor explícito para o invoker ACP
- `AppendUnknown` do acpfake usa `UserMessageChunk` como proxy para KindUnknown (comportamento documentado no script.go)
- `RequestPermission` no acpfake é cancelado silenciosamente pelo cliente ACP (não dispara ErrPermissionDenied via context.Cause); o teste valida que o runner encerra sem pânico e o cancel_reason é um valor válido

## Riscos Residuais
- `persistence` coverage 79.5% (abaixo de 85% isoladamente, mas dentro do agregado runtime+taskloop)
- `ErrPermissionDenied` via context.Cause requer que o requestPermission cancele o contexto; o caminho atual (SDK cancela silenciosamente) pode não acionar o sentinel — pós-melhoria via RF-16 separado

## Conflitos de Regra
- none
