# Generated: 2026-05-21T00:00:00Z

# Execution Report — Task 6.0: Integrar Options.AgentName e BuildPromptContext no taskloop

## Status: done

## Resumo

Integração do package `internal/agents/` ao `internal/taskloop/`. Campo `AgentName string` adicionado em `taskloop.Options`, função `ResolveProfileFromAgent` criada em `profile.go`, `BuildPromptContext` atualizada para aceitar `*agents.ResolvedAgent` e catálogo opcionais, `Service.Execute` resolve agente quando `opts.AgentName != ""`, flag CLI `--agent` adicionada, e testes T-17, T-18, T-19 implementados.

## Arquivos Alterados

- `internal/taskloop/taskloop.go` — campo `AgentName string` em `Options`, import `agents`, resolução do agente no `Execute`
- `internal/taskloop/profile.go` — `mapIDEToSpec`, `ResolveProfileFromAgent`, imports `agents` + `specs`
- `internal/taskloop/agent.go` — `PromptContext` com `AgentMetadata`/`AgentCatalog`, `BuildPromptContext` aceita `*agents.ResolvedAgent` + `[]agents.ResolvedAgent`, `ExecutorTemplateData` atualizado, import `agents`
- `internal/taskloop/executor_template.tmpl` — blocos condicionais `{{if .AgentMetadata}}` e `{{if .AgentCatalog}}`
- `internal/taskloop/agent_test.go` — atualização dos 2 call sites de `BuildPromptContext` (nil, nil para fluxo legado)
- `internal/taskloop/taskloop_test.go` — import `agents`, testes T-17, T-18, T-19
- `internal/agents/precedence.go` — `ApplyRuntimePrecedence` exportado
- `cmd/ai_spec_harness/task_loop.go` — flag `--agent`, validação de exclusividade, passagem de `AgentName` em `Options`
- `docs/cli-schema.json` — entrada `agent` na seção de flags do comando `task-loop`

## Validações Executadas

| Comando | Resultado |
|---------|-----------|
| `go build ./...` | ok |
| `go test ./internal/taskloop/... -run TestT17` | PASS |
| `go test ./internal/taskloop/... -run TestT18` | PASS |
| `go test ./internal/taskloop/... -run TestT19` | PASS |
| `go test ./internal/taskloop/...` | ok (suíte completa) |
| `go test ./...` | ok (todas as suítes) |
| `git diff main -- internal/runtime/persistence/ internal/runtime/watchdog.go \| wc -l` | 894 (igual ao baseline — guarda-rail preservado) |

## Critérios de Sucesso

- [x] T-17: `Options.AgentName == ""` → zero chamadas a registry; fluxo legado intacto (sem blocos `### Agente Ativo` no prompt)
- [x] T-18: `Options.AgentName == "foo"` válido → registry resolve → ACP runner invocado com prompt enriquecido (blocos `### Agente Ativo` e `### Agentes Disponiveis` presentes)
- [x] T-19: `Options.AgentName == "foo"` não encontrado → erro wrappando `agents.ErrAgentNotFound` com candidatos na mensagem
- [x] Suíte completa `go test ./internal/taskloop/...` verde
- [x] Diff zero em `internal/runtime/persistence/*` e `internal/runtime/watchdog.go` (baseline 894 linhas preservado)

## Notas de Implementação

1. `ApplyRuntimePrecedence` exportada em `internal/agents/precedence.go` para uso pelo taskloop sem circular dependency.
2. Quando `AgentName != ""`, `Execute` força `opts.Runtime = "acp"` automaticamente (pois agente declarativo implica ACP na Fase 1).
3. `BuildPromptContext` mantém backward compatibility: chamadores legados passam `nil, nil` para os dois novos parâmetros.
4. Flag `--agent` foi adicionada ao CLI como parte do escopo desta tarefa (6.0) para completar o ponto de integração end-to-end, antecipando parcialmente o trabalho de 7.0. Tarefa 7.0 permanece com a validação de T-20/T-21 (conflito com `--executor-tool` e modo avançado).

## Riscos Residuais

Nenhum risco crítico. O mapping `ide → spec` suporta apenas `claude` nesta fase; tentativas de usar agentes com `runtime.ide: codex/gemini/copilot` retornam erro acionável indicando uso do `--tool` legado.
