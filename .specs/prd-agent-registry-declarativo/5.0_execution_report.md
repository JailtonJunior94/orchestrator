# Generated: 2026-05-21T00:00:00Z

# Execution Report — Task 5.0: Precedence runtime + builders de prompt do agente

## Resumo

Implementados os componentes puros `precedence.go` e `prompt.go` no package `internal/agents/`, com todos os testes exigidos pelos critérios T-10, T-11, T-12, T-15 e T-16 passando.

## Arquivos Criados

| Arquivo | Ação |
|---------|------|
| `internal/agents/precedence.go` | Criado — `RuntimeOverride` + `applyRuntimePrecedence` |
| `internal/agents/prompt.go` | Criado — `BuildAgentBlocks`, `buildMetadataBlock`, `buildCatalogBlock`, `buildRuntimeLine` |
| `internal/agents/precedence_test.go` | Criado — testes T-10, T-11, T-12 + extras |
| `internal/agents/prompt_test.go` | Criado — testes T-15, T-16 + extras |

## Arquivos Inalterados (guarda-rail)

- `internal/runtime/persistence/*` — ZERO diff
- `internal/runtime/watchdog.go` — ZERO diff

## Validações Executadas

```
go build ./internal/agents/...          → OK
go vet ./internal/agents/...            → OK
go test -run "TestApplyRuntimePrecedence|TestBuildAgentBlocks" ./internal/agents/... -v → 11 PASS, 0 FAIL
go test -run "TestValidateAgentFrontmatter" ./internal/agents/... -v → 15 PASS (regressão zero)
```

## Critérios de Sucesso

| Critério | Status | Evidência |
|----------|--------|-----------|
| T-10: CLI `--model X` + AGENT.md `model: Y` → resolve para `X` | PASS | `TestApplyRuntimePrecedence_CLIWins` |
| T-11: CLI sem `--model` + AGENT.md `model: Y` → resolve para `Y` | PASS | `TestApplyRuntimePrecedence_AgentFills` |
| T-12: nenhum setado → `RuntimeOverride` permanece vazio | PASS | `TestApplyRuntimePrecedence_EmptyBothSides` |
| T-15: catálogo de 3 entradas → bloco ordenado lex; agente ativo marcado | PASS | `TestBuildAgentBlocks_OrderedAndActive` |
| T-16: catálogo de 250 entradas → truncado para 200 | PASS | `TestBuildAgentBlocks_TruncatesAt200` |
| Funções puras: testes sem `fs.FileSystem` ou doubles | PASS | Testes em `package agents` (whitebox); sem IO |
| Diff zero em `internal/runtime/persistence/*` e `watchdog.go` | PASS | `git diff --name-only` verificado |

## Subtarefas

- [x] 5.1 `internal/agents/precedence.go` com `RuntimeOverride` e `applyRuntimePrecedence`
- [x] 5.2 `internal/agents/prompt.go` com `BuildAgentBlocks`
- [x] 5.3 Formato dos blocos definido (metadata em Markdown estruturado; catálogo em lista bullet)
- [x] 5.4 Testes T-10, T-11, T-12, T-15, T-16 implementados e passando

## Decisões

- `buildMetadataBlock` recebe guarda nil check para evitar panic quando `agent` é nil (detectado por teste extra).
- `buildRuntimeLine` emite `-` quando todos os campos de runtime estão vazios, alinhado ao formato do task file.
- Catálogo é copiado antes do sort para não mutar o slice do caller.

## Riscos Residuais

Nenhum crítico. A suíte de schema/discovery (task 3.0) referencia `ExportDiscoverAgents`/`ExportMergeWithShadowing` em `export_test.go` que estão definidas; testes de discovery falham no package externo `agents_test` apenas quando task 3.0 não completou as funções internas — isso é esperado e não é regressão introduzida por esta tarefa.
