# Generated: 2026-05-21T00:00:00Z

# Relatório de Execução — Tarefa 2.0: Domínio e JSON Schema do package internal/agents/

## Resumo

Criado o package `internal/agents/` com value objects, erros sentinela, JSON Schema embarcado e função de validação. Todos os critérios de sucesso da tarefa foram atendidos.

## Status Final

`done`

## Arquivos Criados

| Arquivo | Descrição |
|---------|-----------|
| `internal/agents/agent.go` | Value objects: `ResolvedAgent`, `Metadata`, `RuntimeDefaults`, `Scope` (ScopeGlobal/ScopeWorkspace) |
| `internal/agents/errors.go` | Erros sentinela: `ErrFrontmatterInvalid`, `ErrNameDirMismatch`, `ErrVersionInvalid`, `ErrIDEUnsupported`, `ErrAgentNotFound` |
| `internal/agents/agent-frontmatter.schema.json` | JSON Schema 2020-12 com campos obrigatórios (name, description, version) e opcionais (runtime.*) |
| `internal/agents/schema.go` | go:embed do schema + função `ValidateAgentFrontmatter(content []byte, dirName string) (ResolvedAgent, error)` |
| `internal/agents/schema_test.go` | Testes T-05, T-06, T-07, T-08, T-09 + casos positivos |
| `internal/agents/agent_test.go` | Testes de zero value e Scope.String() |

## Arquivos Inalterados (Guardrail)

Diff zero em:
- `internal/runtime/persistence/*` — confirmado via `git diff HEAD -- internal/runtime/persistence/`
- `internal/runtime/watchdog.go` — confirmado via `git diff HEAD -- internal/runtime/watchdog.go`

## Subtarefas Completadas

- [x] 2.1 `ResolvedAgent`, `Metadata`, `RuntimeDefaults`, `Scope` em `agent.go`
- [x] 2.2 `agent-frontmatter.schema.json` (JSON Schema 2020-12)
- [x] 2.3 `schema.go` com `go:embed` e `ValidateAgentFrontmatter`
- [x] 2.4 Erros sentinela em `errors.go`
- [x] 2.5 Consumo de `ParseFrontmatterFields` da tarefa 1.0

## Testes Executados

### Testes da Tarefa

| Caso | Resultado |
|------|-----------|
| T-05: frontmatter sem `description` | PASS — `ErrFrontmatterInvalid` |
| T-06: `version` não-SemVer | PASS — `ErrFrontmatterInvalid` |
| T-07: `runtime.ide` fora do enum | PASS — `ErrFrontmatterInvalid` |
| T-08: `runtime.reasoning_effort` inválido | PASS — `ErrFrontmatterInvalid` |
| T-09: `name` ≠ nome do diretório | PASS — `ErrNameDirMismatch` |
| Positivo: frontmatter válido completo | PASS — `ResolvedAgent` com todos os campos |

### Suíte Completa

```
go test ./... 2>&1
ok  github.com/JailtonJunior94/ai-spec-harness/internal/agents     1.089s
ok  github.com/JailtonJunior94/ai-spec-harness/internal/skills     (cached)
ok  github.com/JailtonJunior94/ai-spec-harness/internal/taskloop   (cached)
... (todos os pacotes: PASS)
```

Total: 44 pacotes testados, 0 falhas.

## go vet

```
go vet ./internal/agents/...
(sem saída — sem erros)
```

## Decisões de Implementação

- **Padrão de schema**: espelhou `internal/skills/schema.go` com `init()` + `compiledAgentFrontmatterSchema` (panic em schema inválido no startup).
- **Consumo de ParseFrontmatterFields**: importado de `internal/skills` — sem duplicação de código (RF-04, A3).
- **Erros sentinela**: compostos com `fmt.Errorf("%w: ...")` para compatibilidade com `errors.Is` (R-ERR-001).
- **R-DDD-001**: todos os tipos são value objects sem estado mutável; construtores validantes via `ValidateAgentFrontmatter`.
- **Sem dependências circulares**: `internal/agents` depende apenas de `internal/skills` e `github.com/santhosh-tekuri/jsonschema/v6`; não depende de `taskloop` ou `runtime/specs`.

## Riscos Residuais

Nenhum risco crítico identificado. A validação de `runtime.model` contra `CompatibilityTable` está fora do escopo desta tarefa (task 8.0).

## Comandos Executados

```bash
go build ./internal/agents/...     # OK
go test ./internal/agents/... -v   # PASS (14 subcasos)
go vet ./internal/agents/...       # OK
go test ./...                      # PASS (44 pacotes)
git diff HEAD -- internal/runtime/persistence/ internal/runtime/watchdog.go  # (vazio)
```
