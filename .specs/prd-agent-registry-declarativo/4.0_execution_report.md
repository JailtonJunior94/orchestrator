# Generated: 2026-05-21T12:10:00Z

# Execution Report — Task 4.0: Registry com cache por instância

## Status Final

`done`

## Objetivo

Expor a interface `Registry` (`Discover`, `Resolve`) e sua implementação default `defaultRegistry` com cache stateful por instância (D-04), concorrência segura via `sync.Once`, e erro acionável `ErrAgentNotFound` listando candidatos descobertos (RF-17).

## Arquivos Criados / Modificados

| Arquivo | Operação |
|---|---|
| `internal/agents/registry.go` | Criado — interface Registry + implementação defaultRegistry |
| `internal/agents/registry_test.go` | Criado — T-14, T-19 e testes adicionais de Discover/Resolve |
| `.specs/prd-agent-registry-declarativo/tasks.md` | Modificado — status 4.0: pending → done |

## Arquivos Inalterados (Guarda-rail)

- `internal/runtime/persistence/*` — diff zero confirmado
- `internal/runtime/watchdog.go` — diff zero confirmado

## Implementação

### registry.go

Interface `Registry`:
```go
type Registry interface {
    Discover(ctx context.Context) ([]ResolvedAgent, error)
    Resolve(name string) (ResolvedAgent, error)
}
```

Implementação `defaultRegistry`:
- `sync.Once` na função `load()` garante que disco é lido exatamente uma vez por instância
- `Discover` chama `load()` e retorna `r.cached`
- `Resolve` chama `load()` e busca por nome; erro com mensagem listando candidatos (RF-17)
- Construtor: `NewDefaultRegistry(fsys fs.FileSystem, workdir, home string) Registry`

## Critérios de Sucesso — Evidências

### T-14: Cache — disco lido 1 vez

Teste `TestRegistry_T14_CacheDiscoChamadoUmaVez` usa `countingFakeFS` (wrapper com contador de ReadFile) e verifica que duas chamadas a `Resolve("foo")` resultam em `reads1 == reads2` (nenhuma leitura adicional na segunda chamada).

### T-19: ErrAgentNotFound com candidatos

Teste `TestRegistry_T19_AgentNaoEncontradoListaCandidatos` verifica:
- `errors.Is(err, ErrAgentNotFound)` = true
- `err.Error()` contém nomes dos candidatos descobertos (RF-17)
- Teste adicional `TestRegistry_T19_SemCandidatos` verifica mensagem "nenhum agente descoberto"

### Concorrência — 100 goroutines

Teste `TestRegistry_Concorrencia_100Goroutines` roda 100 goroutines chamando `Discover` em paralelo com `go test -race`. Todos retornam resultado idêntico sem data race.

## Comandos Executados

```
go build ./internal/agents/             → OK
go vet ./internal/agents/               → OK (após corrigir %w em fmt.Sprintf)
go test ./internal/agents/ -race -run "TestRegistry"  → PASS (7/7 testes)
go test ./...                           → PASS (todos os pacotes)
```

## Output de Testes

```
=== RUN   TestRegistry_T14_CacheDiscoChamadoUmaVez  --- PASS
=== RUN   TestRegistry_T19_AgentNaoEncontradoListaCandidatos  --- PASS
=== RUN   TestRegistry_T19_SemCandidatos  --- PASS
=== RUN   TestRegistry_Concorrencia_100Goroutines  --- PASS
=== RUN   TestRegistry_Discover_Vazio  --- PASS
=== RUN   TestRegistry_Discover_WorkspacePrevaleceEmColisao  --- PASS
=== RUN   TestRegistry_Resolve_WorkspacePrevaleceEmColisao  --- PASS
ok  github.com/JailtonJunior94/ai-spec-harness/internal/agents  1.429s
```

## Suíte Completa

```
go test ./... → todos os 41+ pacotes: PASS
```

## Riscos Residuais

Nenhum crítico. Observações:
- O campo `cachedErr` expõe apenas o primeiro erro de descoberta parcial (robustez sem interrupção); comportamento documentado no código.
- `discoverAgents` e `mergeWithShadowing` estavam marcadas como potencialmente "unusedfunc" antes desta tarefa — agora são consumidas pelo `defaultRegistry.load()`, eliminando o aviso.

## Conformidade com ADR/PRD

- D-04 (cache por instância): implementado via `sync.Once` — sem cache global
- RF-17 (erro acionável com candidatos): implementado em `Resolve`
- RF-10 (workspace prevalece): delegado a `mergeWithShadowing`
- ADR-002 (FakeFileSystem): testes usam `FakeFileSystem`; `countingFakeFS` é wrapper sem mock de filesystem real
- `internal/runtime/persistence/*` e `watchdog.go`: diff zero confirmado via `git diff`
