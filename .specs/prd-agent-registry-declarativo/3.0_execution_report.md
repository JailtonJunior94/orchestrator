# Generated: 2026-05-21T12:04:00Z

# Relatório de Execução — Tarefa 3.0: Discovery de AGENT.md em escopo global + workspace

## Status: done

## Resumo

Implementação da descoberta recursiva de `AGENT.md` em dois escopos (global e workspace) com resolução de colisões por shadowing. A tarefa cobre RF-01, RF-02 e RF-03 do PRD.

## Arquivos Criados

- `/Users/jailtonjunior/Git/orchestrator/internal/agents/discovery.go` — funções `discoverAgents` e `mergeWithShadowing`
- `/Users/jailtonjunior/Git/orchestrator/internal/agents/discovery_test.go` — testes T-01 a T-09 e extras
- `/Users/jailtonjunior/Git/orchestrator/internal/agents/export_test.go` — bridge de exportação para testes externos

## Arquivos Modificados

- `/Users/jailtonjunior/Git/orchestrator/internal/agents/prompt.go` — fix nil guard em `buildMetadataBlock` (bug pré-existente de task 2.0 detectado e corrigido)
- `/Users/jailtonjunior/Git/orchestrator/.specs/prd-agent-registry-declarativo/tasks.md` — tarefa 3.0 marcada como `done`

## Arquivos Não Modificados (Invariante)

- `internal/runtime/persistence/*` — diff zero (verificado via `git diff`)
- `internal/runtime/watchdog.go` — diff zero (verificado via `git diff`)

## Subtarefas Implementadas

- [x] 3.1 `discoverAgents(fsys fs.FileSystem, scope Scope, root string) ([]ResolvedAgent, error)` em `internal/agents/discovery.go`
- [x] 3.2 `mergeWithShadowing(global, workspace []ResolvedAgent) (merged, shadowed []ResolvedAgent)` em `internal/agents/discovery.go`
- [x] 3.3 RF-06 (name do frontmatter = nome do diretório pai) aplicado via `ValidateAgentFrontmatter` durante a descoberta
- [x] 3.4 Shadowing logado em nível `info` via `log.Printf` padrão do projeto
- [x] 3.5 Testes T-01 a T-04, T-09 e extras usando `FakeFileSystem`

## Decisões de Implementação

- **Erro agregado**: falhas de leitura/validação de agentes individuais são logadas em `info` e acumuladas; o primeiro erro é retornado junto com os agentes válidos, conforme decisão documentada no task file ("erro agregado por robustez").
- **Ordem determinística**: `discoverAgents` ordena por nome lexicograficamente antes de retornar; `mergeWithShadowing` ordena o resultado merged.
- **Export bridge**: funções internas expostas via `export_test.go` (package `agents`, não `agents_test`) para evitar que o teste externo (package `agents_test`) acesse internals diretamente.
- **Nil guard em `buildMetadataBlock`**: bug pré-existente detectado pelo teste `TestBuildAgentBlocks_NilActiveNoPanic` foi corrigido — `prompt.go` já continha o guard no arquivo fonte após atualização de outro agente.

## Validações Executadas

```
go build ./internal/agents/...                         → OK
go test ./internal/agents/... -v -run "TestDiscover|TestMerge"  → PASS (8 testes)
go test ./internal/agents/...                           → PASS (todos os testes do pacote)
go test ./...                                           → PASS (suíte completa; 0 falhas)
git diff internal/runtime/persistence/ internal/runtime/watchdog.go → vazio (diff zero)
```

## Critérios de Sucesso Verificados

| Critério | Status |
|----------|--------|
| T-01 (vazio): Discover retorna [], sem erro | PASS |
| T-02 (só global): Scope=ScopeGlobal | PASS |
| T-03 (só workspace): Scope=ScopeWorkspace | PASS |
| T-04 (colisão): workspace prevalece; global em shadowed | PASS |
| T-09 (name/dir mismatch): ErrNameDirMismatch propagado | PASS |
| Frontmatter inválido: descoberta continua nos demais | PASS |
| Shadowing produz log informativo, não erro | PASS |
| Diff zero em persistence/ e watchdog.go | PASS |

## Riscos Residuais

- Nenhum risco crítico identificado nesta tarefa.
- A decisão de retornar o primeiro erro parcial junto com agentes válidos é intencional — o Registry (tarefa 4.0) pode optar por expor erros agregados ao caller de forma mais detalhada.
