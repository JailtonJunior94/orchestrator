# Generated: 2026-05-21T19:45:00Z
# Relatório de Execução de Tarefa

## Tarefa
- ID: 4.0
- Título: runner.go::Run consome spec.BootstrapArgs e prepend ao argv
- Arquivo: .specs/prd-codex-acp-spec/task-4.0-runner-consome-bootstrapargs.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-codex-acp-spec/prd.md
- TechSpec: .specs/prd-codex-acp-spec/techspec.md
- Governança: agent-governance, go-implementation (inferida do diff Go)
- Requisitos cobertos: RF-08 (runner consome BootstrapArgs), RF-19 (argv completo no runtime_init)
- Skills processuais: Nenhuma além das auto-carregadas (governance + linguagem)

## Comandos Executados
- `grep -rn "type Job" ./internal/runtime/` → `internal/runtime/types.go:9:type Job struct`
- `grep -n "FixedArgs\|bootstrap\|argv" ./internal/runtime/runner.go` → vazio (pré-implementação)
- `go build ./internal/runtime/...` → sem erros
- `go test -count=1 ./internal/runtime/...` → ok (0.439s) — 100% verde
- `go test -count=1 -v -run "TestACPRunner_Codex_T17|TestACPRunner_Codex_T18|TestACPRunner_Claude_T19|TestACPRunner_Copilot_T19" ./internal/runtime/...` → PASS (4 testes)
- `go vet ./...` → sem warnings
- `go test ./...` → ok (todas as 43 packages, 100% verde)
- `git diff --stat internal/runtime/persistence/ internal/runtime/watchdog.go internal/runtime/client/` → **vazio (gate R-08 aprovado)**

## Arquivos Alterados
- `internal/runtime/types.go` — campo `Model string` adicionado a Job com go-doc
- `internal/runtime/runner.go` — Run() chama BootstrapArgs, monta argv, cria effectiveLauncher, passa argv a buildRuntimeInitRaw
- `internal/runtime/acp_integration_test.go` — testes T-17 (Codex restricted), T-18 (Codex full), T-19 (Claude regressão), T-19 Copilot + helpers

## Subtarefas Completadas
- [x] 4.1 Localizado ponto em ACPRunner.Run após probe.EnsureAvailable
- [x] 4.2 Chamada a bootstrap := r.spec.BootstrapArgs(j.Model, j.ReasoningEffort, j.AddDirs, j.AccessMode)
- [x] 4.3 argv montado: append([]string{}, launcherArgs...) + append(argv, bootstrap...) sem mutação
- [x] 4.4 effectiveLauncher criado via specs.NewBinaryLauncher(launcherCmd, argv...) para c.Open
- [x] 4.5 buildRuntimeInitRaw recebe argv completo (não apenas FixedArgs)
- [x] 4.6 AccessMode zero-value ("") tratado como no-op por codexBootstrapArgs (consistente com task 3.0)
- [x] 4.7 go test ./internal/runtime/... → 100% verde
- [x] 4.8 Gate R-08: git diff --stat → vazio

## Resultados de Validação
- Testes: pass — go test -count=1 ./internal/runtime/... → ok (100% verde; T-17/T-18/T-19/T-31 aprovados)
- Testes T-17: PASS — args contêm -c model="gpt-5.5", -c model_reasoning_effort="medium", features.code_mode=false, features.code_mode_only=false; NÃO contêm sandbox/approval/web_search
- Testes T-18: PASS — args contêm todos os de T-17 + approval_policy="never", sandbox_mode="danger-full-access", web_search="live"
- Testes T-19 Claude: PASS — args NÃO contêm -c (no-op BootstrapArgs preserva regressão)
- Testes T-19 Copilot: PASS — args NÃO contêm -c (no-op BootstrapArgs preserva regressão)
- Testes T-31 (suíte completa): PASS — go test ./... 100% verde (43 packages)
- Lint: pass — go vet ./... sem warnings
- Build: pass — go build ./... sem erros
- Gate R-08: PASS — git diff --stat internal/runtime/persistence/ internal/runtime/watchdog.go internal/runtime/client/ → vazio
- Veredito do Revisor: APPROVED

## Diff Reviewed

sha=runner-bootstrapargs-implementation
verdict=APPROVED
tool=claude-sonnet-4-6

## Coverage

package=github.com/JailtonJunior94/ai-spec-harness/internal/runtime
delta=+4 testes (T-17, T-18, T-19 Claude, T-19 Copilot)

## Design Decisions

- `effectiveLauncher` criado via `specs.NewBinaryLauncher(launcherCmd, argv...)` para injetar bootstrap sem modificar client.go (ADR R-08 compliance).
- `launcher.Kind()` original preservado para o `runtime_init` event — Kind reflete tipo real do probe (binary/npx), não o effectiveLauncher auxiliar.
- `spec.FixedArgs` NÃO re-appended em runner.go — probe já os inclui em launcherArgs via `specs.NewBinaryLauncher(path, spec.FixedArgs...)`. Re-appending causaria duplicação para Claude/Copilot.
- Campo `Model string` adicionado a Job (necessário para BootstrapArgs; Task 3.0 não o incluiu).
- fakeProber em testes não injeta FixedArgs (apenas produção probe.resolve faz) — T-19 valida ausência de -c, não presença de --bypass-permissions.

## Suposições
- Campo `Model string` não estava em types.go (Task 3.0 adicionou apenas ReasoningEffort/AccessMode/AddDirs); adicionado em Task 4.0 conforme techspec §"Job estendido".
- Zero-value de `AccessMode` ("") tratado como no-op por `codexBootstrapArgs` (sem condicional para AccessModeRestricted exigido por consumo — consistente com decisão da Task 3.0).

## Riscos Residuais
- Production launcher para npx: se codex-acp não estiver no PATH e npx for usado como fallback, `effectiveLauncher` terá Kind "binary" (criado via NewBinaryLauncher), mas `launcher.Kind()` original "npx" é usado no runtime_init event — sem impacto funcional, apenas metadados corretos.

## Conflitos de Regra
- none
