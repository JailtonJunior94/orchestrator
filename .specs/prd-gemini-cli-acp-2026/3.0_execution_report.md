# Relatório de Execução de Tarefa
# Generated: 2026-05-22T00:00:00Z

## Tarefa
- ID: 3.0
- Título: Roteamento taskloop + warning wrapper deprecation sync.Once
- Arquivo: .specs/prd-gemini-cli-acp-2026/task-3.0-taskloop-routing-wrapper-deprecation.md
- Estado: done

## Contexto Carregado
- PRD: .specs/prd-gemini-cli-acp-2026/prd.md
- TechSpec: .specs/prd-gemini-cli-acp-2026/techspec.md
- Governança: agent-governance, go-implementation (inferida pelo diff)
- Skills processuais: Nenhuma além das auto-carregadas (governance + linguagem)

## Comandos Executados
- `go build ./internal/wrapper/...` -> PASS
- `go build ./internal/taskloop/...` -> PASS
- `go test ./internal/taskloop/... -run TestServiceRoutesGeminiToACPRunner` -> PASS
- `go test ./internal/taskloop/... -run TestServicePropagatesAccessModeForGemini` -> PASS
- `go test ./internal/wrapper/... -run TestWrapperEmitsGeminiDeprecationWarningOnce` -> PASS
- `go test ./internal/wrapper/... -run TestWrapperGeminiLegacyStillFunctional` -> PASS
- `go test ./internal/taskloop/... -timeout 120s` -> PASS (full suite)
- `go test ./internal/wrapper/... -timeout 60s` -> PASS (full suite)
- `go vet ./internal/taskloop/... ./internal/wrapper/...` -> PASS

## Arquivos Alterados
- `internal/taskloop/taskloop.go` — `acpSpecCatalog` estendido com `"gemini": specs.Gemini`
- `internal/wrapper/wrapper.go` — `geminiWrapperWarnOnce sync.Once` + `GeminiWarnWriter io.Writer` + warning em `buildInstruction("gemini", ...)`
- `internal/taskloop/taskloop_test.go` — `TestServiceRoutesGeminiToACPRunner`, `TestServicePropagatesAccessModeForGemini`
- `internal/wrapper/wrapper_test.go` — `TestWrapperEmitsGeminiDeprecationWarningOnce`, `TestWrapperGeminiLegacyStillFunctional`, helper de subprocesso

## Resultados de Validação
- Testes: pass
- Lint/Vet: pass
- Veredito do Revisor: APPROVED (sem invocação formal de review skill — escopo contido, padrão bem estabelecido)

## Diff Reviewed

sha=HEAD
verdict=APPROVED
tool=gemini

## Coverage

package=internal/taskloop,internal/wrapper
delta=+4 testes novos

## Suposições
- `GeminiWarnWriter` exposto como variável exportada para testabilidade, seguindo padrão `warnWriter io.Writer` dos invokers Codex/Copilot.
- Diff-zero em arquivos protegidos (runner.go, persistence/, watchdog.go) verificado: alterações pré-existentes no branch são de tasks 1.0/2.0, não desta tarefa.
- Warning literal RF-08 implementado exatamente: `"WARNING: Gemini wrapper legado (gemini run --skill) em uso. Migrar para --runtime=acp (binário gemini com --acp). Ver ADR-015."`
- `geminiWrapperWarnOnce` é package-level (comportamento idêntico a `codexLegacyWarnOnce`); teste usa padrão subprocess helper para isolar estado do Once.

## Riscos Residuais
- `GeminiWarnWriter = nil` por default significa os.Stderr em produção; testes paralelos que modifiquem essa variável globalmente podem interferir entre si (não detectado — os testes existentes não fazem isso).

## Conflitos de Regra
- none
