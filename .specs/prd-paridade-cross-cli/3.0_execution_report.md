# Execution Report — Tarefa 3.0: Paridade de Normalização de Tool-Calls

## Status: done

## Arquivos Alterados

| Arquivo | Operação |
|---------|----------|
| `internal/runtime/events/normalization-rules.yaml` | modificado — aliases Gemini explícitos (RP-04), input_mappings Copilot/Gemini |
| `internal/runtime/events/normalize.go` | modificado — `BuildNormalizedToolCallByDriver(DriverID, ...)` |
| `internal/runtime/events/normalize_test.go` | novo — golden tests por driver (T-33, T-34, T-35) |
| `internal/runtime/runner.go` | modificado — `normalizeEventInline` usa `BuildNormalizedToolCallByDriver` |

## Validações Executadas

- `go build ./...` → limpo
- `go vet ./...` → limpo
- `make test` → todos os pacotes ok (zero falhas)
- `go test -count=1 ./internal/runtime/events/...` → verde
- `go test -count=1 ./internal/runtime/...` → 12/12 pacotes ok

## Invariantes F1 Preservadas

- `RawInput` nunca mutado (normalização cria cópia)
- `--no-normalize` preserva comportamento byte-identical (guard `j.NoNormalize` inalterado)
- `BuildNormalizedToolCall` (string, legado) preservado sem alteração

## Cobertura RP-01, RP-04, RIN-02

- RP-01: normalização unificada por tabela YAML por driver
- RP-04: tabela Gemini explícita sem `inherit_common` (T-34-01/02)
- RIN-02: `BuildNormalizedToolCallByDriver` aceita `DriverID` VO (ADR-020)
