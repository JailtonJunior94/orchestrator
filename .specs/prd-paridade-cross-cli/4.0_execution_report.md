# Execution Report — Tarefa 4.0: Guard de Governança em Runtime

## Status: done

## Arquivos Alterados

| Arquivo | Operação |
|---------|----------|
| `internal/runtime/hooks/spec_drift.go` | novo — SpecDriftHook (RG-01, RG-02, ADR-022) |
| `internal/runtime/hooks/spec_drift_test.go` | novo — 8 testes table-driven |
| `internal/runtime/hooks/dispatcher.go` | modificado — TasksDir em RuntimePreOpenEvent; `any` em SessionPostEndEvent |
| `internal/runtime/types.go` | modificado — SkipDriftGuard bool em Job |
| `internal/runtime/runner.go` | modificado — registro de SpecDriftHook + propagação de TasksDir |

## Validações Executadas

- `go build ./internal/runtime/hooks/...` → limpo
- `go vet ./internal/runtime/hooks/...` → limpo
- `go test ./internal/runtime/hooks/... -count=1` → ok (8 testes SpecDriftHook passam)
- `make test` → todos os pacotes ok (zero falhas)
- Lint fixes: `interface{}` → `any` em SessionPostEndEvent

## Invariantes F1 Preservadas

- `SpecDriftHook.Run` é no-op quando `RuntimePreOpenEvent.TasksDir == ""` (uso ad-hoc/F1)
- `Job.SkipDriftGuard = true` desabilita apenas o SpecDriftHook (sem afetar GovernanceHook)
- `--disable-hooks` desabilita todos os hooks (caminho existente, não alterado)

## Cobertura RG-01, RG-02

- RG-01: hash divergente → `ErrSpecDrift` antes de `c.Open`
- RG-02: PRD não rastreável → `ErrPRDUntracked` antes de `c.Open`
