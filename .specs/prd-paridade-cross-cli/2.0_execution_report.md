# Execution Report — Tarefa 2.0: Harmonização de Métricas

## Status: done

## Arquivos Alterados

| Arquivo | Operação |
|---------|----------|
| `internal/runtime/events/extractor.go` | novo — MetricsExtractor interface + implementações por driver |
| `internal/runtime/events/extractor_test.go` | novo — testes table-driven para todos os drivers |
| `internal/runtime/runner.go` | modificado — usa MetricsExtractor, popula Summary.Metrics |
| `internal/runtime/summary.go` | modificado — campo Metrics events.MetricSet (substitui campos planos) |
| `internal/runtime/persistence/report.go` | modificado — RenderMetricsSection + shim RenderClaudeMetricsSection |
| `internal/runtime/persistence/report_test.go` | modificado — testes atualizados para MetricSet |

## Validações Executadas

- `go build ./...` → limpo
- `go vet ./...` → limpo
- `make test` → todos os pacotes ok (zero falhas)
- `go test -count=1 ./internal/runtime/...` → 12/12 pacotes ok
- Lint fixes aplicados: `fmt.Fprintf` em vez de `WriteString(fmt.Sprintf(...))`, dead code `injectClaudeMetricsSection` removido

## Invariantes F1 Preservadas

- `MetricSet` zero-value → `IsZero()=true` → seção de métricas não renderizada (comportamento F1 mantido)
- `Summary.Metrics` field adicionado; campos planos Claude removidos com shim retrocompatível `RenderClaudeMetricsSection`

## Riscos Residuais

- `ToolCallsNormalizedCount` mantido como campo separado em `Summary` (não migrado para `MetricSet.Extra`); impacto documentado para task 3.0
